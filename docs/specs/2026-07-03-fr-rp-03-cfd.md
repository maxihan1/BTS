# FR-RP-03 CFD (Cumulative Flow Diagram) — 스펙 (백엔드 PR, D1~D5)

> BC: notification-dashboard (물리 모듈 issue-tracking) · slug: fr-rp-03-cfd
> ADR: docs/decisions/2026-07-03-fr-rp-03-cfd.md
> 범위: **백엔드만**. 프론트 D6/D7(recharts 영역 차트 + E2E)는 후속 PR.

## 사용자 시나리오 (Given-When-Then)

### S1. 상태 이력이 있는 프로젝트의 CFD
- **Given** 프로젝트 `PROJ`에 BROWSE 권한이 있는 사용자. 여러 이슈가 지난 30일간 여러 상태를 거침(status 전환 이력 존재)
- **When** `GET /api/v1/projects/PROJ/cfd?from=2026-06-04&to=2026-07-03`
- **Then** 200 OK. 창(window) 내 각 UTC 날짜마다 `{ date, todoCount, inProgressCount, doneCount }` 한 점. 날짜 오름차순.

### S2. 이슈가 없는 프로젝트 / 빈 창
- **Given** BROWSE 권한 있는 사용자. 창 내 존재하는 이슈 0개
- **When** CFD 조회
- **Then** 200 OK. 각 날짜 점의 세 카운트 모두 0(점 자체는 창 길이만큼 존재).

### S3. 기밀 이슈 누출 차단
- **Given** 프로젝트에 사용자가 볼 수 없는(이슈 보안 수준) 이슈가 섞여 있음
- **When** CFD 조회
- **Then** 보이지 않는 이슈는 어느 날짜·어느 카테고리 카운트에도 합산되지 않음.

### S4. 권한 없음
- **Given** 프로젝트에 BROWSE 권한이 없는 사용자
- **When** CFD 조회
- **Then** 403.

### S5. 미인증
- **Given** 인증 토큰 없음
- **When** CFD 조회
- **Then** 401 (리소스 조회 이전에 actor 추출 실패로 거부 — 존재 probe 차단).

### S6. 워크플로우 스킴 미할당 타입
- **Given** 이슈 타입에 워크플로우 스킴이 할당되지 않음(상태→카테고리 매핑 불가)
- **When** CFD 조회
- **Then** 500이 아니라 200. 해당 타입 이슈의 상태는 TODO 카테고리로 폴백(EpicProgress 선례).

### S7. 잘못된 시간 창
- **Given** `from > to` 또는 파싱 불가 날짜 또는 창 길이 상한 초과
- **When** CFD 조회
- **Then** 400 (BAD_REQUEST, 파라미터 검증). 500 아님. (WorklogAggregate 형제 엔드포인트 일관 — 쿼리 파라미터 검증은 400)

### S8. 상태 전환 이력이 없는 이슈
- **Given** 생성 후 상태 전환이 한 번도 없는 이슈(또는 V018 이전 생성 이슈)
- **When** CFD 조회
- **Then** 그 이슈는 존재 기간 내내 `current_state_key`의 카테고리로 집계(한계 — ADR D2 명시).

### S9. 소프트 삭제 이슈 (v1 — 활성 이슈만)
- **Given** 소프트 삭제된 이슈(`deleted_at IS NOT NULL`)
- **When** CFD 조회
- **Then** 어느 날짜·카테고리에도 미포함(**v1은 활성 이슈만 집계**). 정본 보안 술어 `buildActiveSecureWhere`가
  active(deleted_at IS NULL)만 반환 → 삭제 이슈는 전부 제외. 과거 날짜 소폭 언더카운트(한계 — 제약 조건 §에 명시).

## 기능 요구사항 (FR)

- **FR1** 프로젝트 + 시간 창 `[from, to]`에 대해, 창 내 각 UTC 날짜마다 CFD 한 점(`CfdPoint`)을 반환한다.
  각 점은 `todoCount` / `inProgressCount` / `doneCount`(그 날 각 카테고리에 있던 가시 이슈 수)를 담는다.
  - **"누적(Cumulative)"의 의미**: 시간 누적 합이 아니라, 각 날짜의 **순간(point-in-time) 카운트를 카테고리별로
    쌓은(stacked) 영역**을 뜻한다(전형적 CFD 정의). 세 카운트 합 = 그 날 존재하는 가시 이슈 총수.
    재오픈(DONE→IN_PROGRESS)이면 doneCount가 줄 수 있다(현재 상태 정밀 추적 — monotonic done 아님).
- **FR2** day D 카운트 = 다음을 만족하는 각 가시 이슈를 그 날 카테고리로 분류해 합산.
  - **존재 판정 (v1)**: `created_at::date ≤ D`. (집계 대상은 이미 활성 이슈만 — 삭제 이슈 제외, S9. `buildActiveSecureWhere`가 `deleted_at IS NULL` 보장)
  - **그 날 상태 (end-of-day 스냅샷)**: `issue_change_item(field='status')`를 `(group.created_at ASC, group.id ASC)`
    순으로 적용해, `created_at::date ≤ D`인 **마지막** 전환의 `to_value`. 같은 날 여러 전환이 있으면
    그 날 종료 시점(마지막)이 이긴다. 그런 전환이 없으면 **초기 상태**. (tie-break `id ASC`로 결정성 확보 —
    index `idx_issue_change_group_issue(issue_id, created_at DESC, id DESC)`의 안정 정렬과 정합)
  - **초기 상태**: 시간상 첫 status 전환의 `from_value`. status 전환이 전혀 없으면 `issues.current_state_key`.
- **FR3** 상태 키 → 카테고리 매핑은 `WorkflowStateCatalog.listStates(projectKey, issueTypeKey)`의
  `category`(TODO/IN_PROGRESS/DONE)를 이슈 타입별로 캐싱(N+1 차단, FR-EP-02·벨로시티 선례).
  카탈로그에 없는(삭제된) 상태 키 → **TODO 폴백**(EpicProgress EC2/EC3 동형).
- **FR4** `WorkflowStateCatalog.listStates`가 스킴 미할당으로 예외(`WorkflowSchemeNoDefaultException`,
  simpleName 판정)면 해당 타입은 빈 카테고리 맵 → 그 타입 이슈 상태 전부 TODO 폴백. 운영 500 차단.
  `IsolatedWorkflowStateLookup`(REQUIRES_NEW)로 격리 호출 — 공유 트랜잭션 rollback-only 오염 방지(벨로시티 선례).
- **FR5** 시간 창. `from`/`to`는 UTC 날짜(ISO `yyyy-MM-dd`). 둘 다 optional.
  - 둘 다 생략 시 기본 = 최근 30일(`to`=오늘 UTC, `from`=`to`−29일). **'오늘'은 주입된 `Clock`(UTC) 기준**
    — 테스트 결정성(번다운/벨로시티/export의 Clock default 선례). 창은 both-ends 포함, 길이 ≤ 180일.
  - 한쪽만 주면 나머지는 기본 규칙으로 채움(`to`만 → `from`=`to`−29 / `from`만 → `to`=today).
  - **창 길이 상한 = 180일**. 초과 시 400(클램프 아님 — 명시적 거부로 오해 방지).
  - `from > to` 또는 파싱 불가 → 400.
- **FR6** 점은 **날짜 오름차순**(오래된→최신). 차트 X축 좌→우 시간 진행. 창의 모든 날짜에 대해 점 생성(빈 날도 0점).

## 비기능 요구사항 (NFR)

- **NFR1 (N+1 차단)** 쿼리 유계. (a) 프로젝트 가시 이슈 메타(id·created_at·deleted_at·current_state_key·type_id) 배치 1쿼리,
  (b) 그 이슈들의 status 전환 이력 배치 1쿼리(`field='status'`), (c) 카테고리 맵은 이슈 타입별 1회 캐싱.
  이슈/날짜 루프는 in-memory(요청당 상수 쿼리 수).
- **NFR2 (성능 경계)** 창 상한 180일 + 이슈는 프로젝트 스코프로 유계. in-memory 재구성은 이슈별 전환 델타를
  날짜에 누적하는 방식(O(이슈×전환 + 날짜×3))으로 날짜×이슈 이중 루프를 피한다(구현 세부는 plan).
- **NFR3 (보안)** 프로젝트 BROWSE + 이슈별 가시성 필터(`IssueRepository.filterVisibleIssueKeys` →
  `buildActiveSecureWhere` 재사용). 전용 보안 술어 신설/복제 금지(isomorphic-clone 회귀 방지).
- **NFR4 (BC 격리)** issue-tracking이 모든 데이터를 로컬 소유(issues·issue_change). 워크플로우 상태만
  shared-kernel `WorkflowStateCatalog` SPI로 접근. **신규 cross-BC 포트 불필요**(번다운/벨로시티와 달리 스프린트 미참조).
- **NFR5 (완제품)** 401/403/400 코드·핸들러는 **신규 컨트롤러 전용 advice**(`CfdExceptionHandler`,
  basePackages 한정 — WorklogAggregateExceptionHandler 선례). catch-all 미포함.
  catch-all이 401/403/400을 500으로 변질시키지 않도록 `ResponseStatusException` passthrough 유지
  (catch-all-exceptionhandler-swallows / domain-exception-http-handler-basepackage-scope 선례).

## API 인터페이스 (REST)

### `GET /api/v1/projects/{projectKey}/cfd`

- **Query**: `from` (ISO date `yyyy-MM-dd`, optional), `to` (ISO date, optional). FR5 기본/상한 규칙.
- **200 OK** — `DataResponse<CfdResponse>`:

```json
{
  "data": {
    "projectKey": "PROJ",
    "from": "2026-06-04",
    "to": "2026-07-03",
    "points": [
      { "date": "2026-06-04", "todoCount": 12, "inProgressCount": 5, "doneCount": 30 },
      { "date": "2026-06-05", "todoCount": 11, "inProgressCount": 6, "doneCount": 30 }
    ]
  }
}
```

- `from`/`to`는 **해석된(기본값 적용 후) 실제 창**을 echo — 프론트가 실제 범위를 앎.
- `points`는 날짜 오름차순, 창의 모든 날짜 포함(빈 날 0점).
- **401** 미인증 / **403** BROWSE 미충족 / **400** 잘못된 창.
- 프로젝트 미존재는 **별도 404 없음** — BROWSE 미보유면 403, 프로젝트에 데이터 없으면 빈/0점 200
  (WorklogAggregateService 선례 — 존재 probe 차단). BROWSE 체크는
  `IssuePermissionResolver.hasPermission(actor, IssuePermission.BROWSE, IssueScope.Project(projectKey))` →
  실패 시 `IssueAccessDeniedException`(403). 권한 검증을 repo 조회보다 먼저.

### 응답 DTO 형태
- `CfdResponse(projectKey: String, from: LocalDate, to: LocalDate, points: List<CfdPointResponse>)`
- `CfdPointResponse(date: LocalDate, todoCount: Int, inProgressCount: Int, doneCount: Int)`
- `BurndownResponse`/`VelocityResponse`처럼 `from(result)` companion 매핑. `DataResponse`로 래핑.

## 데이터 모델 변경

**없음 (신규 스키마 0).** 기존 재사용.
- `issues`(id·key·project_id·type_id·current_state_key·created_at·deleted_at).
- `issue_change_group`(issue_id, created_at) + `issue_change_item`(field='status', from_value, to_value) — V018.
- 워크플로우 `WorkflowStateCatalog` / `StateCategory{TODO,IN_PROGRESS,DONE}`.
- **인덱스**: **신규 인덱스 0**(확정). status 이력 쿼리(`g.issue_id IN (:ids) JOIN item ON group_id WHERE field='status'
  ORDER BY issue_id, created_at, id`)는 기존 `idx_issue_change_group_issue(issue_id, created_at DESC, id DESC)` +
  `idx_issue_change_item_group(group_id)` + `idx_issue_change_item_field(field)`로 커버. 프로파일링이 병목을
  보이면 후속에서 `issue_change_item(group_id) WHERE field='status'` 부분 인덱스 검토(Simplicity First — 선제 추가 금지).

## 새 도메인 개념 (VO, 영속 아님)

- **CfdResult** — 프로젝트 CFD 조회 결과 read-model. `{ projectKey, from, to, points: List<CfdPoint> }`.
- **CfdPoint** — 시계열 한 점. `{ date, todoCount, inProgressCount, doneCount }`. 애그리거트/테이블 없음.
- 순수 집계 로직(이슈 상태 타임라인 → 날짜별 카테고리 카운트)은 테스트 가능한 순수 함수/도메인 계산기로 분리
  (`BurndownCalculator` 선례).

## 엣지 케이스

| # | 케이스 | 처리 |
|---|---|---|
| E1 | 창 내 이슈 0개 | 각 날짜 0점, 200 |
| E2 | status 전환 없는 이슈 | 존재 기간 내내 current_state_key 카테고리(S8) |
| E3 | 기밀 이슈 섞임 | 가시성 필터 제외 후 집계(S3) |
| E4 | 스킴 미할당 타입 | TODO 폴백, 500 차단(S6) |
| E5 | V018 이전 생성 이슈 | current_state_key로만 계산(한계, ADR D2) |
| E6 | from > to | 400(S7) |
| E7 | 창 > 180일 | 400(클램프 아님) |
| E8 | from/to 생략 | 기본 최근 30일(FR5) |
| E9 | 소프트 삭제 이슈 | v1 전부 제외(활성만, S9) |
| E10 | `to`보다 나중 생성 이슈 | 미집계(존재 판정) |
| E11 | 이력에 삭제된 상태 키 | TODO 폴백(FR3) |
| E12 | 파싱 불가 날짜 | 400(S7) |
| E13 | 타 프로젝트에서 이동해 온 이슈 | 현재 project_id 기준 집계(과거 소속 미복원 — on-the-fly 한계, 벨로시티 동종) |

## 제약 조건

- 신규 DB 스키마 0, 신규 인덱스 0. 스냅샷 테이블/스케줄러 0(on-the-fly, ADR D2). 신규 cross-BC 포트 0.
- 한 PR = 한 BC(issue-tracking). 워크플로우 상태는 shared-kernel `WorkflowStateCatalog` SPI 경유.
- **v1 한계(수용, 명시)**. (a) 삭제 이슈 전부 제외 → 과거 날짜 소폭 언더카운트(S9). (b) V018 이전 이슈는
  current_state_key로만 계산(S8). (c) 타 프로젝트 이동 이슈는 현재 소속 기준(E13). 셋 다 on-the-fly 철학 정합
  (벨로시티 commitment deviation·번다운 추정치 갭과 동종).
- 프론트/E2E는 이 PR 범위 밖(후속 D6/D7).

## 측정 가능한 완료 기준

- [ ] `GET /api/v1/projects/{projectKey}/cfd` 200 응답, S1~S9 시나리오 통합 테스트 통과.
- [ ] 순수 집계 계산기 단위 테스트(상태 타임라인 재구성 · 날짜별 카테고리 카운트 · 초기상태 역산 · 삭제/빈이력 경계).
- [ ] issue-tracking 조회/집계 Testcontainers 테스트(가시성 필터 + 카테고리 매핑 + 이력 재구성 실측, 비-vacuous).
- [ ] S3 기밀 이슈 제외 가드 통합 테스트, S7 400 창 검증 테스트.
- [ ] 모듈 `./gradlew :backend:modules:issue-tracking:test` + ktlint/detekt(--rerun-tasks) green.
- [ ] verify-master-plan 통과(카운트 불변, D단계 마킹).

## Brainstorming Check

✅ 통과 (1회 iteration). 자체 적대 검토로 4건 보강 + 1건 plan 위임.
- **G1 (정확성)** 같은 날 다중 status 전환의 tie-break 미정의 → FR2에 `(created_at ASC, id ASC)` + end-of-day 스냅샷
  semantics 명시(index 안정 정렬과 정합).
- **G2 (테스트 결정성)** 기본 창의 '오늘'이 비결정적 → FR5에 `Clock`(UTC) 주입 명시(번다운/벨로시티/export 선례).
- **G3 (오해 방지)** "누적"을 시간 합으로 오구현할 위험 → FR1에 stacked point-in-time 카운트 정의 + 재오픈 시 done 감소 명시.
- **G4 (성능)** field='status' 필터가 status 아닌 이력 다수를 스캔할 위험 → 데이터모델 §에 부분 인덱스 조건부 추가(D3 db 판정).
- **plan 위임**: (a) issue-tracking 프로젝트 BROWSE 체크 진입점(FR-SR-01 이슈 필터 선례 대조) + 프로젝트 미존재 403 vs 404,
  (b) in-memory 델타-누적 알고리즘 최종 형태, (c) 신규 index 실제 필요 여부(쿼리 플랜).
