# FR-MV-01 D6/D7 프론트 이동 마법사 UI + E2E — 스펙

> slug: fr-mv-01-d6-d7-ui · BC: issue-tracking (apps/web) · type: ui
> 도메인/결정: [docs/plans/2026-06-16-fr-mv-01-d6-d7-ui.md](../plans/2026-06-16-fr-mv-01-d6-d7-ui.md), [ADR issue-move-semantics](../adr/2026-06-16-issue-move-semantics.md)
> 백엔드 완료: #153(단건) + #154(서브태스크 동반). preview/move 2단계 API 소비. **프론트는 소비측 — Zod 1:1 미러.**

## 사용자 시나리오 (Given-When-Then)

### S1. 단건 이동 (완전 호환)
- **Given** 이슈 상세 `ATLAS-12`(자식 없음), 사용자가 대상 `INFRA`에 CREATE 권한 보유
- **When** "이동" 액션 → 마법사 → 대상 `INFRA` 입력 → preview(매핑 불필요) → "이동" 확인
- **Then** move 성공 → 새 키 `INFRA-5`로 이동(상세 페이지 자동 이동), 토스트 "이동 완료"

### S2. 단건 이동 (비호환 매핑 필요)
- **Given** `ATLAS-12`(상태 `in_progress`, 컴포넌트 `Backend`, fix버전 `v1.0`), `INFRA`엔 `in_progress` 없음·동명 컴포넌트 없음·`v1.0` 있음
- **When** preview → 마법사가 "상태 선택 필요"(대상 상태 목록+제안), 컴포넌트 "매핑/제거", `v1.0` 자동매핑 제안 표시 → 사용자 선택 → 이동
- **Then** move에 매핑 페이로드 전달 → 성공

### S3. 서브태스크 동반 이동 (노드별 매핑)
- **Given** Story `ATLAS-12`가 서브태스크 `ATLAS-13`,`ATLAS-14` 보유
- **When** preview → 마법사가 **부모 + 각 자식 노드별 매핑 섹션**(`subtasks` 배열) 표시 → 노드별 확인 → 이동
- **Then** move에 `subtasks` 배열(자식별 매핑) 포함 → 성공 → `movedSubtasks` 토스트("3개 이슈 이동: INFRA-5/6/7")

### S4. 옛 키 redirect (D7 E2E)
- **Given** `ATLAS-12` → `INFRA-5` 이동 완료
- **When** 옛 키 URL `/issues/ATLAS-12` 접근
- **Then** 백엔드 308 → 새 키 `/issues/INFRA-5`로 이동(상세 정상 표시)

### S5. 오류 처리
- **Given** 이동 중 권한 없음(403)/OCC 충돌(409)/다단계 자식(422 `SUBTASK_HAS_OWN_SUBTASKS`)/비호환 상태 미선택(422 `INVALID_TARGET_STATE`)
- **When** move 호출
- **Then** 마법사가 해당 오류 메시지 표시(이동 안 됨, 마법사 유지)

## 기능 요구사항 (FR)

| ID | 요구사항 |
|---|---|
| FR-1 | 이슈 상세 페이지(`issues.$key.tsx`)에 "이동" 액션 진입점(메뉴/버튼). 권한(원본 UPDATE) 게이팅은 기존 권한 훅 재사용 |
| FR-2 | `MoveIssueDialog`(radix-ui Dialog 직접, AddMemberDialog 선례) — `useState`로 단계(step) 전환. **외부 라이브러리 추가 금지**(절대규칙 #17) |
| FR-3 | **Step 1 대상 선택**: 대상 프로젝트 키 직접 입력(프로젝트 목록 API 부재, FR-LK-01 선례). 입력 후 preview 호출 |
| FR-4 | **Step 2 매핑**: preview 응답 기준 — 루트 + `subtasks` 노드별 섹션. 노드별: 워크플로우 상태(`compatible=false`면 `targetStates`에서 select, 기본=`suggestedStateKey`), 컴포넌트/버전(`autoMapping` 기본값+대상목록 select 또는 제거), 커스텀필드(`removed` 표시·`requiredMissing` 입력) |
| FR-5 | **Step 3 확인 + 실행**: move 호출. 페이로드 = 루트 평면 필드 + `subtasks` 배열(모든 직접 자식, 자식 식별 `issueKey`). 자식 없으면 `subtasks=[]`(단건) |
| FR-6 | move 성공 → 새 키(`MoveResponse.issueKey`)로 상세 페이지 이동 + `movedSubtasks` 포함 토스트. 관련 쿼리 invalidate |
| FR-7 | `api/issues.ts`(또는 신규 `api/issue-move.ts`)에 `previewMove(key, targetProjectKey)` + `moveIssue(key, payload)` 함수 신규. **Zod 스키마는 백엔드 `MoveDtos`/`MovePreview` 1:1 미러**(invent 금지, 백엔드 grep) |
| FR-8 | TanStack Query 훅 — `useMovePreview`(또는 단계 진입 시 호출), `useMoveIssue`(mutation) |
| FR-9 | **D7 E2E**: (a) 단건 이동 + 새 키 이동, (b) 서브태스크 동반 이동 + movedSubtasks, (c) **옛 키 308 redirect → 새 키 상세**. MSW로 preview/move/redirect 핸들러 |

## 비기능 요구사항 (NFR)

- 의존성 추가 0 (radix-ui Dialog 기존, TanStack Query 기존, Zod 기존)
- Zod 스키마 백엔드 DTO 1:1 미러 — `@JsonInclude(NON_NULL)` ↔ nullable/optional 정합(백엔드 실측). 단위테스트는 vitest, 타입은 `pnpm typecheck`(tsconfig.app) 동반
- 마법사 단계 전환은 단일 Dialog 내 `useState`(별도 라우트/stepper 라이브러리 없음)
- i18n: 사용자 노출 문자열은 `ko` 로케일 키(하드코딩 금지), 값 콜론 종결 금지(ko.test 검증)
- WCAG: Dialog 포커스 트랩(radix 기본), 키보드 접근

## API 인터페이스 (소비 — 백엔드 1:1 미러)

### preview — `POST /api/v1/issues/{key}/move/preview`
요청 `{ targetProjectKey }`. 응답 `MovePreview`:
```jsonc
{
  "workflow":        { "compatible": bool, "targetStates": [{key,name}], "suggestedStateKey": string|null },
  "components":      { "current": [Component], "target": [Component], "autoMapping": { "<srcUuid>": "<targetUuid>|null" } },
  "affectsVersions": { /* VersionMappingSection 동형 */ },
  "fixVersions":     { /* 동형 */ },
  "customFields":    { "removed": [CustomFieldDefinition], "requiredMissing": [CustomFieldDefinition] },
  "subtasks": [ { "issueKey", "issueTypeKey": string|null, "workflow", "components", "affectsVersions", "fixVersions", "customFields" } ]
}
```
- `Component`/`Version`/`CustomFieldDefinition`/`WorkflowStateView` 타입은 **기존 프론트 타입 재사용**(FR-CM/FR-VR/FR-IS-10). 백엔드 형식과 대조.

### move — `POST /api/v1/issues/{key}/move`
요청 `MoveRequest`:
```jsonc
{
  "targetProjectKey", "expectedVersion", "targetStateKey": string|null, "targetStateIsDone": bool,
  "componentMapping": {"<srcUuid>":"<targetUuid>|null"}, "affectsVersionMapping": {...}, "fixVersionMapping": {...},
  "customFieldValues": {"<key>": any},
  "subtasks": [ { "issueKey", "expectedVersion", "targetStateKey", "targetStateIsDone", "componentMapping", "affectsVersionMapping", "fixVersionMapping", "customFieldValues" } ]
}
```
응답 `MoveResponse`: `{ "issueKey", "previousKey", "movedSubtasks": [{ "previousKey", "issueKey" }] }`

### 옛 키 redirect
- 백엔드 `GET /api/v1/issues/{oldKey}` → 308 + Location(새 키). 프론트는 SPA 내부 이동 처리(apiFetch 308 처리 또는 라우트 가드). D7 E2E로 검증.

## 컴포넌트 / 파일 구조

- `apps/web/src/api/issue-move.ts` (신규) — `previewMove`/`moveIssue` + Zod 스키마(`movePreviewSchema`/`moveResponseSchema`)
- `apps/web/src/api/issue-move.test.ts` (신규)
- `apps/web/src/hooks/use-issue-move.ts`(또는 기존 훅 디렉토리) — `useMoveIssue` mutation
- `apps/web/src/components/issues/MoveIssueDialog.tsx` (신규) — 마법사(step 1~3)
- `apps/web/src/components/issues/MoveIssueDialog.test.tsx` (신규)
- `apps/web/src/routes/issues.$key.tsx` (수정) — "이동" 액션 진입점
- `apps/web/tests/e2e/issue-move.spec.ts` (신규, qa-engineer) — D7

## 엣지 케이스

| EC | 상황 | 처리 |
|---|---|---|
| EC1 | 대상 == 원본 프로젝트 | move 422 `MOVE_SAME_PROJECT` → 오류 메시지 |
| EC2 | 대상 프로젝트 없음 | preview/move 404/422 → "대상 프로젝트를 찾을 수 없음" |
| EC3 | 권한 없음(원본 UPDATE/대상 CREATE) | 403 → "권한 없음". 진입점도 권한 게이팅 |
| EC4 | OCC 충돌 | 409 → "다른 변경 발생, 새로고침 후 재시도" |
| EC5 | 비호환 상태 미선택 | move 버튼 비활성(노드별 상태 미선택 시) + 서버 422 안전망 |
| EC6 | 필수 커스텀필드 미입력 | move 버튼 비활성 + 서버 422 안전망 |
| EC7 | 다단계 자식(자식의 자식) | move 422 `SUBTASK_HAS_OWN_SUBTASKS` → "다단계 서브태스크는 이동 불가" |
| EC8 | 옛 키 접근 | 308 → 새 키 상세(D7 E2E) |
| EC9 | 자식 많음 | subtasks 노드별 섹션 모두 렌더(스크롤), 모든 자식 매핑 자동 포함(불완전 매핑 422 회피) |

## 제약 조건

- 외부 라이브러리 추가 금지(절대규칙 #17). radix-ui Dialog + TanStack Query + Zod 기존.
- Zod 백엔드 1:1 미러(invent 금지). 백엔드 nullable → Zod `.nullable()`/`.optional()` 실측.
- 대상 프로젝트 = 키 직접 입력(목록 API 부재, FR-LK-01 선례). 향후 목록 API 생기면 드롭다운 교체.
- 토큰 localStorage 금지, CSRF 변이 헤더(X-XSRF-TOKEN) 기존 apiFetch 패턴.
- 백엔드 변경 없음(이미 #153/#154 머지). 순수 프론트 + E2E.

## 측정 가능한 완료 기준

- [ ] 이슈 상세 "이동" 액션 → 마법사 열림(권한 게이팅)
- [ ] Step1 대상 입력 → preview 호출 → Step2 노드별 매핑 폼(루트+subtasks)
- [ ] 비호환 워크플로우 상태 select·컴포넌트/버전 매핑·필수필드 입력 동작
- [ ] move 호출 페이로드가 노드별 매핑 정확(자식 issueKey 식별), 단건은 subtasks=[]
- [ ] 성공 → 새 키 상세 이동 + movedSubtasks 토스트
- [ ] EC1~EC7 오류 메시지, 비호환/필수필드 미충족 시 move 비활성
- [ ] Zod 스키마 백엔드 DTO 1:1 미러(typecheck 통과)
- [ ] D7 E2E: 단건 이동 + 서브태스크 동반 + 옛 키 308 redirect
- [ ] FR-MV-01 D6·D7 [x] → **FR-MV-01 전체 종료**

## Brainstorming Check (← Phase B 채움)
