# ADR — FR-UX-07 신설 + 활성 프로젝트(Active Project) 컨텍스트 모델 + 기능 단위 분할

> 날짜: 2026-07-28 (D1~D5) · 2026-07-29 (D6 추가)
> 상태: 결정됨 (Maxi 확정)
> 관련 FR: FR-UX-07 (활성 프로젝트 컨텍스트 — D6 에서 범위 축소) · FR-UX-08~14 (D6 에서 신설) · FR-UX-06 (시각계층 개편, 완료) · FR-UX-05 (키맵 — 컨텍스트 단축키 이연) · FR-PF-01/02 (환경설정·시작페이지)
> 관련 slug: `fr-ux-07-active-project-key`

## 맥락

FR-UX-06 22 PR 체인(#308)이 **시각 계층**(ADS v2 토큰 70종·프리미티브 24종·`_shell` 셸 구조)을 완결했으나 **인터랙션 계층**은 손대지 않았다. Maxi 지적 — "UI/UX가 지라 클라우드 사용성과 많이 다르다".

조사 중 개편 이전의 **실동작 결함**이 먼저 나왔다. `DEFAULT_PROJECT_KEY = 'ATLAS'` 하드코딩(`routes/issues.index.tsx:47,788` · `routes/search.tsx:21,478`) 때문에 이슈 목록으로 가는 **진입로 4개**(사이드바 "이슈" · 명령 팔레트 "내 이슈" · 로그인 후 시작 페이지 · 단축키 `g i`)가 전부 ATLAS 프로젝트만 보여준다. `issuesIndexRoute.validateSearch`(`router.ts:126-158`)에 `projectKey`가 없어 URL로 바꿀 수단조차 없다.

세 가지 미결 결정이 이 PR의 형태를 좌우했다.

1. **FR 등록** — 27 PR 로드맵을 FR-UX-06 확장으로 볼 것인가, 신규 FR로 볼 것인가.
2. **"활성 프로젝트"가 도메인 개념인가** — 그렇다면 무엇이 그것을 결정하는가.
3. **영속 위치** — 서버(`user_preferences`)인가 클라이언트(localStorage)인가.

## 결정

### D1. FR-UX-07 신설. FR-UX-06 확장은 불가

> **★ 범위 정정 (D6, 2026-07-29).** 이 결정의 **신설 판단은 유지**되나, "27 PR 로드맵 전체를 FR-UX-07 하나에 담는다" 는 부분은 **틀렸다**. FR-UX-07 은 F1(활성 프로젝트 컨텍스트) 하나로 좁혀지고 나머지는 FR-UX-08~14 로 분리됐다. 당시 적었던 총수 증가분은 하나였으나 정본은 **D6 의 일곱 증가**(132 → 139)다. **아래 본문은 2026-07-28 시점의 기록이므로 그대로 둔다.**

**근거.**
- `docs/plan/product/personalization.md` §4.4 FR-UX-06의 D1~D7이 **전량 `[x]`** 이고, **D4 백엔드 = "없음 (ADR D5 — 물리 `apps/web`)"** 으로 닫혀 있다. 이번 로드맵은 백엔드 B1(생성 필드)·B2(카드 필드)를 포함하므로 그 판정과 정면 충돌한다.
- 완료 FR의 D단계를 `[x]` → `[~]` 로 되돌리면 `docs/progress.html` 진척률이 역행하고, FR-UX-06을 FR로 만든 이유("진척 가시성 우선")를 스스로 무너뜨린다.
- **신설 근거가 이미 문서에 있다** — `personalization.md:142`(FR-UX-05 §4.3): *"컨텍스트 의존 단축키(`j/k/e/m/s`)는 **후속 FR로 제외**(Maxi 결정 2026-07-05)"*. 이번이 그 후속이다.

**선점 검증(2026-07-28).** `docs/` 전체 `FR-UX-07` **0건** · 열린 PR 0 · worktree 0 · `verify-master-plan.sh` 기준선 EXIT=0 (131/131). 문서에만 있는 `FR-AU-12`·`FR-IS-12`는 **선점 아님** — 전자는 `FR-AU-12 ≡ FR-PM-02`로 흡수 확정(해당 ADR 정정 단락), 후자는 실제로 FR-MV-02로 착지. 둘 다 fr-index 미등록.

> 선점은 `fr-index.md`가 아니라 `docs/specs/`에서 일어난다(learnings 2026-07-17). 그래서 fr-index 뿐 아니라 specs·plans·열린 PR·worktree를 전수 확인했다.

### D2. 논리 BC = personalization / 물리 = `apps/web`

FR-UX-05 D4 · FR-UX-06 D5 선례를 그대로 승계한다. glossary가 이 패턴을 이미 정본화하고 있다 — **퀵 필터**(FR-UX-01) *"물리 BC=agile-planning·논리=personalization"*, **캘린더 피드 토큰**(FR-CA-02) *"personalization 논리 BC / identity-access 물리"*.

> `classify-task.ts`는 이 작업을 `type=backend`/`primary_bc=issue-tracking`으로 오분류했다(같은 오분류 3회째). 변경 파일이 전량 `apps/web`, `backend/` 0건임을 실측해 정정했다.

### D3. 활성 프로젝트(Active Project) = 4단 해소 순서

**활성 프로젝트**는 "사용자가 지금 작업 중인 프로젝트"다. 값은 저장된 상태 하나가 아니라 **해소 함수**로 정의한다.

```
① 현재 URL 의 projectKey        (경로 파라미터 /projects/$projectKey/* 또는 검색 파라미터 ?projectKey=)
② localStorage 마지막 저장값
③ 접근 가능한 첫 프로젝트        (GET /api/v1/projects 응답 순서)
④ 없으면 빈 상태                 (프로젝트 0개 — /projects 로 안내)
```

**근거.**
- **URL 이 최상위여야 하는 이유.** 링크 공유·뒤로가기·새로고침이 같은 화면을 재현해야 한다. 저장값이 URL을 이기면 공유한 링크가 받는 사람에게 다른 프로젝트를 연다.
- **URL 이 프로젝트를 담을 때 저장값을 갱신한다.** 보드(`/projects/ATLAS/board`)를 보다가 사이드바 "이슈"를 누르면 ATLAS 이슈가 나와야 한다 — 이게 "활성"의 의미다.
- ③은 Maxi 확정(2026-07-28). 첫 방문에 빈 화면 대신 곧장 이슈를 보여준다. 프로젝트가 하나뿐인 대다수 사용자에게 정확하다.

### D4. 영속 = localStorage. 서버 `user_preferences` 는 기각

키 `bts.active-project`. `hooks/use-sidebar-collapsed.ts:15-45`(zustand + fail-safe 3중 폴백)를 템플릿으로 복제한다.

**근거.**
- 백엔드 변경 0 — Maxi 결정 2("백엔드 = B1+B2만")와 일치.
- 활성 프로젝트는 **UI 선호값**이지 사용자 데이터가 아니다. `use-sidebar-collapsed`와 같은 성격이며 **절대 규칙 §1.18(토큰 localStorage 저장 금지)과 무관**하다 — 토큰도 PII도 저장하지 않는다.
- 값이 stale해도(프로젝트 삭제·권한 회수) 해소 순서 ③이 자동 복구한다. 서버 영속이면 stale 정리 로직이 별도로 필요하다.

**기각 — 서버 `user_preferences` 확장.**
`user_preferences`(V031 + V032)는 `theme`·`locale`·`date_format`·`start_page`를 담는 실재 테이블이고 PATCH API도 있어 **도메인상으로는 자연스러운 자리**다. 기기 간 동기화라는 실익도 있다. 그러나 Flyway 마이그레이션 + identity-access DTO/service/controller 변경이 필요해 결정 2를 넘는다. **후속 FR 후보로 남긴다** — 채택 시 해소 순서 ②의 소스만 교체되고 ①③④는 불변이라 폭발 반경이 작다.

### D5. `/issues` 는 v1 에서 프로젝트 스코프를 유지한다

지라의 이슈 내비게이터는 프로젝트를 가로지르지만 BTS는 v1에서 못 한다. `IssueApplicationService.kt:1021`이 `assertPermission(actor, BROWSE, IssueScope.Project(projectKey))`로 프로젝트 스코프를 강제하고, `UserCalendarLookupAdapter.kt:25-38`이 *"issue-tracking의 모든 visibility 술어는 `PROJECTS.KEY.eq(projectKey)` 단일 프로젝트 축으로 하드코딩되어 재사용 불가"* 를 명시한다.

프로젝트별 보안등급 집합을 **격리**해 OR 조립해야 하며 합집합으로 두면 fail-open 사고다. 이 작업(로드맵 B3)은 **보안 위험이 로드맵에서 유일**하고 비용이 가장 커 Maxi가 범위에서 제외했다. 따라서 v1의 답은 "프로젝트 무관 조회"가 아니라 **"프로젝트 전환을 쉽게"**(F12 프로젝트 스위처)다.

### D6. FR-UX-07 을 **기능 단위 여덟 개로 분할**한다 (총 132 → 139). 좁아진 FR-UX-07 은 PR #320 이 완결

> 2026-07-29 Maxi 확정. **D1 의 범위 판단을 정정한다** — 신설 자체는 유지하고, 하나로 묶은 범위만 쪼갠다.

**무엇이 잘못됐나.** D1 은 27 PR 로드맵 **전체**를 FR-UX-07 하나에 담았다. 그 결과 `docs/plan/product/personalization.md` §4.5 의 D 마커가 `[~] [~] [x] [ ] [ ] [~] [~]` 로 **세 상태가 섞였고**, D1(도메인) 본문에 *"미착수 — 27 PR 로드맵 잔여"* 가 붙었다.

**D1 이 「절반 완료」인 상태는 성립하지 않는다.** 도메인 정리는 한 기능에 대해 끝났거나 안 끝났거나 둘 중 하나다. `[~]` 가 붙는 순간 그 마커는 "무엇이 남았는지" 를 더 이상 말하지 못하고, `docs/progress.html` 진척률·`README §1` 진척 열도 의미를 잃는다. **D 마커는 완주 단위에 붙어야 한다** — 그게 분할의 근거다.

#### ★ FR-UX-06 은 왜 괜찮았고 FR-UX-07 은 왜 아닌가

FR-UX-06 도 22 PR 짜리 단일 FR 이었고 D1~D7 이 **전량 `[x]` 로 함께 닫혔다**(`personalization.md` §4.4). 표면만 보면 *"22 PR 은 되고 27 PR 은 안 된다"* 는 임의 기준으로 읽히므로, **판별 기준을 여기 못박는다** — 후속 세션이 "그때는 되고 지금은 안 되는 이유" 를 재추론하지 않도록.

| 축 | FR-UX-06 (22 PR — 단일 FR 로 **정당**) | FR-UX-07 원안 (27 PR — **분할 필요**) |
|---|---|---|
| D1 도메인 | **한 벌** — ADS 토큰 + `ui/*` 프리미티브 레지스트리 하나가 22 PR 전부의 도메인 | **다섯 벌 이상** — 활성 프로젝트 · 생성 필드 · 단축키 레지스트리 · 인라인 편집 · 스프린트/백로그가 서로 무관 |
| D2 명세 | **한 벌** — 디자인 스펙 `docs/design/fr-ux-06-jira-redesign.md` 14섹션이 22 PR 전부를 덮는다 | **PR 마다 별벌** — 활성 프로젝트 스펙(S1~S8·E1~E10)은 단축키에 대해 **한 글자도 말하지 않는다** |
| ADR | **한 벌** — 이 문서 형식의 D1~D8 이 22 PR 의 공통 전제 | **기능마다 별개 결정** — 단축키는 `KeymapAction` enum·`user_keymap` CHECK 제약과 얽히고, 활성 프로젝트는 전혀 안 얽힌다 |
| D4 백엔드 | **「없음」 하나로 닫힌다** (§D5 논리≠물리) | **B1·B2 가 서로 다른 기능에 속한다** — 하나의 「있음/없음」 판정으로 못 닫는다 |
| 성격 | **하나의 캠페인** — 같은 디자인 시스템을 전 화면에 바르는 일 | **독립 기능 여덟 개의 묶음** — 공통점이 "지라스럽게" 라는 지향뿐 |

**판별식.** *"이 FR 의 D1(도메인)·D2(명세)를 **한 벌**로 쓸 수 있는가."*
쓸 수 있으면 단일 FR 이다. *"기능 A 의 도메인은 끝났고 기능 B 는 아직"* 이 성립하면 **분할해야 한다**. FR-UX-06 은 전자, FR-UX-07 원안은 후자였다.

#### 분할 매핑

로드맵 정본은 `~/.claude/plans/ui-ux-sorted-kay.md` 이고 아래 `F«n»`·`B«n»` 은 그 §PR 체인의 번호다.

| FR | 이름 | 승계 PR | PR 수 |
|---|---|---|---|
| **FR-UX-07** | 활성 프로젝트 컨텍스트 | F1 | 1 — **PR #320 이 완결** |
| **FR-UX-08** | 프로젝트 전환 · 최근 항목 · 내 작업 | F12, F17 | 2 |
| **FR-UX-09** | 이슈 생성 흐름 (모달 · 진입점) | F2, F3, **B1** | 3 |
| **FR-UX-10** | 컨텍스트 의존 단축키 | F10, F11 | 2 |
| **FR-UX-11** | 인라인 편집 | F8, F9 | 2 |
| **FR-UX-12** | 검색 진입 (커맨드 팔레트 · 전역 검색) | F4, F13 | 2 |
| **FR-UX-13** | 백로그 사용성 | F5, F15, F16 | 3 |
| **FR-UX-14** | 이슈 카드 밀도 | F14, **B2** | 2 |
| *(FR 아님 — chore)* | F6 도움말 배선(FR-UX-05 결손) · F7 댓글 기본탭(FR-CO 결손) · F18~F25 Tier 3 마감(FR-UX-06 결손) | — | 10 |

FR 승계 17 + chore 10 = **27 PR**. **로드맵 총량은 바뀌지 않는다** — 분할은 추적 단위를 나눈 것이지 일감을 늘린 것이 아니다.

> **개수는 직접 셌다.** 로드맵 §PR 체인의 항목은 `F1`~`F25` 스물다섯 개 + `B1`·`B2` 두 개 = 스물일곱 개다. 위 표의 FR 승계분은 `F1~F5`·`F8~F17`(열다섯) + `B1`·`B2`(둘) = 열일곱, chore 는 `F6`·`F7`(둘) + `F18~F25`(여덟) = 열이다. 두 값의 합이 스물일곱으로 원본과 일치한다.

#### 파생 결정 두 건 (2026-07-28 Maxi 원 결정을 승계·정정)

**파생 1 — 백엔드 B1·B2 가 chore 에서 D4 로 승격한다. (정정)**

2026-07-28 Maxi 결정 #3 은 *"B1/B2 는 기존 FR 결손 봉합이라 chore"* 였다. 그 판단은 **분할 이전 전제 위에서만** 맞다 — 27 PR 을 한 FR 에 담으면 B1·B2 를 D4 에 넣을 자리가 없어 chore 로 밀 수밖에 없었다.

분할 후에는 **B1 = FR-UX-09 의 D4(백엔드) · B2 = FR-UX-14 의 D4** 다. 「백엔드 없음」으로 비던 칸이 실제 내용으로 채워지는 쪽이 정확하다. 이 정정은 **조용히 하지 않고 여기 명시한다** — 결정 #3 을 나중에 다시 읽는 사람이 "chore 라고 적혀 있는데 왜 D4 에 있나" 로 되돌아가지 않도록.

**파생 2 — FR-UX-10 은 정본이 이미 예약해 둔 범위다.**

`personalization.md` §4.3(FR-UX-05)이 *"컨텍스트 의존 단축키(`j/k/e/m/s`)는 **후속 FR로 제외**(Maxi 결정 2026-07-05)"* 로 이 범위를 **명시 이연**해 뒀다. D1 은 이 문장을 FR-UX-07 신설의 근거로 인용했으나, 분할 후 그 이연의 **정확한 승계자는 FR-UX-10** 이다. `personalization.md` §4 안의 두 절(FR-UX-05 절 ↔ FR-UX-10 절)에 상호참조를 남긴다.

#### 좁아진 FR-UX-07 의 D 마커 — 전량 `[x]`

PR #320 이 이 범위를 완결한다. 아래는 마커마다의 근거다.

| D | 상태 | 근거 |
|---|---|---|
| D1 도메인 | `[x]` | 이 ADR §D1~§D5 — 활성 프로젝트 4단 해소 순서 확정 |
| D2 명세 | `[x]` | `docs/specs/2026-07-28-fr-ux-07-active-project-key.md` — S1~S8 · FR1~FR9 · NFR1~NFR5 · E1~E10 · L1~L6 |
| D3 데이터 모델 | `[x]` | 없음 — 클라이언트 localStorage 만, Flyway 마이그레이션 0 |
| D4 백엔드 | `[x]` | 없음 — §D2 논리≠물리. B1·B2 는 위 분할로 FR-UX-09·FR-UX-14 로 이관됐다 |
| D5 백엔드 테스트 | `[x]` | 해당 없음 (백엔드 변경 0) |
| D6 프론트 UI | `[x]` | `DEFAULT_PROJECT_KEY` 프로덕션 0건 · 4단 해소 훅 3종 · 게이트 · 라우트 2곳 |
| D7 E2E | `[x]` | `apps/web/e2e/active-project.spec.ts` 13건 통과 (S1~S8 + E2 + E8 + B2 + NFR5) |

> **★ D7 의 알려진 한계 한 건.** S8(시작 페이지 파생 결함)은 MSW 목(mock 서버)이 `GET /api/v1/issues` 를 프로젝트로 **필터링하지 않으므로** 「빈 화면 → 채워짐」을 문자 그대로 재현하지 못한다. 대신 결함의 정확한 원인(`DEFAULT_PROJECT_KEY = 'ATLAS'` 하드코딩)이 사라졌다는 **관측 가능한 증거** — 재로그인 후 실제 발사되는 요청의 `projectKey` 쿼리가 활성 프로젝트(`MIDDLE`)로 나가는지 — 를 네트워크 인터셉트로 확인한다. 완전 검증에는 백엔드의 실 project-scoping 이 필요하고 이는 e2e 스펙 약 120건에 영향을 주므로 범위 밖이다. 스펙 §10 L6 에 등재.

## 결과

**좋아지는 것.** ATLAS 외 프로젝트 사용자가 이슈 목록·검색을 쓸 수 있게 된다. `?projectKey=` 링크 공유가 가능해진다. FR-PF-02 `my_issues` 시작 페이지가 ATLAS에 담당 이슈가 없는 사용자에게 빈 화면을 주던 파생 결함이 해소된다.

**감수하는 것.** 활성 프로젝트가 기기 간에 안 넘어간다(D4 기각 사유 참조). `/issues`는 여전히 한 프로젝트만 본다(D5).

**남는 것.** cross-project 이슈 조회(B3) · 서버 영속 활성 프로젝트 · 프로젝트 스위처(F12 → D6 분할로 FR-UX-08 소관).

**D6 분할의 결과.** 진척 마커가 완주 단위로 돌아온다 — FR-UX-07 은 전량 `[x]` 로 닫히고, 신설 일곱은 전량 `[ ]`(등록만, 미착수)로 열려 **한 FR 안에 세 상태가 섞이는 일이 사라진다**. 대신 총수가 일곱 늘어 전수 동기화 대상 아홉 종을 그만큼 더 손봐야 하고, 로드맵 정본(`ui-ux-sorted-kay.md`)의 `F«n»` 번호와 FR ID 사이에 **번역표가 하나 생긴다**(위 분할 매핑표가 그 정본이다).

## 관련

- 로드맵 정본 — `~/.claude/plans/ui-ux-sorted-kay.md`
- [FR-UX-06 Jira 재설계 ADR](2026-07-17-fr-ux-06-jira-redesign.md) (§D5 논리≠물리)
- [FR-PR-01 사용자 프로필 배치](2026-07-05-fr-pr-01-user-profile-placement.md) (§D1 동일 패턴)
- [FR-UX-05 키맵](2026-07-05-fr-ux-05-keymap.md) (§4.3 "컨텍스트 단축키는 후속 FR" — D1 근거. **분할 후 그 이연의 승계자는 FR-UX-10** — D6 파생 2)
- 분할된 FR 의 정본 등록 위치 — `docs/plan/product/personalization.md` §4 (UX 편의) · `docs/plan/fr-index.md` · `docs/sdd/02-requirements.md`
