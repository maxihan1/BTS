# FR-SL-02 D6 — 사용자 본인 Slack 계정 연결/해제 페이지와 엔드포인트

> slug: fr-sl-02-d6-slack
> type: feature
> agent: frontend-engineer (+ backend-engineer view-layer 엔드포인트)
> BC: slack-integration (`com.bts.slack`) — classify의 identity-access 오분류 정정
> 생성: 2026-07-10

## Brief

FR-SL-02 D6. 일반 사용자가 **본인 Slack 계정을 직접 연결/해제**하는 설정 페이지 + 그것을 노출하는 view-layer 엔드포인트.

- 이미 존재(재사용): `SlackUserMappingService`(link/unlink, `com.bts.slack.application`), `SlackUserMappingRepository`, `SlackUserMapping` 도메인, `user_slack_mapping(user_id, slack_user_id, team_id)` 테이블(V701)
- 신설: link/unlink를 노출하는 me-scope 엔드포인트 + 사용자용 매핑 페이지(apps/web)
- 미결: 연결 UX 스코프(Slack OAuth `identity` 스코프로 사용자 확인 vs Slack user ID 직접 입력) — /bts-spec 단계에서 옵션으로 결정
- 선례: FR-SL-01 D6(#247) 관리자 `/admin/slack` 페이지 + view-layer JSON 엔드포인트 2종. Bearer vs OAuth 전체페이지이동 충돌 해소 패턴 참조.

원문. FR-SL-02 D6 — 사용자 본인 Slack 계정 연결/해제 페이지와 엔드포인트. 백엔드 SlackUserMappingService(link/unlink)와 user_slack_mapping 테이블(V701)은 이미 존재. 신설: link/unlink를 노출하는 view-layer 엔드포인트 + 일반 사용자용 매핑 페이지. 연결 UX 스코프(Slack OAuth identity 스코프 vs 직접 입력)는 spec 단계에서 옵션으로 결정.

## 도메인 정리

- **BC**: slack-integration (`com.bts.slack`). classify의 `identity-access`는 "계정" 키워드 오분류 — 정정.
- **영향 엔티티**: `SlackUserMapping`(기존, `com.bts.slack.domain`). **신규 엔티티 없음**. `user_slack_mapping(user_id PK, slack_user_id, team_id, linked_at)` V701 재사용.
- **재사용 서비스 계약**: `SlackUserMappingService.link(userId, slackUserId, teamId)`(멱등 upsert) · `unlink(userId)`(하드삭제) · `resolveByUserId(userId)`. KDoc이 명시적으로 "D6 연결 플로우(웹 레이어)가 link/unlink 호출"로 이 PR을 예정.
- **용어 주의 (혼동 차단)**: glossary의 **"계정 연결(Account Linking, FR-AU-08)"** 은 로그인용 외부 신원(LDAP/SAML/OIDC) 연결이라 **이번 것과 다른 개념**. 이번은 "알림 수신 대상 Slack 사용자 매핑"이지 로그인 수단이 아님. 새 용어 후보 **"Slack 사용자 매핑 (Slack User Mapping)"** 으로 분리(Maxi 승인 후 glossary 추가). domain 노트 line 18 "SlackUser (Atlas user ↔ Slack user 매핑, FR-SL-02 예정)"의 실체.
- **기존 결정 충돌**: 없음. FR-SL-02 ADR line 60이 "D6 매핑 UI + D7 E2E = 후속 PR"로 이 작업을 예약(재논의 아님).
- **관련 ADR**: [[decisions/2026-07-10-fr-sl-02-slack-notification-delivery]](재사용) · [[decisions/2026-07-08-fr-sl-01-d6-slack-connect-page]](Bearer↔OAuth 전체페이지이동 충돌 해소 패턴 선례).

### ★ spec으로 넘길 핵심 결정 — 연결 UX (웹 레이어가 slackUserId+teamId를 얻는 방법)

`link()`가 raw `slackUserId`(`U…`)+`teamId`를 요구 → 사용자가 이걸 어떻게 얻나가 D6의 본질. 3안, 인프라 비용/보안 차이 큼.

- **A. Sign in with Slack (사용자 신원 OAuth)** — 사용자가 "연결" 클릭 → Slack OAuth(user `identity` 스코프) → 콜백이 인증된 slack_user_id+team_id 회수. **소유권 검증됨(오배송 0)**, domain 노트 line 26 의도 부합. 비용: 봇 install OAuth와 별개인 **신규 사용자 OAuth 흐름** 신설(콜백·서명 state·Bearer↔리다이렉트 충돌 #247 패턴).
- **B. Slack user ID 직접 입력** — 폼에 `U…` 입력 → 기존 `link()` 호출. **최소 비용**(폼+엔드포인트만). 단점: 소유권 미검증 → 오타 시 **타인에게 DM 오배송 위험**, UX 나쁨(사용자가 자기 `U…` 모름).
- **C. 이메일 자동 해석 (봇 `users.lookupByEmail`)** — "연결" 클릭 → 백엔드가 BTS 이메일로 Slack 사용자 조회 → slack_user_id 자동 매핑. UX 최상(원클릭)·이메일 일치로 검증. 비용: 봇 스코프 `users:read.email` 추가 → **워크스페이스 재설치 필요**, BTS 이메일==Slack 이메일 가정.

→ spec Phase A(office-hours/AskUserQuestion)에서 Maxi 결정.

## 스펙

전체 스펙. [docs/specs/2026-07-10-fr-sl-02-d6-slack.md](../specs/2026-07-10-fr-sl-02-d6-slack.md)

**연결 UX 결정(Maxi) = C. 이메일 자동해석.**

핵심 3줄 요약.
- "Slack 연결" 클릭 → 백엔드가 본인 BTS 이메일로 `users.lookupByEmail`(봇 토큰) → slack_user_id+team_id 자동 매핑. 사용자는 자기 Slack ID 몰라도 되고 이메일 일치로 소유권 검증(오배송 0).
- me-scope 엔드포인트 3종(GET/POST/DELETE `/api/v1/slack/me/connection`), JWT-only(PAT 401). OAuth 왕복 없음 → #247 Bearer↔리다이렉트 충돌 소멸.
- 재사용: `SlackUserMappingService`·`UserLookupPort.findEmailById`(이미 배선, 신규 cross-BC 포트 0)·`user_slack_mapping`(V701, 신규 마이그레이션 0)·SDK `users.lookupByEmail`(slack-api-client 1.45.4).

deviation. 봇 스코프 `users:read.email` 추가 → 기존 설치 1회 재연결 운영 의존(런북). 범위: D6+D7(E2E) 동시.

## Brainstorming Check

✅ 통과 (1회, 코드-근거 적대적 자기점검). 부각(BLOCKER 아님): scope 추가 재연결 의존은 EC4 409로 graceful 안내. 후속: 이메일 불일치 수동입력 폴백·연결 handle 표시.

## Plan

연결 흐름(백엔드). `SlackActorExtractor.extract()` → `UserLookupPort.findEmailById(userId)`(null→422) → `SlackInstallRepository.findCurrentInstallation()`(null→409) → `SlackBotTokenResolver.resolve(teamId)` → `SlackUserLookupClient.lookupByEmail(botToken, email)`(MissingScope→409·NotFound→404·Transient→503) → `SlackUserMappingService.link(userId, slackUserId, teamId)`. 외부 HTTP는 tx 밖, link()만 트랜잭션.

### Task 1. SlackUserLookupClient — users.lookupByEmail 래퍼

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/slack-integration/src/main/kotlin/com/bts/slack/message/SlackUserLookupClient.kt`, `backend/modules/slack-integration/src/test/kotlin/com/bts/slack/message/SlackUserLookupClientTest.kt`]
- depends-on: []

**RED**: mock `MethodsClient`로 `usersLookupByEmail` 성공→`Found(slackUserId, teamId)`, `users_not_found`→`NotFound`, `missing_scope`→`MissingScope`, 429/IOException→`Transient` 분류 검증. 봇 토큰이 reason/로그에 없음 단언.
**GREEN**: sealed `SlackUserLookupResult`(Found/NotFound/MissingScope/Transient) + `lookupByEmail(botToken, email)`. `MethodsClient` 주입 기본값 `Slack.getInstance().methods()`(부팅 안전, SlackMessageClient 동형).
**REFACTOR**: 오류코드 분기 상수화 + KDoc(봇 토큰 3중 미노출 명시).
**검증**: `./gradlew :backend:modules:slack-integration:test --tests SlackUserLookupClientTest`

### Task 2. DEFAULT_SLACK_SCOPES += users:read.email

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/slack-integration/src/main/kotlin/com/bts/slack/config/SlackProperties.kt`, `backend/modules/slack-integration/src/test/kotlin/com/bts/slack/config/SlackScopesTest.kt`]
- depends-on: []

**RED**: `DEFAULT_SLACK_SCOPES`가 `users:read.email`을 포함하고 기존 6스코프도 보존하는지 검증(신규 test).
**GREEN**: 상수에 `,users:read.email` 추가.
**REFACTOR**: KDoc에 "lookupByEmail용, 기존 설치 재연결 필요" 주석.
**검증**: `./gradlew :backend:modules:slack-integration:test --tests SlackScopesTest`

### Task 3. SlackUserConnectionService — 오케스트레이터(email→lookup→link)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/slack-integration/src/main/kotlin/com/bts/slack/application/SlackUserConnectionService.kt`, `backend/modules/slack-integration/src/main/kotlin/com/bts/slack/application/SlackConnectionExceptions.kt`, `backend/modules/slack-integration/src/test/kotlin/com/bts/slack/application/SlackUserConnectionServiceTest.kt`]
- depends-on: [1]

**RED**: mockk(`UserLookupPort`·`SlackInstallRepository`·`SlackBotTokenResolver`·`SlackUserLookupClient`·`SlackUserMappingService`)로 — happy(link 호출·teamId=lookup 결과)·email null→`EmailUnavailableException`·install null→`WorkspaceNotInstalledException`·MissingScope→`SlackScopeMissingException`·NotFound→`SlackUserNotFoundException`·Transient→`SlackTemporarilyUnavailableException`·getStatus(연결/미연결)·disconnect(unlink 위임) 검증. **외부 lookup이 link()보다 먼저**(tx 밖) 단언.
**GREEN**: `connect(userId): ConnectionStatus`·`getStatus(userId): ConnectionStatus`·`disconnect(userId)`. 예외 타입 정의. `@Transactional` 미부착(외부 HTTP 포함)—link()의 내부 @Transactional만 사용(SlackInstallService CONCERN-1 교훈).
**REFACTOR**: KDoc·ConnectionStatus VO(connected/workspaceName/linkedAt).
**검증**: `./gradlew :backend:modules:slack-integration:test --tests SlackUserConnectionServiceTest`

### Task 4. SlackConnectionController + ExceptionHandler — me-scope GET/POST/DELETE

**메타**.
- agent: `backend-engineer` (권한/me-scope는 security-engineer 리뷰)
- files: [`backend/modules/slack-integration/src/main/kotlin/com/bts/slack/web/SlackConnectionController.kt`, `backend/modules/slack-integration/src/main/kotlin/com/bts/slack/web/SlackConnectionExceptionHandler.kt`, `backend/modules/slack-integration/src/main/kotlin/com/bts/slack/web/SlackConnectionResponses.kt`, `backend/modules/slack-integration/src/test/kotlin/com/bts/slack/web/SlackConnectionControllerTest.kt`]
- depends-on: [3]

**RED**: `@WebMvcTest` 슬라이스(SlackUserConnectionService mockk) — GET 상태 200·POST 연결 200·DELETE 200·예외별 상태매핑(422/409/409/404/503). `SlackActorExtractor.extract()` 경유 미인증 401.
**GREEN**: 컨트롤러(`/api/v1/slack/me/connection` GET/POST/DELETE)·응답 DTO(connected/workspaceName/linkedAt, slack_user_id/email 미포함)·로컬 `@ExceptionHandler`(컨트롤러 패키지 한정, 403→500 변질 방지 교훈). PAT는 JWT subject 부재로 401(SlackActorExtractor).
**REFACTOR**: 에러코드 enum·KDoc.
**검증**: `./gradlew :backend:modules:slack-integration:test --tests SlackConnectionControllerTest`

### Task 5. 백엔드 통합테스트(test-boot) — 전 흐름 + 미노출 negative-probe

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/slack-integration/src/test/kotlin/com/bts/slack/web/SlackConnectionIntegrationTest.kt`, `backend/modules/slack-integration/src/test/kotlin/com/bts/slack/config/StubUserLookupPort.kt`(email 반환 추가)]
- depends-on: [2, 4]

**RED**: Testcontainers 부팅 — 연결(happy)/해제/재연결(멱등)/미설치 409/scope부족 409/이메일미발견 404/email null 422/PAT 401/미인증 401. `SlackUserLookupClient`는 mock @Bean(외부 Slack 호출 대체). negative-probe(응답·로그에 봇토큰·이메일·slack_user_id 부재). StubUserLookupPort가 `findEmailById` 반환하도록 확장.
**GREEN**: 배선 조정(mock lookup client @Bean 같은 task—new-crossbc-dep 회귀 회피는 신규포트0이라 무관하나 mock client는 필요).
**검증**: `./gradlew :backend:modules:slack-integration:test --tests SlackConnectionIntegrationTest`

### Task 6. 프론트 api/slack.ts 확장 — 연결 상태/연결/해제 + Zod

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/slack.ts`, `apps/web/src/api/slack.test.ts`]
- depends-on: []

**RED**: `getMyConnection`/`connectSlack`/`disconnectSlack` + `SlackConnectionSchema`(connected/workspaceName nullable/linkedAt nullable) 파싱·apiFetch(Bearer) 호출 검증. 오류 응답 코드 매핑.
**GREEN**: 함수 3종 + Zod 스키마 추가(#247 admin 함수와 같은 파일 공존).
**REFACTOR**: 타입 export.
**검증**: `pnpm --filter web test -- slack.test`

### Task 7. SlackUserConnectionCard 컴포넌트

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/settings/SlackUserConnectionCard.tsx`, `apps/web/src/components/settings/SlackUserConnectionCard.test.tsx`]
- depends-on: [6]

**RED**: 미연결→"Slack 연결" 버튼·연결됨→워크스페이스명+linkedAt+"연결 해제"·오류 배너(미설치/재연결필요/이메일미발견 각 메시지)·로딩/에러 role=alert 검증(api 모듈 mock).
**GREEN**: 카드 컴포넌트(SlackConnectionCard 비주얼 패턴 차용, 사용자 스코프). useMutation onSuccess→상태 invalidate.
**REFACTOR**: 메시지 상수화.
**검증**: `pnpm --filter web test -- SlackUserConnectionCard`

### Task 8. /settings/slack 라우트 + router 등록

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/settings.slack.tsx`, `apps/web/src/router.ts`, `apps/web/src/routes/__tests__/settings.slack.test.tsx`]
- depends-on: [7]

**RED**: 페이지가 `SlackUserConnectionCard` 렌더·requireAuth 가드. router에 `settingsSlackRoute` 등록(라우트 수 +1, 상단 주석 카운트 갱신).
**GREEN**: page + RouteAdapter(code-based, router.ts 수동 등록 — learnings 선례). settings 사이드 링크(있으면).
**REFACTOR**: —
**검증**: `pnpm --filter web test -- settings.slack`

### Task 9. MSW slack-user-connection 핸들러(시드가능 stateful + 오류 토글)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/mocks/slack-user-connection-handlers.ts`, `apps/web/src/mocks/handlers.ts`]
- depends-on: []

**RED/GREEN**: GET/POST/DELETE `/api/v1/slack/me/connection` stateful mock(연결/해제 상태 공유 store·오류 시나리오 localStorage 토글: not-installed/scope-missing/not-found — msw-derived-behavior-shared-store 선례). aggregator 등록.
**검증**: E2E(T10)에서 소비.

### Task 10. E2E — 연결/해제/오류

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/slack-connect.spec.ts`]
- depends-on: [8, 9]

**시나리오**: 연결 happy(버튼→"연결됨" 배너)·연결 해제·오류(이메일 미발견/미설치 배너). MSW 토글로 오류 유도. 기존 settings E2E 무회귀 확인.
**검증**: `pnpm --filter web test:e2e -- slack-connect`

### Task 11. 문서 전수 동기화 + ADR

**메타**.
- agent: `backend-engineer`
- files: [`docs/plan/product/slack-integration.md`, `docs/decisions/2026-07-10-fr-sl-02-d6-slack-user-connection.md`]
- depends-on: []

**내용**: product D6·D7 `[x]` + deviation 주석(C 이메일 자동해석·users:read.email·me-scope 엔드포인트). ADR 신설(연결 UX=C 결정·3안 비교·scope 추가 운영의존·엔드포인트·PAT 401). FR 카운트 **불변 123**(D-step 완료). glossary "Slack 사용자 매핑" 항목은 merge 시 Obsidian 반영(Maxi 승인).
**검증**: `bash scripts/verify-master-plan.sh` (123/123)

## Plan 메타

- task 수: 11 (백엔드 5 + 프론트 4 + E2E 1 + 문서 1). D6+D7 풀스택 단일 PR(#247 18-task 선례 — 쪼개면 머지순서 의존만 생김).
- 예상 wave: 4 (W1: T1·T2·T6·T9·T11 병렬 → W2: T3·T7 → W3: T4·T8 → W4: T5·T10).
- 신규 마이그레이션 0 · 신규 cross-BC 포트 0(UserLookupPort 기존 배선) · SecurityConfig 무변경(/api/** 기본 인증).
- TDD 강제: yes. 추가 검증: ktlint·detekt(`--rerun-tasks` 캐시 false-green 방어)·typecheck(tsconfig.app)·vitest·playwright.
- 주의: SlackInstallService 생성자 **미변경**(plan-files-constructor-injection 회귀 회피 — 신규 서비스는 별 클래스).

## 리뷰 결과 (← /bts-review-plan 채움)
