# FR-SR-03 — 필터 저장 및 공유

> slug: fr-sr-03
> type: backend
> agent: backend-engineer
> 생성: 2026-06-26

## Brief

FR-SR-03 필터 저장 및 공유 — search-export-import BC, SDD §2.3.
classify: type=backend, agent=backend-engineer.

## 도메인 정리

- **BC**: search-export-import (7번째 모듈, FR-SR-02 신설). **영속성 첫 도입** — 현재 AQL 파서+컨트롤러만, DB 의존성(flyway/jOOQ/postgres/Testcontainers) 없음. SavedFilter가 이 모듈 첫 영속 엔티티.
- **영향 엔티티/테이블**:
  - `saved_filters(id, owner_id UUID, name, aql_query, project_key, created_at, updated_at, version)` (신규)
  - `saved_filter_shares(filter_id, share_type, target_id)` (신규, 공유 다대다)
  - `favorites`(FR-UX-02, notification BC) **재사용** — target_type=FILTER (즐겨찾기, 신규 컬럼 없음)
- **새 용어**: SavedFilter(저장된 필터), ShareType(PROJECT/GROUP/AUTHENTICATED — Jira식 대상 지정 공유)
- **Maxi 확정 결정 2건**:
  1. 즐겨찾기 = 기존 FR-UX-02 favorites 재사용 (지라 정합, 사용자별 북마크가 정확한 모델). saved_filters에 is_favorite 없음.
  2. 공유 = 특정 대상 지정까지 (PROJECT/GROUP/AUTHENTICATED). 단일 enum 아님.
- **SDD 10.3 deviation** (ADR 정정):
  - `isFavorite: Boolean` 인라인 → favorites 재사용 (공유 시 단일 컬럼 모델 붕괴)
  - `shareScope` 단일 enum + `shareTargetIds: List<Long>` → `saved_filter_shares` 조인 테이블
  - id/ownerId `Long` → UUID (BTS 전 모듈 관례)
- **신규 cross-BC 포트 필요** (가시성 판정):
  - `GroupMembershipPort` (identity-access 구현) — 그룹 공유 가시성. 그룹 멤버십은 현재 identity-access 내부(JdbcUserGroupRepository)에만 있음.
  - 프로젝트 멤버십 판정 — 기존 권한 인프라(IssueVisibilityPort/permission resolver) 재사용 우선 조사, 부재 시 신규 포트.
  - fail-closed (IssueVisibilityPort 선례, allow-all default 금지).
- **마이그레이션 범위**: search-export-import에 **V600~V699** 신규 할당 (DATA.md 표 전수 동기화 대상). 첫 마이그 `V600__saved_filters.sql`.
- **기존 결정 충돌**: 없음. SR-01/SR-02 엔드포인트 무변경, 검색 실행은 기존 IssueSearchPort 재사용.
- **관련 ADR**: [docs/decisions/2026-06-26-fr-sr-03-saved-filters.md](../decisions/2026-06-26-fr-sr-03-saved-filters.md) (생성됨)
- **범위/분할 권고**: 백엔드 D1~D5 / 프론트 D6~D7 분리 (FR-SR-01/02 선례). 백엔드도 부트스트랩+CRUD+PRIVATE → 공유 2분할 검토 (plan에서 확정).

## 스펙

전체 스펙. [docs/specs/2026-06-26-fr-sr-03-saved-filters.md](../specs/2026-06-26-fr-sr-03-saved-filters.md)

핵심 시나리오 요약.
- AQL을 이름 붙여 저장(`POST /api/v1/filters`) — 저장 시 AQL 구문검증(파서 재사용), project_key 박제, 이름 owner내 유니크
- 대상 지정 공유(PROJECT/GROUP/AUTHENTICATED) — saved_filter_shares 다대다, PRIVATE=공유 0행
- 가시성 4경로(owner/AUTHENTICATED/PROJECT멤버/GROUP소속), 비가시 404·비소유 수정 403
- 실행(`GET /filters/{id}/search`) — viewer 권한으로 AQL 재실행(IssueSearchPort 재사용, 권한상승 불가)
- 즐겨찾기는 기존 FR-UX-02 favorites 재사용(본 PR 백엔드 밖)

신규 cross-BC 포트. GroupMembershipPort(groupsOf) + 프로젝트 접근(canBrowse + accessibleProjectKeys). fail-closed.
데이터. V600 saved_filters + saved_filter_shares (search BC 첫 영속성 — flyway/jOOQ/Testcontainers 부트스트랩).

## Brainstorming Check

✅ 통과 (1회 iteration). gap 5건 보강 — 서버측 실행 엔드포인트 확정·목록 N+1 회피(accessibleProjectKeys)·project_key 불변·이름중복 409 dual-catch·shareType 검증. Maxi 결정 필요 항목 없음.

## Plan

> **범위 = PR1** (Maxi 2-PR 분할 확정). BC 영속성 부트스트랩 + SavedFilter CRUD(PRIVATE 전용) + 실행 엔드포인트.
> 가시성 = owner. 공유(saved_filter_shares)·멤버십 포트·가시성 4경로는 PR2(후속 /bts).
> 패키지 루트. `com.bts.search.savedfilter.{domain,application,persistence,web}`.

### Task 1. BC 영속성 부트스트랩 (build.gradle + V600 + jOOQ codegen + application.yml + DATA.md)

**메타**.
- agent: `db-engineer`
- files: [`backend/modules/search-export-import/build.gradle.kts`, `backend/modules/search-export-import/src/main/resources/db/migration/search-export-import/V600__saved_filters.sql`, `backend/modules/search-export-import/src/main/resources/db/codegen/init_codegen.sql`, `backend/modules/search-export-import/src/main/resources/application.yml`, `backend/modules/search-export-import/src/test/kotlin/com/bts/search/savedfilter/migration/SavedFiltersMigrationTest.kt`, `DATA.md`]
- depends-on: []

**RED** (eng-review NIT — 컴파일실패형 대신 information_schema 런타임 검증).
- 파일: `.../savedfilter/migration/SavedFiltersMigrationTest.kt`
- 테스트(Testcontainers postgres + flyway 적용): `saved_filters` 테이블 존재 + 컬럼(id/owner_id/name/aql_query/project_key/created_at/updated_at/version) + **타입 단언**(created_at/updated_at = `timestamptz` — DATA.md §4.1#4 타임존 강제) + `UNIQUE(owner_id,name)` 제약 존재 + 인덱스.
- 실패: 마이그레이션/모듈 DB 인프라 부재(flyway location 미설정·테이블 없음).

**GREEN**.
- build.gradle.kts에 flyway+jooq 플러그인/deps/codegen 설정 추가 (notification build.gradle 템플릿 그대로 — `jooqGenerator` classpath + Testcontainers `TC_INITSCRIPT=init_codegen.sql` + target package `com.bts.search.jooq` + `compileKotlin dependsOn generateJooq` + Testcontainers test deps). ※ `detekt-baseline.xml`은 이미 존재(무변경, files 미포함).
- `V600__saved_filters.sql` — 스펙 §데이터 모델의 `saved_filters` 테이블만(saved_filter_shares는 PR2). UUID PK + owner_id + name + aql_query + project_key + created_at/updated_at(timestamptz) + version + `UNIQUE(owner_id,name)` + owner/project 인덱스.
- `init_codegen.sql` — V600 DDL 미러 (jOOQ codegen 입력, 메모리 [[jooq-init-codegen-mirror]]).
- **`application.yml` 신규 생성** (모듈별 — `backend/src` 조립앱 없음, [[no-cross-bc-deployment-assembly]]). notification 템플릿: `spring.flyway.locations: classpath:db/migration/search-export-import` + `baseline-on-migrate: true` + `placeholder-replacement: false`.
- `DATA.md` 2곳 동기화. (a) 버전 범위 표에 `search-export-import | V600~V699 | V600` 행. (b) **§3 하드 삭제 허용 영역**에 `saved_filters` 행 추가 — "저장된 필터 삭제는 행 즉시 제거, 외부 영구 인용 약함. deleted_at 없음. ADR 2026-06-26-fr-sr-03-saved-filters (Maxi 확정)".

**REFACTOR**. build.gradle 주석 한국어 1줄 + init_codegen 헤더 주석.

**검증**: `./gradlew :backend:modules:search-export-import:generateJooq` 성공 + `:compileKotlin` 통과 + `SavedFiltersMigrationTest` green.

### Task 2. 도메인 — SavedFilter 모델 + 불변식

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/search-export-import/src/main/kotlin/com/bts/search/savedfilter/domain/SavedFilter.kt`, `backend/modules/search-export-import/src/test/kotlin/com/bts/search/savedfilter/domain/SavedFilterTest.kt`]
- depends-on: []

**RED**.
- 테스트: `create` 팩토리 — name blank→IllegalArgument, name>100→reject, aqlQuery blank→reject, aqlQuery>2000→reject, projectKey blank→reject. 정상값 생성 성공.
- 실패: `SavedFilter` 클래스 없음.

**GREEN**.
- `SavedFilter(id: UUID?, ownerId: UUID, name: String, aqlQuery: String, projectKey: String, createdAt: Instant?, updatedAt: Instant?, version: Long)` data class + `create` 팩토리(불변식 검증). 신규=id/createdAt/updatedAt null, 영속계층 채움.

**REFACTOR**. MAX_NAME_LENGTH/MAX_AQL_LENGTH 상수 + KDoc(L1 한국어 주석).

**검증**: `./gradlew :backend:modules:search-export-import:test --tests '*SavedFilterTest'`

### Task 3. Repository — port + jOOQ 구현 + Testcontainers 통합테스트

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/search-export-import/src/main/kotlin/com/bts/search/savedfilter/application/SavedFilterRepository.kt`, `backend/modules/search-export-import/src/main/kotlin/com/bts/search/savedfilter/persistence/JooqSavedFilterRepository.kt`, `backend/modules/search-export-import/src/test/kotlin/com/bts/search/savedfilter/persistence/JooqSavedFilterRepositoryIntegrationTest.kt`, `backend/modules/search-export-import/src/test/kotlin/com/bts/search/savedfilter/persistence/SearchPersistenceTestBase.kt`]
- depends-on: [1, 2]

**RED**.
- 테스트(Testcontainers, search BC 첫 통합테스트 — singleton container 패턴 [[concurrent-testcontainers-suite-flaky]]): save→findById 라운드트립, findByOwner 목록(타 owner 제외), update 시 version+1 + OCC(stale version→0행), delete 후 findById null. unique(owner,name) 위반 시 예외.
- 실패: Repository 미존재.

**GREEN**.
- `SavedFilterRepository` 포트(application) — save/findById/findByOwner/update(OCC)/deleteById.
- `JooqSavedFilterRepository` — jOOQ DSLContext. update는 `WHERE id=? AND version=?` 조건부 + `version+1, updated_at=now()` RETURNING(0행이면 OCC 충돌 신호 — **updated_at 동반 갱신** eng-review CONCERN). 이름중복은 native 예외 그대로 전파(서비스가 409 변환).
- `SearchPersistenceTestBase` — Testcontainers postgres(singleton 패턴) + flyway 적용 + DSLContext. ※ 테스트 수동 DSLContext는 JooqExceptionTranslator 미등록이라 Spring `DuplicateKeyException` 경로가 안 뜸 → 409 검증은 jOOQ-native SQLState 23505 catch가 안전망([[jooq-exception-translator-409-dependency]]). 운영은 Spring Boot JooqAutoConfiguration이 translator 자동 등록.

**REFACTOR**. SQL 상수 추출 + KDoc.

**검증**: `./gradlew :backend:modules:search-export-import:test --tests '*JooqSavedFilterRepositoryIntegrationTest'`

### Task 4. SavedFilterService — CRUD + AQL 구문검증 + 409 + OCC + owner 게이트

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/search-export-import/src/main/kotlin/com/bts/search/savedfilter/application/SavedFilterService.kt`, `backend/modules/search-export-import/src/main/kotlin/com/bts/search/savedfilter/application/SavedFilterExceptions.kt`, `backend/modules/search-export-import/src/test/kotlin/com/bts/search/savedfilter/application/SavedFilterServiceTest.kt`]
- depends-on: [2, 3]

**RED**.
- 테스트(mockk repo): create — AQL 구문오류(AqlLexer/Parser 실패)→ValidationException, 정상→저장. 이름중복(repo가 DuplicateKey/23505)→DuplicateNameException(409 신호). get — 타 owner→NotFound(404 신호), 본인→반환. list(owner). update — 비owner→Forbidden(403 신호), OCC 0행→Conflict(409 신호), projectKey 무시(기존 유지). delete — 비owner→Forbidden.
- 실패: Service 미존재.

**GREEN**.
- `SavedFilterService` (@Service @Transactional [[transaction-self-invocation-requires-new]] 주의) — create(파서 재사용 구문검증 + 이름중복 dual-catch [[jooq-exception-translator-409-dependency]] → 409), getByIdForOwner(404), listByOwner, update(owner 403 + OCC 409 + projectKey 불변 FR-10), delete(owner 403). owner 게이트=actorId 비교.
- `SavedFilterExceptions` — ValidationException/DuplicateNameException/NotFoundException/ForbiddenException/ConflictException(또는 ResponseStatusException 매핑).

**REFACTOR**. AQL 검증 헬퍼 추출 + KDoc.

**검증**: `./gradlew :backend:modules:search-export-import:test --tests '*SavedFilterServiceTest'`

### Task 5. web 공통(DTO·예외핸들러·actor 헬퍼) + SavedFilterController 5 CRUD

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/search-export-import/src/main/kotlin/com/bts/search/savedfilter/web/SavedFilterController.kt`, `backend/modules/search-export-import/src/main/kotlin/com/bts/search/savedfilter/web/dto/SavedFilterDtos.kt`, `backend/modules/search-export-import/src/main/kotlin/com/bts/search/savedfilter/web/SavedFilterExceptionHandler.kt`, `backend/modules/search-export-import/src/main/kotlin/com/bts/search/savedfilter/web/SavedFilterActorExtractor.kt`, `backend/modules/search-export-import/src/test/kotlin/com/bts/search/savedfilter/web/SavedFilterControllerTest.kt`]
- depends-on: [4]

**RED**.
- 테스트(MockMvc, 서비스 mockk): POST 201 생성, GET 목록, GET/{id} 단건, PUT 수정, DELETE 204. 미인증 401, 잘못 AQL 400, 이름중복 409, 비owner 403, 비가시 404, name>100/aql>2000 400.
- 실패: Controller 미존재.

**GREEN**.
- `SavedFilterActorExtractor` — SecurityContextHolder→UUID 추출 공통 헬퍼(조회보다 먼저, probe 차단 [[auth-extraction-before-resource-lookup]], 미인증 401). T5/T6 두 컨트롤러 공유(eng-review CONCERN — 병렬 wave 파일겹침 회피 위해 단일 파일 귀속).
- `SavedFilterController` `/api/v1/filters` (CRUD 5종). PR1 요청 DTO는 `shares` 미포함.
- `SavedFilterDtos` — Request{name,aqlQuery,projectKey} + Response{id,ownerId,name,aqlQuery,projectKey,createdAt,updatedAt,version,isOwner}. 명시적 검증(notification처럼 Hibernate Validator 부재 가능 — SearchController validateRequest 패턴).
- `SavedFilterExceptionHandler` — `@RestControllerAdvice(basePackages=["com.bts.search.savedfilter.web"])` (eng-review B1 — `assignableTypes`는 T6 클래스 순환참조라 basePackages로 두 컨트롤러 동시 커버). savedfilter.web 패키지 한정이라 형제 SearchExceptionHandler(com.bts.search.web)·타 BC 미간섭. 400/401/403/404/409 명시 매핑 + **ResponseStatusException은 catch-all로 삼키지 않고 그대로 전파/매핑**([[catch-all-exceptionhandler-swallows-responsestatusexception]]).

**REFACTOR**. DTO 변환 from() 추출 + KDoc.

**검증**: `./gradlew :backend:modules:search-export-import:test --tests '*SavedFilterControllerTest'`

### Task 6. 실행 엔드포인트 — GET /filters/{id}/search (IssueSearchPort 재사용)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/search-export-import/src/main/kotlin/com/bts/search/savedfilter/web/SavedFilterSearchController.kt`, `backend/modules/search-export-import/src/test/kotlin/com/bts/search/savedfilter/web/SavedFilterSearchControllerTest.kt`]
- depends-on: [4, 5]

**RED**.
- 테스트(MockMvc, 서비스+IssueSearchPort mockk): 본인 필터 실행→`Page<AqlSearchHit>` 200(IssueSearchPort.search 호출, viewerUserId=actor). 비가시(타 owner)→404(T5의 SavedFilterExceptionHandler basePackages가 이 컨트롤러도 커버해야 404 — eng-review B1 검증). 미인증 401. 저장된 AQL 파싱 후 IssueSearchQuery 구성 검증.
- 실패: Controller 미존재.

**GREEN**.
- `SavedFilterSearchController` `GET /api/v1/filters/{id}/search?page=&size=` — `SavedFilterActorExtractor`(T5) 재사용 → 필터 로드(service getByIdForOwner, PR1=owner 가시성) → AqlLexer/Parser로 aql_query 파싱 → `IssueSearchQuery(projectKey=filter.projectKey, ast, sort, viewerUserId=actor, page, size)` → `issueSearchPort.search` → `Page<AqlSearchHit>`. SearchController 응답 형식 재사용. ※ 예외 핸들러는 T5의 basePackages advice가 커버.

**REFACTOR**. 응답 변환 헬퍼 정리.

**검증**: `./gradlew :backend:modules:search-export-import:test --tests '*SavedFilterSearchControllerTest'`

### Task 7. 부트 인프라 + 풀스택 통합테스트 + ArchUnit BC 격리

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/search-export-import/src/test/kotlin/com/bts/search/SearchTestBootApplication.kt`, `backend/modules/search-export-import/src/test/kotlin/com/bts/search/SearchTestConfig.kt`, `backend/modules/search-export-import/src/test/kotlin/com/bts/search/savedfilter/SavedFilterIntegrationTest.kt`, `backend/modules/search-export-import/src/test/kotlin/com/bts/search/savedfilter/architecture/SavedFilterBcArchTest.kt`]
- depends-on: [5, 6]

**RED**.
- 풀스택 통합테스트(Testcontainers + 실 와이어링): CRUD 라이프사이클 end-to-end + AQL검증 400 + 이름중복 409(**single-thread**, [[fr-bl-02-sprint-backend-done]] race 가짜그린 주의) + OCC 409 + 비owner 403 + 비가시 404 + 미인증 401 + 실행 엔드포인트 happy/404.
- ArchUnit: savedfilter 패키지가 issue-tracking/identity-access 내부 import 0 (shared-kernel `IssueSearchPort`만), vacuous 아님([[archunit-vacuous-rule-silent-pass]] — 일부러 위반 넣어 fail 확인).

**GREEN** (eng-review CONCERN — 부트 인프라 명시).
- `SearchTestBootApplication` — test 전용 `@SpringBootApplication`(search BC 첫 부트 클래스 — 현재 0개). 컴포넌트 스캔 `com.bts.search`.
- `SearchTestConfig` — `@TestConfiguration`. (a) Testcontainers DataSource + DSLContext + txManager, (b) **`IssueSearchPort` stub 빈** — 인터페이스 default는 빈 생성 안 함 → 컨트롤러 주입 실패 부팅깸 방지([[no-cross-bc-deployment-assembly]] cross-BC stub빈 패턴). 실행 엔드포인트는 stub이 고정 Page 반환.
- 와이어링/설정 보강으로 통과.

**REFACTOR**. 테스트 헬퍼/fixture 정리.

**검증**: `./gradlew :backend:modules:search-export-import:test`

## Plan 메타

- task 수: 7 (각 TDD 사이클)
- 예상 wave: 6 — W1{T1,T2} → W2{T3} → W3{T4} → W4{T5} → W5{T6} → W6{T7} (T6가 T5 예외핸들러/actor헬퍼 의존 — eng-review B1 반영으로 직렬화)
- TDD 강제: yes (test→feat 커밋 순서 검증)
- 범위: PR1 (PRIVATE CRUD + 부트스트랩 + 실행). 공유=PR2 후속.
- 주요 함정(메모리): jooq-init-codegen-mirror · concurrent-testcontainers-suite-flaky · jooq-exception-translator-409-dependency · domain-exception-http-handler-basepackage-scope · archunit-vacuous-rule-silent-pass · auth-extraction-before-resource-lookup
- 전수 동기화: DATA.md(V600 범위, T1) — fr-index/SDD/product/README는 FR 카운트·BC 매핑 무변경(FR-SR-03 기존 등재, 진행상태만 추후 D단계 마킹)

## 리뷰 결과

### 독립 eng 리뷰 (2026-06-26, 백엔드 — autoplan 대신 집중 리뷰)

**BLOCKER 3건 → 모두 plan 반영 완료**.
- B1. T6 실행 컨트롤러에 예외 핸들러 스코프 부재(404→500 위험) → SavedFilterExceptionHandler를 `basePackages=["com.bts.search.savedfilter.web"]`로 두 컨트롤러 동시 커버 + actor 헬퍼 T5 귀속 + T6 depends-on [4,5]로 교정.
- B2. application.yml 경로 오류(`backend/src/...` 부재) → 모듈별 `backend/modules/search-export-import/src/main/resources/application.yml` 신규 생성으로 교정.
- B3. DATA.md §3 하드삭제 레지스트리 누락 → T1에 saved_filters 행 등록 추가(버전범위 행과 함께 2곳 동기화).

**CONCERN 5건 → 반영**.
- 부트 인프라(test @SpringBootApplication + Testcontainers @TestConfiguration + IssueSearchPort stub 빈) T7 files 명시.
- 409 dual-catch: 테스트 수동 DSLContext는 native 23505가 안전망(운영은 JooqAutoConfiguration translator) — T3 명시.
- actor 추출 공유 헬퍼 T5 단일 귀속(병렬 파일겹침 회피).
- T3 update에 updated_at 동반 갱신.
- PR1 list 스코프 = listByOwner(+선택 ?projectKey), ?ownedOnly는 PR1 무의미(전부 owner) — 유보.

**NIT**. detekt-baseline 이미 존재(무변경, files 제외). T1 RED를 information_schema 타입검증(timestamptz)으로 강화.

**BLOCKER 잔여: 없음.** 진행 가능.
