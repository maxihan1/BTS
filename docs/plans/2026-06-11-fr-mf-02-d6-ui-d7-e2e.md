# FR-MF-02 D6/D7 — 백업코드(Recovery Codes) 프론트엔드 UI + E2E

> slug: fr-mf-02-d6-ui-d7-e2e
> type: ui (+ qa)
> agent: frontend-engineer (주) + qa-engineer (D7 E2E)
> 생성: 2026-06-11

## Brief

FR-MF-02 백업 코드(Recovery Codes)의 후속 프론트엔드 작업.
- 백엔드 D1~D5는 PR #117(squash `b567a607`)로 완료. generate/regenerate/verify API 존재.
- D6: 프론트엔드 UI — 백업코드 생성/재생성 화면(`/settings/mfa` 영역) + 로그인 2단계에서 백업코드 검증 분기.
- D7: E2E — Playwright 시나리오.
- 형제 선례: FR-MF-01 TOTP 프론트/E2E(PR #116), FR-PM-03 D6/D7(PR #72).

classify: type=qa(제목 E2E 키워드 영향) → 실제는 ui 주도 + qa 보조 혼합으로 재판정.

## 도메인 정리

- **BC**: identity-access (MFA / 2FA)
- **영향 범위**: 프론트엔드 view layer 전용. 신규 도메인 엔티티/테이블 없음 — 백엔드 #117에서 `user_mfa_backup_codes`(V023) + `MfaBackupCodeService` + `BackupCodeAttemptLimiter`로 확정됨.
- **새 용어**: 없음. "백업 코드(Recovery Codes)"는 #117에서 이미 도입 → glossary 갱신 불요.
- **기존 결정 충돌**: 없음. FR-MF-01 TOTP(#113/#116)의 형제이며 동형 패턴 확장.
- **관련 ADR**: 없음 (프론트 view layer 작업, 신규 도메인 결정 없음). 백엔드 설계 결정은 #117 PR/메모리에 박제.

### 백엔드 API 계약 (ground truth — #117 실측)

| 기능 | 엔드포인트 | 인증 | 요청 | 응답(200) | 에러 |
|---|---|---|---|---|---|
| 생성/재생성 | `POST /api/v1/auth/mfa/backup-codes` | JWT 전용 | body 없음 | `{ codes: string[10] }` (평문 `xxxxx-xxxxx`, 1회 노출) | 409 `totp_not_active` · 403 `session_management_requires_interactive_login`(PAT) · 401 |
| 남은 개수 | `GET /api/v1/auth/mfa/backup-codes` | JWT 전용 | — | `{ generated: bool, remaining: int }` | 403 PAT · 401 |
| 로그인 검증 | `POST /api/v1/auth/mfa/verify` | permitAll + CSRF skip (이미 등록) | `{ mfa_challenge_token, code, method:"backup_code" }` | `{ access_token, token_type, expires_in }` | 400 `invalid_method` · 401 `invalid_code` · 429 `too_many_attempts` |

- 재생성은 동일 엔드포인트(replaceAll, 사용/미사용 무관 전량 무효화).
- `/mfa/verify`의 `method` 기본값 `"totp"` (기존 클라이언트 호환). 백업코드는 `"backup_code"` 명시.
- rate-limit: `BackupCodeAttemptLimiter` 5회/5분 → 429 (TOTP와 별도 카운터).
- 에러 바디: `{ "error": "<code>" }` 일원화.

### 프론트 동형 패턴 (ground truth — #116 실측)

| 항목 | 위치 | 비고 |
|---|---|---|
| 설정 화면 | `apps/web/src/components/auth/MfaSettings.tsx` + `routes/settings.mfa.tsx` | 백업코드 섹션 추가 대상 |
| API client | `apps/web/src/api/mfa.ts` | setup/enable/disable=`apiFetch`+수동 `X-XSRF-TOKEN`, verify=raw `fetch` (401 자동refresh 회피, [[auth-pre-session-401-raw-fetch]]) |
| Zod | `apps/web/src/api/schemas.ts` | union `mfa_required` 리터럴 우선 |
| 로그인 2단계 | `apps/web/src/auth/LoginForm.tsx` `LoginMfaStep`(L359-449) | 챌린지토큰=prop 메모리, 백업코드 전환 분기 추가 대상 |
| CSRF 헬퍼 | `apps/web/src/api/sessions.ts` `readXsrfToken()` | 쿠키 파싱 |
| MSW | `apps/web/src/mocks/mfa-handlers.ts` + `auth-fixtures.ts` mfaStore | stateful ([[msw-mutation-stateful-refetch]]) |
| i18n | `apps/web/src/i18n/ko.ts` `mfaErrorMessage` | 에러코드→한국어 |
| E2E | `apps/web/e2e/mfa-settings.spec.ts`, `mfa-login.spec.ts` | localStorage 플래그 토글 ([[e2e-msw-scenario-toggle-localstorage-flag]]) |
| 단위테스트 | `apps/web/src/api/mfa.test.ts`, `components/auth/MfaSettings.test.tsx` | |

### spec에서 결정할 UX 포인트 (열린 질문)

1. 로그인 2단계에서 TOTP 입력 화면 ↔ 백업코드 입력 화면 **전환 방식** ("백업코드로 로그인" 링크 + 뒤로가기).
2. 설정 화면에서 백업코드 **생성 시점 평문 10개 1회 노출** UX (복사/다운로드, 닫기 후 재확인 불가 경고).
3. **남은 개수 부족 경고** 표시 기준(예: remaining ≤ 3).
4. 재생성 시 **기존 코드 전량 무효화 확인** 모달.

## 스펙

전체 스펙: [docs/specs/2026-06-11-fr-mf-02-d6-ui-d7-e2e.md](../specs/2026-06-11-fr-mf-02-d6-ui-d7-e2e.md)

핵심 시나리오.
- 설정 화면(TOTP 활성 시만): 백업코드 생성/재생성(확인 모달) + 평문 10개 1회 노출(복사/다운로드) + 잔여 개수(remaining≤3 경고).
- 로그인 2단계: "백업 코드로 로그인" 링크 토글 → 백업코드 입력 → `verify(method:"backup_code")` → 정식 세션.
- 평문은 메모리 비영속, verify는 raw fetch, 생성/재생성은 apiFetch+X-XSRF-TOKEN, mutation 후 invalidate-only.

Maxi UX 결정: (1) 복사+다운로드(.txt), (2) 링크 토글, (3) remaining≤3 경고, (4) 재생성 확인 모달.

## Brainstorming Check

✅ 통과 (1회). verify method 하위호환·평문 비영속·raw fetch·TOTP 선행 반영 확인. 보강: 백업코드 입력 형식 느슨 검증+백엔드 위임(EC-9).

## Plan

> 전 task 프론트엔드 단일 worktree(apps/web). 백엔드 변경 0(#117 완료). detekt/ktlint 무관 — 검증은 `pnpm lint`(eslint) + `pnpm typecheck`(tsconfig.app) + `pnpm test`(vitest) + `pnpm test:e2e`(playwright).
> 같은 worktree 커밋 race 주의(learning fr-pm-03 D6/D7): wave 내 병렬 시 lint-staged는 자기 파일만 stage.

### Task 1. 백업코드 API 클라이언트 + Zod 스키마 + i18n 문자열

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/schemas.ts`, `apps/web/src/api/mfa.ts`, `apps/web/src/api/mfa.test.ts`, `apps/web/src/i18n/ko.ts`]
- depends-on: []

**RED**: `apps/web/src/api/mfa.test.ts`
- `generateBackupCodes()` → `POST /api/v1/auth/mfa/backup-codes`, X-XSRF-TOKEN 헤더 포함, `{codes:[...]}` 파싱. 409 `totp_not_active` → ApiError(409).
- `getBackupCodesStatus()` → `GET /api/v1/auth/mfa/backup-codes`, `{generated, remaining}` 파싱.
- `verifyMfa(token, code, 'backup_code')` → body에 `method:"backup_code"` 포함 검증. **기존 `verifyMfa(token, code)` 호출은 body에 `method:"totp"` 포함**(하위호환 — 기존 verifyMfa 테스트가 깨지면 method 기본값 검증으로 갱신).
- 실패(예상): `generateBackupCodes`/`getBackupCodesStatus` 미존재, `BackupCodesResponseSchema` 미존재.

**GREEN**:
- `schemas.ts`: `BackupCodesResponseSchema = z.object({ codes: z.array(z.string().min(1)).min(1) })`, `BackupCodesStatusResponseSchema = z.object({ generated: z.boolean(), remaining: z.number() })` + 타입 export.
- `mfa.ts`: `generateBackupCodes()`(apiFetch + readXsrfToken, 응답 parse), `getBackupCodesStatus()`(apiGet), `verifyMfa(challengeToken, code, method: 'totp'|'backup_code' = 'totp')`로 시그니처 확장 + body에 method 추가. `MfaErrorCode`에 `TOTP_NOT_ACTIVE: 'totp_not_active'` 추가.
- `i18n/ko.ts` `mfaStrings`에 백업코드 문자열 추가(설정: `backupSectionTitle`/`backupSectionDescription`/`backupGenerateButton`/`backupRegenerateButton`/`backupRemainingPrefix`/`backupLowWarning`/`backupNoneWarning`/`backupSaveWarning`/`backupCopyButton`/`backupCopiedLabel`/`backupDownloadButton`/`backupCloseButton`/`backupCopyFailed`/`backupDownloadFileName`('bts-backup-codes.txt')/`backupDownloadHeader`/`backupRegenerateConfirmTitle`/`backupRegenerateConfirmBody`/`backupRegenerateConfirmButton`/`backupRegenerateCancelButton`; 로그인: `loginUseBackupCode`('백업 코드로 로그인')/`loginUseTotp`('Authenticator 코드로 돌아가기')/`loginBackupCodeLabel`/`loginBackupStepGuide`). `mfaErrorMessage` switch에 `case 'totp_not_active'`(먼저 2단계 인증 활성화 안내) + `case 'invalid_method'`(일반 메시지) 추가.

**REFACTOR**: KDoc(verify method 하위호환 사유), 에러코드 상수 정렬.

**검증**: `pnpm --filter web test -- mfa.test` + `pnpm --filter web typecheck`

---

### Task 2. MfaSettings 백업코드 섹션 UI (생성/재생성/평문노출/경고/모달)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/auth/MfaSettings.tsx`, `apps/web/src/components/auth/MfaSettings.test.tsx`]
- depends-on: [1]

**RED**: `MfaSettings.test.tsx` (api/mfa는 vi.mock)
- TOTP 활성(enabled=true) 시 백업코드 섹션 렌더, 미활성 시 미노출(S6).
- generated=false → "백업 코드 생성" 버튼. 클릭 → `generateBackupCodes` 호출 → 평문 10개 + 복사/다운로드 버튼 + 저장 경고 표시.
- 복사 클릭 → `navigator.clipboard.writeText` 호출(mock). 다운로드 클릭 → Blob/anchor 트리거(mock).
- generated=true, remaining=2 → "남은 백업 코드: 2개" + remaining≤3 경고 배너(S2). remaining=0 → none 경고(EC-7).
- "백업 코드 재생성" 클릭 → 확인 모달 노출 → 확인 시 `generateBackupCodes` 재호출(S3), 취소 시 미호출.
- generate 성공 후 `invalidateQueries(['mfa','backup-codes'])` 호출(NFR-5, stateful refetch).
- 실패(예상): 백업코드 섹션 컴포넌트 미존재.

**GREEN**: `MfaSettings.tsx`
- `isEnabled` 분기 안에 `<BackupCodesSection />` 하위 컴포넌트 추가. `useQuery(['mfa','backup-codes'], getBackupCodesStatus)` + `useMutation(generateBackupCodes)`.
- 평문 표시: state 메모리 보관(NFR-1, 비영속), 복사(navigator.clipboard, 실패 시 backupCopyFailed), 다운로드(인라인 헬퍼 — Blob `text/plain` + anchor download, 파일명 backupDownloadFileName, 헤더+코드 줄바꿈), 닫기 시 평문 state null 리셋.
- remaining≤3 경고 배너 / remaining=0 강조. **재생성 확인 = 인라인 확인 UI**(리뷰 CONCERN-1): components/ui에 Dialog/AlertDialog 부재 + radix-dialog 미설치 → **새 의존성 추가 금지**(절대규칙#17). 기존 disable 플로우의 `showDisableForm` 조건부 렌더 패턴과 동형으로 `showRegenerateConfirm` state + 인라인 확인 박스(경고문 + [재생성][취소] 버튼)로 구현. 의존성 0.
- generate onSuccess: 평문 set + `invalidateQueries`. onError: extractErrorCode → mfaErrorMessage(totp_not_active 포함).

**REFACTOR**: 백업코드 하위 컴포넌트 파일 분리 검토(MfaSettings 비대화 시 `BackupCodesSection.tsx`), 다운로드 헬퍼 KDoc.

**검증**: `pnpm --filter web test -- MfaSettings` + typecheck

---

### Task 3. LoginForm 백업코드 로그인 토글

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/auth/LoginForm.tsx`, `apps/web/src/auth/LoginForm.test.tsx`]
- depends-on: [1]

**RED**: `LoginForm.test.tsx` (verifyMfa는 vi.mock)
- MFA step에서 "백업 코드로 로그인" 링크 표시. 클릭 → 입력 라벨이 백업코드(loginBackupCodeLabel)로, 안내가 loginBackupStepGuide로 전환 + "Authenticator 코드로 돌아가기" 링크.
- 백업코드 입력 + 확인 → `verifyMfa(token, code, 'backup_code')` 호출(S4). TOTP 모드는 기존대로 `verifyMfa(token, code, 'totp')`.
- 오답 시 인라인 에러 + 화면 유지(S5). 모드 전환 시 입력값/에러 초기화.
- 실패(예상): 백업코드 토글 미존재.

**GREEN** (리뷰 CONCERN-2 — zodResolver는 useForm 첫 렌더에 고정되어 mode useState로 스키마가 안 바뀜):
- `LoginMfaStep`이 `mode: 'totp'|'backup_code'` state를 들고 **mode별 별도 하위 컴포넌트**(`LoginTotpInput`/`LoginBackupCodeInput`, 각자 useForm+zodResolver)를 분기 렌더하거나, 단일 입력 컴포넌트를 `key={mode}`로 재마운트해 resolver를 갈아끼운다. (인라인 단일 useForm + mode 스키마 useMemo는 resolver 미반영이라 금지.)
- 스키마: totp=`z.string().length(6)`, backup=`z.string().trim().min(1)`(느슨, EC-9). 입력 필드 속성도 mode별(backup은 inputMode/maxLength 완화). 라벨/안내는 loginBackupCodeLabel/loginBackupStepGuide.
- verify는 mode를 `verifyMfa(token, code, mode)` 3번째 인자로 전달. 토글 링크 2개(loginUseBackupCode/loginUseTotp, ghost 버튼). 모드 전환 시 입력값/에러 초기화(재마운트면 자동).

**REFACTOR**: mode 의존 파생값(스키마/라벨) `useMemo` 또는 헬퍼 추출.

**검증**: `pnpm --filter web test -- LoginForm` + typecheck

---

### Task 4. MSW 백업코드 핸들러 + fixtures 확장

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/mocks/mfa-handlers.ts`, `apps/web/src/mocks/auth-fixtures.ts`, `apps/web/src/mocks/mfa-handlers.test.ts`]
- depends-on: []

**RED**: `mfa-handlers.test.ts`(또는 기존 핸들러 테스트 위치)
- `POST /api/v1/auth/mfa/backup-codes`: CSRF 필수, `mfaStore.enabled=false`면 409 `totp_not_active`, 성공 시 codes 10개 + `backupCodesGenerated=true`/`backupCodesRemaining=10`.
- `GET /api/v1/auth/mfa/backup-codes`: `{generated, remaining}` 반환.
- verify 핸들러 `method:"backup_code"` 분기: `MFA_VALID_BACKUP_CODE` 일치 시 성공 + remaining 차감, 불일치 401 `invalid_code`.
- 실패(예상): 핸들러/store 필드 미존재.

**GREEN**: `auth-fixtures.ts` `MfaStore`에 `backupCodesGenerated: boolean`, `backupCodesRemaining: number` 추가 + 초기값 + `resetMfaStore` 갱신 + `MFA_VALID_BACKUP_CODE = 'aaaaa-bbbbb'` 상수. `mfa-handlers.ts`에 backup-codes POST/GET 핸들러 추가 + verify 핸들러에 method 분기(기본 totp 유지) + `mfaHandlers` 배열 등록.

**REFACTOR**: 백업코드 생성 헬퍼(고정 10개 mock), 주석.

**검증**: `pnpm --filter web test -- mfa-handlers` + typecheck

---

### Task 5. E2E — 백업코드 설정 + 로그인 시나리오

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/mfa-backup-codes.spec.ts`]
- depends-on: [2, 3, 4]

**RED→GREEN** (E2E는 실패 시나리오 먼저 작성 후 통과 확인):
- `e2e/mfa-backup-codes.spec.ts` 신규. `loginAsAlice` fixture(issue-fixtures, 2단계 호환) 재사용.
- **설정 시나리오**: MFA 활성 상태 시드 → /settings/mfa → 백업코드 생성 → 평문 10개 표시 + 복사/다운로드 버튼(S1). 잔여 경고(remaining≤3 시드, S2). 재생성 확인 모달(S3).
- **로그인 시나리오**: `__bts_e2e_mfa_enabled` + 백업코드 시드(addInitScript localStorage) → 로그인 2단계 → "백업 코드로 로그인" 토글 → `MFA_VALID_BACKUP_CODE` 입력 → 대시보드(S4). 오답 → 인라인 에러(S5).
- 텍스트 중복 시 `exact:true`/컨테이너 한정(learning playwright-getbyrole-exact-strict-mode). serviceWorkers 'block' 금지(MSW 핸들러로, learning e2e-msw-serviceworker-block).

**검증**: `pnpm --filter web test:e2e -- mfa-backup-codes` + **기존 `mfa-settings`/`mfa-login` 회귀 0**.

## Plan 메타

- task 수: 5
- wave 추정: Wave1=[T1, T4](depends []), Wave2=[T2, T3](depends [1], 파일 비겹침), Wave3=[T5](depends [2,3,4]) → 3 wave
- TDD 강제: yes (각 task RED→GREEN→REFACTOR)
- agent: T1~T4 frontend-engineer, T5 qa-engineer
- 추가 검증: `pnpm verify`(lint+typecheck+test+build) + `pnpm test:e2e` 회귀
- 관련 learnings: auth-pre-session-401-raw-fetch · mutation-setquerydata-partial-response-flicker · msw-mutation-stateful-refetch · e2e-msw-scenario-toggle-localstorage-flag · playwright-getbyrole-exact-strict-mode · zod-schema-strengthen-inline-mock-fanout · dialog-submiterror-ownership-dead-path · fr-au-08-d6-d7(shadcn Dialog 래퍼 부재)

## 리뷰 결과

### eng 집중 리뷰 — 적대적 ground-truth (2026-06-11)

type=ui이나 신규 비주얼이 아닌 기존 MFA 화면 동형 확장이라, design-review 풀 실행 대신 핵심 엔지니어링 리스크를 실제 코드로 검증(learning bts-review-plan-autoplan-overkill 정신).

**PASS**:
- ✅ `verifyMfa` 호출자는 `LoginForm.tsx:371` 단 1곳 — method 기본값 `'totp'`로 기존 호출 무변경. 기존 테스트 `T-MFA-5c`는 `mfa_challenge_token`/`code` 필드만 검증 → method 추가로 안 깨짐(T1에서 method 검증만 추가).
- ✅ MSW verify 핸들러 method 분기는 기본 `totp` 유지 → 기존 `mfa-login.spec.ts` 회귀 0.
- ✅ 평문 비영속(state 메모리, NFR-1), raw fetch(verify), invalidate-only(NFR-5) 모두 plan 반영.

**CONCERN-1 (해소 — plan T2 보강)**: 재생성 "확인 모달" — `components/ui`에 Dialog/AlertDialog 부재 + radix-dialog 미설치. 새 외부 의존성 추가는 절대규칙#17(Maxi 승인). → 기존 disable의 인라인 확인 패턴(`showDisableForm`)과 동형으로 `showRegenerateConfirm` 인라인 확인 UI 구현(의존성 0). **게이트1에서 Maxi 확인 필요**: "확인 모달" 요구를 인라인 확인 UI로 충족.

**CONCERN-2 (해소 — plan T3 보강)**: `LoginMfaStep` mode 전환 시 `zodResolver`가 useForm 첫 렌더에 고정 → mode useState로 스키마 미반영. mode별 별도 하위 컴포넌트 분리(각자 useForm) 또는 `key={mode}` 재마운트로 해결 명시.

**BLOCKER: 없음.**
