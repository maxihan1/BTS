# FR-SR-04 D6/D7 — AQL 검색창 syntax highlight `text` 키워드 + E2E

> slug: fr-sr-04-d6-d7
> type: ui
> agent: frontend-engineer (D6) + qa-engineer (D7 E2E)
> 생성: 2026-06-27

## Brief

FR-SR-04(한글 형태소 기반 전문 검색)의 프론트 후속. 백엔드 D1~D5는 #195로 완료
(AQL 신규 `text` 가상 필드 — `~` 전용, summary+description 전문 검색 / V032 search_vector
generated column + GIN + trigram 인덱스). D6/D7만 남음.

- D6. 프론트 UI — FR-SR-02에서 만든 AQL 검색창 syntax highlight 토크나이저에
  `text` 키워드 강조 추가. (책임. frontend-engineer)
- D7. E2E — `text` 키워드 검색 시나리오. (책임. qa-engineer)

classify가 "E2E" 키워드로 qa 오판 → ui/frontend-engineer 정정
(메모리 fr-ux-02/fr-sr-02 동일 학습).

## 도메인 정리

- **BC**: search-export-import(논리) / issue-tracking(물리). FR-SR 시리즈 패턴 계승.
  새 BC·엔티티·관계 없음. 순수 프론트엔드 작업(검색창 토크나이저 + E2E).
- **영향 파일**:
  - `apps/web/src/lib/aql-tokenizer.ts` — `AQL_FIELDS` 화이트리스트 (현 누락 대상)
  - `apps/web/src/components/search/AqlHighlighter.tsx` — 토큰→색 매핑(FIELD 색 재사용)
  - 각 `.test.ts(x)` + E2E `apps/web/e2e/`
- **계약 drift 발견(핵심)**: 프론트 `AQL_FIELDS = [status, label, summary, priority]` ↔
  백엔드 정본 `AqlFields.MVP_FIELDS = {status, label, summary, priority, **text**}`.
  백엔드가 #195(FR-SR-04 D4)에서 `text` 가상 필드를 추가했으나 프론트 하이라이터는
  미반영 → `text ~ "검색어"` 입력 시 `text`가 PLAIN으로 처리돼 강조 안 됨.
  → D6 = `AQL_FIELDS`에 `'text'` 추가(백엔드 정본과 동기화).
- **백엔드 `text` 필드 계약**(검증된 사실):
  - 가상 FTS 필드(실제 DB 컬럼 아님, `issues.search_vector` 경유 tsvector+trigram 하이브리드)
  - `~`(CONTAINS) 전용. `=`/`!=`/`in`/`not in` 금지(`AqlFields.isOperatorForbidden`)
  - 대소문자 무시(`classify("TEXT") == SUPPORTED`)
  - ORDER BY 정렬 불가(`SORTABLE_FIELDS`에 미포함, `ORDER BY text` → 400 SEARCH_UNKNOWN_FIELD)
- **새 용어**: "text 가상 필드"(AQL FTS 검색 진입점). glossary에 FTS·AQL 항목 존재 →
  사소한 보강 후보, plan에 기록(머지 단계 Obsidian sync에서 반영 검토).
- **기존 결정 충돌**: 없음. ADR [2026-06-26-fr-sr-04-korean-fts.md](../decisions/2026-06-26-fr-sr-04-korean-fts.md)
  D4가 정확히 이 프론트 통합을 예약("검색창 동일·같은 보안 술어"). 충돌이 아니라 **이행**.
- **관련 ADR**: 위 FR-SR-04 ADR(기존). 신규 ADR 불필요.
- **classify 정정**: "E2E" 키워드로 qa 오판 → ui/frontend-engineer(메모리 fr-ux-02/fr-sr-02 동일 학습).

## 스펙

전체 스펙. [docs/specs/2026-06-27-fr-sr-04-d6-d7.md](../specs/2026-06-27-fr-sr-04-d6-d7.md)

핵심 3줄.
- `aql-tokenizer.ts`의 `AQL_FIELDS`에 `'text'` 추가 → `text ~ "검색어"`의 `text`가 FIELD 색 강조(백엔드 MVP_FIELDS 정합).
- 기존 `text-syntax-field` 색 재사용. 신규 토큰 타입·색·디자인 변형 없음.
- (선택) placeholder에 `text ~ "키워드"` 전문 검색 예시 추가(발견성). D7 E2E로 강조+검색 검증.

엣지(EC1~EC5)는 기존 토크나이저 구조로 자연 처리(대소문자/부분식별자/문자열내부/연산자제약/ORDER BY) — 추가 분기 불필요.

## Brainstorming Check

✅ 통과 (1회, 직접 sanity-check — 완료 FR 프론트 이행이라 대화 세션 대신 직접 작성. gap 없음)

## Plan

### Task 1. 토크나이저 `AQL_FIELDS`에 `text` 추가

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/lib/aql-tokenizer.ts`, `apps/web/src/lib/aql-tokenizer.test.ts`]
- depends-on: []

**RED**: `aql-tokenizer.test.ts`
- 기존 `it('AQL_FIELDS — 4개 필드 고정')`(line 15)를 `5개 필드 고정 (text 포함)`으로 갱신
  → 현 코드는 4개라 실패.
- `describe('FIELD 토큰')`에 추가:
  - `it('text를 FIELD로 분류한다')` — `tokenizeAql('text ~ "x"')[0]`가 `{type:'FIELD', value:'text'}`
  - `it('TEXT(대문자)를 FIELD로 분류한다 (대소문자 무시)')`
- 실패(예상): `AQL_FIELDS`에 text 없어 PLAIN 분류 → 단언 실패

**GREEN**: `aql-tokenizer.ts`
- `AQL_FIELDS`(line 65)에 `'text'` 추가 (백엔드 `AqlFields.MVP_FIELDS` 정합)
- 타입 주석(line 7-8) `FIELD: ... (status/label/summary/priority...)` → `text` 포함으로 갱신

**REFACTOR**: 주석에 백엔드 정본 경로 유지 확인(이미 명시됨), 불필요 변경 없음

**검증**: `pnpm --filter web test aql-tokenizer`

### Task 2. 하이라이터 `text` 강조 회귀 테스트

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/search/AqlHighlighter.test.tsx`]
- depends-on: [1]   # 토크나이저가 text를 FIELD로 분류해야 강조됨

**RED**: `AqlHighlighter.test.tsx` `describe('토큰 색상 span')`에 추가
- `it('text 가상 필드는 text-syntax-field 클래스를 가진다')` — `value="text ~ \"검색어\""` 렌더 후
  overlay의 `.text-syntax-field` span 중 textContent가 `'text'`인 것 존재 검증
- 실패(예상, Task 1 미적용 시): text가 PLAIN → field span 없음

**GREEN**: Task 1의 토크나이저 변경으로 자동 통과(컴포넌트 코드 변경 없음 — best-effort 강조가 상수 미러를 그대로 사용)

**REFACTOR**: 없음

**검증**: `pnpm --filter web test AqlHighlighter`

### Task 3. 검색창 placeholder에 `text` 전문검색 예시 추가 (FR-3 발견성)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/search.tsx`, `apps/web/src/routes/search.test.tsx`]
- depends-on: []

**RED**: `search.test.tsx`
- placeholder가 `text ~` 전문검색 예시를 포함하는지 단언하는 테스트 추가
  (mock AqlHighlighter가 placeholder를 textarea로 렌더 — line 44-56)
- 실패(예상): 현 placeholder에 text 예시 없음

**GREEN**: `search.tsx` line 351 placeholder
- `"AQL 쿼리를 입력하세요. 예: status = open AND priority IN (1, 2)"` →
  `"AQL 쿼리를 입력하세요. 예: status = open AND priority IN (1, 2), text ~ \"로그인\""`
- **제약**: "AQL 쿼리를 입력하세요" 접두사 보존 필수 — E2E 셀렉터가
  `textarea[placeholder*="AQL 쿼리를 입력하세요"]` 부분매칭(`*=`)이라 접두사만 유지하면 무회귀

**REFACTOR**: 없음

**검증**: `pnpm --filter web test search.test`

### Task 4. D7 E2E — `text` 전문 검색 시나리오

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/search.spec.ts`, `apps/web/src/mocks/handlers.ts`(필요시 text 쿼리 MSW)]
- depends-on: [1]

**RED**: `e2e/search.spec.ts`에 시나리오 추가
- `text ~ "키워드"` 입력 → overlay에 `.text-syntax-field`('text') 강조 확인
- Cmd/Ctrl+Enter 검색 실행 → 결과 목록 표시(MSW가 text 쿼리에 응답)
- 기존 MSW search 핸들러가 `text ~` 쿼리를 처리하는지 확인, 미처리 시 핸들러 보강
  (메모리 msw-mutation-stateful-refetch / e2e-msw-scenario-toggle 패턴 참고)

**GREEN**: Task 1 적용 + MSW 핸들러 응답으로 통과

**REFACTOR**: 셀렉터를 기존 `fillAndSearch` 헬퍼 재사용(중복 방지)

**검증**: `pnpm --filter web test:e2e search`

## Plan 메타

- task 수: 4
- 예상 wave: 2 (Wave1=T1,T3 병렬 / Wave2=T2,T4 병렬). 파일 교집합 없음.
- TDD 강제: yes (T1=회귀스냅샷 RED→GREEN, T2/T3=신규 테스트 RED, T4=E2E)
- 추가 검증: pnpm verify (lint+typecheck+test+build) + E2E
- BC: 순수 프론트(apps/web). 백엔드 변경 0.

## 리뷰 결과 (← /bts-review-plan 채움)
