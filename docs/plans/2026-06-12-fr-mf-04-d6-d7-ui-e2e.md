# FR-MF-04 D6/D7 — MFA 강제 정책 프론트엔드 게이팅 UI + E2E

> slug: fr-mf-04-d6-d7-ui-e2e
> type: auth
> agent: security-engineer (구현 task는 frontend-engineer / qa-engineer 분담)
> 생성: 2026-06-12

## Brief

FR-MF-04(MFA 강제 정책 — 관리자 + 민감 프로젝트)의 **D6 프론트 UI + D7 E2E** 후속 작업.
백엔드 D1~D5는 PR #123(`2002866b`)으로 완료.

- 백엔드 계약(이미 머지됨).
  - `GET /api/v1/users/me/whoami` 응답에 `mfaEnrollmentRequired: Boolean` 필드 노출
    (`WhoamiResponse.kt:30`, JWT 클레임 `mfa_enrollment_required`를 읽기만 — 단일 출처).
  - `MfaEnrollmentGateFilter` — 미등록 강제대상의 요청을 ALLOW_LIST 외 모든 경로에서 **403**
    `{"error":"mfa_enrollment_required"}`로 차단. ALLOW_LIST = `/api/v1/auth/mfa/**` ·
    `/api/v1/users/me/whoami` · `/api/v1/auth/logout` · `/api/v1/auth/refresh`.
- 프론트 목표(D6). whoami `mfaEnrollmentRequired === true`이면 MFA 등록(step-up) 화면으로 강제
  리다이렉트. `requirePasswordChanged`(routeGuard.ts:86, `mustChangePassword` 선례) 동형.
- E2E 목표(D7). 강제대상 미등록 → step-up 리다이렉트 / 등록 완료 후 해제 시나리오.

선례. FR-AU-05 `mustChangePassword`(routeGuard.requirePasswordChanged), FR-MF-01 #116 / FR-MF-02 #121
(MFA 프론트/E2E 분리 PR).

## 도메인 정리

- **BC**. identity-access (프론트 게이팅은 apps/web, 백엔드 계약은 identity-access).
- **영향 엔티티**. 신규 0건. 기존 백엔드 계약(`whoami.mfaEnrollmentRequired`, `MfaEnrollmentGateFilter`)을 프론트가 소비할 뿐.
- **새 용어**. 0건. 기존 glossary "2FA" 재사용.
- **용어 정합 주의 (drift 차단)**. glossary "재인증 / step-up"(FR-AU-08)은 *민감 동작 직전 1회 재인증*(5분 윈도우)이다.
  product 문서가 D6를 "step-up 페이지"로 표기했으나 실제 개념은 *MFA 등록 자체를 강제하는 게이트*로 별개다.
  → 프론트 코드/스펙에서 이 개념을 **"MFA 등록 강제 게이트(MFA enrollment gate)"**로 명명하고
  "step-up"은 FR-AU-08 재인증 의미로 보존한다. (가드 함수명 후보: `requireMfaEnrolled`.)
- **기존 결정 충돌**. 없음. ADR `2026-06-12-mfa-enforcement-policy` D4가 "프론트 게이팅 UI + E2E는 D6/D7 후속"으로 명시.
- **관련 ADR**. [docs/decisions/2026-06-12-mfa-enforcement-policy.md](../decisions/2026-06-12-mfa-enforcement-policy.md) (기존, FR-MF-04 백엔드 시 생성). **신규 ADR 불요** — 프론트는 결정된 계약 소비.
- **선례 동형**. FR-AU-05 `mustChangePassword` → `requirePasswordChanged`(routeGuard.ts:86) 강제 리다이렉트 패턴.

## 스펙

전체 스펙. [docs/specs/2026-06-12-fr-mf-04-d6-d7-ui-e2e.md](../specs/2026-06-12-fr-mf-04-d6-d7-ui-e2e.md)

핵심 요구사항 요약.
- **FR-D6-1** whoami Zod에 `mfaEnrollmentRequired: z.boolean()` 추가 + 픽스처(makeWhoami/aliceUser/bobUser)·MSW·인라인 리터럴 전수 동기화.
- **FR-D6-2** `requireMfaEnrolled` 가드(routeGuard.ts, `requirePasswordChanged` 동형) → 강제대상 미등록을 `/settings/mfa`로 리다이렉트, 보호 라우트 체인 합성.
- **FR-D6-3** `/settings/mfa` 상단 강제 안내 배너(`mfaEnrollmentRequired===true`일 때만).
- **FR-D6-4 (정합성 필수)** enable 성공+강제모드 → 토큰 refresh + whoami 재조회 + setSession + /dashboard 이동(없으면 store stale로 영구 락).
- **D7** Playwright E2E 4종(강제 리다이렉트/게이트 고정/등록 후 해제/비강제 회귀) + 기존 회귀 0. 기존 `MFA_E2E_ENABLED_KEY` 토글 패턴 확장.

리다이렉트 대상 = 기존 `/settings/mfa` + 안내 배너 (Option A2, Maxi 확정 — 신규 라우트 0).

## Brainstorming Check

✅ 통과 (자가 적대적 검토). 핵심 갭 1건(등록 후 게이트 stale → 영구 락) 발견 후 FR-D6-4로 해제 명시. 잔여 BLOCKER 0.

## Plan

> 모든 task = 프론트(apps/web). TDD red→green→refactor. 검증 명령은 worktree 루트의 `apps/web`에서.
> 공통 검증: `pnpm --filter web typecheck && pnpm --filter web lint`. (프로젝트 관례에 맞춰 `pnpm typecheck` 등으로 치환 가능.)

### Task 1. whoami 계약에 mfaEnrollmentRequired 추가 + 픽스처/핸들러/인라인 리터럴 전수 동기화

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/schemas.ts`, `apps/web/src/api/schemas.test.ts`, `apps/web/src/mocks/auth-fixtures.ts`, `apps/web/src/mocks/auth-handlers.ts`, `apps/web/src/router.test.tsx`, `apps/web/src/auth/routeGuard.test.tsx`, `apps/web/src/components/auth/MfaSettings.test.tsx`, `apps/web/src/auth/LoginForm.test.tsx`, `apps/web/src/components/Header.test.tsx`, `apps/web/src/auth/useLoginMutation.test.tsx`, `apps/web/src/auth/authStore.test.ts`, `apps/web/src/api/client.test.ts`, `apps/web/src/api/interceptor.test.ts`, `apps/web/src/mocks/auth-handlers.test.ts`, `apps/web/src/components/admin/MemberList.test.tsx`, `apps/web/src/components/admin/AddMemberDialog.test.tsx`, `apps/web/src/components/field-permissions/__tests__/FieldPermissionList.test.tsx`, `apps/web/src/components/custom-fields/__tests__/CustomFieldList.test.tsx`, `apps/web/src/routes/__tests__/projects.$projectKey.settings.custom-fields.test.tsx`, `apps/web/src/routes/__tests__/projects.$projectKey.settings.components.test.tsx`, `apps/web/src/routes/__tests__/projects.$projectKey.settings.versions.test.tsx`, `apps/web/src/routes/__tests__/projects.$projectKey.settings.members.test.tsx`, `apps/web/src/routes/__tests__/settings.account-links.test.tsx`]
- depends-on: []

**RED**. `apps/web/src/api/schemas.test.ts`에 추가.
```ts
it('whoami 스키마는 mfaEnrollmentRequired(boolean)를 필수로 요구한다', () => {
  const base = { username: 'alice', email: 'a@bts.local', authMethod: 'jwt',
    userId: '00000000-0000-0000-0000-000000000001', mustChangePassword: false, isSystemAdmin: false }
  expect(() => WhoamiResponseSchema.parse(base)).toThrow()             // 누락 시 실패
  expect(WhoamiResponseSchema.parse({ ...base, mfaEnrollmentRequired: true }).mfaEnrollmentRequired).toBe(true)
})
```
실패(예상). 필드 부재 → 첫 expect의 toThrow 미충족.

**GREEN**.
- `schemas.ts` `WhoamiResponseSchema`에 `mfaEnrollmentRequired: z.boolean()` 추가(required, `isSystemAdmin` 다음 줄).
- `auth-fixtures.ts` `aliceUser`/`bobUser`에 `mfaEnrollmentRequired: false` 추가(→ `makeWhoami` 자동 동기화).
- `auth-handlers.ts` whoami 응답 생성부에 필드 포함.
- 인라인 whoami 리터럴(위 files의 test) — `grep -n "mustChangePassword:" <file>` 위치마다 `mfaEnrollmentRequired: false` 동봉.

**REFACTOR**. 가능한 테스트는 인라인 리터럴 대신 `makeWhoami({...})` 팩토리 호출로 치환(추가 drift 차단). 단 surgical — 이미 팩토리 쓰는 곳만, 새 리팩터 남발 금지.

**검증**. `pnpm --filter web typecheck` (Zod/타입 0 에러) + `pnpm --filter web test -- schemas` + 전체 `pnpm --filter web test`(회귀 0).

### Task 2. requireMfaEnrolled 라우트 가드 신설

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/auth/routeGuard.ts`, `apps/web/src/auth/routeGuard.test.tsx`]
- depends-on: [1]

**RED**. `routeGuard.test.tsx`에 `requireMfaEnrolled` 4분기 테스트.
```ts
// user null → 통과(throw 안 함)
// pathname === '/settings/mfa' → 통과
// user.mfaEnrollmentRequired === true (다른 경로) → redirect('/settings/mfa') throw
// user.mfaEnrollmentRequired === false → 통과
```
실패(예상). `requireMfaEnrolled` export 부재.

**GREEN**. `routeGuard.ts`에 `requirePasswordChanged` 동형으로 추가.
```ts
export function requireMfaEnrolled({ location }: GuardContext): void {
  const user = useAuthStore.getState().user
  if (user === null) return
  if (location.pathname === '/settings/mfa') return
  if (user.mfaEnrollmentRequired === true) throw redirect({ to: '/settings/mfa' })
}
```

**REFACTOR**. KDoc(FR-MF-04, ADR 링크, mustChangePassword와의 순서 관계 명시).

**검증**. `pnpm --filter web test -- routeGuard`.

### Task 3. 보호 라우트 beforeLoad 체인에 requireMfaEnrolled 합성

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/router.ts`, `apps/web/src/router.test.tsx`]
- depends-on: [1, 2]

**RED**. `router.test.tsx` — 강제대상 미등록(`mfaEnrollmentRequired:true`) 세션에서 보호 라우트 beforeLoad 실행 시 `/settings/mfa` redirect. `/settings/mfa` 라우트 자신은 통과. mustChangePassword 동시 true면 password 우선.

**GREEN (ground-truth 실측 반영)**. router.ts는 공유 const로 가드를 합성한다.
- **router.ts:6** `requireAuthAndPasswordChanged = composeGuards(requireAuth, requirePasswordChanged)` 한 곳에
  `requireMfaEnrolled`를 append → `composeGuards(requireAuth, requirePasswordChanged, requireMfaEnrolled)`.
  이 const가 ~15개 보호 라우트에 일괄 적용되므로 단일 수정으로 대부분 커버. 이름이 오해 소지가 되면
  정확히 리네임(예: `requireAuthPasswordMfa`) — 단 const 참조 전수 치환(mechanical), 동작 불변.
- **admin 3개 라우트**(audit-logs:202 / notification-policies:212 / users/new:222)의
  `composeGuards(requireAuth, requireSystemAdmin, requirePasswordChanged)` 끝에 `requireMfaEnrolled` 추가
  (requirePasswordChanged 다음 — 기존 순서 일관).
- `/settings/mfa` 라우트(settings.mfa)는 requireMfaEnrolled 미적용 또는 적용 무관(가드 내부 자기경로 예외로 통과).
  단 사용자가 등록 흐름을 밟으려면 이 라우트는 반드시 접근 가능해야 하므로 합성 대상에서 제외.
- **범위 밖(불변)**. fieldPermissions(router.ts:165)은 현재 `requireAuth`만 — 기존에 requirePasswordChanged도 빠진
  pre-existing 불일치. 본 PR에서 손대지 않는다(surgical).

**REFACTOR**. const 리네임 시 KDoc/주석 정합. 과한 추상화 금지.

**검증**. `pnpm --filter web test -- router`.

### Task 4. MfaSettings 강제 안내 배너 + 등록 후 게이트 해제

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/auth/MfaSettings.tsx`, `apps/web/src/components/auth/MfaSettings.test.tsx`, `apps/web/src/i18n/ko.ts`, `apps/web/src/api/client.ts`, `apps/web/src/api/client.test.ts`]
- depends-on: [1]

**RED**. `MfaSettings.test.tsx`.
- (배너) `mfaEnrollmentRequired:true` 사용자 → 안내 배너(role="status") 노출. false → 미노출.
- (해제) 강제모드에서 enable 성공 → refresh(POST /refresh) 호출 + whoami 재조회 + setSession + `/dashboard` navigate. 비강제모드 enable → refresh/navigate 미발생(기존 status invalidate만).
실패(예상). 배너/해제 로직 부재.

**GREEN**.
- `client.ts` — refresh+whoami+setSession을 묶는 export 헬퍼 `refreshSession()` 추가(기존 `doRefresh` 재사용, whoami는 `apiGet`+`WhoamiResponseSchema`, 성공 시 `setSession`). 반환 = 갱신된 `WhoamiResponse`.
- `MfaSettings.tsx` — `useAuthUser()`로 `mfaEnrollmentRequired` 구독. true면 배너 렌더. `enableMutation.onSuccess`에서 강제모드였으면 `refreshSession()` → 성공 시 `useNavigate()({ to: '/dashboard' })`. 실패 시 기존 폴백(client.ts의 clearSession).
- `i18n/ko.ts` — `mfaStrings.enforcementBanner`(예: "보안 정책에 따라 2단계 인증 등록이 필요합니다. 등록을 완료해야 계속할 수 있습니다.") 추가.

**REFACTOR**. 배너를 소형 하위 컴포넌트로 분리(MfaSetupView 패턴), secret 잔존 등 기존 CONCERN-state 불변 유지.

**검증**. `pnpm --filter web test -- MfaSettings client` + `pnpm --filter web typecheck`.

### Task 5. E2E — MFA 강제 게이트 4 시나리오 + 회귀 0

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/mfa-enforcement.spec.ts`, `apps/web/src/mocks/auth-fixtures.ts`, `apps/web/src/mocks/auth-handlers.ts`]
- depends-on: [1, 2, 3, 4]

**RED→GREEN(E2E는 시나리오 우선)**.
- 인프라. `auth-fixtures.ts`에 `__bts_e2e_mfa_enforcement` localStorage 키 + 인메모리 enforcement 플래그(공유 store). `auth-handlers.ts` whoami가 (플래그 ON && mfaStore.enabled===false)→`mfaEnrollmentRequired:true` 파생, refresh 핸들러가 enable 후 false 반환(클레임 재계산 모사). `addInitScript`로 goto 전 시드(`e2e-msw-scenario-toggle-localstorage-flag`).
- E2E-1. 강제대상 미등록 로그인 → `/settings/mfa` 리다이렉트 + 배너 노출 + 활성화 버튼 보임(EC-1 페이지 정상).
- E2E-2. 강제 상태 `/dashboard` 이동 시도 → `/settings/mfa`로 되돌림.
- E2E-3. setup→enable(MFA_VALID_CODE) → 게이트 해제 → `/dashboard` 진입 + 배너 사라짐.
- E2E-4. 비강제 사용자 → 리다이렉트·배너 없음. 기존 `mfa`/`login` E2E 회귀 0.

**검증**. `pnpm --filter web test:e2e -- mfa-enforcement` + 기존 mfa/login E2E 재실행(회귀 0).

## Plan 메타

- task 수: 5
- 예상 wave: 4 (W1=[T1] / W2=[T2,T4] / W3=[T3] / W4=[T5]). 파일 겹침·의존성으로 대부분 직렬.
- TDD 강제: yes (red→green→refactor)
- 구현 분담: D6=frontend-engineer(T1~T4), D7=qa-engineer(T5)
- 추가 검증: typecheck · lint · vitest(단위) · playwright(E2E)
- 백엔드 변경 0 (이미 #123). 신규 라우트 0 (Option A2).

## 리뷰 결과

> 방식. type=auth → eng+ceo+security 관점. 대화형 리뷰 스킬 대신 코드베이스 직접 대조(ground-truth)
> — 이미 정의된 FR + 메모리 교훈(bts-review-plan autoplan overkill, FR-PM-08 직접 ground-truth).

### plan-eng-review (직접, 2026-06-12)
- ✅ `requireMfaEnrolled`가 `requirePasswordChanged`(routeGuard.ts:86) 정확 동형 — 검증됨.
- ✅ FR-D6-4 해제 흐름 정합 — `doRefresh`(refresh_token 쿠키)→whoami(`apiGet`)→`setSession` 순서로 store stale 해소. 백엔드가 클레임 재계산.
- 🔧 **Task 3 정밀화(BLOCKER 아님, 모호→실측)**. router.ts:6 공유 const `requireAuthAndPasswordChanged` 1곳 + admin 3라우트(audit-logs/notification-policies/users-new)만 수정하면 전부 커버. 원안의 "보호 라우트에 합성"을 실 wiring으로 교체.
- ⚠️ fieldPermissions 라우트(router.ts:165)는 requirePasswordChanged조차 빠진 pre-existing 불일치 — 범위 밖(surgical).
- ⚠️ whoami Zod fanout(~22 test 리터럴) — `makeWhoami` 팩토리로 churn 최소화 + `typecheck`가 누락 safety net.
- BLOCKER: 없음.

### plan-ceo-review (직접, 2026-06-12)
- ✅ 범위 타이트 — 프론트 D6/D7만, 신규 라우트 0, 백엔드 변경 0. ADR D4 + 형제 PR(#116 FR-MF-01, #121 FR-MF-02)와 1:1 분할 일관.
- ✅ 리다이렉트 대상 Option A2 Maxi 확정. scope creep 없음.
- BLOCKER: 없음.

### security-review (직접, 2026-06-12 — auth 필수)
- ✅ 클라이언트 가드 = UX 편의. 권위 출처는 백엔드 게이트(403, `MfaEnrollmentGateFilter`) — NFR-1 명시. 가드는 additive라 우회 신설 없음.
- ✅ 해제는 httponly refresh 쿠키 + 백엔드 클레임 재계산 경로 — 클라이언트 위조 우회 불가.
- ✅ 해제 후 /dashboard 이동해도, 미등록이면 백엔드가 모든 비-allowlist 요청 403 유지(클라 네비게이션이 강제 무력화 못 함).
- BLOCKER: 없음.

## PR 단위 코드리뷰 (게이트 2, 2026-06-12)

### superpowers:code-reviewer — CONCERNS (BLOCKER 0)
절대 규칙 19개 위반 0, 보안 가드 additive(우회 없음), 백엔드 1:1 계약 정확, 단위 2399 그린. CONCERN 3건 — 전부 머지 전 수정 완료(commit `1cbc80eb`).
- **CONCERN-2(해결)** 배너가 미정의 `warning` 토큰 사용 → 무색 렌더(시각 결함, E2E 미탐지). → 정의된 `primary` 토큰으로 교체(ChangePasswordForm 선례).
- **추가 A(해결)** E2E 양성 `getByRole('status')`가 로딩 스켈레톤·백업코드 status와 strict-mode 다중 매칭 취약. → `.filter({ hasText: enforcementBanner })` 한정.
- **CONCERN-1(해결)** `refreshSession().then()` `.catch` 부재 → unhandled rejection. → 의도 주석 `.catch` 추가(거부 시 clearSession 폴백, spec EC-3).
- **관찰 B(범위 밖)** fieldPermissions 라우트(router.ts:165)는 requirePasswordChanged·requireMfaEnrolled 둘 다 누락 — pre-existing, 이 PR 불수정(surgical). 후속 정리 후보(Maxi 판단).

### 컨트롤러 직접 검증(/review 대체)
모든 task git log TDD 순서 확인 + diff 대조 + typecheck(tsconfig.app.json, CI 동일)/lint/2399 단위/E2E 4신규·17회귀 직접 실행. 메모리 교훈(verifier 진실출처=controller) 준수.

### 재검증(수정 후)
typecheck/lint clean · 단위 2399 · mfa-enforcement E2E 4/4 · 기존 mfa/login E2E 회귀 0.
