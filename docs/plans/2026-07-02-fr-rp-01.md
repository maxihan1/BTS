# FR-RP-01 번다운/번업 차트

> slug: fr-rp-01
> type: backend
> agent: backend-engineer
> primary_bc: notification-dashboard (fr-index §4.1 정본 — classify는 agile-planning으로 판정, domain 단계 확정 필요)
> 생성: 2026-07-02

## Brief

사용자 원문: "fr-rp-01 진행하자"

FR-RP-01 = 번다운(Burndown) / 번업(Burnup) 차트 (fr-index §4.1, notification-dashboard BC).
스프린트의 잔여 작업량(번다운) 및 완료 누적량(번업)을 시간축으로 시각화.

classify 결과:
- slug: fr-rp-01
- type: backend
- agent: backend-engineer
- primary_bc: agile-planning (⚠ fr-index는 notification-dashboard — domain에서 확정)

## 도메인 정리

- **모듈**: agile-planning (엔드포인트 `GET /api/v1/sprints/{id}/burndown`, SprintController 이웃). fr-index 논리 BC 라벨(notification-dashboard)은 유지 — 물리 모듈 ≠ 논리 BC. FR 카운트 변동 없음(FR-TT 선례).
- **영향 엔티티/데이터**: Sprint·sprint_issues(agile-planning, 로컬) + issues.original_estimate_seconds·worklogs(issue-tracking, cross-BC 포트로 읽기 전용).
- **새 용어(read-model VO)**: BurndownPoint {date, remainingSeconds, idealSeconds}, BurnupPoint {date, completedSeconds, scopeSeconds}. 애그리거트/테이블 없음.
- **Y축 지표(Maxi 확정)**: 잔여 추정 시간(초). 스토리 포인트 미구현이라 대체. 총 스코프 = Σ original_estimate_seconds, 잔여(D) = 스코프 − Σ time_spent(started_at ≤ D).
- **데이터 소스(Maxi 확정)**: On-the-fly in-memory 계산. sprint_burndown_snapshot 테이블 + 스케줄러 미채택. 신규 스키마 0, 과거 스프린트 소급 가능.
- **cross-BC 포트**: shared-kernel에 SprintWorklog 조회 포트 신설(issue-tracking 어댑터 구현, agile-planning 소비). TimelineLookupPort 선례.
- **기존 결정 충돌**: SDD §13.5.1(스토리포인트)·§13.6(스냅샷)·§6.5와 deviation → 같은 PR에서 SDD 갱신 필수(ADR §SDD 동기화 대상).
- **관련 ADR**: [docs/decisions/2026-07-02-fr-rp-01-burndown-burnup.md](../decisions/2026-07-02-fr-rp-01-burndown-burnup.md)

## 스펙

전체 스펙. [docs/specs/2026-07-02-fr-rp-01.md](../specs/2026-07-02-fr-rp-01.md)

핵심 시나리오 3줄 요약.
- `GET /api/v1/sprints/{id}/burndown` — 번다운(Actual/Ideal) + 번업(Completed/Scope) 시계열을 단일 응답으로. 지표=추정시간(초).
- On-the-fly: 스코프=Σ original_estimate, day D 잔여=max(0, 스코프 − Σ worklog(started_at UTC ≤ D)). Ideal은 선형, Scope는 평탄.
- 권한=프로젝트 BROWSE(SprintController.get 미러). cross-BC 포트로 estimate/worklog 조회. 신규 스키마 0.

범위. 백엔드 D1~D5(도메인·계산·API·테스트·포트). 프론트 recharts(D6)+E2E(D7)는 후속 PR — FR-TL-01 선례. **게이트1에서 Maxi 확인.**

## Brainstorming Check

✅ 통과 (1회 자체 적대 검토). 보강: 보안 그레인(NFR5, 프로젝트 BROWSE·이슈별 필터 미적용·security 리뷰), remaining_estimate 미사용, 타임존 UTC·Clock 주입·클램프·엣지 커버.

## Plan

> TDD red→green→refactor 강제. 각 task 메타(agent/files/depends-on)로 bts-impl이 wave 계산.
> 모듈 컴파일 직렬화(learnings) 고려 — depends-on은 실 코드 의존만.

### Task 1. shared-kernel 번다운 조회 포트

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/shared-kernel/src/main/kotlin/com/bts/shared/burndown/SprintBurndownLookupPort.kt`, `backend/modules/shared-kernel/src/test/kotlin/com/bts/shared/burndown/SprintBurndownLookupPortTest.kt`]
- depends-on: []

**RED**. 포트 계약 테스트 — default 구현이 빈 `BurndownSource(0, emptyList())` 반환(fail-safe).
```kotlin
@Test fun `default fetchBurndownSource returns empty fail-safe`() { ... }
```
실패: `SprintBurndownLookupPort` 없음.

**GREEN**. interface + default 메서드 + `BurndownSource`/`WorklogContribution` data class. TimelineLookupPort 미러(fail-safe default, KDoc에 BC 격리·의존 방향).
- `fun fetchBurndownSource(issueKeys: Set<String>): BurndownSource = BurndownSource(0, emptyList())`
- `WorklogContribution(startedOnUtcDate: LocalDate, timeSpentSeconds: Long)` — **UTC 날짜별 pre-aggregate**(NFR3).

**REFACTOR**. KDoc 정리(fail-safe 사유, issueKeys 빈 집합 처리).
**검증**. `./gradlew :backend:shared-kernel:test --tests SprintBurndownLookupPortTest`

### Task 2. agile-planning 번다운 계산기 (순수 도메인)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/domain/burndown/BurndownPoint.kt`, `backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/domain/burndown/BurndownCalculator.kt`, `backend/modules/agile-planning/src/test/kotlin/com/bts/agileplanning/domain/burndown/BurndownCalculatorTest.kt`]
- depends-on: []   # 원시 타입만 입력, 포트 미참조 → 병렬 가능

**RED**. 순수 함수 단위 테스트 (스펙 §계산 알고리즘 + 엣지).
```kotlin
@Test fun `S1 - remaining 16h to 10h, ideal linear, completed cumulative, scope flat`()
@Test fun `1-day sprint (end==start) - single point, no div-by-zero`()
@Test fun `empty scope - all remaining 0, ideal 0 flat`()
@Test fun `logged over estimate - remaining clamped at 0`()
@Test fun `future days after asOf - remaining and completed null`()
@Test fun `worklog before start folds into start bucket`()
```
실패: `BurndownCalculator` 없음.

**GREEN**. `BurndownCalculator.calculate(start, end, scopeSeconds, worklogByUtcDate, today): List<BurndownPoint>` 순수 함수.
- `BurndownPoint(date, remainingSeconds: Long?, idealSeconds: Long, completedSeconds: Long?, scopeSeconds: Long)`.
- asOf = min(end, today). ideal 선형(Long 반올림, end==start 분기). start 이전 worklog는 start 버킷 합산. clamp max(0,·).

**REFACTOR**. 상수 추출, KDoc(입력 계약·null 의미).
**검증**. `./gradlew :backend:agile-planning:test --tests BurndownCalculatorTest`

### Task 3. issue-tracking 번다운 조회 어댑터 (jOOQ)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/adapter/outbound/burndown/SprintBurndownLookupAdapter.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/adapter/outbound/burndown/SprintBurndownLookupAdapterIntegrationTest.kt`]
- depends-on: [1]

**RED**. Testcontainers 통합 테스트 — issues(추정치) + worklogs 시드 후 `fetchBurndownSource(keys)`가 estimate 합 + UTC 날짜별 worklog 집계 반환. 미삭제만.
```kotlin
@Test fun `sums original_estimate and aggregates worklog by UTC date, excludes deleted`() { ... }
```
실패: `SprintBurndownLookupAdapter` 없음.

**GREEN**. `@Component` 어댑터. 2쿼리 이하 — (a) Σ original_estimate_seconds (issue_key IN, deleted_at IS NULL, NULL=0 COALESCE), (b) worklogs GROUP BY (started_at AT TIME ZONE 'UTC')::date, SUM(time_spent_seconds) (미삭제 이슈·worklog). issue_key→issues.id 조인. `SprintBurndownLookupPort` 구현.

**REFACTOR**. SQL 상수화, KDoc(UTC 버킷·FR-TT-02 선례).
**검증**. `./gradlew :backend:issue-tracking:test --tests SprintBurndownLookupAdapterIntegrationTest`

### Task 4. agile-planning 번다운 애플리케이션 서비스 + 포트 배선

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/application/SprintBurndownService.kt`, `backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/application/SprintBurndownExceptions.kt`, `backend/modules/agile-planning/src/test/kotlin/com/bts/agileplanning/application/SprintBurndownServiceTest.kt`, `backend/modules/agile-planning/src/test/kotlin/com/bts/agileplanning/AgilePlanningTestcontainersConfig.kt`]
- depends-on: [1, 2]

**RED**. 서비스 단위 테스트 (mockk: SprintRepository·IssuePermissionResolver·SprintBurndownLookupPort, 고정 Clock).
```kotlin
@Test fun `missing BROWSE permission - 403`()
@Test fun `sprint not found - 404`()
@Test fun `start or end date null - 422 SprintDatesRequired`()
@Test fun `happy path - delegates to calculator with port data`()
```
실패: `SprintBurndownService` 없음.

**GREEN**.
- `SprintBurndownService(sprintRepository, permissionResolver, burndownPort, calculator, clock: Clock)`.
- getBurndown(actorId, sprintId): findById(404 SprintNotFoundException) → hasPermission(BROWSE, Project(projectKey))(403 ResponseStatusException) → start/end null 검사(422 `SprintDatesRequiredException`) → findIssueKeys → port.fetchBurndownSource → calculator.calculate(clock UTC date) → 결과 VO.
- **AgilePlanningTestcontainersConfig에 `SprintBurndownLookupPort` stub 빈 등록**(learnings: cross-BC SPI 부팅함정 — 미등록 시 전체-컨텍스트 부팅 실패). BoardIssueLookupPort/TimelineLookupPort stub 선례.

**REFACTOR**. 권한/조회 헬퍼 정리, KDoc.
**검증**. `./gradlew :backend:agile-planning:test --tests SprintBurndownServiceTest`

### Task 5. agile-planning 번다운 REST 엔드포인트

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/web/SprintBurndownController.kt`, `backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/web/dto/BurndownResponse.kt`, `backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/web/SprintExceptionHandler.kt`, `backend/modules/agile-planning/src/test/kotlin/com/bts/agileplanning/web/SprintBurndownControllerTest.kt`]
- depends-on: [4]

**RED**. MockMvc 웹슬라이스 — 200(응답 형식·null 미래일) + 422(dates) + 401(미인증).
```kotlin
@Test fun `GET burndown returns 200 with points and nullable future days`()
@Test fun `dates missing returns 422`()
```
실패: 엔드포인트 없음.

**GREEN**.
- **신규** `SprintBurndownController` `@RequestMapping("/api/v1/sprints")` `@GetMapping("/{id}/burndown")`. currentActorId(401) → service.getBurndown. (SprintController 미변경 → 기존 테스트 회귀 0)
- `BurndownResponse`(DataResponse 래핑, LocalDate ISO, 초 정수, remaining/completed nullable). VO→DTO 매핑.
- **SprintExceptionHandler**: `assignableTypes`에 `SprintBurndownController::class` 추가 + `SprintDatesRequiredException`→422 ProblemDetail 핸들러 추가(catch-all이 ResponseStatus/도메인예외 삼키지 않게 기존 패턴 유지).

**REFACTOR**. KDoc(엔드포인트 계약·예외 표), 매핑 함수 추출.
**검증**. `./gradlew :backend:agile-planning:test --tests SprintBurndownControllerTest`

### Task 6. agile-planning 번다운 통합 테스트 (D5)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/agile-planning/src/test/kotlin/com/bts/agileplanning/integration/SprintBurndownIntegrationTest.kt`]
- depends-on: [5]

**RED→GREEN**. `@SpringBootTest(RANDOM_PORT)` + Testcontainers. sprints/sprint_issues 시드, 테스트용 `SprintBurndownLookupPort` 구현이 통제된 `BurndownSource` 반환(BC 격리 — 실 issue-tracking 어댑터는 Task 3에서 검증). 검증: 200 수치(S1: remaining 16h→10h·ideal 선형·completed 누적·scope 평탄) + 401/403/404/422. 고정 Clock 주입(결정성).
- identity-access prod+RANDOM_PORT 부팅 레시피/기존 SprintIntegrationTest 패턴 재사용.
**검증**. `./gradlew :backend:agile-planning:test --tests SprintBurndownIntegrationTest`

### Task 7. SDD 동기화 (docs)

**메타**.
- agent: `backend-engineer`
- files: [`docs/sdd/13-board-backlog-timeline.md`, `docs/sdd/06-scenarios.md`]
- depends-on: []

**변경** (TDD 무관 — 문서). §명세 변경 전수 동기화(ADR §SDD 동기화 대상).
- §13.5.1 "Y축: 남은 스토리 포인트" → "잔여 추정 시간(초)" + 스토리포인트 미구현 명시.
- §13.6 `sprint_burndown_snapshot` 테이블·매일 자정 잡 → on-the-fly in-memory 채택으로 정정(스냅샷 미채택 사유 + ADR 링크).
- §6.5 "매일 자정 스냅샷" 시나리오 문구 정정.
- fr-index/README/CLAUDE FR 카운트 변동 없음(BC 라벨 유지). `bash scripts/verify-master-plan.sh` 통과 확인.
**검증**. `bash scripts/verify-master-plan.sh`

## Plan 메타

- task 수: 7
- 예상 wave: 4 (W1: T1·T2·T7 병렬 / W2: T3·T4 / W3: T5 / W4: T6). 모듈 컴파일은 직렬화(learnings).
- TDD 강제: yes (T7 docs 제외)
- 신규 스키마/마이그레이션: 없음 (on-the-fly)
- cross-BC 신규 포트: SprintBurndownLookupPort (shared-kernel 정의 · issue-tracking 구현 · agile-planning 소비)
- 추가 검증: ktlint, detekt, verify-master-plan.sh(SDD 동기화), 기존 SprintController/SprintApplicationService 테스트 회귀 0
- 후속 PR: 프론트 recharts(D6) + E2E(D7)

## 리뷰 결과 (← /bts-review-plan 채움)
