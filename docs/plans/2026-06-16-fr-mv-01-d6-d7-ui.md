# FR-MV-01 D6/D7 프론트 이동 마법사 UI + E2E

> slug: fr-mv-01-d6-d7-ui
> type: ui
> agent: frontend-engineer (designer 디자인 확인 → frontend 구현, qa-engineer E2E)
> BC: issue-tracking (apps/web)
> 생성: 2026-06-16

## Brief

프로젝트 간 이슈 이동 다이얼로그(Jira "Move Issue Wizard"). 백엔드 #153(단건)+#154(서브태스크 동반, 노드별 매핑) 완료.
preview(POST /{key}/move/preview)+move(POST /{key}/move) 2단계 API 소비.
마법사: 대상 프로젝트 선택 → preview 노드별 비호환 매핑(컴포넌트/버전/커스텀필드/워크플로우 상태) 계산 →
사용자 매핑 확인 → move 실행. 단건 + 서브태스크(부모+직접 자식 노드별 subtasks 배열) 지원. 응답 movedSubtasks.
D7: 이동 + 옛 키 308 redirect E2E (FR-IS-01 D7에서 이관). 완료 시 FR-MV-01 전체 종료.

## 도메인 정리

- **BC**: issue-tracking (apps/web 프론트). 백엔드 도메인/API는 #153/#154에서 확정 — 프론트는 **소비측**.
- **새 도메인 모델/용어**: 없음 (기존 이동 의미론 그대로 — Jira Move Wizard, 노드별 매핑, redirect).
- **기존 결정 충돌**: 없음. ADR [docs/adr/2026-06-16-issue-move-semantics.md](../adr/2026-06-16-issue-move-semantics.md) 소비.
- **영향 (apps/web)**:
  - 이동 마법사 다이얼로그 컴포넌트 (신규) — 다단계(대상 프로젝트 선택 → 매핑 확인 → 실행)
  - 이슈 상세 페이지 "이동" 액션 진입점
  - `api/issues` 이동 함수(preview/move) + TanStack Query 훅
  - **Zod 스키마** — `MovePreview`(workflow/components/affectsVersions/fixVersions/customFields + **subtasks 노드별 섹션**), `MoveRequest`(평면 루트 + **subtasks 배열**, 자식 식별 `issueKey`), `MoveResponse`(issueKey/previousKey + **movedSubtasks**)
- **핵심 리스크 (메모리 교훈)**:
  - **Zod ↔ 백엔드 DTO 1:1 미러** — invent 금지, 백엔드 `MoveDtos.kt`/`MovePreviewService.kt`(머지됨)를 spec서 grep 검증 ([[frontend-zod-backend-dto-contract-gap]])
  - `@JsonInclude(NON_NULL)` ↔ Zod nullable/optional 정합 (백엔드 nullable 필드 실측)
  - 옛 키 308 redirect — SPA 라우팅에서 새 키로 이동 처리 (E2E 검증)
- **관련 ADR**: docs/adr/2026-06-16-issue-move-semantics.md (§후속 결정 = 서브태스크 동반)

## 스펙

전체 스펙. [docs/specs/2026-06-16-fr-mv-01-d6-d7-ui.md](../specs/2026-06-16-fr-mv-01-d6-d7-ui.md)

핵심 시나리오 3줄 요약.
- 이슈 상세 "이동" → radix-ui Dialog 마법사(step useState): 대상 프로젝트 키 입력 → preview 노드별 매핑(루트+subtasks) → 확인/실행
- 단건+서브태스크 노드별 매핑(상태 select·컴포넌트/버전 매핑/제거·필수필드 입력), move 페이로드 노드별, 성공 시 새 키 이동+movedSubtasks 토스트
- Zod 백엔드 1:1 미러, 옛 키 308 redirect → 새 키 SPA navigate (D7 E2E)

## Brainstorming Check

✅ 통과 (1회 iteration). adversarial gap 4건, Maxi 확정.
- **G1/G2 (중대)**: preview가 move 구성에 부족(자식 version·상태 isDone 부재) → **백엔드 view layer patch 이 PR 포함**(Maxi 확정, BC 예외). issue-tracking version + shared-kernel WorkflowStateView.isDone + project-workflow Impl
- G3 308 redirect SPA navigate, G4 미매핑=제거 select
- **범위 주의**: 프론트 + 백엔드 3모듈 view layer. 게이트1 규모 검토(과하면 백엔드 선행 PR 분리 가능)

## Plan

**Goal**. 이슈 이동 마법사(단건+서브태스크 노드별 매핑) UI + E2E. 백엔드 preview/move(#153/#154) 소비 + view layer 보강(version/isDone).

**Architecture**. 백엔드 view layer patch(T1·T2, BC 예외) → 프론트 api+훅(T3) → 마법사 Dialog(T4) → 진입점+redirect(T5) → E2E(T6). 강한 의존 체인.

**Tech Stack**. React 19/TS strict, radix-ui Dialog(직접), TanStack Query, Zod(백엔드 1:1 미러). 의존성 추가 0. 백엔드 Kotlin/jOOQ.

### Task 1. 백엔드 — WorkflowStateView.isDone (shared-kernel + project-workflow)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/shared-kernel/src/main/kotlin/com/bts/shared/workflow/WorkflowStateView.kt`, `backend/modules/project-workflow/src/main/kotlin/com/bts/workflow/scheme/adapter/inbound/WorkflowStateCatalogImpl.kt`, `backend/modules/project-workflow/src/test/kotlin/com/bts/workflow/scheme/adapter/inbound/WorkflowStateCatalogImplTest.kt`, `backend/modules/shared-kernel/src/test/kotlin/com/bts/shared/workflow/WorkflowStateCatalogContractTest.kt`]
- depends-on: []
- 참고: `isDone = false` 기본값이라 issue-tracking 테스트(IssueMoveServiceTest/ToctouTest/MovePreviewServiceTest)는 컴파일 보존(미수정). DONE 단언 필요 시 해당 테스트가 명시(T2에서 다룸)

- [ ] **Step 1: RED** — `WorkflowStateCatalogImplTest`에 listStates 결과 각 상태가 DONE 카테고리면 `isDone=true`, 아니면 false 검증. (`WorkflowStateView.isDone` 없어 컴파일 실패)
- [ ] **Step 2: 실패 확인** — `cd backend && ./gradlew :modules:project-workflow:test --tests "*WorkflowStateCatalogImplTest" -i`
- [ ] **Step 3: GREEN**
  - `WorkflowStateView`에 **`val isDone: Boolean = false`** 추가 (shared-kernel). **기본값 `= false` 필수** — 리뷰 확인 결과 생성처 15곳(`WorkflowStateCatalogContractTest`·`IssueMoveServiceTest`·`IssueMoveServiceToctouTest`·`MovePreviewServiceTest`·Impl)이 현재 `(key,name)` 2-arg라, 기본값 없으면 전부 컴파일 깨짐. 기본값으로 컴파일 보존
  - `WorkflowStateCatalogImpl.listStates`(`:78`)가 `isDone = state.category == StateCategory.DONE`으로 채움. **category 실존 확인됨**(`WorkflowState.category: StateCategory`, `StateCategory.DONE`)
  - ImplTest에서 DONE 상태는 `isDone=true`, 그 외 false 명시 단언
- [ ] **Step 4: 통과 확인** — 위 명령 PASS
- [ ] **Step 5: REFACTOR** — KDoc(isDone 의미), ktlint/detekt 파일 단위
- [ ] **Step 6: 커밋** — `feat: WorkflowStateView.isDone (이동 마법사 targetStateIsDone 출처)`

### Task 2. 백엔드 — MovePreview/SubtaskPreviewNode version (issue-tracking)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/application/MovePreviewService.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/application/MovePreviewServiceTest.kt`]
- depends-on: [1]   # 같은 preview 응답 형식 일관 (isDone+version)

- [ ] **Step 1: RED** — `MovePreviewServiceTest`에 preview 응답의 `MovePreview.version`(루트 issue.version) + 자식 있을 때 `SubtaskPreviewNode.version`(자식 issue.version) 검증. (필드 없어 컴파일 실패)
- [ ] **Step 2: 실패 확인** — `--tests "*MovePreviewServiceTest"`
- [ ] **Step 3: GREEN** — `MovePreview`에 `version: Long`, `SubtaskPreviewNode`에 `version: Long` 추가. `preview()`가 조회한 root/child issue.version 채움
- [ ] **Step 4: 통과 확인** → PASS
- [ ] **Step 5: REFACTOR** — KDoc, ktlint/detekt
- [ ] **Step 6: 커밋** — `feat: MovePreview/SubtaskPreviewNode version (move OCC 출처)`

### Task 3. 프론트 — api/issue-move.ts (previewMove/moveIssue + Zod + useMoveIssue 훅)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/issue-move.ts`, `apps/web/src/api/issue-move.test.ts`]
- depends-on: [1, 2]   # 백엔드 API 형식(version/isDone) 확정 후 Zod 1:1 미러

- [ ] **Step 1: RED** — `issue-move.test.ts`에 (a) `movePreviewSchema.parse`가 version/isDone/subtasks 포함 응답 통과, (b) `previewMove`/`moveIssue`가 올바른 경로·바디 호출, (c) `moveResponseSchema`가 movedSubtasks 파싱. MSW로 백엔드 응답 mock
- [ ] **Step 2: 실패 확인** — `pnpm test issue-move`
- [ ] **Step 3: GREEN** (issue-links.ts 선례 — apiFetch/ApiError + 로컬 `dataResponseSchema`)
  - Zod 스키마: `movePreviewSchema`(workflow{compatible,targetStates:[{key,name,isDone}],suggestedStateKey}, components/affects/fixVersions{current,target,autoMapping}, customFields{removed,requiredMissing}, version, subtasks:[{issueKey,issueTypeKey,version,...}]) — **백엔드 grep해 nullable/optional 실측 미러**
  - **nullable 정합 (리뷰 확인 실측)**: `issueTypeKey` `.nullable()`, `suggestedStateKey` `.nullable()`, `autoMapping`/`componentMapping` 등 `z.record(키, z.string().uuid().nullable())`(value nullable), `subtasks`/`movedSubtasks`는 백엔드 `emptyList()`라 non-null `z.array()`, `MoveRequest.subtasks`는 `.default([])`. 백엔드 application DTO는 `@JsonInclude` 없어 null이 `"key":null`로 직렬화 → Zod `.nullable()`
  - `previewMove(key, targetProjectKey)` POST move/preview, `moveIssue(key, payload)` POST move
  - `useMoveIssue` mutation 훅 (onSuccess invalidate)
- [ ] **Step 4: 통과 확인** — `pnpm test issue-move` + `pnpm typecheck`
- [ ] **Step 5: REFACTOR** — 타입 export, 기존 Component/Version/CustomFieldDefinition 타입 재사용
- [ ] **Step 6: 커밋** — `feat: issue-move api + Zod 스키마(백엔드 1:1 미러)`

### Task 4. 프론트 — MoveIssueDialog 마법사

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/issues/MoveIssueDialog.tsx`, `apps/web/src/components/issues/MoveIssueDialog.test.tsx`, `apps/web/src/i18n/ko.ts`]
- depends-on: [3]

- [ ] **Step 1: RED** — `MoveIssueDialog.test.tsx`: (a) Step1 대상 키 입력→preview 호출, (b) Step2 노드별(루트+subtasks) 매핑 폼 렌더(비호환 상태 select·컴포넌트/버전 매핑/제거·필수필드 입력), (c) Step3 move 호출 페이로드 노드별 정확(자식 issueKey·version·targetStateIsDone=선택상태 isDone), (d) 단건(subtasks 빈)도 동작, (e) 비호환 상태/필수필드 미충족 시 move 비활성
- [ ] **Step 2: 실패 확인** — `pnpm test MoveIssueDialog`
- [ ] **Step 3: GREEN** — radix-ui Dialog 직접(AddMemberDialog 선례) + `useState` step(1~3). preview→노드별 폼→move. 미매핑=select "(제거)" 항목. i18n ko 키(콜론 종결 금지)
- [ ] **Step 4: 통과 확인** — `pnpm test MoveIssueDialog` + `pnpm typecheck`
- [ ] **Step 5: REFACTOR** — 노드 매핑 폼을 `NodeMappingSection` 하위 컴포넌트로 분리(루트/자식 재사용), 컴포넌트 200줄 이내
- [ ] **Step 6: 커밋** — `feat: MoveIssueDialog 이동 마법사(노드별 매핑)`

### Task 5. 프론트 — 진입점 + 옛 키 308 redirect

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/issues.$key.tsx`, `apps/web/src/routes/issues.$key.test.tsx`]
- depends-on: [4]

- [ ] **Step 1: RED** — `issues.$key.test.tsx`: (a) "이동" 액션이 권한 보유 시 노출·MoveIssueDialog 열림, (b) move 성공 시 새 키로 navigate, (c) 옛 키 접근(308) → 새 키 라우트로 navigate(브라우저 URL 갱신)
- [ ] **Step 2: 실패 확인** — `pnpm test issues.\$key`
- [ ] **Step 3: GREEN** — 이슈 상세에 "이동" 액션(권한 게이팅 기존 훅) + MoveIssueDialog 마운트.
  - **308 처리 (리뷰 확인 구체화)**: `apiFetch`는 `redirect:'follow'`(기본)라 same-origin 308을 **자동 추적** → 200 응답 + `response.redirected===true` + `response.url`=`/api/v1/issues/{newKey}`(API 경로). 현재 `fetchIssue`(`issues.ts`)는 `redirected` 미확인 → 옛 키 URL에 새 키 데이터 표시되는 불일치. **수정**: 이슈 상세 fetch에서 `response.redirected` 확인 → `response.url`에서 `{newKey}` 정규식 추출 → `navigate({to:'/issues/$key', params:{key:newKey}, replace:true})`(브라우저 URL 갱신). 구현 시 백엔드 Location 형식(`IssueExceptionHandler` redirect) 재확인
- [ ] **Step 4: 통과 확인** — `pnpm test issues.\$key` + `pnpm typecheck`
- [ ] **Step 5: REFACTOR** — 308 처리 헬퍼 분리(재사용 가능 시), KDoc
- [ ] **Step 6: 커밋** — `feat: 이슈 이동 진입점 + 옛 키 308 redirect 처리`

### Task 6. E2E — 이동 + 서브태스크 + 옛 키 redirect (qa-engineer)

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/tests/e2e/issue-move.spec.ts`]
- depends-on: [5]

- [ ] **Step 1: 시나리오 작성** (구현 코드 수정 금지, MSW 핸들러 추가)
  - (a) 단건 이동: 이슈 상세 → 이동 마법사 → 대상 입력 → preview → 확인 → 새 키 상세 표시
  - (b) 서브태스크 동반: 부모 이동 → 노드별 매핑 → move → movedSubtasks 토스트
  - (c) 옛 키 308 redirect: 옛 키 URL 접근 → 새 키 상세 표시
  - MSW: preview/move/308 redirect 핸들러. **stateful store 필수**(메모리 [[msw-mutation-stateful-refetch]]) — move 핸들러가 "이동됨" 상태를 store에 기록 → 이후 옛 키 GET이 308(새 키) 반환. 브라우저 시드 가능 공유 store([[msw-derived-behavior-shared-store-e2e]])
- [ ] **Step 2: 실행** — `pnpm test:e2e issue-move`
- [ ] **Step 3: 커밋** — `test: issue-move E2E (단건+서브태스크+옛키 redirect)`

## Plan 메타

- task 수: 6 (백엔드 view layer 2 + 프론트 3 + E2E 1)
- wave: 거의 직렬 (T1→T2→T3→T4→T5→T6, 강한 의존 체인). 백엔드 T1/T2 다른 모듈이나 preview 응답 형식 일관 위해 순차
- TDD 강제: yes (프론트 vitest test→feat, 백엔드 gradle)
- **범위 주의**: 백엔드 3모듈(shared-kernel·project-workflow·issue-tracking) view layer + 프론트 + E2E. BC 격리 예외(frontend 요구 view layer, Maxi 확정). 게이트1에서 규모 검토 — 과하면 백엔드 보강(T1·T2)을 선행 PR로 분리 가능
- 추가 검증: 백엔드 `:modules:*:test`+detekt+ktlint(--rerun-tasks), 프론트 `pnpm typecheck lint test`, `pnpm test:e2e`

## 리뷰 결과

### plan 독립 리뷰 (backend-engineer, 2026-06-16)

plan-design-review/autoplan 미설치 → backend-engineer 독립 적대적 리뷰(백엔드 view layer 실현가능성 중점).

**BLOCKER 1건 — plan 보강으로 해소**.
- **검증5 (308 redirect)**: apiFetch `redirect:'follow'`(기본)가 308 자동 추적 → `response.url`=`/api/v1/issues/{newKey}`. 현재 `fetchIssue`가 `redirected` 미확인 → 옛 키 URL에 새 키 데이터 불일치. **해소**: T5 Step3에 `response.redirected` 확인→`response.url`서 newKey 추출→`navigate(replace:true)` 구체화.

**CONCERN 4건 — 반영**.
- **검증1/2 (isDone 생성처)**: `WorkflowStateView` 생성처 15곳 → `isDone: Boolean = false` **기본값**으로 컴파일 보존(T1 GREEN 명시). category 실존 확인(`StateCategory.DONE`).
- **검증4 (Zod nullable)**: issueTypeKey/suggestedStateKey `.nullable()`, Map value nullable, subtasks/movedSubtasks non-null array → T3 GREEN 실측 목록 명시.
- **검증7 (T1 files)**: `WorkflowStateCatalogContractTest` files 추가.
- **검증7 (T6 MSW)**: stateful store 패턴 명시(이동 후 옛 키 308 유지).

**통과**: 검증3(BC 예외 Maxi 확정·view layer 읽기전용이라 단일 PR 적절), 검증6(단건 회귀 subtasks=[] 보존), task 의존 체인.

**plan 수정 필요: YES → BLOCKER 1·CONCERN 4 전부 본문 반영 완료. BLOCKER 0 상태로 게이트1 진입.**
