# FR-AT-02 D6 액션 빌더 UI + D7 E2E

> slug: fr-at-02-d6-d7-ui
> type: ui
> agent: frontend-engineer (+ qa-engineer for D7 E2E)
> primary_bc: automation
> 생성: 2026-07-12

## Brief

사용자 원문: "fr-at-02 d6 d7 진행하자"

FR-AT-02 (automation §2.2) 백엔드 코어 D1~D5 완료(PR #256), prod 조립 완료(#259) 상태에서
**D6 — 프론트 UI(액션 빌더)** + **D7 — E2E**를 이어감.

기존 `projects/$projectKey/settings/automation` 룰 설정 화면(FR-AT-01 D6/D7, PR #254)에
4종 액션(SET_FIELD/ASSIGN/ADD_COMMENT/CALL_WEBHOOK) 편집 UI + rule actor 선택을 추가.
백엔드 룰 CRUD payload는 이미 `actions[]`·`actorUserId`로 확장됨(FR-AT-02 Task 11).

- D6. 프론트 UI — 액션 빌더 (designer → frontend-engineer)
- D7. E2E (qa-engineer)

SDD 참조: 08장 (자동화 엔진). 선행: FR-AT-02 D1~D5(PR #256), FR-AT-01 D6/D7(PR #254).

## 도메인 정리

- **BC**: automation (뷰 레이어 확장 — 새 도메인 모델 없음)
- **성격**: 순수 프론트. 백엔드 도메인(액션·rule actor·dry-run·execution depth)은 FR-AT-02 D1~D5(PR #256)에서 ADR로 확정됨. 이번 작업은 그 확정 계약을 소비하는 UI.
- **새 용어**: 없음 (glossary "액션 = Action. 자동화 룰의 실행 결과" 이미 존재). rule actor·dry-run은 백엔드 내부 개념, UI 노출 문구는 스펙에서 확정.
- **기존 결정 충돌**: 없음. 관련 ADR: [docs/decisions/2026-07-11-fr-at-02-automation-actions.md](../decisions/2026-07-11-fr-at-02-automation-actions.md) (D1~D5, 소비만 함).

### 백엔드 계약 (정본 — 프론트 Zod가 invent 금지, 이 형태에 1:1 정합)

응답 `AutomationRuleResponse` (기존 프론트 Zod에 **누락됨 — D6에서 추가 필수**):
- `actions: ActionResponse[]` — `{ type: ActionType, config: object }` (config는 **객체**/Map)
- `actorUserId: UUID` (**non-null** 응답)

요청 `Create/PatchAutomationRuleRequest`:
- `actions: ActionRequest[]` — `{ type: string, config: JSON문자열 }` (config는 **JSON 문자열**)
- `actorUserId: UUID?` (생성 시 null=생성자 폴백, PATCH null=미변경)

**⚠️ config 비대칭**: 응답 config=객체, 요청 config=JSON 문자열. 읽기는 객체 파싱, 쓰기는 `JSON.stringify` 직렬화 (triggerConfig `serializeTriggerConfig` 선례 동형).

### 4종 액션 config 형식 (Action.kt / ActionConfig.kt 도메인 검증)

| 타입 | config 형식 | 도메인 제약 |
|---|---|---|
| SET_FIELD | `{field, value}` | field=비어있지 않은 문자열(**자유 입력, 실존 검증은 실행 시점 위임**), value=임의 JSON |
| ASSIGN | `{assigneeId}` | uuid 문자열 또는 **null(=담당자 해제)** |
| ADD_COMMENT | `{body}` | 비어있지 않은 문자열, `{{템플릿}}` 지원 |
| CALL_WEBHOOK | `{url, method?, headers?, body?}` | url=http(s):// prefix(템플릿 지원, 엄격 검증은 실행 시점), method 기본 POST, headers=문자열 맵 |

### 재사용 자산
- `apps/web/src/api/project-members.ts` + `use-project-members.ts` — actor/assignee 피커용 사용자 목록(`userId·displayName·username`)
- `AutomationRuleFormDialog.tsx` — 확장 대상 폼(react-hook-form·zodResolver·key 재마운트·409 폼닫기 처리 기존 완비)
- `serializeTriggerConfig` 선례 — actions 직렬화 헬퍼 동형 패턴

### 범위 확정
- dry-run/preview 엔드포인트 **미노출**(컨트롤러 POST/GET/PATCH/DELETE만) → D6에 dry-run UI **불포함**
- 스펙에서 확정할 UX 결정: **SET_FIELD field 입력 방식**(자유입력 vs 큐레이션 드롭다운)

## 스펙

전체 스펙. [docs/specs/2026-07-12-fr-at-02-d6-d7-ui.md](../specs/2026-07-12-fr-at-02-d6-d7-ui.md)

핵심 시나리오 3줄 요약.
- 기존 룰 폼에 4종 액션(SET_FIELD 필드타입별 위젯/ASSIGN 담당자피커/ADD_COMMENT 템플릿/CALL_WEBHOOK) 편집 + 다중 액션 순서변경(위/아래)
- rule actor(actorUserId) 피커 추가(생성 기본=미설정→백엔드 생성자 폴백, 명시선택 시만 전송)
- 프론트 Zod에 actions·actorUserId 추가 + config 비대칭 직렬화(읽기=객체, 쓰기=JSON문자열)

**Maxi 결정 게이트 2건**: SET_FIELD=필드타입별 위젯(자유입력 아님, 6종 큐레이션) · 다중액션+순서변경(drag-drop 없이 위/아래 버튼).

## Brainstorming Check

✅ 통과 (1회 iteration, 수정 가능 gap 4건 발견 후 스펙 보강 — priority/impact 숫자강제·actor 생성기본값 null·unknown SET_FIELD field fallback·Zod config loose record).

## Plan

> TDD red→green→refactor 강제. 각 task 메타(agent/files/depends-on)로 bts-impl이 wave 계산.
> 전 task frontend-engineer(D7 E2E만 qa-engineer). 순수 프론트, 백엔드 변경 0.
> **핵심 함정**: (1) config 비대칭(응답=객체/요청=JSON문자열) — serialize/parse 쌍 분리. (2) Zod 필수필드
> 추가 fanout([[zod-schema-strengthen-inline-mock-fanout]]) — 모든 룰 응답 mock에 actions·actorUserId 필요.
> (3) MSW 신규 아님 — 기존 `automation-rule-handlers.ts` 확장([[msw-global-handler-registration-gap]] 회피).

### Task 1. Zod 계약 + 직렬화 헬퍼 (actions·actorUserId·serialize/parse)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/automation-rules.types.ts`, `apps/web/src/api/automation-rules.types.test.ts`]
- depends-on: []

**RED**: types.test.ts에 (a) `automationRuleResponseSchema`가 `actions`(배열)·`actorUserId`(uuid) 파싱,
(b) `actionTypeSchema` 4종, (c) `parseActionConfig(type, obj)`↔`serializeActionConfig(type, formState)` round-trip
— SET_FIELD priority 값 **숫자 강제**(EC9: `{value:3}` 숫자, `"3"` 아님)·labels 배열·ASSIGN null(해제)·
CALL_WEBHOOK headers 맵, (d) unknown SET_FIELD field 텍스트 fallback(EC10) 단언 → 미구현 실패.
**GREEN**: `actionTypeSchema = z.enum([SET_FIELD,ASSIGN,ADD_COMMENT,CALL_WEBHOOK])`,
`actionResponseSchema = z.object({type: actionTypeSchema, config: z.record(z.string(), z.unknown())})`(EC11 loose),
`automationRuleResponseSchema`에 `actions`·`actorUserId: z.string()` 추가.
`CreateAutomationRuleInput`/`PatchAutomationRuleInput`에 `actions?: ActionRequestInput[]`·`actorUserId?: string`
추가(api가 `body:input` 통째 전달 → 자동 forward, automation-rules.ts 변경 불요).
`serializeActionConfig`/`parseActionConfig` 순수 함수(`serializeTriggerConfig` 선례 동형).
**REFACTOR**: 액션 타입별 형식 상수화, KDoc(config 비대칭 명시). ⚠️ 이 스키마 강화가 룰 응답 mock 전체에
fanout — T2(fixtures)·T6/T7(컴포넌트 test mock)가 각자 해소, 최종 verify가 drift 적발.
**검증**: `apps/web/node_modules/.bin/vitest run src/api/automation-rules.types.test.ts`

### Task 2. MSW fixtures + handlers 확장 (actions·actorUserId stateful) + 스키마 fanout 해소

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/mocks/automation-rule-fixtures.ts`, `apps/web/src/mocks/automation-rule-handlers.ts`, `apps/web/src/mocks/automation-rule-handlers.test.ts`, `apps/web/src/api/automation-rules.test.ts`]
- depends-on: [1]

**RED**: handlers.test.ts에 create가 actions·actorUserId 저장·응답 반영, PATCH actions **전체 교체**+actorUserId
변경, GET 반영 단언 → 미구현 실패. 기존 automation-rules.test.ts가 T1 스키마 강화로 깨진 것도 RED로 간주.
**GREEN**: fixtures 팩토리에 `actions:[]`·`actorUserId` 추가(기본값), stateful store가 create/patch 요청의
actions·actorUserId를 저장·echo. PATCH actions는 replace(부분병합 아님). automation-rules.test.ts 인라인 mock에
필드 추가([[zod-schema-strengthen-inline-mock-fanout]]).
**REFACTOR**: fixture 헬퍼 정리. 기존 `automation-rule-handlers.ts` 확장(신규 파일 아님 — 전역 등록 유지).
**검증**: `apps/web/node_modules/.bin/vitest run src/mocks/automation-rule-handlers.test.ts src/api/automation-rules.test.ts`

### Task 3. ProjectMemberSelect — 재사용 멤버 피커 (ASSIGN·actor 공용)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/automation/ProjectMemberSelect.tsx`, `apps/web/src/components/automation/ProjectMemberSelect.test.tsx`]
- depends-on: []

**RED**: `use-project-members`(mock) 기반으로 멤버 목록 렌더, 선택 시 onChange(userId), displayName 표시,
value가 목록에 없으면 "알 수 없는 사용자"(EC4) fallback, optional "담당자 해제"(null) 항목(prop 토글) 단언.
**GREEN**: `use-project-members(projectKey)` 소비 select 컴포넌트. `allowUnassign` prop으로 null 옵션 노출.
`displayName ?? username ?? userId`. 로딩/에러 상태 처리.
**REFACTOR**: aria-label, 라벨 헬퍼 분리.
**검증**: `apps/web/node_modules/.bin/vitest run src/components/automation/ProjectMemberSelect.test.tsx`

### Task 4. ActionConfigEditor — 액션 타입별 조건부 필드 (4종)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/automation/ActionConfigEditor.tsx`, `apps/web/src/components/automation/ActionConfigEditor.test.tsx`]
- depends-on: [1, 3]

**RED**: 타입 선택 시 조건부 필드 전환 — SET_FIELD(필드 드롭다운 6종 + 타입별 값 위젯: 문자열=text,
priority=1~5 select, impact=1~3 select, labels=태그입력), ASSIGN(ProjectMemberSelect+해제), ADD_COMMENT
(textarea+템플릿 힌트 `{{ issue.key }}`), CALL_WEBHOOK(url·method select·headers 키값행·body). onChange가
구조화 formState 방출 단언. EC10 unknown field 텍스트 fallback.
**GREEN**: `ActionConfigEditor`(value: 폼상태, onChange). SET_FIELD 값 위젯은 필드에 따라 분기. labels 태그칩은
기존 TriggerConfigFields fields 패턴 재사용. `TriggerConfigFields` 선례 동형(조건부 서브컴포넌트).
**REFACTOR**: 타입별 서브컴포넌트 분리(detekt 대응 없음 — TS), 라벨 상수화.
**검증**: `apps/web/node_modules/.bin/vitest run src/components/automation/ActionConfigEditor.test.tsx`

### Task 5. ActionListEditor — 다중 액션 + 순서변경(위/아래) + 추가/삭제

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/automation/ActionListEditor.tsx`, `apps/web/src/components/automation/ActionListEditor.test.tsx`]
- depends-on: [1, 4]

**RED**: 액션 추가(+버튼) → ActionConfigEditor 행 추가, 삭제(×), 위(↑)/아래(↓) 이동으로 순서 변경(S5),
빈 리스트 허용(EC5), onChange가 순서 반영 배열 방출 단언. drag-drop 라이브러리 미사용.
**GREEN**: `ActionListEditor`(value: 액션 formState 배열, onChange). 각 행 ActionConfigEditor + 순서/삭제 버튼.
첫 행 ↑ disabled, 마지막 행 ↓ disabled.
**REFACTOR**: 이동 헬퍼(swap) 순수 함수 추출, aria-label.
**검증**: `apps/web/node_modules/.bin/vitest run src/components/automation/ActionListEditor.test.tsx`

### Task 6. AutomationRuleFormDialog 통합 — actions·actor 배선(로드/직렬화/제출)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/automation/AutomationRuleFormDialog.tsx`, `apps/web/src/components/automation/AutomationRuleFormDialog.test.tsx`]
- depends-on: [1, 3, 4, 5]

**RED**: (a) 생성 시 actions·(선택된)actorUserId가 POST 바디에 포함(actor 미선택 시 actorUserId 미전송, EC/FR8),
(b) 편집 모드는 rule.actions(응답 객체 config)→`parseActionConfig`로 폼 로드(S7)·actor 로드, (c) 제출 시
`serializeActionConfig`로 각 config JSON문자열화(S1~S5), (d) 편집 대상 전환 시 stale 없음(key 재마운트, EC6),
(e) 409 기존 처리 유지(S8). **기존 dialog 테스트가 새 바디(actions[]) 반영하도록 갱신**.
**GREEN**: FormBody에 `actions` 상태(ActionListEditor) + `actorUserId` 상태(ProjectMemberSelect) 추가.
onValid에서 actions→ActionRequest[] 직렬화, actorUserId는 값 있을 때만 포함. 편집 초기값 parse. formKey 재마운트 유지.
**REFACTOR**: 폼상태 타입 정리, KDoc.
**검증**: `apps/web/node_modules/.bin/vitest run src/components/automation/AutomationRuleFormDialog.test.tsx`

### Task 7. AutomationRuleList — 액션 타입 배지

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/automation/AutomationRuleList.tsx`, `apps/web/src/components/automation/AutomationRuleList.test.tsx`]
- depends-on: [1]

**RED**: 룰 행에 액션 타입 배지 목록 표시(예: "SET_FIELD · ASSIGN"), 액션 0개면 "액션 없음" 또는 미표시(FR11) 단언.
기존 list 테스트가 스키마 강화로 깨진 것도 함께 해소(fixture actions 필드).
**GREEN**: `AutomationRuleList`에 `rule.actions` 배지 렌더 추가.
**REFACTOR**: 배지 라벨 상수(ACTION_LABELS), aria.
**검증**: `apps/web/node_modules/.bin/vitest run src/components/automation/AutomationRuleList.test.tsx`

### Task 8. E2E — 액션 빌더 happy path (D7)

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/automation-actions.spec.ts`, `apps/web/src/mocks/automation-rule-fixtures.ts`]
- depends-on: [2, 6, 7]

**RED**: E2E 시나리오 — 룰 생성 다이얼로그에서 SET_FIELD(priority=3) 액션 추가 + actor 선택 → 저장 → 목록 배지 확인,
편집 열어 ASSIGN 액션 추가 + 순서변경(↑) → 저장 → 재조회 반영, 4종 액션 각 happy path. MSW 시나리오
(seed 가능 공용 store [[msw-derived-behavior-shared-store-e2e]]). 기존 automation E2E(FR-AT-01) 회귀 없음 확인.
**GREEN**: Playwright 스펙 + (필요 시)fixture seed 보강. `serviceWorkers:'block'` 금지([[e2e-msw-serviceworker-block]]).
**REFACTOR**: 셀렉터 getByRole exact([[playwright-getbyrole-exact-strict-mode]]), 헬퍼.
**검증**: `apps/web/node_modules/.bin/pnpm --dir apps/web test:e2e -- automation-actions.spec.ts` (E2E orphan vite kill [[e2e-orphan-vite-after-worktree-remove]])

## Plan 메타

- task 수: 8 (각 TDD 사이클, D6=T1~7 / D7=T8)
- 예상 wave: 약 5 (W1: T1·T3 → W2: T2·T4·T7 → W3: T5 → W4: T6 → W5: T8). 프론트라 모듈 컴파일 직렬화 없음(TS).
- 예상 시간: 직렬 약 30분, wave 적용 약 15~20분
- TDD 강제: yes (test 커밋 선행 — bts-impl 자동 검증)
- 크로스-BC touch: 없음 (순수 apps/web, 백엔드 변경 0)
- 재사용: `use-project-members`(피커)·`serializeTriggerConfig` 선례·기존 `AutomationRuleFormDialog`/`handleSubmitFailure`(409)
- 추가 검증: tsc(tsconfig.app.json [[ci-typecheck-tsconfig-app-vs-local]])·eslint·vitest·playwright
- 핵심 함정: config 비대칭 / Zod 강화 fanout / MSW 기존 핸들러 확장

## 리뷰 결과
