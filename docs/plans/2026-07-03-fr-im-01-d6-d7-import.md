# FR-IM-01 D6/D7 — CSV/JSON Import 프론트엔드 UI

> slug: fr-im-01-d6-d7-import
> type: ui
> agent: frontend-engineer
> 생성: 2026-07-03

## Brief

FR-IM-01(CSV/JSON Import, Jira 마이그레이션)의 프론트엔드 UI 단계.
백엔드 PR1~PR4(#218/#221/#224/#226)로 코어·컴포넌트/버전·댓글/Worklog·첨부/이력 Import가 모두 완료됨.
이제 D6(파일 업로드 + 진행률) + D7(E2E)를 구현한다.

- 백엔드 API. `POST /api/v1/imports`(멀티파트: CSV/JSON 파일 + optional attachmentsZip), `GET /api/v1/imports/{id}`(진행률/상태 폴링), 결과 로그 CSV 다운로드.
- 구조 참고. FR-EX-02(비동기 Export) 프론트(#206)의 역방향 미러 — ExportDialog 상태머신 + 폴링 hook 패턴 재사용.
- 범위 밖. FR-IM-02(매핑 마법사)는 별도 FR.

## 도메인 정리

- **BC**. search-export-import(논리). 프론트는 REST 소비 뷰 레이어(apps/web). 새 도메인 엔티티·용어 0 — ImportJob·dry-run·errorLog·status(PENDING/RUNNING/COMPLETED/FAILED)는 백엔드 PR1~4에서 확정.
- **API 계약**(정밀 확인 완료 — grep 근거).
  - `POST /api/v1/imports` (multipart, 202) — `file`(필수)·`attachmentsZip`(선택)·`projectKey`(필수)·`format`("CSV"/"JSON" 대소문자 무관)·`dryRun`(기본 false) → `ImportJobResponse`
  - `GET /api/v1/imports/{jobId}` (200) — 폴링 → `ImportJobResponse`
  - `GET /api/v1/imports/{jobId}/errors` (200) — 실패행 에러 로그 CSV 스트림(Content-Disposition attachment)
  - `ImportJobResponse` = { jobId:UUID, status:String, progress:Int(0~100), totalRows:Long?, succeededRows:Long, failedRows:Long, errorCode:String?, errorLogReady:Boolean, dryRun:Boolean }. `@JsonInclude(NON_NULL)` → nullable 필드는 Zod `.nullish()`.
  - 에러코드. HTTP: IMPORT_ACCESS_DENIED(403)·IMPORT_FILE_TOO_LARGE(413)·IMPORT_UNSUPPORTED_FORMAT(400)·IMPORT_VALIDATION_FAILED(400)·IMPORT_INTERNAL_ERROR(500). 잡 컬럼값: IMPORT_ROW_LIMIT_EXCEEDED·IMPORT_PARSE_FAILED.
- **미러 대상**(FR-EX-02 역방향). `apps/web/src/hooks/use-export-job-polling.ts`(react-query v5 refetchInterval=(query)=>, 종단 COMPLETED/FAILED 중단, error 중단) + `apps/web/src/components/search/ExportDialog.tsx`(상태머신).
- **진입점**. projectKey 필수 → 프로젝트 스코프. `/projects/$projectKey/settings/import` 전용 페이지 유력(components/versions/custom-fields 형제). **spec 단계 Maxi 확인**.
- **기존 결정 충돌**. 없음. 신규 ADR 불요(에픽 ADR `docs/decisions/2026-07-02-fr-im-01-csv-json-import.md` 재사용, view-layer 소비).
- **회귀 주의**. router.ts 라우트 카운트 하드코딩 금지(동시세션 충돌 대비, FR-RP-03 교훈). Zod↔백엔드 DTO drift(frontend-zod-backend-dto-contract-gap).

## 스펙

전체 스펙. [docs/specs/2026-07-03-fr-im-01-d6-d7-import.md](../specs/2026-07-03-fr-im-01-d6-d7-import.md)

**Maxi 결정**. ①진입점=프로젝트 설정 전용 페이지 `/projects/$projectKey/settings/import` ②dry-run=2-step(검증 먼저→실제) ③첨부 zip=v1 포함.

핵심 3줄.
- 설정 페이지 폼(format·파일·JSON일 때 첨부 zip) → [검증만 실행]/[Import 시작] → 진행률 폴링(1500ms) → done.
- dry-run done에서 "이 파일로 실제 Import"(같은 file 재사용). errorLogReady면 에러 로그 CSV 다운로드.
- FR-EX-02 Export 프론트 역방향 미러(상태머신 + 폴링 hook + Zod .nullish). 백엔드 변경 0.

## Brainstorming Check

✅ 통과 (1회 iteration). gap 1건: CSV는 attachmentsZip 무시(백엔드 하위호환) → format=JSON일 때만 첨부 입력 노출로 해소(FR-2a).

## Plan

전 task `agent: frontend-engineer`(헤더 기본값). T6은 qa-engineer. 백엔드 변경 0.

### Task 1. `api/imports.ts` — Zod 스키마 + API 함수 + 에러코드

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/imports.ts`, `apps/web/src/api/imports.test.ts`]
- depends-on: []

**RED**. `imports.test.ts` —
- `importJobStatusSchema.parse` 가 202 PENDING 응답(totalRows/errorCode 키 부재)을 성공 파싱(nullish 검증).
- `submitImportJob` 이 FormData(file·attachmentsZip·projectKey·format·dryRun)로 `apiFetch` POST 호출(mock apiFetch, FormData 필드 assert), 202 body를 schema로 반환.
- `fetchImportJobStatus` GET 파싱. `downloadImportErrorLog` 가 Content-Disposition 파일명 파싱 + blob 반환.
- `IMPORT_ERROR_CODES[code]` 한국어 매핑(7종) 존재.
- 실패(예상): `api/imports.ts` 없음.

**GREEN**. `api/imports.ts` — `EXPORT` 정본(`api/search.ts`) 미러:
- `IMPORT_POLL_INTERVAL_MS=1500`, `importJobStatusSchema`(jobId uuid·status enum 4·progress int0~100·totalRows nonneg nullish·succeededRows nonneg·failedRows nonneg·errorCode nullish·errorLogReady bool·dryRun bool), `ImportJobStatus` 타입.
- `submitImportJob({projectKey,format,file,attachmentsZip?,dryRun})` — `new FormData()` append 후 `apiFetch('/api/v1/imports',{method:'POST',body:fd})`; !ok→`ApiError`; ok→`importJobStatusSchema.parse`.
- `fetchImportJobStatus(jobId)`·`downloadImportErrorLog(jobId)`(`/errors`, blob+filename, export downloadExportJobResult 미러).
- `IMPORT_ERROR_CODES` — IMPORT_ACCESS_DENIED "이 프로젝트에 이슈를 생성할 권한이 없습니다." / IMPORT_FILE_TOO_LARGE "파일 크기가 허용 한도를 초과합니다." / IMPORT_UNSUPPORTED_FORMAT "지원하지 않는 파일 형식입니다." / IMPORT_VALIDATION_FAILED "요청 형식이 올바르지 않습니다." / IMPORT_ROW_LIMIT_EXCEEDED "행 수가 허용 한도를 초과합니다." / IMPORT_PARSE_FAILED "파일을 파싱하지 못했습니다. 형식을 확인하세요." / IMPORT_INTERNAL_ERROR "서버 오류가 발생했습니다." + fallback.

**REFACTOR**. 파일 L1 한글 주석 + 정본 링크 주석(백엔드 `ImportJobResponse.kt` 변경 시 동반).

**검증**. `pnpm --filter web test -- imports.test`.

### Task 2. `use-import-job-polling.ts` — 진행률 폴링 hook

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/hooks/use-import-job-polling.ts`, `apps/web/src/hooks/use-import-job-polling.test.ts`]
- depends-on: [1]

**RED**. `use-import-job-polling.test.ts` — jobId=null이면 disabled(fetch 미호출); 종단 COMPLETED/FAILED에서 refetchInterval false; `query.state.status==='error'`에서 false; 진행 중이면 1500ms. (export hook 테스트 미러.)

**GREEN**. `use-export-job-polling.ts` 1:1 미러 — `useImportJobPolling(id, enabled)`, `importJobQueryKey(id)`, `TERMINAL_STATUSES={COMPLETED,FAILED}`, `refetchInterval:(query)=>` v5 시그니처.

**REFACTOR**. KDoc 상태머신 다이어그램 + v5 함정 주석(export 미러).

**검증**. `pnpm --filter web test -- use-import-job-polling`.

### Task 3. `ImportForm.tsx` — 3-phase 상태머신 컴포넌트

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/import/ImportForm.tsx`, `apps/web/src/components/import/ImportForm.test.tsx`]
- depends-on: [1, 2]

**RED**. `ImportForm.test.tsx`(api/imports 함수 `vi.mock`) —
- form: 파일 미선택 시 [검증만 실행]/[Import 시작] disabled; format=CSV면 첨부 zip 입력 미노출, JSON이면 노출(FR-2a); CSV로 되돌리면 선택 zip 초기화.
- [검증만 실행]→`submitImportJob({dryRun:true})` 호출·tracking 전환; [Import 시작]→dryRun:false.
- 폴링 mock COMPLETED(dryRun) → done: "검증 완료" + 성공/실패 수 + [이 파일로 실제 Import](같은 file로 dryRun:false 재제출) + (errorLogReady) [에러 로그 다운로드].
- 폴링 COMPLETED(real) → "Import 완료"; FAILED → errorCode 한국어 + [다시 시도].
- submit 에러(ApiError 403) → form 인라인 role=alert; done 다운로드 실패 → submitError 렌더(dead-path).
- 접근성: progressbar aria-valuenow, role=status/alert.

**GREEN**. `ImportForm.tsx` — ExportForm 미러 적응(phase 'form'|'tracking'|'done', useMutation submit/download, useImportJobPolling, useEffect 종단→done). file/attachmentsZip/lastDryRun state 보존. `resolveImportError`(ApiError detail)·`getFailureMessage(errorCode)`·`STATUS_LABELS`. Tailwind 토큰 export 미러.

**REFACTOR**. 상태머신 KDoc 다이어그램 + FR-2a/dead-path 교훈 주석.

**검증**. `pnpm --filter web test -- ImportForm`.

### Task 4. 설정 라우트 페이지 + router.ts 등록

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/projects.$projectKey.settings.import.tsx`, `apps/web/src/routes/projects.$projectKey.settings.import.test.tsx`, `apps/web/src/router.ts`]
- depends-on: [3]

**RED**. `...settings.import.test.tsx` — `ProjectImportSettingsPage`(props projectKey)가 헤더 + `ImportForm` 렌더(라우터 비의존, components 설정 선례). Adapter는 useParams 추출.

**GREEN**. `ProjectImportSettingsRouteAdapter`(useParams strict:false) + `ProjectImportSettingsPage`(헤더 "가져오기(Import)" + `<ImportForm projectKey=.../>`) export. router.ts에 `projectImportSettingsRoute` createRoute(`/projects/$projectKey/settings/import`) 추가 + `addChildren` 등록. router.ts:1 주석 카운트 **실측 정정**(41→42, "프로젝트 설정 8"→"9", 하드코딩 금지·실측).

**REFACTOR**. 페이지 KDoc.

**검증**. `pnpm --filter web test -- settings.import`, `pnpm --filter web typecheck`.

### Task 5. MSW import 핸들러 (stateful jobId-keyed)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/mocks/import-handlers.ts`, `apps/web/src/mocks/handlers.ts`]
- depends-on: [1]

**RED**. `import-handlers.ts` 픽스처를 `importJobStatusSchema.parse`로 drift 가드하는 단위 assert(export/cfd 핸들러 선례). stateful: POST /imports→jobId 생성·store에 진행 시뮬(PENDING→RUNNING→COMPLETED), GET /{id}→상태, GET /{id}/errors→CSV blob. `resetImportStore`/`seedImportJob` export.

**GREEN**. `import-handlers.ts`(msw http) + `handlers.ts` 등록(export/burndown/velocity/cfd 핸들러 선례, MSW 파생동작 공유 store 패턴 msw-derived-behavior-shared-store-e2e).

**REFACTOR**. 시드 헬퍼 KDoc.

**검증**. `pnpm --filter web test -- import-handlers`.

### Task 6. E2E — import.spec.ts (S1/S2/S3)

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/import.spec.ts`]
- depends-on: [4, 5]

**RED/GREEN**. Playwright + MSW(addInitScript localStorage 시나리오 토글, e2e-msw-scenario-toggle 선례) —
- S1. `/projects/ATLAS/settings/import` 진입 → format 라디오·파일 입력 렌더.
- S2. CSV 파일 setInputFiles → [검증만 실행] → 진행률 → "검증 완료" → [이 파일로 실제 Import] → "Import 완료".
- S3. seedImportJob(FAILED, IMPORT_PARSE_FAILED) → 에러 메시지 + [다시 시도].
- 기존 E2E 무회귀 확인(ui-pr-defer-e2e-regression-latent) — 관련 스위트 함께 실행.

**검증**. `pnpm --filter web test:e2e -- import.spec`.

### Task 7. 전수 동기화 — FR-IM-01 완료 마킹 + 대시보드

**메타**.
- agent: `frontend-engineer`
- files: [`docs/plan/product/search-export-import.md`, `docs/plan/README.md`, `docs/plan/fr-index.md`, `CLAUDE.md`, `docs/plan/progress.html`]
- depends-on: [6]   # 완료 마킹은 E2E 검증 후 (완료 상태 박제)

**RED**. `bash scripts/verify-master-plan.sh` — 마킹 전 통과(FR 총수 123 불변) 확인. FR-IM-01 §4.1 D1~D7 `[ ]`→`[x]` + "D6/D7 완료 → FR-IM-01 전체 완료" 노트 추가.

**GREEN**. 진척 카운트 정본 갱신(FR 총수 123 불변, 완료 FR +1). `node scripts/build-dashboard.mjs` 재생성. fr-index/README/CLAUDE에 FR-IM-01 완료 반영(카운트 표기 있으면).

**REFACTOR**. 없음(docs).

**검증**. `bash scripts/verify-master-plan.sh` 통과.

## Plan 메타

- task 수: 7 (T6 qa-engineer, 나머지 frontend-engineer)
- 예상 wave: T1·T7시드불가라 T1 먼저 → T2/T5 병렬 → T3 → T4 → T6 → T7. 자연 직렬 사슬(api→hook→component→page→e2e) + T7 완료마킹 최후. bts-impl이 depends-on/files로 wave 계산.
- TDD 강제: yes (T7은 verify-master-plan을 RED/GREEN 대체)
- 추가 검증: typecheck, lint, vitest, playwright(qa), verify-master-plan
- BC 격리: 백엔드 변경 0 (search-export-import 소비만)
