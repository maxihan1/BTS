# FR-RP-03 D6/D7 — CFD (Cumulative Flow Diagram) 프론트엔드

> slug: fr-rp-03-d6-d7-cfd
> type: ui
> agent: frontend-engineer
> 생성: 2026-07-03

## Brief

**원문(Maxi)**: FR-RP-03 D6/D7 — CFD 프론트엔드 (recharts 누적 영역 차트 3띠 TODO/IN_PROGRESS/DONE + E2E). 백엔드 D1~D5는 PR #225로 머지 완료. 렌더러는 FR-RP-01 번다운(#220)/FR-RP-02 벨로시티(#223) 미러 — reports 네임스페이스 전용 라우트 + 백로그 nav 진입. 완료 후 notification-dashboard.md §4.3 D6/D7 마킹 + SDD §13.5.4 반영 + 라우트 카운트 drift 정정.

**classify**: type=ui, agent=frontend-engineer, slug=fr-rp-03-d6-d7-cfd.
- ⚠️ classify-task 원출력은 type=qa/qa-engineer 였으나 "E2E" 키워드 오분류 → ui/frontend-engineer 정정 (FR-RP-01 #220·FR-RP-02 #223 동일 오분류 정정 선례, learnings 확인).

**동시 작업 인지**: 다른 세션이 FR-IM-01 PR4(#226, 첨부/이력 Import) 병렬 진행 중. Maxi 확인 후 병렬 계속. 잠재 충돌점 = router.ts 라우트 등록 / 백로그 nav (머지 시점 주의).

**미러 대상(선례)**:
- FR-RP-01 D6/D7 #220 — 번다운 recharts 라인차트, `/projects/$key/sprints/$id/burndown`
- FR-RP-02 D6/D7 #223 — 벨로시티 recharts 바차트, `/projects/$projectKey/reports/velocity`, 백로그 nav 진입

## 도메인 정리

- **BC**: 논리=notification-dashboard(라벨 유지, 카운트 14 불변), 물리 모듈=issue-tracking (ADR D3).
- **영향 엔티티/VO**: 신규 0. 백엔드가 이미 정의한 read-model `CfdPoint`(VO, 영속 아님) = `{ date, todoCount, inProgressCount, doneCount }` 소비. 번다운 `BurndownPoint`·벨로시티 `VelocityPoint` 동형.
- **API 계약(백엔드 확정)**: `GET /api/v1/projects/{projectKey}/cfd?from=&to=`. from/to = UTC 날짜 ISO `yyyy-MM-dd`, 둘 다 생략 가능(기본 최근 30일), 창 상한 180일, 잘못된 창(`from>to`/파싱실패/상한초과) → **400**(WorklogAggregate 형제 일관).
- **3띠 카테고리**: TODO/IN_PROGRESS/DONE stacked area (상태별 N띠 아님, ADR D1). 프론트는 이 3띠를 누적 영역으로 렌더.
- **새 용어**: 없음(프론트 미러). CFD/CfdPoint는 백엔드 ADR `2026-07-03-fr-rp-03-cfd`에서 정의됨.
- **기존 결정 충돌**: 없음.
- **관련 ADR**: [docs/decisions/2026-07-03-fr-rp-03-cfd.md](../decisions/2026-07-03-fr-rp-03-cfd.md) (백엔드가 생성, 프론트 신규 ADR 불필요).
- **⚠️ glossary 갭(게이트1 보고 대상)**: `Maxi_wiki/BTS/glossary.md`에 벨로시티(FR-RP-02)는 있으나 CFD 항목 없음. glossary는 수동/Maxi 영역이라 자동 추가 안 함. FR-RP-02 선례상 리포트 용어는 glossary 등재되므로 CFD도 추가 권장 — Maxi 승인 후 반영.

## 스펙

전체 스펙. [docs/specs/2026-07-03-fr-rp-03-d6-d7-cfd.md](../specs/2026-07-03-fr-rp-03-d6-d7-cfd.md)

핵심 요약.
- 전용 라우트 `/projects/$projectKey/reports/cfd`(requireAuth) + 백로그 nav "누적 흐름도" Link. 벨로시티(#223) 미러.
- recharts `AreaChart` + 3 stacked `Area`(DONE/IN_PROGRESS/TODO), X=날짜·Y=이슈 수(정수). 순수변환 `toCfdSeries`만 단위 테스트, 실렌더 E2E 위임(jsdom width0).
- 백엔드 계약 grep 확정: `DataResponse<CfdResponse>`, points=`{date,todoCount,inProgressCount,doneCount}` **전부 non-null Int**(`.nullish()` 불필요). 기본 30일·상한 180·400/401/403.
- **빈 상태 CFD 고유**: points는 항상 채워짐 → 빈 판정 = 전 카운트 합 0(`isCfdEmpty`), 벨로시티 `sprints.length===0`과 다름.

열린 질문(게이트1): OQ1 날짜 피커 v1 미노출(범위 밖 제안)·OQ2 스택순서·색(디자인 리뷰)·OQ3 glossary CFD 등재(Maxi 승인).

## Brainstorming Check

✅ 통과 (인라인 sanity check, 미러 작업이라 office-hours 생략). gap 4건 스펙 반영: 빈 상태 조건·스택 순서·날짜 피커 범위·non-null counts.

## Plan

> 벨로시티(#223) 미러. 순수 함수(`toCfdSeries`/`isCfdEmpty`)는 단위 테스트, recharts 실렌더는 E2E 위임(jsdom width0). 모든 task frontend-engineer.

### Task 1. i18n cfd-labels.ts

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/i18n/cfd-labels.ts`, `apps/web/src/i18n/cfd-labels.test.ts`]
- depends-on: []

**RED**: `cfd-labels.test.ts` — 라벨 그룹(page/series/status/chart) 존재 + 한국어 문장 콜론 종결 0(글로벌 §5, `burndown-labels.test.ts` 미러).
**GREEN**: `cfd-labels.ts` — `cfdLabels` const. series={todo:'할 일', inProgress:'진행 중', done:'완료'}, status(loading/forbidden/empty/loadFailed), chart(ariaLabel/yAxisTitle='이슈 수'), page(title='누적 흐름도', description).
**REFACTOR**: `as const` + KDoc.
**검증**: `pnpm test cfd-labels`

### Task 2. api/cfd.ts — Zod 스키마 + fetch + isCfdEmpty

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/cfd.ts`, `apps/web/src/api/cfd.test.ts`]
- depends-on: []

**RED**: `cfd.test.ts` — `cfdResponseSchema.parse` valid/invalid(누락 필드), `isCfdEmpty`(전 point 카운트 합 0 → true, 하나라도 >0 → false, points=[] → true).
**GREEN**: `cfd.ts` — `cfdPointResponseSchema`({date:string, todoCount/inProgressCount/doneCount: number **non-null**}), `cfdResponseSchema`({projectKey, from:string, to:string, points:array}), `fetchProjectCfd(projectKey)`(DataResponse 외피 parse, from/to 미노출), `isCfdEmpty(response)`. (recharts import 0 — 순수 계층.)
**REFACTOR**: `dataResponseSchema` 헬퍼 재사용(velocity 동형), KDoc에 grep 대조 명시.
**검증**: `pnpm test api/cfd`

### Task 3. components/cfd/CfdChart.tsx — recharts 누적 영역 차트

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/cfd/CfdChart.tsx`, `apps/web/src/components/cfd/CfdChart.test.tsx`]
- depends-on: [1, 2]

**RED**: `CfdChart.test.tsx` — `toCfdSeries(response)` 순수 변환(`points→{date,todo,inProgress,done}[]`). 렌더 아닌 순수 함수만(jsdom width0 회피).
**GREEN**: `CfdChart.tsx` — `export function toCfdSeries` + `AreaChart` + 3 `Area`(공유 `stackId="cfd"`, dataKey done/inProgress/todo, 스택 아래→위 DONE→IN_PROGRESS→TODO), XAxis dataKey="date", YAxis `allowDecimals={false}` label='이슈 수', Tooltip, Legend, CartesianGrid. 컨테이너 `role="img"` + aria-label. 색=보드 카테고리 색 재사용(DESIGN.md/board 정합, WCAG 대비+Legend/Tooltip 병행).
**REFACTOR**: 색/높이/마진 상수 추출, KDoc에 "실렌더 E2E 위임" 명시.
**검증**: `pnpm test CfdChart`
**⚠️ design-review D-C1**: "보드 카테고리 색 재사용" 실현 불가 — TODO/IN_PROGRESS/DONE 표준 색 토큰 부재(보드는 category=데이터, DESIGN.md `--chart-1~5`는 회색조). **3띠는 인접 누적이라 서로 구분되는 새 색 3개 손수 선정 필요**(벨로시티 2막대보다 강한 요구). 번다운/벨로시티 선례(hex 손수, 슬레이트+인디고 muted). 제안 triad(게이트1 OQ2 확정): TODO=`#cbd5e1`(연슬레이트, 미시작) / IN_PROGRESS=`#f59e0b`(앰버, 활성·번다운 ideal 톤) / DONE=`#6366f1`(인디고, 완료·벨로시티 톤). 인접 대비 확인 + Legend/Tooltip 텍스트 병행(색-단독 금지).

### Task 4. components/cfd/CfdReport.tsx — 상태 분기

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/cfd/CfdReport.tsx`, `apps/web/src/components/cfd/CfdReport.test.tsx`]
- depends-on: [2, 3]

**RED**: `CfdReport.test.tsx` — `fetchProjectCfd` mock으로 로딩(role=status)/403(forbidden 문구)/기타에러(loadFailed)/빈(`isCfdEmpty` true→empty 문구, 차트 미표시)/성공(CfdChart 표시) 분기.
**GREEN**: `CfdReport.tsx` — `useQuery(['cfd', projectKey], fetchProjectCfd)`, 분기순서 로딩→에러(resolveErrorMessage: 403→forbidden, else loadFailed)→빈(`isCfdEmpty(data)`)→`<CfdChart>`. VelocityReport 미러. 403 데이터 비노출.
**REFACTOR**: StatusMessage 서브컴포넌트/resolveErrorMessage 헬퍼 추출(velocity 동형).
**검증**: `pnpm test CfdReport`

### Task 5. routes/projects.$projectKey.reports.cfd.tsx — Page + RouteAdapter

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/projects.$projectKey.reports.cfd.tsx`, `apps/web/src/routes/projects.$projectKey.reports.cfd.test.tsx`]
- depends-on: [1, 4]

**RED**: route 테스트 — `CfdReportPage`(props projectKey) 헤더(h1=title) + CfdReport 렌더(라우터 비의존).
**GREEN**: `ProjectCfdReportRouteAdapter`(useParams strict:false) + `CfdReportPage`({projectKey}) — 헤더(cfdLabels.page) + `<CfdReport>`. 벨로시티 route 미러.
**REFACTOR**: KDoc.
**검증**: `pnpm test reports.cfd`

### Task 6. mocks/cfd-handlers.ts — 시드 가능 store + 픽스처

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/mocks/cfd-handlers.ts`, `apps/web/src/mocks/cfd-handlers.test.ts`, `apps/web/src/mocks/handlers.ts`]
- depends-on: [2]

**RED**: `cfd-handlers.test.ts` — GET `/cfd` 분기(FORBIDDEN→403, EMPTY→200 전카운트0, store 시드→200 저장값, 미시드→200 기본), `DEFAULT_CFD`가 `cfdResponseSchema.parse` 통과(drift 가드).
**GREEN**: `cfd-handlers.ts` — `cfdStore` Map + `resetCfdStore`/`seedCfd`, `DEFAULT_CFD`(projectKey='ATLAS' backlog-fixtures 정합, ~30일 우상향 누적 counts), `FORBIDDEN_PROJECT_KEY`/`EMPTY_PROJECT_KEY`(전 point 카운트 0), 모듈 로드 시 자동 시드(MODE≠test). `handlers.ts`에 `...cfdHandlers` 등록.
**⚠️ eng-review E-C2**: `DEFAULT_CFD`/`EMPTY_CFD` points는 **프로그래매틱 생성**(from~to 루프로 30점 배열 map, 날짜 `+i`일·counts 단조증가 함수)—30개 리터럴 손수 나열 금지(drift·오타). `cfdResponseSchema.parse`로 최종 가드.
**REFACTOR**: KDoc에 msw-derived-store-e2e·contract-gap 교훈 명시.
**검증**: `pnpm test cfd-handlers`

### Task 7. router.ts 등록 + 백로그 nav Link

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/router.ts`, `apps/web/src/routes/projects.$projectKey.backlog.tsx`, `apps/web/src/routes/projects.$projectKey.backlog.test.tsx`, `apps/web/src/i18n/backlog-labels.ts`]
- depends-on: [1, 5]

**RED**: `backlog.test.tsx` — nav[프로젝트 뷰 전환]에 "누적 흐름도" link, href=`/projects/ATLAS/reports/cfd`(velocity link 테스트 미러).
**GREEN**: `backlog-labels.ts`에 `cfdLink:'누적 흐름도'`; `backlog.tsx` nav에 velocity 뒤 CFD `<Link to="/projects/$projectKey/reports/cfd">`; `router.ts` — adapter import + `projectCfdRoute`(createRoute, requireAuth, velocity 인근) + route tree 추가 + **카운트 주석 40→41**(1행 enumeration + 513행 + "프로젝트 벨로시티 1" 뒤 "프로젝트 CFD 1" 추가).
**REFACTOR**: 주석 정합.
**검증**: `pnpm test backlog && pnpm test router && pnpm typecheck`
**⚠️ eng-review E-C1 (충돌)**: router.ts는 FR-IM-01 PR4(#226)와 잠재 충돌점. PR4가 라우트를 추가하면 현재 40이 41이 됨 → **카운트는 하드코딩(41) 신뢰 금지, Task 7 착수 시·머지 직전 `grep -c 'createRoute(' router.ts` 실측으로 확정**(현재+1). 주석 enumeration도 실측 반영.

### Task 8. e2e/project-cfd.spec.ts — E2E

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/e2e/project-cfd.spec.ts`]
- depends-on: [6, 7]

**RED/GREEN**: `project-velocity.spec.ts` 미러. S1(백로그 "누적 흐름도" 링크 클릭→URL 이동 + role="img" 차트 컨테이너 표시), S2(EMPTY_PROJECT_KEY 직접 진입→empty 문구, 차트 미표시). `loginAsAlice`(fixture userId 정합), serviceWorkers:'block' 금지.
**검증**: `pnpm test:e2e project-cfd`

## Plan 메타

- task 수: 8
- 예상 wave: ~6 (W1: T1·T2 / W2: T3·T6 / W3: T4 / W4: T5 / W5: T7 / W6: T8). 벨로시티 7task 6wave와 동형.
- TDD 강제: yes (순수함수 RED→GREEN, 실렌더 E2E)
- 병렬 dispatch 주의: 프론트 wave lint-staged stash 경합(worktree-lint-staged-shared-git-stash-collision) → 자기 파일만 stage 또는 --no-verify.
- 추가 검증: typecheck, lint, vitest, playwright.

## 리뷰 결과

> type=ui → design 렌즈 주. 미러 작업이라 대화형 /plan-design-review 대신 집중 eng+design 리뷰(learnings `bts-review-plan-autoplan-overkill`). 벨로시티 파일·백엔드 계약·learnings 컨텍스트 기반.

### eng-review (2026-07-03)
- ✅ **계약 정합**: DataResponse<CfdResponse>·counts non-null Int grep 확정. Zod `.nullish()` 불필요(번다운과 다름). 정확.
- ✅ **jsdom 함정**: 순수함수(`toCfdSeries`/`isCfdEmpty`) 단위, 실렌더 E2E. `toCfdSeries` 테스트는 recharts import하되 렌더 안 함(import OK, render만 문제 — 벨로시티 선례).
- ✅ **빈 상태 deviation**: points 항상 채워짐 → `isCfdEmpty`(전카운트0)로 재정의. 벨로시티 inline `.length===0`과 다른 정당한 deviation.
- ⚠️ **E-C1 (CONCERN, 반영)**: router.ts FR-IM-01 PR4(#226) 동시작업 충돌 가능. 카운트 41 하드코딩 금지 → 실측 확정(Task 7 반영).
- ⚠️ **E-C2 (CONCERN, 반영)**: DEFAULT_CFD 30점 픽스처 프로그래매틱 생성(리터럴 나열 금지) → Task 6 반영.
- BLOCKER: 없음.

### design-review (2026-07-03)
- ⚠️ **D-C1 (CONCERN, 반영)**: "보드 카테고리 색 재사용" 실현 불가 — 표준 색 토큰 부재. 인접 누적 3띠 구분 색 3개 손수 선정 필요. 제안 triad(TODO 연슬레이트/IN_PROGRESS 앰버/DONE 인디고) → Task 3 반영 + 게이트1 OQ2 확정.
- 📋 **D-N1 (taste)**: 스택 순서 DONE(바닥)→IN_PROGRESS→TODO(위) — 클래식 CFD 표준(Jira/Azure). 게이트1 확정.
- 📋 **D-N2 (minor)**: 누적 툴팁에 총합(3띠 합) 병기 고려 — 선택. 벨로시티 미적용이라 미러 범위 밖, 후속 가능.
- ✅ **접근성**: role="img"+aria-label·색-단독 금지(Legend/Tooltip 병행)·Y축 정수(allowDecimals=false). 벨로시티 동형.
- BLOCKER: 없음.

### 게이트1 taste 결정 (Maxi)
- OQ1 날짜 피커 v1 미노출(범위 밖) · OQ2 스택순서+색 triad · OQ3 glossary CFD 등재.
