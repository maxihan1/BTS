# FR-TL-01 타임라인/로드맵 뷰 (Gantt)

> slug: fr-tl-01-gantt
> type: backend
> agent: backend-engineer
> BC: agile-planning
> SDD: §4.1
> 생성: 2026-06-26

## Brief

사용자 원문: "fr-tl-01 진행해줘"
FR-TL-01 — 타임라인/로드맵 뷰 (Gantt). 필수 우선순위. agile-planning BC. SDD §4.1.
이슈의 Start/Due/Target Date (FR-PL-01 도입) 기반으로 타임라인/로드맵을 Gantt 형태로 시각화.

classify: type=backend, agent=backend-engineer

## 도메인 정리

- **BC**: agile-planning (모듈 `backend/modules/agile-planning/`)
- **SDD 근거**: §13.3.1 — 이슈를 "시작일~종료일" 기간 막대로 표시, Epic 부모 라인 / Story·Task 자식 라인.
- **데이터 토대 (신규 스키마 0)**: FR-PL-01 의 `issues.start_date / due_date / target_date` (DATE NULL, V025) 활용. issue-tracking BC 소유.
- **cross-BC 격리**: agile-planning 은 issue-tracking 을 직접 import 금지. 날짜 데이터는 **shared-kernel 신규 포트**로 조회.
  - 기존 `BoardIssueLookupPort.BoardIssueView` 에는 날짜 필드 없음 → 타임라인 전용 신규 포트/뷰 필요 (port 형태는 spec 에서 확정 — 신규 `TimelineLookupPort` 후보).
- **새 용어 (Maxi 승인 대기)**: **타임라인 아이템 (TimelineItem)** — 타임라인 막대 1개 = 이슈 1개. start_date~due_date 기간. Epic 부모/자식 그룹화 단위.
- **범위 결정 (Maxi 확정 2026-06-26)**: **백엔드 우선 (D1~D5)**. 타임라인 조회 API + cross-BC 포트 + 백엔드 테스트까지. 프론트 Gantt 렌더(D6/D7) + Gantt 라이브러리 ADR(fr-index §A.3 #2)은 **후속 PR**.
- **범위 제외**: FR-TL-02(의존성 라인 blocks), FR-TL-03(줌)은 별도 FR.
- **기존 결정 충돌**: 없음.
- **관련 ADR**: cross-BC 타임라인 포트 형태 결정 = ADR 후보 (spec/plan 단계 확정). §A.3 #2 Gantt 라이브러리 ADR 은 후속 PR 로 이연.

## 스펙

전체 스펙. [docs/specs/2026-06-26-fr-tl-01-gantt.md](../specs/2026-06-26-fr-tl-01-gantt.md)

핵심 시나리오 3줄 요약.
- `GET /api/v1/timeline?project={key}` → 날짜(start/due 중 1개+) 있는 가시 이슈를 "타임라인 아이템" 목록으로 반환 (BROWSE 권한, truncated 500).
- cross-BC 격리 — shared-kernel 신규 `TimelineLookupPort` + issue-tracking `TimelineLookupAdapter`(보드 adapter 와 동일 보안 2단 게이트 재사용).
- Epic 부모/자식 트리 조립은 프론트(D6) 책임, 백엔드는 평면 목록 + epicKey 만 반환.

## Brainstorming Check

✅ 통과 (자체 sanity 점검 — 완료 FR 기술 스펙 직접 작성, office-hours 부적합 learning 적용). 날짜 0/1/2개 분기·보안 누출 차단·truncated·트리 조립 이연·project 누락 400 점검.

## Plan

> TDD red→green→refactor 강제. 각 task 메타(agent/files/depends-on)로 bts-impl 이 wave 계산.
> 계층별 분해 (BC 격리): shared-kernel → issue-tracking → agile-planning.

### Task 1. shared-kernel — TimelineLookupPort + TimelineItemView + TimelineItemPage

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/shared-kernel/src/main/kotlin/com/bts/shared/timeline/TimelineLookupPort.kt`, `backend/modules/shared-kernel/src/test/kotlin/com/bts/shared/timeline/TimelineLookupPortTest.kt`]
- depends-on: []

**RED**.
- `TimelineLookupPortTest` — (1) `TimelineItemView` VO 필드 구성 검증, (2) 포트 default 메서드 `listTimelineItemsByProject` 가 빈 `TimelineItemPage(items=[], truncated=false)` 반환(adapter 미등록 fail-safe).
- 실패: `TimelineLookupPort`/`TimelineItemView`/`TimelineItemPage` 클래스 없음.

**GREEN**.
- `TimelineLookupPort` 인터페이스 — `fun listTimelineItemsByProject(projectKey: String, viewerUserId: UUID): TimelineItemPage = TimelineItemPage(emptyList(), false)` (fail-safe default, 보드 포트 동일 방향).
- `TimelineItemView(key, summary, issueType, currentStateKey, assigneeId: UUID?, startDate: LocalDate?, dueDate: LocalDate?, epicKey: String?)`.
- `TimelineItemPage(items: List<TimelineItemView>, truncated: Boolean)`.

**REFACTOR**. KDoc — BC 격리 사유·fail-safe 방향·visibility 필터 책임(구현체)·날짜 0/1/2개 의미 명시 (BoardIssueLookupPort KDoc 톤 일치).

**검증**: `./gradlew :backend:modules:shared-kernel:test --tests '*TimelineLookupPortTest'`

### Task 2. issue-tracking — IssueRepository.listVisibleForTimeline SQL (jOOQ)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/repository/IssueRepository.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/repository/IssueRepositoryTimelineTest.kt`]
- depends-on: []

**RED**.
- `IssueRepositoryTimelineTest` (Testcontainers) — 시드: 날짜 둘다/한쪽만/둘다null, 삭제, 비가시 보안등급, cross-project epic. 검증:
  - 날짜 1개+ 있는 가시·미삭제 이슈만 반환 (EC1/EC2/EC7)
  - 보안등급 access 집합 밖 이슈 제외 (S4)
  - cross-project epicKey → null (EC3)
  - 501건 시드 → 500 반환 + truncated true (EC6)
- 실패: `listVisibleForTimeline` 메서드 없음.

**GREEN**.
- `IssueRepository.listVisibleForTimeline(projectKey, viewerUserId, accessibleLevels): TimelineFetchResult` (내부 entry 타입 `TimelineIssueEntry(issue 필드들 + epicKey)`, 보드의 `BoardIssueEntry`/`listVisibleForBoard` 형제).
- SQL WHERE: `project_key=? AND deleted_at IS NULL AND (start_date IS NOT NULL OR due_date IS NOT NULL) AND <보안등급 IN access>`. epicKey self-join 동일프로젝트 필터(보드 P1-A 패턴). LIMIT TIMELINE_FETCH_LIMIT+1 로 truncated 판정.
- `TIMELINE_FETCH_LIMIT = 500` 상수 (NFR 정렬).

**REFACTOR**. 보드 쿼리와 공통 보안 술어 추출 가능하면 재사용, 아니면 KDoc 으로 의미 일치 명시. (신규 인덱스 미추가 — project-scoped + LIMIT 500, 기존 project 인덱스로 충분. perf 리뷰 지적 시 재검토.)

**검증**: `./gradlew :backend:modules:issue-tracking:test --tests '*IssueRepositoryTimelineTest'`

### Task 3. issue-tracking — TimelineLookupAdapter (포트 구현)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/adapter/outbound/timeline/TimelineLookupAdapter.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/adapter/outbound/timeline/TimelineLookupAdapterTest.kt`]
- depends-on: [1, 2]

**RED**.
- `TimelineLookupAdapterTest` (Testcontainers) — adapter 호출 시 (1) `IssueSecurityDirectory.accessibleLevels` + `IssueRepository.listVisibleForTimeline` 경로로 가시 이슈만, (2) `TimelineIssueEntry`→`TimelineItemView` 매핑 정확, (3) truncated 전파.
- 실패: `TimelineLookupAdapter` 없음.

**GREEN**.
- `@Component TimelineLookupAdapter(issueRepository, securityDirectory) : TimelineLookupPort` (`BoardIssueLookupAdapter` 형제 구조).
- `@Transactional(readOnly=true) override listTimelineItemsByProject(...)` — accessibleLevels 1회 + repository 호출 + entry→view 매핑.
- private `TimelineIssueEntry.toTimelineItemView()` 매핑 함수.

**REFACTOR**. KDoc — 보드 adapter 와 동일 2단 보안 게이트 재사용·fail-safe 방향 명시.

**검증**: `./gradlew :backend:modules:issue-tracking:test --tests '*TimelineLookupAdapterTest'`

### Task 4. agile-planning — TimelineApplicationService (권한 + 포트 + 정렬)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/application/TimelineApplicationService.kt`, `backend/modules/agile-planning/src/test/kotlin/com/bts/agileplanning/application/TimelineApplicationServiceTest.kt`]
- depends-on: [1]

**RED**.
- `TimelineApplicationServiceTest` (mockk: `IssuePermissionResolver` + `TimelineLookupPort`) —
  - BROWSE 없음 → `ResponseStatusException(403)` (S5, fail-closed)
  - 정렬: startDate ASC NULLS LAST → dueDate ASC NULLS LAST → key ASC (FR4)
  - truncated passthrough (S8)
  - 빈 결과 → 빈 목록 (EC4)
- 실패: `TimelineApplicationService` 없음.

**GREEN**.
- `@Service @Transactional(readOnly=true) TimelineApplicationService(permissionResolver, timelineLookupPort)` — `BacklogApplicationService` 형제.
- `getTimeline(actorId, projectKey): TimelineResult` — (1) `hasPermission(actor, BROWSE, IssueScope.Project(projectKey))` 거부 시 403, (2) 포트 호출, (3) 정렬, (4) `TimelineResult(items, truncated)` 반환.
- 정렬 비교자 (BacklogService `issueComparator` 패턴).

**REFACTOR**. 정렬 비교자 private + KDoc. cross-BC 격리 주석.

**검증**: `./gradlew :backend:modules:agile-planning:test --tests '*TimelineApplicationServiceTest'`

### Task 5. agile-planning — TimelineController + DTO + ExceptionHandler (HTTP)

**메타**.
- agent: `backend-engineer` (권한 게이트는 security-engineer codereview 검토)
- files: [`backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/web/TimelineController.kt`, `backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/web/dto/TimelineResponses.kt`, `backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/web/TimelineExceptionHandler.kt`, `backend/modules/agile-planning/src/test/kotlin/com/bts/agileplanning/web/TimelineControllerTest.kt`]
- depends-on: [4]

**RED**.
- `TimelineControllerTest` (BacklogController 통합테스트 패턴 — cross-BC 포트 stub 빈) —
  - 200 + items 봉투 (S1, happy)
  - 401 미인증 (S6)
  - 403 BROWSE 없음 (S5)
  - 400 `project` 쿼리 파라미터 누락 (S7)
- 실패: `TimelineController` 없음.

**GREEN**.
- `@RestController @RequestMapping("/api/v1/timeline") TimelineController(service)` — `@GetMapping @RequestParam project` (누락 시 Spring 400). actor 추출(`currentActorId`, 미인증 401) → service 위임.
- `TimelineResponses.kt` — `TimelineResponse(items, truncated)` + `TimelineItemResponse.from(view)` + `DataResponse` 봉투(기존 재사용).
- `TimelineExceptionHandler` — **`@RestControllerAdvice(assignableTypes = [TimelineController::class])`** 한정 (형제 advice 가 예외 가로채는 사고 방지, memory: domain-exception-http-handler-basepackage-scope / fr-bl-02 assignableTypes).

**REFACTOR**. KDoc — 처리 순서(actor→권한→조회)·예외 핸들러 스코프 사유.

**검증**: `./gradlew :backend:modules:agile-planning:test --tests '*TimelineControllerTest'`

## Plan 메타

- task 수: 5
- 예상 wave: 3 (W1: T1·T2 병렬 / W2: T3·T4 병렬 / W3: T5)
- TDD 강제: yes
- 파일 겹침: 없음 (계층별 분리)
- 추가 검증: ktlint, detekt, ArchUnit BC 격리(agile→issue 직접 import 0), 풀 모듈 test
- cross-BC 함정 주의 — ① 컨트롤러 통합테스트 cross-BC 포트 stub 빈(@MockBean 부팅 함정 회피), ② `@RestControllerAdvice(assignableTypes)` 한정, ③ resolver non-null fail-closed, ④ actor 추출을 권한 판정보다 먼저.

## 리뷰 결과 (← /bts-review-plan 채움)
