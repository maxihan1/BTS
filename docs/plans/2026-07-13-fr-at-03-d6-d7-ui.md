# FR-AT-03 조건 분기 D6/D7 — 조건 빌더 UI + E2E

> slug: fr-at-03-d6-d7-ui
> type: ui (classify=qa 오분류 → 선례 기반 정정)
> agent: frontend-engineer (D6) + qa-engineer (D7)
> 생성: 2026-07-13

## Brief

FR-AT-03 조건 분기의 D6/D7을 구현한다. 백엔드 D1~D5(구조화 조건 모델 sealed `Condition`
And/Or/Not/Comparison 데이터 트리, PATCH condition 엔드포인트, `IssueSnapshotPort`,
createdBy 게이팅)는 PR #262로 main에 머지 완료(8d2603a99).

- **D6**. 프론트 UI — automation 룰 폼(`AutomationRuleFormDialog`)에 조건 빌더 추가.
  And/Or/Not/Comparison 트리 편집 + `var` 필드 화이트리스트 기반 필드 선택 +
  백엔드 PATCH condition 엔드포인트 소비. (designer → frontend-engineer)
- **D7**. E2E. (qa-engineer)

**선례**. FR-AT-01/02 D6/D7 — 순수 프론트, 기존 `AutomationRuleFormDialog` 확장.
- FR-AT-02 D6/D7 (PR #260): config 비대칭(응답=객체/요청=JSON문자열) `parseActionConfig`↔
  `serializeActionConfig` 분리, 안정 key(crypto.randomUUID), Zod 계약에 필드 추가,
  게이트2 코드리뷰 CONCERNS 수정.

**완료 시**. automation BC 2/7 → 3/7 (FR-AT-03 전체 D1~D7 완료).

**classify 원본**. `{ type: "qa", agent: "qa-engineer", slug: "fr-at-03-d6-d7-ui-e2e" }`
→ 본체가 D6 조건 빌더 UI라 type=ui/agent=frontend-engineer로 정정, D7만 qa-engineer.

**멀티세션 주의**. 동시 세션이 `.worktrees/fr-sl-06-channel-mapping`(PR #264) 사용 중.
`.bts-cache/classify.json` 공유 오염 가능 — 이 plan 파일이 진실 출처.

## 도메인 정리

- **BC**. automation
- **영향 엔티티**. `AutomationRule.condition`(기존, PR #262 확립) — sealed `Condition` 트리:
  `And(conditions[])` / `Or(conditions[])` / `Not(condition)` / `Comparison(var, op, value)`.
- **모델 규정(ADR 2026-07-12-fr-at-03-automation-conditions)**.
  - Comparison `var` 화이트리스트 9종: `issue.key / issue.type / issue.status / issue.priority /
    issue.assignee / issue.reporter / issue.labels / issue.summary / issue.projectKey`.
  - 제약: `MAX_DEPTH=10`, `MAX_NODES=100`, 리터럴 배열 원소 ≤100 (저장 시점 검증).
  - 저장: `V304__automation_conditions`(rule_id PK · expression JSONB · ON DELETE CASCADE).
  - 평가: `ConditionEvaluator` 순수 트리워크 fail-safe. 조건 미설정=게이트 통과, 불충족=`SKIPPED`.
  - 보안: 평가 주체=`createdBy`(위조 불가), actorUserId 아님(§12.4 read 오라클 차단).
- **새 용어**. 없음. "조건 빌더 / 트리 편집기 / 노드"는 UI 표현 계층 용어 — 도메인 엔티티 아니라
  glossary 추가 불필요.
- **기존 결정 충돌**. 없음. ADR 2026-07-12-fr-at-03-automation-conditions 준수(뷰 계층 소비).
- **관련 ADR**. [docs/decisions/2026-07-12-fr-at-03-automation-conditions.md](../decisions/2026-07-12-fr-at-03-automation-conditions.md) (기존, 신규 ADR 불필요)
- **grill-with-docs**. 스킵 — 모델이 PR #262에서 완전 확립·소비만 하는 뷰 계층이라 신규 용어 0.

### spec 단계로 이월할 열린 질문
1. **조건 제거 3-state**. 백엔드 PATCH가 `condition=null`을 "미변경"으로 해석(plan #262 §296:
   "명시 제거 3-state는 D6 후속"). 조건을 **명시적으로 제거**하려면 set/clear/unchanged 3-state가
   필요 — 백엔드 소량 터치(clear 시맨틱) 필요 여부 확정. issue-tracking DatePatch 3-state 선례 참고.
2. **백엔드 API 정확한 계약**. condition JSON 직렬화 shape(sealed 클래스 판별자 형식) + operator 집합 +
   PATCH 엔드포인트 경로/요청 형식을 backend 소스 grep으로 확정(Frontend Zod↔backend DTO 계약갭 방지).

## 스펙

전체 스펙. [docs/specs/2026-07-13-fr-at-03-d6-d7-ui.md](../specs/2026-07-13-fr-at-03-d6-d7-ui.md)

핵심 요약.
- 기존 `AutomationRuleFormDialog`에 **조건 섹션(5번째)** 추가 — `ActionListEditor` 위임 패턴 동형
  `<ConditionBuilder>`. And/Or/Not 그룹 + Comparison(9필드×9연산자) 트리 편집.
- **순수 프론트**(백엔드 0). Zod `condition: z.string().nullable()` + Create/Patch input 확장,
  `parseConditionExpression`/`serializeConditionExpression` 왕복(null·`{"and":[]}` ↔ 빈 트리).
- Maxi 확정 2건. **[D1] 조건 제거=empty-AND**(빈 트리→`{"and":[]}`, 백엔드 0). **[D2] 값 위젯=재사용만
  드롭다운**(priority/impact=숫자select, assignee/reporter=ProjectMemberSelect, 나머지=텍스트).
- D7 E2E 신규 `automation-conditions.spec.ts`(추가·편집·제거 S5), 기존 12개 회귀 0.

## Brainstorming Check

✅ 통과 (1회 iteration). gap 2건 발견·보강(Maxi 결정 불필요, 구현 정제).
- **G1**. patch가 조건 null 룰을 `{"and":[]}`로 뒤집는 플립 → FR7 create 생략·patch 조건부 전송 규칙(a/b/c).
- **G2**. 빈 Or 그룹=항상-거짓 항등원 footgun → FR7b serialize 시 빈 그룹 prune.

## Plan (← /bts-plan 채움)

## 리뷰 결과 (← /bts-review-plan 채움)
