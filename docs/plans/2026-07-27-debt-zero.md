<!-- 기술부채 0 만들기 작업의 실측 결과·계획·결정 기록 (context-notes 겸용) -->

# 부채 0 — 실측 · 계획 · 결정 기록

**목표 (Maxi, 2026-07-27)**. 현재까지 구현된 기능들의 **조립 상태**와 **부채**를 실측/검증하고,
`TODOS.md` 내용을 포함하여 **부채가 0인 상태**로 만든다.

**기준선**. `main` @ `ee7f1f78d`. 미머지 PR 0건. worktree 0개.

---

## 1. 실측 — 조립(assembly) 상태

| 항목 | 실측값 | 명령 |
|---|---|---|
| BC 모듈 | 9 + 조립 모듈 `app` = 10 | `backend/settings.gradle.kts` |
| 조립 모듈이 끌어오는 BC | 9/9 전량 | `backend/modules/app/build.gradle.kts` |
| Flyway 마이그레이션 | 117 (8 네임스페이스) | `find backend -path "*/db/migration/*" -name "*.sql" -not -path "*/build/*"` |
| REST 엔드포인트 (src/main) | 303 (GET 122 · POST 87 · DELETE 47 · PATCH 40 · PUT 7) | `grep -rhoE "@(Get\|Post\|Put\|Delete\|Patch)Mapping" backend/modules/*/src/main --include="*.kt"` |
| `@RestController` 파일 | 141 | 〃 |
| MSW 모크 핸들러 | 276 (GET 117 · POST 81 · DELETE 40 · PATCH 33 · PUT 5) | `grep -rhoE "http\.(get\|post\|put\|delete\|patch)\(" apps/web/src/mocks` |
| 조립 부팅 테스트 | 11 파일 (`backend/modules/app/src/test/.../app/`) | `find backend/modules/app/src/test -name "*.kt"` |
| `verify-master-plan.sh` | **PASS** (FR 131/131, 카운트 정합) | `bash scripts/verify-master-plan.sh` |
| dev Postgres | `bts-postgres-dev` 5433 healthy | `docker ps` |

### ★ 조립 상태에서 발견한 구조적 갭 (TODOS 미등재 — 신규)

**A1. 계약 검증이 8/303 엔드포인트에만 있다.** PR #317 이 `docs/contracts/workflow-schemes.snapshot.json`
으로 워크플로우 스킴 8 endpoint 에 백엔드↔프론트 계약 스냅샷을 세웠다. 나머지 **295 endpoint 는
「MSW 가 MSW 와 맞는다」 상태**다 (TODOS L91 이 명명한 바로 그 상태). 프론트 테스트 전량 초록이
백엔드와의 정합을 보장하지 않는다. → TODOS 의 D17(댓글 MSW 불일치)은 이 구조적 갭의 **한 사례**일 뿐이다.

**A2. ★백엔드 CI 가 없다.** `.github/workflows/` 에는 `frontend-ci.yml` · `infra-ci.yml` 둘뿐이다.
**백엔드 테스트 940 파일이 CI 에서 한 번도 안 돈다.** 머지 검증이 전적으로 로컬 수동 실행에 의존한다
(메모리 `no-backend-ci-and-assembly-merge-verification-traps` 가 기록한 그 상태).
- Testcontainers 사용 테스트 428 파일 (러너 Docker 로 가능)
- 외부 5433 postgres 의존 테스트 10 파일 (`app` 조립 부팅 — `quay.io/tembo/pg16-pgmq` 서비스 컨테이너 필요)
- `infra-ci.yml` 이 `backend/**` 를 경로 트리거로 걸어두었으나 그 잡은 nginx 마스킹 봉인만 돌린다.

**A3. 프론트 테스트가 CPU 경합에 취약하다 (거짓 빨강).** 백엔드 gradle 과 동시 실행 시
`EpicChildrenSection.test.tsx` · `workflows.$key.test.tsx` 2건이 **5초 기본 타임아웃**으로 실패했다
(전체 소요 642초). 같은 2파일 격리 재실행 → **26/26 통과, 9.29초**. CI 는 단독 실행이라 초록이고
로컬 병행 실행에서만 빨강이 된다.
- ⚠️ 이 관측 과정에서 `pnpm verify 2>&1 | tail -80` 이 **종료 코드를 삼켜** exit 0 으로 보고됐다
  (메모리 `zsh-pipestatus-1-based-false-green` 재발). 이후 모든 파이프 명령에 `set -o pipefail` 적용.

---

## 2. 실측 — 부채 인벤토리 (TODOS 밖 포함)

| 부채 종류 | 실측값 | 판정 |
|---|---|---|
| `TODOS.md` 열린 항목 | 20 섹션 (+ #317 섹션 안 이연 12건) | **주 대상** |
| detekt baseline 억제 | **303건** (identity-access 47+177 · project-workflow 54 · issue-tracking 24 · app 1) | 대상 (유형별 선별) |
| ktlint baseline | 0건 (`identity-access/config/ktlint/baseline.xml`) | 부채 없음 |
| 소스 내 `TODO`/`FIXME`/`HACK` | 실질 **1건** (`WorkflowDiagram.tsx:77` 토큰화) — 나머지 3건은 오탐(i18n 라벨·enum 키·테스트 주석) | 대상 |
| `@Disabled` 테스트 | 실질 **1건** (`DevSeedHashGenerator` — 의도된 dev 도구) | 부채 아님 |
| 프론트 `test.skip` | 4건 (3건은 "다른 테스트가 커버" 명시, 1건은 주석) | 확인 필요 |
| `docs/plan/product/*.md` 미완 체크박스 | **114** — 단 **FR D단계는 909/909 전량 `[x]`, 미완 0** | 문서 부채 |
| 로컬 브랜치 | **168** (원격 10) — 머지 후 미정리 | 하우스키핑 |
| 미추적 파일 | `.branch-cleanup-backup-2026-06-12.txt` (gitignore 미등재) | 하우스키핑 |

### ★ FR 진척 실측 — 131/131 완료인데 정본이 "미진행"으로 적혀 있다

```
grep -rhcE '^- \[x\] D[0-9]+\.' docs/plan/product/*.md  →  909
grep -rhcE '^- \[ \] D[0-9]+\.' docs/plan/product/*.md  →    0
grep -rhcE '^- \[~\] D[0-9]+\.' docs/plan/product/*.md  →    0
grep -rhcE '^- \[!\] D[0-9]+\.' docs/plan/product/*.md  →    0
```

**131 FR 의 D1~D7 단계가 전량 완료다.** 미완 114건은 FR 이 아니라 ①BC 진입 선행조건 ②§1 기술 검증(PoC)
③BC 완료 게이트다. 그런데 정본 문서들은 여전히 "진행 중"으로 적혀 있다.

### 문서 드리프트 (실측 대조)

| # | 위치 | 기록 | 실측 | 판정 |
|---|---|---|---|---|
| DOC-1 | `docs/plan/README.md §1` 진척 열 | 9 BC 전부 `☐` (미진행) | D단계 909/909 완료 | ❌ 드리프트 |
| DOC-2 | `docs/plan/README.md §0.1` 트리 | `fr-index.md (122 FR …)` | 131 | ❌ 드리프트 |
| DOC-3 | `CLAUDE.md:10` | `docs/plan/progress.html` | **파일 없음**. 실물 `docs/progress.html` | ❌ 깨진 경로 |
| DOC-4 | `CLAUDE.md:10` | "Phase 1 진행 중 … 구현 중" | 131 FR D단계 전량 완료 | ❌ 드리프트 |
| DOC-5 | `CLAUDE.md` 디렉토리 | `backend/modules/ # 7개 모듈` | **10개** (automation·slack-integration·app 누락) | ❌ 드리프트 |
| DOC-6 | `CLAUDE.md` 디렉토리 | `.claude/skills/bts-*/ # 워크플로우 스킬 9개` | **10개** (bts-* 기준) | ❌ 드리프트 |
| DOC-7 | `CLAUDE.md` 디렉토리 | `.claude/agents/ # sub-agent 6개` | 6개 | ✅ 정합 |
| DOC-8 | `CLAUDE.md` 디렉토리 | `.claude/settings.json # Bash deny 6종` | 6종 | ✅ 정합 |
| DOC-9 | 9 BC 완료 게이트 | `CHANGELOG.md 정리` 요구 | **파일 자체가 없음** | ❌ 부재 |

⚠️ `verify-master-plan.sh` 는 이 9건 중 **한 건도 잡지 못한다** — 검증 대상이 FR ID 정합과
특정 서식의 카운트뿐이고, 진척 마커·경로 실재·산문 카운트는 범위 밖이다.

### ⚠️ 내가 낸 오탐 1건 (기록)
`FR-[A-Z]{2}-[0-9]+` 로 세어 fr-index 가 127 이라 판단했으나, **3글자 프리픽스 `FR-API-01~04`** 를
빠뜨린 것이었다(127+4=131, 드리프트 없음). verify 스크립트는 `[A-Z]+` 를 쓴다.
→ 메모리 `spec-stated-count-becomes-blindfold` 의 변종. **내 정규식도 눈가리개가 된다.**

---

## 3. 결정 기록

*(작업 진행하며 추가)*

### DZ-1. 부채의 정의 범위
Maxi 지시 "todos에 있는 내용을 **포함하여**" → TODOS 는 부분집합이고 상위 집합은 위 §2 표 전체다.
단, **BC 완료 게이트의 "Maxi 1인 선언"** 은 에이전트가 대신 할 수 없다 (선언 주체가 Maxi).
이 항목은 "미완"으로 남기고 그 사실을 명시 보고한다.

