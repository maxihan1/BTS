# FR-LK-01 D6/D7 — 이슈 링크 UI(추가/제거 패널 + parent-child) + E2E

> slug: fr-lk-01-d6-d7-ui-e2e
> type: ui
> agent: frontend-engineer (D6) + qa-engineer (D7)
> primary_bc: issue-tracking
> 생성: 2026-06-13

## Brief

FR-LK-01의 프론트엔드 후속. 백엔드 D1~D5는 PR #135로 머지 완료. 이슈 상세 화면에서 링크(blocks/relates/duplicates/clones)와 parent-child를 추가/제거할 수 있는 UI(D6) + E2E(D7)를 구현한다.

- classify(교정): type=ui, agent=frontend-engineer, slug=fr-lk-01-d6-d7-ui-e2e
  - 원 classify는 "E2E" 키워드로 `qa` 오판정 → D6 프론트 UI가 본체라 ui/frontend-engineer로 교정(기존 D6/D7 후속 관례 #124/#127 등).
- D7 E2E는 bts-impl 막바지에 qa-engineer dispatch.

### 백엔드 API 계약 (PR #135, 박제 — 재발굴 금지)

베이스: `/api/v1/issues/{key}`. 응답은 모두 `DataResponse<T>` = `{ data: T }` 래퍼.

1. `POST /links` → 201
   - req `{ targetKey: string, linkType: string(소문자 "blocks"/"relates"/"duplicates"/"clones") }`
   - res `data: IssueLinkResponse`
2. `GET /links` → 200
   - res `data: { outward: IssueLinkResponse[], inward: IssueLinkResponse[] }`
3. `DELETE /links/{linkId}` → 204 (linkId = number/BIGINT)
4. `PATCH /parent` → 200
   - req `{ parentKey: string | null }` (null = 해제)
   - res `data: { key: string, parent?: { key: string, summary: string } }`
     - parent는 null이면 **JSON 키 자체 생략**(@JsonInclude NON_NULL) → Zod `.nullish()` / optional 처리 필수.

**IssueLinkResponse** = `{ id: number, linkType: string(대문자 "BLOCKS"), direction: "OUTWARD"|"INWARD", label: string, otherIssue: { key: string, summary: string, statusKey: string } }`

**에러코드**(ProblemDetail `errorCode`): ISSUE_NOT_FOUND(404), LINK_SELF_REFERENCE(422), DUPLICATE_LINK(409), LINK_CYCLE(409), LINK_NOT_FOUND(404), PARENT_SELF_REFERENCE(422), PARENT_CYCLE(409), INVALID_LINK_TYPE(400), VALIDATION_FAILED(400).

## 도메인 정리

- **BC**. issue-tracking (프론트는 apps/web SPA 단일).
- **신규 도메인 용어**. 없음. 링크/링크타입/역방향표시/parent-child는 백엔드 ADR(`2026-06-13-issue-link-vs-parent-child-separation`)·glossary에서 이미 확정. 프론트는 그 용어를 표시만 함.
- **기존 결정 충돌**. 없음. 신규 ADR 불필요(프론트 후속).
- **관련 ADR**. [docs/decisions/2026-06-13-issue-link-vs-parent-child-separation.md](../decisions/2026-06-13-issue-link-vs-parent-child-separation.md) (백엔드에서 생성됨, 재사용).

### 통합 지점 (Explore 매핑)
- **이슈 상세 라우트**. `apps/web/src/routes/issues.$key.tsx` — `IssueDetailPage` + 우측 `IssueMetaPanel`. 링크/parent 패널은 메타패널 내 새 섹션 또는 본문 하단 카드(변경이력 `IssueChangelog` 상단)로 배치.
- **재사용 UI 골격**. `components/issue/VersionMultiSelect.tsx`(검색+칩+다중선택), `IssueMetaPanel.tsx:806~858`의 `IssueAssigneeSelect`(debounce 검색→`useUsers`→목록 선택) — 링크 대상 이슈 검색에 이 debounce 패턴 차용.
- **api 관례**. `api/client.ts` `apiFetch`(Authorization 자동·credentials include·CSRF 쿠키 의존·401 retry), `api/issues.ts` `dataResponseSchema`(`{data:T}` 언랩), Zod 스키마는 api 파일 내 정의.

### ⚠️ 계약 차이 (버전 링크 패턴 그대로 복사 금지)
형제 FR-VR-03(버전 링크)은 **이슈 응답에 임베드된 배열(`affectsVersionIds`)을 PATCH로 통째 교체 + `expectedVersion` OCC**. **이슈 링크는 메커니즘이 다름**.
- `GET /links` → 이슈와 **별개 쿼리**(`{outward[], inward[]}`). 자체 queryKey 필요(예: `issueLinksKey(key)`).
- 추가 = `POST /links` **단건**(`{targetKey, linkType소문자}`). 제거 = `DELETE /links/{linkId}` **단건**. **expectedVersion/OCC 없음**.
- parent = `PATCH /parent` (`{parentKey|null}`). 응답 `parent`는 null 시 키 생략(NON_NULL) → Zod `.nullish()`.
- 따라서 mutation은 "전체배열 교체"가 아니라 "단건 add/remove" + `invalidateQueries(issueLinksKey)`. linkType 코드는 요청 소문자 / 응답 대문자(`BLOCKS`)로 비대칭 — 박제 주의.

### 신규 필요 (선례 없음)
- **링크 대상 이슈 검색/선택**. 전용 컴포넌트 없음. `IssueAssigneeSelect` debounce 패턴 차용해 이슈 키/요약 검색 입력 신규. 백엔드 이슈 검색 api 존재 여부는 spec에서 확정(없으면 범위/대안 Maxi 확인).

## 스펙

전체 스펙. [docs/specs/2026-06-13-fr-lk-01-d6-d7-ui-e2e.md](../specs/2026-06-13-fr-lk-01-d6-d7-ui-e2e.md)

핵심 요약.
- 이슈 상세에 `IssueLinksPanel` 전용 카드 — 링크(outward/inward 표시 + 단건 추가/제거) + parent-child(현재 부모 표시 + set/clear).
- 대상 이슈는 **키 직접 입력**(Maxi 확정). POST가 존재/자기참조/중복/순환 검증 → 한국어 **인라인 에러**(토스트 아님). 성공은 토스트.
- api `issue-links.ts` — 자체 queryKey + **단건 POST/DELETE/PATCH mutation**(버전 PATCH-replace+OCC 패턴 복사 금지) + invalidate.
- **백엔드 FR-B1(같은 BC view-layer, Maxi 확정 옵션 1)**. `IssueResponse`에 `parent{key,summary}?` 추가 — **단건 조회 경로만** self LEFT JOIN으로 채움, 목록 경로는 null(N+1 회피). `issueResponseSchema.parent` nullish 정합.
- E2E 5 시나리오(추가/에러/제거/부모 set/clear) + 기존 이슈 상세 E2E 회귀 0.

## Brainstorming Check

✅ 통과 (1회 iteration). gap 1건 — 현재 부모 표시 불가(IssueResponse parentId 미노출 + parent GET 부재) → Maxi 확정 옵션 1(IssueResponse.parent 단건 노출 + UI 포함)로 해소. 링크는 GET /links 완전 지원 gap 0. 대상 선택(키 입력)·패널 배치(본문 하단 카드)는 선례·Maxi 확정 기반.

## Plan

> 경로 약어. `B/` = `backend/modules/issue-tracking/src/`, `W/` = `apps/web/src/`, `E/` = `apps/web/e2e/`.

### Task 1. 백엔드 FR-B1 — IssueResponse.parent 단건 조회 노출 (self LEFT JOIN)

**메타**.
- agent: `backend-engineer`
- files: [`B/main/kotlin/com/bts/issue/adapter/inbound/rest/IssueResponse.kt`, `B/main/kotlin/com/bts/issue/repository/IssueRepository.kt`, `B/test/kotlin/com/bts/issue/repository/IssueRepositoryParentResponseTest.kt`]
- depends-on: []

**RED**. `IssueRepositoryParentResponseTest`(Testcontainers, 실 repo + 이슈 시드) — 부모가 있는 자식 이슈를 `findByKeyWithType(key)`로 조회 시 `parent={key,summary}` 채워짐, 부모 없으면 `parent=null`. `list(projectKey,...)`로 조회 시 **parent=null**(목록 경로 미채움). 실패. IssueResponse에 parent 필드 없음.

**GREEN**.
- `IssueResponse`에 `parent: ParentSummary? = null` 추가(+ `data class ParentSummary(key, summary)`), `@JsonInclude(NON_NULL)` 정합(기존 resolution 패턴 따름). `from(...)`에 `parent` 파라미터 추가(기본 null — 기존 호출 보호).
- `findByKeyWithType`(IssueRepository.kt:436) — issues 자기 self LEFT JOIN(parent on `issues.parent_id = parent.id`, parent.deleted_at IS NULL)으로 parent key+summary 조회 후 `from(parent=...)`. **단일 조인, cartesian 무위험**(parent_id 1:1, 메모리 cartesian-product-jooq-leftjoin-count). self-join alias는 `parent.KEY.as("parent_key")` 식 명시 alias로 ISSUES 컬럼과 충돌 차단(`record.into(ISSUES)` 경로 보존).
- **목록 경로는 `listWithType`(IssueRepository.kt:487, IssueResponse 빌드 :533)** — `parent=null` 명시(조인 추가 안 함, N+1/비용 회피). ⚠️ `list()`(:360)는 `Issue` 애그리거트 반환이라 **건드리지 않음**(eng-review CONCERN-A 정정).

**REFACTOR**. 조인 alias 상수/KDoc("parent는 단건 조회만 채움, 목록은 null").

**검증**. `:modules:issue-tracking:test --tests "IssueRepositoryParentResponseTest"` + **기존 IssueResponse/list/findByKey 매핑 회귀 0**(`:modules:issue-tracking:test` 모듈 전체 + ktlintCheck detekt).

### Task 2. 프론트 issueResponseSchema.parent 필드

**메타**.
- agent: `frontend-engineer`
- files: [`W/api/issues.ts`, `W/api/issues.test.ts`]
- depends-on: []

**RED**. `issues.test.ts` — `issueResponseSchema`가 `parent: {key, summary}` 포함 응답을 파싱하고, **parent 키 생략(undefined) 응답도 통과**(`.nullish()`). 실패. parent 필드 없음.

**GREEN**. `issueResponseSchema`에 `parent: z.object({ key: z.string(), summary: z.string() }).nullish()` 추가. export 타입 갱신. **nullish라 기존 인라인 mock 깨지지 않음**(메모리 zod-schema-strengthen-inline-mock-fanout — required 아님이라 fanout 없음, 단 `pnpm typecheck` 확인).

**REFACTOR**. 스키마 주석.

**검증**. `pnpm vitest run src/api/issues.test.ts` + `pnpm typecheck`.

### Task 3. 프론트 api/issue-links.ts + MSW 핸들러 (계약 + 단건 mutation)

**메타**.
- agent: `frontend-engineer`
- files: [`W/api/issue-links.ts`, `W/api/issue-links.test.ts`, `W/mocks/issue-link-handlers.ts`, `W/mocks/issue-handlers.ts`, `W/mocks/handlers.ts`, `W/mocks/issue-fixtures.ts`]
- depends-on: []

**RED**. `issue-links.test.ts`(MSW server) —
- `fetchIssueLinks(key)` → `GET /links` 200 언랩 `{outward,inward}`(대문자 linkType 파싱, otherIssue 정합).
- `createLink(key,{targetKey,linkType소문자})` → `POST /links` 201 언랩.
- `deleteLink(key,linkId)` → `DELETE /links/{linkId}` 204.
- `setParent(key,parentKey)` / `clearParent(key)` → `PATCH /parent` 200, parent nullish.
- 에러 매핑 — 404/422/409/400 시 **`ApiError(status, body)`** throw(생성자 2번째 인자=ProblemDetail body 전체). errorCode는 `(error.body as Record<string,unknown>).errorCode`로 추출(선례 `versions.ts:263`). 실패. 모듈/핸들러 없음.

**GREEN**.
- `issue-links.ts` — `apiFetch` + **`dataResponseSchema`는 로컬 정의**(`const dataResponseSchema = z.object({ data: ... })` — issues.ts:128은 **미export**, 형제 issue-versions.ts:32도 로컬 재정의. import 금지 — eng-review CONCERN-B). Zod(`issueLinkResponseSchema` linkType 대문자, `linkListResponseSchema`, `issueParentResponseSchema` parent nullish). 함수 + TanStack `useIssueLinks`(query, `issueLinksKey(key)`) / `useCreateLink` / `useDeleteLink` / `useSetParent`(mutation, onSettled invalidate `issueLinksKey` + 부모는 `issueQueryKey` 동반 invalidate). **버전 PATCH-replace/OCC 패턴 복사 금지**(단건). ⚠️ **api 레이어에 토스트/i18n 문자열 넣지 말 것**(형제 issue-versions.ts:9가 api에서 토스트 import하나, 본 작업은 인라인 에러=T4 패널 소유 — eng-review watch-item). api는 순수 fetch+throw.
- `issue-link-handlers.ts` — **링크 store(stateful + 브라우저 시드 가능)**(Map<key, links[]>): POST 추가→GET 반영, DELETE 제거(메모리 msw-mutation-stateful-refetch / msw-derived-behavior-shared-store-e2e). 에러 시나리오는 특정 키/localStorage 플래그로 토글(메모리 e2e-msw-scenario-toggle-localstorage-flag). `resetIssueLinkStore()` export. `handlers.ts`에 등록.
- **⚠️ PATCH /parent 핸들러는 `issue-handlers.ts`에 둠**(별도 Map 금지) — `getIssueHandler`(issue-handlers.ts:159)가 `issueOverrides`(:106, private)에서 이슈를 읽으므로, set/clear가 **`issueOverrides`의 해당 이슈에 `parent`를 직접 변이**해야 이슈쿼리 invalidate/새로고침 후에도 parent가 유지됨(eng-review CONCERN-D — 별도 파일 Map에 두면 getIssueHandler가 못 읽어 가짜그린). 필요 시 issue-handlers.ts가 seed/reset setter export. `issue-fixtures.ts`에 parent 필드(undefined) 정합.

**REFACTOR**. 에러코드 상수, store 초기화 helper.

**검증**. `pnpm vitest run src/api/issue-links.test.ts` + `pnpm typecheck`.

### Task 4. IssueLinksPanel 컴포넌트 + i18n

**메타**.
- agent: `frontend-engineer`
- files: [`W/components/issue/IssueLinksPanel.tsx`, `W/components/issue/IssueLinksPanel.test.tsx`, `W/i18n/ko.ts`]
- depends-on: [2, 3]

**RED**. `IssueLinksPanel.test.tsx`(MSW + QueryClient wrapper) —
- outward/inward 링크 목록 렌더(label + otherIssue.key/summary/status), 빈 상태.
- 추가 폼 — 유형 select(4종) + 대상 키 input + 추가 버튼. happy(S2) → 목록 갱신. 빈 키면 버튼 disabled.
- 에러 인라인 — 자기참조(S4 422)/중복(S5 409)/미존재(S3 404)/순환(S6 409) → errorCode 매핑 한국어 인라인 메시지(토스트 아님).
- 제거(S7) → 행 사라짐.
- parent 섹션 — `parent` prop 있으면 "부모: KEY (요약)" + 해제; 없으면 키 input + 지정. set(S8)/clear(S10)/에러(S9). 실패. 컴포넌트 없음.

**GREEN**.
- `IssueLinksPanel({ issueKey, parent, disabled })` **컨테이너**(자체 `useIssueLinks` 쿼리 + mutation 훅 사용, presentational 아님 — 링크는 자체 쿼리라 route 배선 최소화). 현재 부모는 `parent` prop(이슈 쿼리 유래)에서 표시. 인라인 에러 영역 + 성공 토스트. DESIGN.md 토큰 + 기존 섹션 헤더/IssueAssigneeSelect 입력 패턴 재사용.
- `ko.ts` — 라벨/플레이스홀더/유형명/에러메시지/토스트 키 추가. **문장 콜론 종결 금지**(ko.test 자동검증).

**REFACTOR**. errorCode→메시지 맵 상수, 서브컴포넌트(LinkRow/AddLinkForm/ParentSection) 분리.

**검증**. `pnpm vitest run src/components/issue/IssueLinksPanel.test.tsx` + `pnpm vitest run src/i18n`(콜론/누락 키) + `pnpm typecheck`.

### Task 5. 라우트 통합 (issues.$key.tsx)

**메타**.
- agent: `frontend-engineer`
- files: [`W/routes/issues.$key.tsx`, `W/routes/issues.$key.test.tsx`]
- depends-on: [2, 4]

**RED**. `issues.$key.test.tsx` — 이슈 상세 렌더 시 `IssueLinksPanel`이 `issueKey` + 이슈 쿼리의 `parent`로 렌더됨(변경이력 상단/본문 하단). 실패. 패널 미배치.

**GREEN**. `IssueDetailPage`에 `<IssueLinksPanel issueKey={issueKey} parent={issue.parent} disabled={...} />` 배치(IssueChangelog 상단). 권한 placeholder는 기존 게이팅 컨벤션 따름.

**REFACTOR**. 섹션 배치 정리.

**검증**. `pnpm vitest run src/routes/issues.$key.test.tsx` + **기존 route 테스트 회귀 0** + `pnpm typecheck`.

### Task 6. E2E (D7, qa-engineer)

**메타**.
- agent: `qa-engineer`
- files: [`E/issue-links.spec.ts`, `E/fixtures/issue-fixtures.ts`]
- depends-on: [3, 5]

**RED/시나리오**. `issue-links.spec.ts`(Playwright + MSW) — loginAsAlice → 이슈 상세 →
- E2E-1 링크 추가 happy(blocks BTS-2) → 목록 표시.
- E2E-2 에러(자기참조 또는 중복) 인라인.
- E2E-3 링크 제거 → 사라짐.
- E2E-4 부모 set + clear.
- E2E-5(선택) blocks 순환 409 인라인(MSW 토글).
- MSW link store는 T3의 브라우저 시드 가능 store 사용. CSRF 쿠키 수동 시드(메모리 frontend-api-convention-per-bc), 드롭다운/로딩 대기(메모리 worktree-stale-base-rebase-and-e2e-msw-traps).

**GREEN**. 구현 src 무수정(qa 범위 — 테스트/픽스처만). 필요한 fixture 확장만.

**검증**. `pnpm test:e2e issue-links.spec.ts` + **기존 이슈 상세 E2E 회귀 0**(함께 실행, 메모리 ui-pr-defer-e2e-regression-latent).

## Plan 메타

- task 수: 6
- 예상 wave: 4 (wave1. T1·T2·T3 병렬 / wave2. T4 / wave3. T5 / wave4. T6). T1은 backend 단독(Gradle 모듈 직렬 무영향), T2·T3은 프론트 파일 비겹침.
- TDD 강제: yes (test 커밋이 feat 커밋보다 먼저)
- 병렬 dispatch: bts-impl이 depends-on + files로 wave 계산
- 추가 검증: 백엔드 ktlintCheck/detekt + 모듈 회귀, 프론트 typecheck(tsconfig.app)/vitest/ko.test/playwright
- 비목표: FR-LK-02 그래프·이력/알림·권한 실게이팅·hierarchy_level·이슈 검색 API

## 리뷰 결과

### plan eng 독립 리뷰 (2026-06-13, code-reviewer 서브에이전트, 실코드 대조)

**BLOCKER 0 / CONCERN 5.** 전부 plan 문구 정정 수준 — impl 가능 상태. 5건 모두 plan에 선반영 완료.

- **백엔드 API 계약 정합 PASS**. 엔드포인트/상태코드/에러코드 8종/linkType 비대칭 전부 실코드(IssueLinkController·LinkExceptionHandler·DTO) 일치.
- **T1 self-join 회귀 PASS**. data class 맨 뒤 defaulted optional + from 기본 null → 호출처 2곳(IssueRepository:451/533) 보호. 테스트 named-arg라 무손상. cartesian 무위험.
- **T2 nullish PASS**. resolution/securityLevelId 동일 선례, required 아님 → fanout 0.
- **CONCERN-A(반영)**. 목록 IssueResponse 빌드 메서드는 `listWithType`(:487/:533), `list()`(:360)는 Issue 애그리거트 — Task1 정정.
- **CONCERN-B(반영)**. `dataResponseSchema`는 issues.ts:128 미export → 로컬 정의(issue-versions.ts:32 선례). Task3 정정.
- **CONCERN-C(반영)**. `ApiError(status, body)` — errorCode는 body.errorCode 추출(versions.ts:263 선례). Task3 RED 정정.
- **CONCERN-D(반영, 가장 실질적)**. parent 라운드트립 — `getIssueHandler`(issue-handlers.ts:159)가 private `issueOverrides`(:106)에서 읽으므로 PATCH /parent는 별도 Map이 아니라 `issueOverrides`를 변이해야 새로고침/invalidate 후 parent 유지(아니면 가짜그린). Task3에 "PATCH /parent 핸들러=issue-handlers.ts, issueOverrides 변이" 명시.
- **CONCERN-E(약, D 종속)**. clear 후 stale parent는 D 해소로 함께 해결, E2E-4(set+clear)가 검증.
- **watch-item(반영)**. 형제 issue-versions.ts:9가 api에서 토스트 import — 본 작업은 인라인 에러=T4 패널 소유, api(T3)는 순수 fetch+throw. Task3 명시.

**게이트1 권고**. BLOCKER 0 → 승인 가능. CONCERN 5건 전부 plan 선반영.
