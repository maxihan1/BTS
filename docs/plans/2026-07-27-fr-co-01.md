# 이슈 댓글 작성 + 목록 조회 (FR-CO-01 신설)

> slug: fr-co-01
> type: backend (classify 판정) — 브랜치/PR 접두사 `backend` (#313 `backend/global-advice-instance-token-leak` 선례)
> agent: backend-engineer (주) + frontend-engineer (UI task 는 plan 메타 지정, CLAUDE.md §sub-agent)
> primary_bc: issue-tracking
> 생성: 2026-07-27

## Brief

### 사용자 원문

> 댓글 기능이 미구현이라고 전달 받았는데 관련 내용 확인 해줄래?
> → (조사 후) pr 314 번 진행 중인데 병행해서 구현 가능한지 확인 해주고 이슈 없는지 체크 해줘
> → (병행 가능 확인 후) 1번 먼저 진행해줘 = 「댓글 FR 분할 결정 → /bts 로 spec·plan 까지」

### classify 결과

```json
{
  "title": "이슈 댓글 작성 + 목록 조회 (FR-CO-01 신설)",
  "slug": "fr-co-01",
  "type": "backend",
  "agent": "backend-engineer",
  "primary_bc": "issue-tracking",
  "task_count": 0
}
```

`task_count: 0` 은 classify 스크립트의 기본값이다. 실제 task 수는 `/bts-plan` 에서 확정한다.

제목에 "UI" 를 넣어 재분류하면 `type=ui`/`frontend-engineer` 로 뒤집힌다(실측). 채택하지 않았다 —
CLAUDE.md §sub-agent 가 "UI 포함 feature 는 backend-engineer 가 기본, UI task 만 plan 메타로
frontend-engineer 에 dispatch" 라고 정하고 있어 `type=backend` 가 관례 정합이다.

### Maxi 확정 결정 (2026-07-27)

| # | 결정 | 값 | 근거 |
|---|---|---|---|
| D1 | FR 분할 | **2분할** — FR-CO-01(작성+목록) / FR-CO-02(수정+삭제) | 알림 활성화 위험과 "누가 남의 댓글을 지우나" 권한 정책을 서로 다른 리뷰 단위로 격리 |
| D2 | FR ID 체계 | **신규 `FR-CO` 그룹** | `FR-CM` 은 컴포넌트가 선점. `FR-PJ` 신설 선례(#277) |
| D3 | FR 등록 시점 | **첫 기능 PR 안에서** 두 행 동시 등록 (129 → 131) | #277 이 첫 PR 에서 프리픽스를 등록한 선례. 한 번의 동기화로 종결 |

### 착수 전 실측 (2026-07-27, 코드 직접 확인)

**댓글 백엔드는 Import 의 부산물이며 쓰기 REST 노출이 0 이다.**

| 계층 | 파일 | 실제 |
|---|---|---|
| DB | `V035__comments.sql` | 있음 (`deleted_at` 소프트삭제 컬럼 포함) |
| 도메인 | `comment/domain/Comment.kt` | 전 필드 `val` — 본문 수정 메서드 없음 |
| 저장소 | `comment/repository/CommentRepository.kt` | `insert` · `listByIssue` **2개뿐** (update/delete 없음) |
| 서비스 | `comment/application/CommentApplicationService.kt` | `create()`(:78) · `list()`(:138). 수정·삭제 메서드 없음 |
| **컨트롤러** | `comment/web/CommentController.kt` | **`@GetMapping` 1개뿐.** 패키지 전체에서 POST/PATCH/DELETE **0건** |
| 프론트 | `apps/web/src` | `api/comments.ts` 없음 · `/comments` 호출 grep **0건** · MSW 핸들러 0건 |

`create()` 의 프로덕션 호출자 — **REST 가 아닌 내부 경로 2개**.
1. `IssueImportAdapter.kt:729` — Import 시 원본 작성자·작성시각 보존 (FR-IM-01 PR3, #224)
2. `AutomationIssueMutationAdapter.kt:144` — 자동화 규칙 "댓글 추가" 액션 (FR-AT-02)

### 🔴 이 작업의 가장 큰 함정 — 알림 경로는 이미 살아 있고, 사람이 두 번째 생산자가 된다

> **정정 (2026-07-27).** 이 절의 초안은 "POST 노출이 잠자던 알림을 깨운다" 였다. **실측으로 기각.**
> 경로는 **이미 프로덕션에서 발화 중**이다. 정확한 위험은 "활성화" 가 아니라 **"기존 소비자에게
> 사람 저작(human-authored) 이라는 새 입력 분포가 유입된다"** 는 것이다.

`CommentApplicationService.create()` 는 **이미 `IssueCommented` 를 발행한다**
(`:102-110`, `Propagation.MANDATORY` 같은 트랜잭션).

**이미 살아 있음의 근거 3축 (실측).**

| 축 | 근거 |
|---|---|
| 정책 시드 | `V401__seed_default_policies.sql:24-26` — `issue.commented` × REPORTER/ASSIGNEE/WATCHER × `IN_APP` × **`enabled=TRUE`** |
| 프로덕션 생산자 | `ActionExecutor.kt:233-236` `AddCommentAction` → `issueMutationPort.addComment()` → `AutomationIssueMutationAdapter:144` → `create()`. automation FR-AT-02 완결 = **도달 가능** |
| 소비자 | `NotificationTitleBuilder.kt:31` — `"$issueRef 에 댓글이 작성되었습니다"` 문구 보유. notification 모듈에 `IssueCommentedNotificationIntegrationTest` 존재 |

**위험 범위는 인앱 한정.** `NotificationEventType.ISSUE_COMMENTED("issue.commented", publishable = **false**)`
— `publishable=false` 는 외부 웹훅·이메일 채널 **불가**, 내부 인앱 채널 전용이라는 뜻이다(enum KDoc).
따라서 사외 유출·메일 폭주 종류의 위험은 구조적으로 없다.

**그래서 스펙에서 확정·검증해야 하는 것.**
1. **작성자 제외가 사람 저작에서도 성립하는가.** automation 경로는 `actor = 룰 actor` 였다. 사람 경로는
   `actor = 작성자` 이므로 "작성자 제외" 규칙이 처음으로 **실제 의미를 갖는다** — 자기 댓글에 자기가
   알림받는 회귀가 여기서 처음 드러날 수 있다.
2. **REPORTER/ASSIGNEE/WATCHER 3역할 fanout 이 실제 발화하는지** 통합 테스트로 고정.
3. `create()` 의 `authorId` 파라미터는 Import 용으로 `actor` 와 분리돼 있다. **사람 경로에서는
   `authorId == actor` 강제**가 필요하다(임의 저작자 위조 차단) — 현재 서비스 시그니처는 이를 막지 않는다.

관련 메모리. `preseeded-event-producer-activates-notifications` ("무해" 전제 금지) — 결론은 유지되나
기전은 위와 같이 정정한다.

**부수 확인.** `GadgetType.COMMENTS_RECENT` 는 카탈로그에 있으나 `enabled = false` 이고 데이터 조회
구현이 0 이다. 이 PR 범위 밖(FR-DB-02 가 자기 FR 에서 해제).

### 기존 FR 확장 대상 (신규 FR 아님 — 이 PR 범위 밖, 각자 자기 FR 에서 이연 해제)

- `FR-MN-01` — *"댓글 멘션은 댓글 기능 부재로 제외(댓글 FR 도입 시 `sourceField="comment"` 로 확장)"*
- `FR-IS-06` (클론) — *"Attachment/Watcher/IssueComment 미구현이라 이번 범위에서 제외"*
- `FR-HS-01` (이력) — 댓글을 전제한 문구 보유

### 병행 작업 충돌 실측 (PR #314, project-workflow)

**#314 는 다른 세션이 실시간 구현 중이다** (Task 1 TDD 3커밋 완료, Task 2 진행 중).

파일 충돌 — **`TODOS.md` 하나 외 0건**.

| 영역 | #314 | FR-CO-01 | 겹침 |
|---|---|---|---|
| 백엔드 BC | project-workflow | issue-tracking | 없음 |
| 마이그레이션 | 0건 | 0건 예상 (V035 기존) | 없음 |
| shared-kernel | 0건 (`VIEW_SCHEME` enum 신설 = 기각) | 0건 예상 (`IssuePermission.UPDATE` 재사용) | 없음 |
| 프론트 api/hooks | `workflow-schemes.*` | `comments.*` (신규) | 없음 |
| MSW | `scheme-handlers.ts` — `handlers.ts` 무변경 | `comment-handlers.ts` 신규 + `handlers.ts` 등록 1줄 | 없음 |
| i18n | `workflow-scheme-labels.ts` | `ko.ts`(`issueDetailStrings:44`) + `ko.test.ts` | 없음 |
| 라우트/컴포넌트 | `routes/projects.$projectKey.settings.workflow-scheme.tsx` | `components/issue/IssueActivityTabs.tsx` | 없음 |
| FR 카운트 | **불변 129** (spec L9 명기) | 129 → 131 | 경합 없음 |

**FR-CO 프리픽스 선점 없음 — 전 원격 브랜치 `docs/` 전수 스캔 0건.**
`fr-index.md` 단독 grep 은 불충분하다(선점은 `docs/specs/` 에서 일어난다 —
learnings 2026-07-17 #279↔#277 사고). 전 브랜치 `git grep` 으로 확정했다.

### ⚠️ 공유 상태 위험 (#314 가 실시간 진행 중이라 살아 있음)

| 위험 | 처방 |
|---|---|
| pre-commit `lint-staged` 가 **공유 `git stash`** 사용 (worktree 는 `.git` 공유) | 동시 커밋 금지. 필요 시 `--no-verify` (#305 선례) |
| Testcontainers 동시 suite | 실패 시 **단독 재실행으로 확정** 후 판단 |
| E2E vite 포트 경합 · orphan 프로세스 | E2E 는 직렬 |
| worktree `node_modules` 부재 | **레포 루트에서** `pnpm install` (`apps/web` 안에서 금지 — pnpm 워크스페이스) |
| `.bts-cache/classify.json` 이 이름 없는 단일 공유 파일 | #314 분량은 `classify.workflowscheme-api-n4.json` 으로 백업 완료 |
| 마이그레이션 V번호 | issue-tracking 다음은 **V038**. #314 는 0건. 머지 직전 재확인 |

### FR 카운트 동기화 대상 (129 → 131, 실측한 지점)

`verify-master-plan.sh` 는 **product/*.md 의 실제 FR ID 집합**을 정본(`PLAN_COUNT`)으로 삼고
**SDD 와 개수를 대조**한다. 따라서 SDD 누락 시 종료 4 로 차단된다.

1. `docs/plan/fr-index.md` — L1 주석 · §A.1 헤더 · FR 행 2개 신설 · §A.2 issue-tracking 35→37 · 합계 행
2. `docs/sdd/02-requirements.md` — FR ID 표 (**빠뜨리면 verify 실패**)
3. `docs/plan/product/issue-tracking.md` — FR § 본문 · D단계 체크박스 · §N 헤더 · L1 주석 · `소속 FR. N개` · BC 완료 게이트
4. `docs/plan/README.md` — L117 `**합계**. 129 FR.` · L121 · §1 BC 테이블 행
5. `CLAUDE.md` — L10 · L52
6. ADR — `docs/decisions/2026-07-27-fr-co-comment-feature.md` (신규)
7. `docs/progress.html` — `node scripts/build-dashboard.mjs` 재생성
8. Obsidian `Maxi_wiki/BTS/` — history · glossary · domain/issue-tracking · decisions·plans 미러

## 도메인 정리

- **BC**. issue-tracking (단일). cross-BC 변경 0 — notification 은 기존 소비자를 그대로 두고,
  shared-kernel `IssuePermission` 은 건드리지 않는다(D3).
- **영향 엔티티**. `Comment` (기존, Import 부산물 → 정식 승격) · `Issue` (읽기만)
- **새 용어**. **댓글 / Comment** — glossary 등재 완료(Maxi 승인). `domain/issue-tracking.md`
  핵심 엔티티에도 추가. Worklog 도 같이 등재해 선례를 명시했다.
- **기존 결정 충돌**. **없음.** 오히려 정합·해소다 —
  `2026-07-17-fr-ux-06-jira-redesign` §D6 *"댓글은 별도 FR"* 이행,
  `2026-07-13-fr-at-04-conflict-analysis` 가 갭으로 기록한 *"댓글 권한 부재"* 를 **의도된 재사용**으로 정본화.
- **관련 ADR**. [docs/decisions/2026-07-27-fr-co-comment-feature.md](../decisions/2026-07-27-fr-co-comment-feature.md) (생성됨)

### grill-with-docs 결과 — 열린 질문 3개 중 2개는 코드가 답했다

| Q | 결론 | 근거 |
|---|---|---|
| Q1 glossary 등재 문구·범위 | **댓글만 등재** (Maxi 확정). Worklog·Attachment·Watcher 동질 부채는 `TODOS.md` | glossary 는 수동 영역(`_index` L61) → Maxi 승인 필요 사안 |
| Q2 애그리게이트 경계 | **Issue 종속 child entity, Worklog 동형 확정** | 내가 "혼합형 anomaly" 로 의심한 구조가 **확립된 형제 패턴**이었다. Worklog 가 같은 4계층·중첩경로·전용 예외핸들러·`IssueScope.Issue` 게이트 |
| Q3 권한 enum | **`IssuePermission.UPDATE` 재사용. `ADD_COMMENT` 신설 기각** | Worklog create/update/delete 전부 `UPDATE`. 신설 폭발반경(shared-kernel enum + resolver + 시드 마이그레이션 + `PermissionSchemaMigrationTest`)은 **PR #314 ADR 이 `VIEW_SCHEME` 를 같은 사유로 기각한 선례** |

### ★ grill 과정에서 발견한 구조 결함 (FR-CO-01 의 실질 과제)

`create()` 가 `authorId` 를 **호출자 지정**으로 열어두고 있다. 이대로 POST 를 노출하면
**남의 이름으로 댓글을 쓸 수 있는 표면**이 생긴다.

Worklog 는 이 문제를 **메서드 분리**로 해결했다.

| | Worklog | Comment (현재) | Comment (목표, D4) |
|---|---|---|---|
| 사람 경로 | `create()` — `authorId = actor.value` 강제 (`:120`) | `create(…, authorId, createdAt)` | `create(actor, issueKey, body)` — 강제 |
| Import 경로 | `createImported(…, authorId, …)` (`:178`) | 같은 `create()` 재사용 | `createImported(…)` 분리 |

**리팩토링이 아니라 보안 요구다.** 파라미터를 열어둔 채 노출하면 컨트롤러의 성실성에만 의존하게
되고 그 의존은 테스트로 고정되지 않는다.

### FR-CO-02 에 선례가 예고하는 것 (이 PR 범위 밖)

Worklog 는 수정·삭제를 **작성자 한정**(`existing.authorId != actor` → 403, `:254`·`:328`)으로 두고
**관리자 우회를 두지 않았다.** CO-02 는 이 대칭을 따를지 모더레이션을 도입할지 자기 ADR 에서 결정한다.

## 스펙

전체 스펙. [docs/specs/2026-07-27-fr-co-01.md](../specs/2026-07-27-fr-co-01.md)
— 시나리오 10 · FR 22 · NFR 7 · 엣지 21 · 완료기준 14

### 핵심 3줄

- 기존 `create()` 를 `POST /api/v1/issues/{key}/comments`(201) 로 노출하고, `authorId` 파라미터를
  없애 **저작자 위조 창구를 타입 수준에서 제거**한다 (Import 는 `createImported` 로 분리).
- 이슈 상세 활동 영역에 **댓글 탭을 4번째로 추가**(기본 탭은 이력 유지)하고 `WorklogSection` 을
  미러링한 `CommentSection` 을 붙인다.
- `FR-CO-01`·`FR-CO-02` 두 행을 등재해 **129 → 131** 을 8종 정본에 전수 동기화한다.

### 워크플로우 편차 기록

**`office-hours` 생략.** 메모리 `bts-spec-office-hours-mismatch` — office-hours 는 "만들 가치 있냐"
아이디어 진단 도구다. 댓글은 검증할 아이디어가 아니라 `FR-MN-01`·`FR-IS-06`·`FR-HS-01` 이 명시
이연하며 대기 중인 기정사실이므로 프레임이 맞지 않는다. 기존 `docs/specs/*.md` 형식으로 직접
기술 스펙을 작성했다(2026-05-29 FR-IS-02 에서 Maxi 가 같은 선택을 한 선례).

**`design-shotgun` 생략.** `WorklogSection.tsx` 가 **같은 화면·같은 위치·같은 구조**의 형제 UI 로
존재한다. 새 변형 4종을 뽑는 것이 아니라 선례를 미러링하는 것이 맞다. `DESIGN.md` 는 이미 존재하므로
`design-consultation`(A-1)도 대상이 아니다.

### Maxi 추가 확정 2건 (스펙 단계)

| # | 결정 | 값 | 왜 결정이 필요했나 |
|---|---|---|---|
| D5 | 본문 길이 상한 | **32,000자** 초과 400 | issue-tracking 전역에 길이 상한 선례가 **0건**이었다. 목록이 페이지네이션 없이 전량 HTML 렌더라 상한 부재가 이슈 화면을 마보하는 경로 |
| D6 | 목록 페이지네이션 | **없음** | 기존 GET 계약 무변경 = 회귀 0. Worklog 동형 |
| D7 | 상한 검증 위치 | **서비스 `create()`** | ★Worklog 선례가 **전이되지 않는다**. 생산자가 둘(REST + automation)이고 `TemplateRenderer` 에 길이 제한이 없어, 컨트롤러에만 걸면 자동화가 우회한다 |
| D8 | 기본 활성 탭 | **이력 유지** | CO-01 의 본질은 쓰기 노출. 읽기 UX 재설계를 섞으면 회귀 원인 분리가 안 된다 |

## Brainstorming Check

✅ **통과 (1회 iteration).** gap **7건** 발견 → 전부 스펙 반영. Maxi 결정 2건 확정(D7·D8).

| # | gap | 반영 |
|---|---|---|
| G1 | 상한 검증 위치 — Worklog 선례를 따르면 **automation 이 우회** | D7 · FR-6 · EC-17/18 · 완료기준 11 |
| G2 | 이벤트 발행 실패 거동 미명시 → outbox 라 **부분 성공 없음** | FR-9 · EC-16 |
| G3 | 프론트 캐시 전략 미명시 (낙관적 갱신으로 갈 여지) | FR-18 · 기각 대안 2건 |
| G4 | 작성자 표시·탈퇴 사용자 미명시 | FR-19 · EC-19 |
| G5 | `ko.test.ts` 가 named import 라 **커버리지가 자동이 아님**(vacuous 위험) | FR-21 · 완료기준 12 |
| G6 | 기존 탭 테스트의 **열거가 눈가리개** (미마운트 단정이 2개 하드코딩) | FR-22 · 완료기준 13 |
| G7 | `canUpdate=false` 사용자 UX 미명시 | FR-20 · EC-20 · 완료기준 14 |

**범위를 늘리지 않은 반증 5건.** 신규 권한 요약 API 불필요(`IssueActivityTabs:86` 이 이미 배선) ·
소프트삭제 제외 이미 구현 · 마이그레이션 불필요 · DTO shape 변경 불필요 · 아카이브 차단 이미 구현.

## Plan

> **워크플로우 편차 기록.** `superpowers:writing-plans` 를 거치지 않고 BTS 형식으로 직접 분해했다.
> 사유 — 이 스킬 Step 2 가 검증하는 **메타 블록(agent/files/depends-on)** 이 `bts-impl` 의 wave 계산
> 입력인데 writing-plans 의 산출 형식이 그와 다르고, 도메인·스펙 단계에서 **정확한 파일 경로·줄번호·
> 선례 구조를 이미 확보**해 재분해가 정보를 더하지 않는다. Step 2 형식 검증은 그대로 적용했다.

### Task 1. `create()` / `createImported()` 분리 + 호출자 2곳 전환 + 32,000자 상한 — **[x] 완료**

> **실행 결과 (2026-07-27).** TDD 3커밋 — `test:` `35a8f326a` → `feat:` `2eb7a956b` → `refactor:` `8bee7b56f`.
> tests **92 / fail 0 / skip 0** (XML 집계). `IssueImportAdapterTest` 60건 · `CommentControllerIntegrationTest`
> 4건 무회귀(NFR-5·NFR-6 충족). ktlint + detekt `--rerun-tasks` 통과.
>
> **뮤테이션 봉인 확인.** 길이 검증을 `if (false)` 로 무력화 → `CO-2` 가 **빨개짐**. 원복 후 green 복귀.
> 기준선 EXIT=0 은 주입 전에 확인했고, 뮤테이션은 커밋 후에만 수행했다.
>
> **★리뷰 C1 이 실증됐다.** `AutomationIssueMutationAdapterTest.kt:338`·`:367` 이 실제로
> `No value passed for parameter 'authorId'` 로 컴파일 실패했다. 리뷰에서 files 목록에 넣지 않았다면
> 구현자가 못 보고 빌드가 깨진 채 진행됐을 것이다.
>
> **함정 1건 실측 — MockK × Kotlin value class.** `verify(exactly = 0) { createImported(any(), …) }` 가
> `IssueKey`(`@JvmInline` + `require(REGEX.matches(value))`)에 무작위 문자열을 넣어
> `IllegalArgumentException: Invalid issue key format: -241a24307f9b1cfc` 로 터졌다.
> 처방 = 그 음성 verify 를 제거. `commentApplicationService` 가 **strict mock**(`mockk()`, relaxed 아님)이라
> 어댑터가 다른 메서드를 부르면 stub 부재로 이미 실패한다 — **strict mock 자체가 음성 가드**였고
> 추가 verify 는 중복이었다. 그 기전을 테스트 KDoc 에 기록했다.

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/comment/application/CommentApplicationService.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/comment/domain/CommentExceptions.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/comment/application/CommentApplicationServiceTest.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/adapter/outbound/imports/IssueImportAdapter.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/adapter/outbound/automation/AutomationIssueMutationAdapter.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/adapter/outbound/imports/IssueImportAdapterTest.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/adapter/outbound/automation/AutomationIssueMutationAdapterTest.kt`]
- depends-on: []

> **★ 왜 한 task 인가.** `create()` 시그니처를 바꾸면 호출자가 **즉시 컴파일 실패**한다.
> 시그니처와 호출자를 쪼개면 중간 커밋이 빌드 불가 상태가 된다 (메모리 `constructor-change-cross-module-callsite-blindspot`).

**호출자 전수 (리뷰 C1 에서 보강 — 초안은 프로덕션 2곳만 세고 테스트를 빠뜨렸다).**

| 파일 | 위치 | 성격 |
|---|---|---|
| `IssueImportAdapter.kt` | `:729` | 프로덕션 → `createImported` |
| `AutomationIssueMutationAdapter.kt` | `:144` | 프로덕션 → `create` 3인자 |
| `CommentApplicationServiceTest.kt` | 호출 **12곳** (`:167`·`:183`·`:226`·`:272`·`:297`·`:320`·`:349`·`:363` 등) | 테스트 — 전량 마이그레이션 |
| **`AutomationIssueMutationAdapterTest.kt`** | `:323`·**`:351`** | ★**MockK `verify` 가 `authorId = actor` 인자를 명시**해 시그니처 변경 시 컴파일 실패 |

> `AutomationIssueMutationAdapterTest:351` 은
> `commentApplicationService.create(actor = actor, issueKey = issueKey, body = any(), authorId = actor)` 다.
> **이 파일이 files 목록에 없으면 구현자가 못 보고 빌드가 깨진다.** 전 모듈 grep 으로 확정했다.

### ★ 리뷰 C2 — D7 의 숨은 파생 요구사항. 무엇을 던지는가

검증을 컨트롤러에서 서비스로 옮기면 **예외 종류도 같이 바뀌어야 한다.**

Worklog 는 컨트롤러에서 `ResponseStatusException(BAD_REQUEST)` 를 던진다. 그건 **컨트롤러였기 때문에**
맞았다. 서비스에서 같은 걸 던지면 두 가지가 깨진다.

1. **automation 이 HTTP 예외를 받는다.** `AutomationIssueMutationAdapter.runAttempt:212-215` 는
   `IssueAccessDeniedException` **하나만** 포트 계약 타입(`IssueMutationPermissionDeniedException`)으로
   번역한다. `ResponseStatusException` 은 번역 없이 `ActionExecutor` 까지 전파돼 자동화 실행 기록에
   웹 관심사가 샌다 (메모리 `crossbc-failure-classification-typed-not-name`).
2. **`CommentExceptionHandler:89-91` 의 `HttpStatus.valueOf(ex.statusCode.value())` 경로를 탄다.**
   그 패턴에는 **이미 기록된 위험**이 있다 — 메모리 `fr-db-03-public-dashboard-error-instance-token-leak-done`
   의 *"`HttpStatus.valueOf` 상태보존은 핸들러를 터뜨려 `/error` 재유출"*.

**처방.** `comment/domain/CommentExceptions.kt` 신설 — `IssueDomainException` 계열 관례
(`domain/IssueExceptions.kt` 의 `sealed class` 12종)를 따라 `CommentBodyBlankException` ·
`CommentBodyTooLongException(actual: Int, max: Int)` 를 정의하고, `CommentExceptionHandler` 에
**`400` 매핑을 추가**한다(Task 2). 서비스는 도메인 예외만 던지고 HTTP 는 웹 계층이 결정한다.

실측 근거 — `issue-tracking` main 에서 `ResponseStatusException` 을 쓰는 파일은 **거의 전부
`web/` 또는 `adapter/inbound/rest/`** 다. 서비스 계층 사용은 소수 예외이고 관례가 아니다.

**RED**.
```kotlin
// CommentApplicationServiceTest — 기존 호출 12곳을 3인자/createImported 로 마이그레이션한 뒤 신규 추가
@Test fun `create 는 authorId 를 actor 로 강제한다`()                   // 3인자 호출 → authorId == actor
@Test fun `create 는 32001자 본문에 CommentBodyTooLongException 을 던진다`()   // 경계 초과 (도메인 예외)
@Test fun `create 는 32000자 본문을 허용한다`()                           // 경계 정상 (EC-2 양쪽)
@Test fun `create 는 공백만인 본문에 CommentBodyBlankException 을 던진다`()    // EC-1
@Test fun `createImported 는 원본 authorId 와 createdAt 을 보존한다`()
@Test fun `createImported 는 32001자를 허용한다`()                        // EC-18 면제 (D7)

// AutomationIssueMutationAdapterTest — ★automation 우회 경로 직접 타격 (완료기준 11)
@Test fun `addComment 는 32001자 본문을 도메인 예외로 거부한다`()
@Test fun `addComment 는 dryRun 에서도 길이 위반을 감지한다`()              // runAttempt 가 attempt() 를 실행함
```
**실패 메시지 (예상).** `create` 3인자 오버로드 없음 / `createImported` 미존재 / `CommentBodyTooLongException` 미존재.

**GREEN**.
- `comment/domain/CommentExceptions.kt` 신설 — `CommentBodyBlankException` ·
  `CommentBodyTooLongException(actual: Int, max: Int)`. `domain/IssueExceptions.kt` 의 `sealed class`
  관례를 따른다. **`ResponseStatusException` 금지** (리뷰 C2).
- `create(actor, issueKey, body)` — `authorId = actor.value` 내부 고정, `createdAt = Instant.now(clock)`.
  본문 검증(`isBlank` / `length > 32_000`)을 **여기**에 둔다 (D7).
- `createImported(actor, issueKey, body, authorId, createdAt)` — 기존 동작 그대로, **길이 검증 없음**.
- 공통 로직은 private helper 로 추출 (권한 → 아카이브 가드 → 이슈 resolve → insert → 이벤트 발행).
- `IssueImportAdapter:729` → `createImported(...)`.
- `AutomationIssueMutationAdapter:144` → `create(actor, key, cmd.body)` (기존이 `authorId = actor` 라 의미 변화 0).

**REFACTOR**. `MAX_BODY_LENGTH = 32_000` 을 companion 상수로 + KDoc 에 "왜 서비스인가(생산자 2개)" ·
"왜 도메인 예외인가(automation 이 HTTP 예외를 받지 않게)" 근거 명시.

> **dry-run 실측 (리뷰에서 확인).** `runAttempt:204-221` 은 dry-run 에서도 `attempt()` 를 **실제 실행**
> 하고 트랜잭션에만 rollback-only 를 표시한다. 따라서 상한 검증이 **dry-run 에서도 발화**한다 —
> 이것은 결함이 아니라 바람직한 동작이다(dry-run 의 목적이 사전 검증). 데이터 오염도 없다(어차피 롤백).

**검증**. `cd backend && ./gradlew :modules:issue-tracking:test --tests '*CommentApplicationServiceTest' --tests '*IssueImportAdapterTest'`

---

### Task 2. `POST /api/v1/issues/{key}/comments` 노출

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/comment/web/CommentController.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/comment/web/AddCommentRequest.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/comment/web/CommentExceptionHandler.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/comment/web/CommentControllerIntegrationTest.kt`]
- depends-on: [1]

> `AddCommentRequest` 는 `comment/web/` 에 둔다 — 이 패키지의 기존 관례(`CommentResponse.kt` 가
> `dto/` 하위가 아니라 `web/` 직속). worklog 는 `worklog/web/dto/` 지만 **지역 관례를 따른다.**

**RED**.
```kotlin
@Test fun `POST 는 201 과 생성된 댓글을 반환한다`()
@Test fun `POST 는 요청의 authorId 필드를 무시하고 actor 를 저작자로 쓴다`()   // S2 / EC-5
@Test fun `POST 는 미인증 요청을 401 로 거부한다`()                            // 이슈 미존재보다 401 우선 (EC-8)
@Test fun `POST 는 UPDATE 권한 없으면 403 이다`()
@Test fun `POST 는 body 누락 시 400 이다`()
@Test fun `POST 는 32001자 본문에 400 과 CODE 를 반환한다`()                  // 도메인 예외 → 400 번역 (C2)
@Test fun `POST 는 공백만 본문에 400 을 반환한다`()
```
**실패 메시지 (예상).** `@PostMapping` 없음 → 405. 이후 도메인 예외 미매핑 → 500.

**GREEN**.
- `AddCommentRequest(val body: String?)` — **저작자 필드 없음**(S2. 창구 자체를 만들지 않는다).
- `addComment()` — `CurrentActor.current()` 를 **리소스 조회보다 먼저**, `service.create(actor, IssueKey(key), body)`,
  `ResponseEntity.status(HttpStatus.CREATED).body(DataResponse(data = CommentResponse.from(...)))`.
  `body` 가 `null` 이면 컨트롤러에서 `400`(요청 형식 문제 = 웹 관심사). **길이·공백 판정은 서비스 소관**(D7).
- `CommentExceptionHandler` — `@ExceptionHandler(CommentBodyBlankException, CommentBodyTooLongException)`
  → `400` + `ProblemDetail`. **`instance` 를 반드시 명시**한다(미설정 시 Spring 이 요청 URI 로 자동 채움 —
  메모리 `fr-db-03-public-dashboard-error-instance-token-leak-done` / `global-advice-instance-token-leak-done`).
  기존 핸들러 3종(403/404/`ResponseStatusException`)과 catch-all 은 **건드리지 않는다**.
- `@Operation`/`@ApiResponses` 201·400·401·403·404 기재 (worklog `addWorklog` 미러).

**REFACTOR**. 클래스 KDoc 의 엔드포인트 목록에 POST 한 줄 추가.

> **★핸들러 등록 순서 주의.** `CommentExceptionHandler` 에는 `@ExceptionHandler(Exception::class)`
> catch-all 이 있다(`:115`). 신규 예외 핸들러가 catch-all 보다 **구체 타입**이라 Spring 이 우선
> 선택하지만, 등록 누락 시 조용히 500 이 된다 (메모리 `catch-all-exceptionhandler-swallows-responsestatusexception`).
> RED 에서 500 → 400 전환을 **본문 코드까지** 판별자로 잡는다.

**검증**. `cd backend && ./gradlew :modules:issue-tracking:test --tests '*CommentControllerIntegrationTest'`

**★주의**. `@PatchMapping`·`@DeleteMapping` 을 **넣지 않는다** (FR-CO-02 범위). `Comment` 도메인은
전 필드 `val` 유지, `CommentRepository` 에 쓰기 메서드 신설 금지.

---

### Task 3. 작성자 제외 알림 — **[x] 완료 (신규 테스트 0건, 뮤테이션으로 기존 봉인 실효 확인)**

> **★ 스펙 NFR-1·NFR-2 는 착수 전부터 충족돼 있었다. 내가 없는 갭을 주장했다.**
>
> 스펙은 *"기존 테스트가 automation 저작(actor ≠ 작성자) 경로만 덮어 사람 저작 케이스가 없다"* 고
> 적었다. **실측으로 기각.**
>
> **① 알림 워커는 actor 가 사람인지 자동화인지 구분할 수 없다.** 이벤트 페이로드가 동일하다
> (`IssueCommented(issueKey, projectKey, commentId, actorId, occurredAt)`). "사람 저작" 변형 테스트를
> 추가해도 automation 케이스와 **바이트 단위로 같은 입력**이라 새 커버리지가 0이다.
>
> **② actor 자기 제외는 이미 역할 전수로 테스트돼 있다.** `EventRecipientResolverTest`(720줄) —
> MENTIONED(`:105`) · REPORTER(`:157`) · ASSIGNEE(`:201`) · WATCHER(`:266`) · COMPONENT_LEAD(`:324`)
> + PREVIOUS_ASSIGNEE · PROJECT_MEMBER · PROJECT_ADMIN. 내가 EC-11(역할 겸임)로 걱정한 열거가 이미 있다.
>
> **③ 제외는 단일 지점 필터다.** `EventRecipientResolver.kt:84` 가 `matches.flatMap{resolveRole}` 로
> 전 역할 수신자를 **모은 뒤** `.filter { it.userId != event.actorId }` 를 1회 적용한다. 역할 겸임은
> 구조적으로 커버되며 per-role 누락이 원리적으로 불가능하다.
>
> **④ 이벤트가 `actorId = 작성자` 를 싣는다는 것은 T1 이 증명했다** (`T3-H`).
>
> **그래서 T3 은 신규 테스트 0건으로 종료한다.** 중복 테스트는 유지비만 늘리고 "사람 경로를 검증했다"
> 는 **거짓 확신**을 만든다. 대신 유일하게 미증명이던 **봉인 실효성**을 뮤테이션으로 확인했다.
>
> **뮤테이션 결과 (2026-07-27).** 기준선 43 tests / 0 fail (EXIT=0) 선확인 →
> `:84` 를 `.filter { true }` 로 무력화 → **10건 빨개짐**:
> 역할 8종 전수 + `(e) actor 제외 + dedup 이후의 distinct 집합으로 visibility 를 1회 적용한다`
> + e2e `IC-1`. 원복 후 green 복귀.
>
> → **봉인은 실효한다.** `BUILD SUCCESSFUL` 이 나왔다면 나쁜 소식이었을 것이다
> (메모리 `seal-blinds-existing-guard`).

<details>
<summary>초안 계획 (참고용 — 위 사유로 미실행)</summary>

### (미실행) Task 3 초안

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/notification/src/test/kotlin/com/bts/notification/worker/IssueCommentedNotificationIntegrationTest.kt`]
- depends-on: [2]

> **BC 격리 위반 아님.** 테스트는 notification 모듈에 둔다 — 검증 대상이 notification 의 수신자
> 산출 로직이다. 메모리 `global-advice-instance-token-leak-done` — *"BC 격리는 프로덕션 의존
> 규칙이지 테스트 배치 규칙이 아니다."*

**RED**.
```kotlin
@Test fun `작성자가 REPORTER 이면 자신은 수신자에서 제외된다`()                    // S3
@Test fun `작성자가 REPORTER·ASSIGNEE·WATCHER 를 모두 겸하면 수신자가 0 이다`()   // EC-11
@Test fun `작성자가 아닌 REPORTER·ASSIGNEE·WATCHER 는 모두 수신한다`()            // NFR-2 열거 대조
```
**실패 메시지 (예상).** 기존 테스트가 automation 저작(actor ≠ 작성자) 경로만 덮어 사람 저작 케이스가 없음.

**GREEN**. 사람 저작 시나리오 픽스처 추가 (`actorId == authorId`). **프로덕션 코드 변경 0 예상** —
제외 로직이 이미 있으면 테스트만 추가된다. 없으면 그때 구현이 GREEN 이다.

**REFACTOR**. 수신자 단정을 집합 비교로 통일 (개수 단정 금지 — 메모리 `negative-guard-needs-body-discriminator`).

**검증 + ★뮤테이션 봉인**.
1. 기준선 확인 — `cd backend && ./gradlew :modules:notification:test --tests '*IssueCommented*'` → **EXIT=0 선확인**
2. 작성자 제외 로직을 일부러 무력화 → 위 테스트가 **빨개지는지** 확인
3. 원복 후 재확인. `BUILD SUCCESSFUL` 이 나오면 **나쁜 소식**이다 (메모리 `seal-blinds-existing-guard`)
4. 뮤테이션은 **커밋 후에만** 수행 (미커밋 상태에서 `git checkout --` 원복 = 작업 소실)

---

</details>

---

### Task 4. FR 등재 129 → 131 전수 동기화

**메타**.
- agent: `backend-engineer`
- files: [`docs/plan/fr-index.md`, `docs/sdd/02-requirements.md`, `docs/plan/product/issue-tracking.md`, `docs/plan/README.md`, `CLAUDE.md`, `docs/progress.html`, `/Users/maxi.moff/Maxi_wiki/BTS/history.md`, `/Users/maxi.moff/.claude/projects/-Users-maxi-moff-Projects-BTS/memory/MEMORY.md`]
- depends-on: []

> 코드 의존이 없어 Task 1 과 병렬 가능. 단 #314 병행 중이라 **직렬 권장**(아래 Plan 메타).

**★ 리뷰 C3 — 초안은 CLAUDE.md 8종 중 6종만 덮었다.** 누락 2종을 보강했다.

| CLAUDE.md §전수 동기화 항목 | 초안 | 상태 |
|---|---|---|
| 1. `fr-index.md` (행·§A.2·합계·L1 주석) | ✅ | |
| 2. `docs/sdd/02-requirements.md` | ✅ | |
| 3. `product/issue-tracking.md` | ✅ | |
| 4. `plan/README.md` | ✅ | |
| 5. `CLAUDE.md` | ✅ | |
| 6. ADR · plan · `progress.html` | ✅ | ADR·plan 은 이미 생성됨 |
| **7. Obsidian `Maxi_wiki/BTS/`** | ❌ **누락** | `glossary`·`domain/issue-tracking` 은 domain 단계에서 완료. **`history.md` 는 미완** — `bts-merge` 의 `sync-obsidian.ts` 가 자동 처리하나 **plan 에 명시가 없으면 다음 세션이 확인을 빠뜨린다** |
| **8. 자동 메모리** | ❌ **누락** | 세션 중 2건 갱신(`comment-backend-is-import-byproduct-read-only` 갱신 · `sibling-precedent-validation-placement-depends-on-producer-count` 신설). **머지 시점에 done-topic 추가 필요** |

**RED**. 테스트가 아니라 **검증 스크립트가 RED 판정자**다.
```bash
bash scripts/verify-master-plan.sh   # 먼저 실행 → 현재 PASS(129) 확인 = 기준선
# fr-index 에만 2행 추가 후 재실행 → 종료 4 (카운트 drift) 확인 = RED 가 작동함을 증명
```

**GREEN**. 8종 전수.
1. `docs/plan/fr-index.md` — L1 주석 `129→131` · §A.1 헤더 · **`### 댓글 (FR-CO, 2개)` 절 신설** + 2행 · §A.2 issue-tracking `35→37` + 그룹 문자열에 `CO(2)` · 합계 `129→131`
2. `docs/sdd/02-requirements.md` — FR ID 표에 2행 (**빠뜨리면 `SDD_COUNT ≠ PLAN_COUNT` 로 실패**)
3. `docs/plan/product/issue-tracking.md` — `§4.4 댓글 (FR-CO, 2개)` 신설 + FR 본문 2개 + D단계 체크박스 + L1 주석 + `소속 FR. N개` + BC 완료 게이트 `(FR-XX N개)`
4. `docs/plan/README.md` — L117 합계 · L121 · §1 BC 테이블 issue-tracking 행
5. `CLAUDE.md` — L10 · L52 의 `129 FR`
6. `docs/progress.html` — `node scripts/build-dashboard.mjs` 재생성

**REFACTOR**. 없음 (문서).

**검증**. `bash scripts/verify-master-plan.sh` → **PASS(131)**. 종료 4 없음.

**★함정**. 산문 안의 `FR-CO-01` 언급을 헤더 스캐너가 오인해 EXIT 1 을 내는 전력이 있다
(메모리 `verify-master-plan-header-scanner-false-match`, 사유 미출력). 실패 시 헤더 형식부터 의심.

---

### Task 5. 프론트 API 계약 + 훅

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/comments.ts`, `apps/web/src/api/__tests__/comments.test.ts`, `apps/web/src/hooks/use-comments.ts`, `apps/web/src/hooks/__tests__/use-comments.test.tsx`]
- depends-on: [2]

> `depends-on: [2]` 는 **계약 의존**이다. 응답 형태를 Task 2 의 실제 Kotlin `CommentResponse` 에서
> 확정한다 (메모리 `frontend-zod-backend-dto-contract-gap` — DTO invent 금지).

**0단계 (TDD 이전, 필수).** **레포 루트**에서 `pnpm install`.
worktree 에 `node_modules` 가 없다. 이 단계 없이는 T5~T7 의 모든 프론트 명령이 "모듈 없음" 으로
터지고 그 증상이 코드 결함으로 오인된다 (메모리 `worktree-node-modules-partial-install`).
**`apps/web` 안에서 실행 금지** — pnpm 워크스페이스라 루트 설치여야 한다.

**RED**.
```ts
it('commentResponseSchema — 6필드를 파싱한다', () => { ... })       // id/authorId/body/bodyHtml/createdAt/updatedAt
it('fetchComments — GET /api/v1/issues/:key/comments 를 호출한다', () => { ... })
it('addComment — POST 로 { body } 만 보낸다', () => { ... })        // authorId 미전송 확인
it('useComments — 목록을 반환한다', () => { ... })
it('useAddComment — 성공 시 commentQueryKey 만 invalidate 한다', () => { ... })  // FR-18
```

**GREEN**.
- `comments.ts` — `commentResponseSchema`(`id: z.string().uuid()` 등), `fetchComments`, `addComment`,
  `COMMENT_KEYS.list(issueKey)` 상수. `api/worklogs.ts` 의 `dataOf` 봉투 헬퍼 관례 재사용.
- `use-comments.ts` — `useComments(issueKey)`, `useAddComment(issueKey)`.
  성공 시 **`invalidateQueries(commentQueryKey)` 만.** 이슈 단건 쿼리는 **invalidate 하지 않는다**
  (댓글은 이슈 행을 바꾸지 않음). `setQueryData`·낙관적 갱신 **금지**.

**REFACTOR**. 매직 문자열을 `COMMENT_KEYS` 로 통일.

**검증**. `pnpm test -- comments` · `pnpm test -- use-comments`

**★주의**. `z.string().uuid()` 는 Zod v4 에서 픽스처 엄격도가 올라간다 — 테스트 픽스처의 UUID 는
실제 형식을 쓴다 (메모리 `zod-v4-uuid-fixture-strictness`).

---

### Task 6. `CommentSection` + 댓글 탭 + 문구 + MSW

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/issue/CommentSection.tsx`, `apps/web/src/components/issue/CommentSection.test.tsx`, `apps/web/src/components/issue/IssueActivityTabs.tsx`, `apps/web/src/components/issue/__tests__/IssueActivityTabs.test.tsx`, `apps/web/src/i18n/ko.ts`, `apps/web/src/i18n/ko.test.ts`, `apps/web/src/mocks/comment-handlers.ts`, `apps/web/src/mocks/handlers.ts`]
- depends-on: [5]

**RED**.
```tsx
// CommentSection.test.tsx
it('목록을 작성 시각 오름차순으로 렌더한다', () => { ... })
it('작성자 displayName 을 표시하고, 해석 실패 시 authorId UUID 를 표시한다', () => { ... })  // FR-19 / EC-19
it('canUpdate=false 이면 작성 폼 대신 권한 안내를 보여준다', () => { ... })                   // FR-20 / EC-20
it('작성 실패 시 입력 본문을 보존한다', () => { ... })                                        // EC-15
it('목록 403 이면 권한 안내를 보여주고 빈 목록과 구분한다', () => { ... })                    // EC-14
it('댓글 0건이면 빈 상태 문구를 보여준다', () => { ... })                                     // 리뷰 N4
it('제출 중에는 버튼이 비활성이고 라벨이 전환된다', () => { ... })                            // 리뷰 N5

// __tests__/IssueActivityTabs.test.tsx  — ★기존 테스트 (b) 보강
it('(b) 기본 활성 탭은 이력이다 — 작업로그/연결/댓글 모두 미마운트', () => { ... })            // FR-22 / EC-21
it('댓글 탭이 존재한다', () => { ... })

// ko.test.ts — ★named import 에 commentStrings 추가
it('commentStrings 에 commentBodyLabel 키가 존재한다', () => { ... })                        // FR-21
```

**GREEN**.
- `CommentSection.tsx` — `WorklogSection.tsx` 미러. `useComments` + `useAddComment`,
  `useUsersByIds(authorIds)` → `displayName ?? comment.authorId`, `toast` 성공/실패,
  `Button` 프리미티브 사용(원시 `<button>` 금지 — FR-UX-06 PR22 정책).
- `IssueActivityTabs.tsx` — `ACTIVITY_TABS.COMMENT` 추가 + `TabsTrigger`/`TabsContent` 1쌍.
  **`defaultValue` 는 `HISTORY` 그대로**(D8). `canUpdate` prop 을 `CommentSection` 에 전달.
- `ko.ts` — `commentStrings` 신설 + `issueDetailStrings.activityCommentTabLabel` 추가.
  **빈 상태·제출 중 문구 포함** — `commentEmptyState` · `commentAddButton` · `commentAddPending` ·
  `commentAddSuccess` · `commentAddError` · `commentNoPermission`
  (선례 — `WorklogSection:622-624` 가 `worklogStrings.worklogEmptyState` 로 0건을 표시하고,
  `:267-271` 이 `disabled={… || isPending}` + `isPending ? '추가 중...' : …` 로 제출 중을 표현한다).
- `mocks/comment-handlers.ts` 신설(GET + POST, stateful) + **`mocks/handlers.ts` 에 import + spread 등록**
  (메모리 `msw-global-handler-registration-gap` — 등록 누락이 전역 갭을 만든다).

**REFACTOR**. 댓글 1건 렌더를 하위 컴포넌트로 분리 (`WorklogSection` 의 행 컴포넌트 분리 패턴 동형).

**검증**. `pnpm test -- CommentSection` · `pnpm test -- IssueActivityTabs` · `pnpm test -- ko` · `pnpm typecheck`

**★네비/탭 aria 계약.** 탭 레이블은 E2E 계약이다. `issueDetailStrings` 를 경유하고 하드코딩하지 않는다
(메모리 `frontend-nav-aria-label-e2e-contract`).

---

### Task 7. E2E + 브라우저 눈확인

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/issue-comment.spec.ts`]
- depends-on: [6]

**RED**. 시나리오 4개.
```ts
test('댓글 탭 진입 → 목록이 보인다')
test('댓글 작성 → 목록에 반영되고 성공 토스트가 뜬다')
test('권한 없는 사용자는 작성 폼 대신 안내를 본다')
test('기본 진입 시 활성 탭은 이력이다')                 // D8 회귀 고정
```

**GREEN**. MSW 시나리오 토글로 403 케이스 구성 (`e2e-msw-scenario-toggle-localstorage-flag` 관례).

**REFACTOR**. 셀렉터를 `getByRole` + 정확 일치로 (메모리 `playwright-getbyrole-exact-strict-mode`).

**검증**.
- `pnpm test:e2e -- issue-comment` — ★positional 필터가 삼켜지는 전력이 있어 **실행 개수로 판정**한다
  (메모리 `e2e-playwright-filter-arg-drop`). 필터가 안 먹으면 바이너리 직접 호출.
- **브라우저 눈확인 (완료기준 10).** 실제로 이슈 상세를 열어 댓글이 보이고 써지는지 사람이 본다.
  FR-UX-06 이 22 PR 을 끝내고도 이걸 미실시로 남긴 전력이 있다.

## Plan 메타

- **task 수**. 7
- **예상 시간**. task × 약 4분 = 약 28분 (직렬 기준)
- **TDD 강제**. yes — `test:` 커밋이 `feat:` 보다 먼저인지 `bts-impl` 이 git log 로 자동 검증
- **의존 그래프**.
  ```
  T1 ──▶ T2 ──▶ T3
           └──▶ T5 ──▶ T6 ──▶ T7
  T4 (독립)
  ```
- **wave 계산상 가능한 병렬**. W1={T1, T4} · W2={T2} · W3={T3, T5} · W4={T6} · W5={T7}
- **★그러나 직렬 dispatch 를 권장한다.** PR #314 를 **다른 세션이 실시간 구현 중**이다. pre-commit
  `lint-staged` 가 공유 `git stash` 를 쓰므로 병렬 dispatch 의 동시 커밋이 저쪽 미커밋 파일을
  날릴 수 있다 (메모리 `parallel-dispatch-precommit-hook-race` · `worktree-lint-staged-shared-git-stash-collision`).
  #305 도 같은 이유로 7 태스크를 직렬 + `--no-verify` 로 돌렸다.
- **파일 겹침**. **없다** (리뷰 N2 에서 초안 서술을 정정). 초안은 "T1↔T2 는 `CommentApplicationService`
  경유, T5↔T6 는 `api/comments.ts` 경유로 겹침" 이라 적었으나 **실제 `files` 교집합은 두 쌍 모두 ∅** 다
  (T2 는 컨트롤러·DTO·핸들러만, T6 는 컴포넌트·i18n·mocks 만 건드린다). 직렬화는 파일 겹침이 아니라
  **선언된 `depends-on` 으로만** 일어난다.
- **추가 검증**. `ktlintCheck` · `detekt` · `pnpm verify` · `bash scripts/verify-master-plan.sh`
- **범위 침범 감시**. `comment` 패키지의 HTTP 매핑이 최종 **2개**(GET·POST)인지, `CommentRepository`
  공개 메서드가 **2개**(insert·listByIssue)인지, `Comment` 도메인이 **전 필드 `val`** 인지 머지 전
  재확인 (FR-CO-02 경계). 판별식은 하드코딩 목록이 아니라 **grep 카운트**로 둔다.
- **cross-BC 정확한 표현** (리뷰 N1). "cross-BC 변경 0" 은 부정확하다 — T3 가 **notification 모듈의
  테스트 1건**을 수정한다. 정확히는 **"cross-BC 프로덕션 의존 0, 테스트 배치 1건"** 이다. BC 격리
  규칙은 프로덕션 의존에 걸리는 것이고 테스트 배치 규칙이 아니다.

## 리뷰 결과

### 리뷰 체인 선택 (2026-07-27)

**★스킬 결함 발견 (N6).** `bts-review-plan` Step 2 의 타입별 분기 표에 **`type=backend` 가 없다**
(`auth`·`migration`·`ui`·`api`·`feature`·`design` + `{bugfix,chore,qa}` skip 만 존재). classify 가
`backend` 를 내보내는데 라우팅 규칙이 없어 분기가 미정의다. → `TODOS.md` 등재 대상.

메모리 `bts-review-plan-autoplan-overkill`(Maxi 피드백) 에 따라 **eng 집중 + design 추가**로 취사선택했다.
- ✅ **eng 리뷰** — BC 격리 · 시그니처 폭발반경 · 트랜잭션 · 회귀 · 의존 그래프
- ✅ **design 리뷰** — UI 포함 작업(T5~T7)이므로 추가 (메모리가 명시한 조건)
- ❌ CEO 리뷰 — "10-star 제품 재구상" 은 백엔드 FR plan 에 부적합
- ❌ DevEx 리뷰 — 신규 공개 API 가 REST 1개뿐, 별도 개발자 경험 검토 대상 아님

> **⚠️ 편향 한계 명시.** 메모리는 *"plan 작성자가 자기 plan 을 리뷰하면 편향 → 독립 시각
> (code-reviewer/Plan 에이전트 dispatch)이 필요"* 라고 적고 있다. 그런데 **이 세션은 사용자 요청 없는
> sub-agent dispatch 가 금지**돼 있어 독립 에이전트 리뷰를 쓸 수 없었다. 아래는 **자기 리뷰**이며,
> 그 한계를 감안해 "주장 검증"에 집중했다 — 서술을 읽고 판단하는 대신 **모든 주장을 grep·소스로
> 대조**했고, 그 결과 초안의 사실오류 3건을 잡았다. 독립 리뷰가 필요하면 Maxi 가 요청해야 한다.

### plan-eng-review (2026-07-27, 자기 리뷰)

**BLOCKER: 없음.**

**CONCERN 3건 — 전부 plan 반영 완료.**

| # | 발견 | 근거 | 반영 |
|---|---|---|---|
| **C1** | **T1 `files` 에 `AutomationIssueMutationAdapterTest.kt` 누락 → 컴파일 실패 유발** | `:351` 이 MockK `verify` 로 `authorId = actor` 인자를 **명시**한다. 시그니처에서 `authorId` 를 없애면 이 파일이 깨지는데 files 목록에 없어 구현자가 못 본다. 전 모듈 grep 으로 확정 | T1 files 추가 + 호출자 전수표(프로덕션 2 · 테스트 14곳) |
| **C2** | **D7 의 숨은 파생 요구사항 — 서비스에서 무엇을 던지는가** | 검증을 컨트롤러→서비스로 옮기면 `ResponseStatusException`(웹 관심사)은 부적합해진다. ① `runAttempt:212-215` 가 `IssueAccessDeniedException` **하나만** 포트 타입으로 번역해 HTTP 예외가 `ActionExecutor` 까지 전파 ② `CommentExceptionHandler:91` 의 `HttpStatus.valueOf` 경로에는 **이미 기록된 위험**이 있다(#310 `/error` 재유출) | `CommentExceptions.kt` 신설 + 핸들러 400 매핑을 T1·T2 에 추가 |
| **C3** | **T4 가 CLAUDE.md 8종 중 6종만 덮음** | Obsidian `history.md` 미러(7) · 자동 메모리(8) 누락. 실제로는 merge 단계·세션 작업이 커버하지만 **plan 에 없으면 다음 세션이 빠뜨린다** | T4 files·표 보강 |

**주의 3건 — 서술 정확성 (반영 완료).**

| # | 발견 | 정정 |
|---|---|---|
| N1 | "cross-BC 변경 0" 이 부정확 — T3 가 notification 모듈 테스트를 수정한다 | **"cross-BC 프로덕션 의존 0, 테스트 배치 1건"** 으로 정정. BC 격리는 프로덕션 의존 규칙 |
| N2 | Plan 메타의 "파일 겹침 T1↔T2 / T5↔T6" 가 사실과 다름 | 실제 `files` 교집합은 **두 쌍 모두 ∅**. 직렬화는 `depends-on` 으로만 일어난다고 정정 |
| N3 | 기존 `CommentApplicationServiceTest` 호출 **12곳** 마이그레이션이 과소평가 | RED 주석에 "기존 12곳 마이그레이션 후 신규 추가" 명시 |

**검증 통과 — 초안 주장이 실측으로 확인된 것.**

| 주장 | 실측 |
|---|---|
| dry-run 에 상한 검증 부작용 없나 | ✅ 문제 없음. `runAttempt:204-221` 이 dry-run 에서도 `attempt()` 를 **실행**하고 트랜잭션만 rollback-only → **상한이 dry-run 에서도 발화**한다. 바람직(사전 검증 목적) + 데이터 오염 0 |
| `createImported` 이름이 적절한가 | ✅ `WorklogService.createImported` 가 이미 있어 **대칭**이다. 다른 클래스라 충돌 없음 |
| 마이그레이션 0건이 맞나 | ✅ `V035__comments.sql` 이 부분 인덱스까지 보유 |
| 완료기준 14건이 측정 가능한가 | ✅ 13건 자동 측정 가능. 기준 10(브라우저 눈확인)만 사람 판정 — **의도적**이며 FR-UX-06 이 미실시로 남긴 항목을 되살린 것 |
| T3 테스트를 notification 에 두는 것이 BC 격리 위반인가 | ✅ 위반 아님. 검증 대상이 notification 의 수신자 산출 로직. 메모리 근거 있음 |
| 의존 그래프에 순환이 있나 | ✅ 없음. T1→T2→T3 / T2→T5→T6→T7 / T4 독립 |

### plan-design-review (2026-07-27, 자기 리뷰 — UI 포함이라 추가)

**BLOCKER: 없음.** `CommentSection` 이 `WorklogSection` 을 미러링하므로 시각 언어는 이미 정해져 있고
새 디자인 결정이 없다. `DESIGN.md`(43KB)·ADS v2 토큰 위에서 작업한다.

**주의 2건 — 반영 완료.**

| # | 발견 | 근거 | 반영 |
|---|---|---|---|
| N4 | **빈 상태 UI 미명시.** 댓글 0건일 때 무엇을 보여주는지 스펙·plan 어디에도 없었다 | `WorklogSection:622-624` 가 `worklogStrings.worklogEmptyState` 로 0건 문구를 표시하는 선례 보유 | T6 RED 케이스 + `commentEmptyState` 문구 추가 |
| N5 | **제출 중 상태 미명시.** 중복 제출 방지·피드백이 빠져 있었다 | `WorklogSection:267-271` 이 `disabled={… \|\| isPending}` + `isPending ? '추가 중...' : …` | T6 RED 케이스 + `commentAddPending` 문구 추가 |

**확인된 사항.**
- 탭이 3 → 4개로 늘어난다. `TabsList` 는 Radix 기반이고 기존 3개가 이미 같은 패턴이라 레이아웃 위험 낮음.
- 탭 레이블은 `issueDetailStrings` 경유 = **E2E 계약**. 하드코딩 금지가 plan 에 이미 명시돼 있다.
- 원시 `<button>` 금지(FR-UX-06 PR22 정책) → `Button` 프리미티브 사용이 plan 에 이미 명시돼 있다.
- 아바타 미표시는 **의도적 범위 밖** — 1차는 이름+시각(`WorklogSection` 동형).

### 종합

- **BLOCKER 0** → 게이트 1 진입 가능
- CONCERN 3 · 주의 5 **전부 반영 완료** (plan·spec 수정)
- 초안의 **사실오류 3건**(C1 호출자 누락 · N1 cross-BC 표현 · N2 파일 겹침 서술)을 잡았다.
  세 건 모두 **서술을 읽어서가 아니라 grep 으로 대조해서** 나왔다.
- **잔여 — 독립 리뷰 미실시.** 자기 리뷰의 편향 한계는 위에 명시했다.
