# FR-BD-03 D6/D7 — WIP 제한 컬럼 헤더 경고 + 스윔레인 그룹 (프론트 UI + E2E)

> slug: fr-bd-03-d6-d7-wip-ui-e2e
> type: ui
> agent: frontend-engineer (E2E는 qa-engineer 보조)
> 생성: 2026-06-22

## Brief

FR-BD-03 (WIP 제한 + 스윔레인)의 D6/D7. 백엔드 D1~D5는 PR #172 완료.
- D6. 프론트 UI — 보드 컬럼 헤더 WIP 초과 경고 + 스윔레인 그룹 (책임. designer → frontend-engineer)
- D7. E2E (책임. qa-engineer)

백엔드 신호(PR #172):
- 컬럼별 `wipLimit`, `wipExceeded` 신호 노출
- `swimlaneField` (NONE/ASSIGNEE/PRIORITY) echo
- PATCH 2종(컬럼 wipLimit 설정 + 보드 swimlaneField 설정, 권한 CREATE)
- WIP 초과 = 시각 경고만 (이동 차단 없음, ADR 결정 1)

## 도메인 정리

- **BC**: agile-planning (확정). 순수 view-layer 작업 — 도메인 모델/엔티티 신규 0. 백엔드 D1~D5(PR #172)로 Board.swimlaneField / BoardColumn.wipLimit 확정.
- **영향 파일(프론트)**: `apps/web/src/api/boards.ts`(Zod 스키마 확장), `components/board/BoardColumn.tsx`·`KanbanBoard.tsx`·`BoardCard.tsx`, `mocks/board-handlers.ts`·`board-fixtures.ts`, `routes/projects.$projectKey.board.tsx`, `i18n/`, `e2e/`
- **새 용어**: "스윔레인(Swimlane — 보드를 담당자/우선순위별로 가로 분리)", "WIP 제한(Work In Progress limit — 컬럼당 최대 카드 수)". 백엔드에서 이미 도입. glossary 미등록 → merge 시 추가 후보(Maxi 승인).
- **관련 ADR**: [docs/decisions/2026-06-22-fr-bd-03-wip-swimlane.md](../decisions/2026-06-22-fr-bd-03-wip-swimlane.md) — 충돌 없음(같은 FR). 결정 1(WIP 경고 전용·이동 무차단), 결정 2(스윔레인 그룹핑 프론트 위임), 결정 3(EPIC 이연).

### 백엔드 신호(실측, GET /api/v1/boards/{id} = BoardDetailResponse)

- `columns[].wipLimit: Int?`(null=무제한), `columns[].wipExceeded: Boolean`(카드수 > wipLimit, strictly greater)
- `swimlaneField: String`("NONE"/"ASSIGNEE"/"PRIORITY")
- 카드: `{ issueKey, summary, assigneeId: UUID?, version }` — **priority 없음**(BoardResponses.kt:139 NIT-1로 제외)
- 설정 쓰기(실측 경로): `PATCH /api/v1/boards/{id}`(body `{swimlaneField}` → BoardMetaResponse), `PATCH /api/v1/boards/{id}/columns/{columnId}`(body `{wipLimit}` → ColumnMetaResponse) — 둘 다 권한 IssuePermission.CREATE on Project
- 권한 사전 조회: GET `/api/v1/users/me/project-permissions?projectKey=` → `permissions.CREATE: boolean`(project-permissions.ts)

### 현재 프론트 미러 상태

- `boards.ts`의 Zod 스키마에 `wipLimit`·`wipExceeded`·`swimlaneField` **미반영**(백엔드 D4 신호 미러 필요). 카드에 priority 없음.

### 도메인 갭 3종 (spec서 Maxi 확인 — 갈림길)

1. **PRIORITY 스윔레인 priority 부재**: `BoardIssueView.priority`(BoardIssueLookupPort.kt:132)는 존재하나 `BoardCardResponse`가 NIT-1로 제외 → 카드 응답에 priority 없음. ADR 결정 2 근거("priority가 이미 노출")와 구현 모순(PR #13 learning 패턴). PRIORITY 스윔레인 그리려면 ⓐ 백엔드 view-layer patch로 priority 재노출(same-BC, PR #13 옵션C 선례) 또는 ⓑ PRIORITY 이연(enum은 백엔드 존재).
2. **ASSIGNEE displayName**: 카드엔 assigneeId(UUID)만 → 담당자 이름 표시에 users 조회 필요. ADR "기존 사용자 조회 패턴 재사용".
3. **설정 UI 범위**: product D6는 "헤더 경고 + 스윔레인 그룹" 표시만 명시. PATCH 2종(WIP 설정 + 스윔레인 선택) 설정 UI를 D6에 포함할지.

## 스펙

전체 스펙. [docs/specs/2026-06-22-fr-bd-03-d6-d7-wip-ui-e2e.md](../specs/2026-06-22-fr-bd-03-d6-d7-wip-ui-e2e.md)

**Maxi 게이트 결정(2026-06-22)**.
- D6 범위 = 표시(WIP 경고 + 스윔레인 그룹) + **스윔레인 셀렉터(서버 저장, PATCH /boards/{id})**. WIP 제한 '편집' UI는 이연.
- PRIORITY 스윔레인 = **백엔드 view-layer patch로 BoardCardResponse에 priority 재노출**(same-BC, PR #13 옵션C 선례). NONE/ASSIGNEE/PRIORITY 3종 완전 지원.
- 스윔레인 레이아웃 = **컬럼 내부 레인 그룹(세로)**. droppable column id 보존 → 드래그 이동(FR-BD-01) 회귀 0. 레인 간 드래그로 담당자/우선순위 변경은 범위 아님.

핵심 시나리오 요약.
- WIP 초과 컬럼 헤더에 시각 경고(이동 무차단, ADR 결정 1)
- CREATE 권한자에게 스윔레인 셀렉터(NONE/ASSIGNEE/PRIORITY) 노출 → 선택 시 PATCH 서버 저장 + 보드 invalidate
- 컬럼 내부에서 담당자/우선순위별 서브그룹으로 카드 묶어 표시

## Brainstorming Check

✅ 통과 (1회 iteration). gap = 스윔레인 레이아웃의 드래그 회귀 위험 → Maxi 결정으로 컬럼 내부 레인 그룹(droppable 보존) 채택. 부차 항목은 plan 세부.

## Plan

> 모든 경로는 repo 루트 기준. 프론트 검증은 worktree에서 `pnpm`, 백엔드는 `backend/`에서 `./gradlew`.

### Task 1. 백엔드 view-layer patch — BoardCardResponse.priority 재노출

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/agile-planning/src/main/kotlin/com/bts/agileplanning/web/dto/BoardResponses.kt`, `backend/modules/agile-planning/src/test/kotlin/com/bts/agileplanning/web/dto/BoardResponsesTest.kt`]
- depends-on: []

**RED**. `BoardResponsesTest`에 카드 응답이 priority를 노출하는지 단언 추가 — `BoardCardResponse.from(view)` 결과 `.priority == view.priority`. (현재 priority 미노출이라 컴파일 실패/단언 실패)

**GREEN**. `BoardCardResponse`에 `val priority: Int` 추가. `from(card)`에서 `priority = card.priority` 매핑. NIT-1 주석은 "PRIORITY 스윔레인(FR-BD-03 D6) 그룹화 근거로 재노출 — Maxi 확정"으로 갱신.

**REFACTOR**. KDoc `@property priority` 추가(작을수록 높은 우선순위, BoardIssueLookupPort 정합).

**검증**. `cd backend && ./gradlew :modules:agile-planning:test --tests '*BoardResponsesTest' :modules:agile-planning:ktlintMainSourceSetCheck` (정확한 task명은 impl에서 확인). BoardControllerIntegrationTest는 카드 개별 필드 미단언이라 비파괴.

### Task 2. 프론트 Zod 스키마 + MSW fixtures/handlers 미러 확장

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/boards.ts`, `apps/web/src/api/boards.test.ts`, `apps/web/src/mocks/board-fixtures.ts`, `apps/web/src/mocks/board-handlers.ts`, `apps/web/src/mocks/board-handlers.test.ts`]
- depends-on: []

**RED**. `boards.test.ts`에 추가 — `boardCardSchema`가 `priority` 파싱, `boardColumnSchema`가 `wipLimit(number|null)`·`wipExceeded(boolean)` 파싱, `boardDetailSchema`가 `swimlaneField(enum NONE/ASSIGNEE/PRIORITY)` 파싱. 누락 시 실패.

**GREEN**. `boards.ts`:
- `boardCardSchema`에 `priority: z.number().int()` (백엔드 Task 1 미러)
- `boardColumnSchema`에 `wipLimit: z.number().int().nullable()`, `wipExceeded: z.boolean()`
- `boardDetailSchema`에 `swimlaneField: z.enum(['NONE','ASSIGNEE','PRIORITY'])`
- `board-fixtures.ts`: `StoredCard`(BoardCard 상속이라 priority 자동 요구) 시드에 `priority` 추가, `StoredBoardDetail.columns`에 `wipLimit`/`wipExceeded`, top-level `swimlaneField` 추가. DEFAULT_BOARD/FILTER_BOARD 갱신.
- `board-handlers.ts`: GET 응답 매핑에 priority/wipLimit/wipExceeded/swimlaneField 포함.

**REFACTOR**. 스키마 KDoc 갱신.

**검증**. `pnpm test boards.test.ts board-handlers.test.ts && pnpm typecheck`. ★메모리 [Zod 스키마 강화 mock 파급] — required 추가 후 인라인 mock grep 전수 + tsc 통과 필수(board 외 board fixture 참조처 grep).

### Task 3. updateBoardSwimlane API 함수 + useUpdateSwimlane 훅

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/boards.ts`, `apps/web/src/api/boards.test.ts`, `apps/web/src/hooks/use-update-swimlane.ts`, `apps/web/src/hooks/use-update-swimlane.test.tsx`]
- depends-on: [2]

**RED**. `boards.test.ts` — `updateBoardSwimlane(boardId,'ASSIGNEE')`가 PATCH `/api/v1/boards/{id}` body `{swimlaneField}` 호출, BoardMeta 응답 파싱. `use-update-swimlane.test` — mutate 성공 시 보드 쿼리 invalidate.

**GREEN**. `boards.ts`에 `boardMetaSchema`(boardId/projectKey/name/swimlaneField) + `updateBoardSwimlane(boardId, field)` (apiPatch 사용 — client.ts에 있는지 확인, 없으면 boards.ts 내 fetch 패턴 따름). `use-update-swimlane.ts` — useMutation, onSuccess에서 `queryClient.invalidateQueries(['board', boardId, ...])`.

**REFACTOR**. CSRF 헤더 등 기존 mutation 패턴(use-move-card) 정합.

**검증**. `pnpm test boards.test.ts use-update-swimlane && pnpm typecheck`. ★메모리 [Frontend API convention per BC] — 같은 BC(boards) 선례 grep + CSRF 수동.

### Task 4. WIP 경고 컬럼 헤더 UI

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/board/BoardColumn.tsx`, `apps/web/src/components/board/BoardColumn.test.tsx`, `apps/web/src/i18n/board-labels.ts`, `apps/web/src/i18n/board-labels.test.ts`]
- depends-on: [2]

**RED**. `BoardColumn.test` — wipLimit=5·카드2 → "2/5" 표기. wipLimit=2·카드3·wipExceeded=true → 경고 스타일(role/aria-label "WIP 초과") 표시. wipLimit=null → 기존 카드 수만.

**GREEN**. 헤더 카드 수 배지 옆에 wipLimit 있으면 `{count}/{wipLimit}`. wipExceeded면 경고 색(DESIGN.md 토큰: destructive/amber) + `aria-label`. null이면 기존 그대로. 사용자 문자열은 `board-labels.ts` i18n 경유.

**REFACTOR**. 경고 배지 헬퍼 컴포넌트 분리(가독성).

**검증**. `pnpm test BoardColumn.test board-labels && pnpm typecheck`. ★i18n 콜론 종결 금지(ko.test 회귀 가드).

### Task 5. 스윔레인 그룹 렌더 — 헬퍼 + BoardColumn 바디 + KanbanBoard props

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/lib/swimlane-group.ts`, `apps/web/src/lib/swimlane-group.test.ts`, `apps/web/src/components/board/BoardColumn.tsx`, `apps/web/src/components/board/BoardColumn.test.tsx`, `apps/web/src/components/board/KanbanBoard.tsx`]
- depends-on: [4]

**RED**. `swimlane-group.test` — 순수 헬퍼 `groupCardsBySwimlane(cards, swimlaneField, assigneeNames)` → 그룹 배열(`{label, cards}`). NONE=단일 그룹(라벨 없음), ASSIGNEE=담당자별+미배정 마지막, PRIORITY=priority asc. `BoardColumn.test` — swimlaneField=ASSIGNEE면 컬럼 안에 서브헤더 그룹 렌더(droppable column id 불변 검증).

**GREEN**. `swimlane-group.ts` 순수 헬퍼. `BoardColumn`에 `swimlaneField`·`assigneeNames` props 추가 → NONE이면 기존 리스트, 그 외엔 그룹별 서브헤더+카드. droppable id(columnId)/draggable id(issueKey) 불변(EC6). `KanbanBoard`가 `board.swimlaneField`를 BoardColumn에 전달.

**REFACTOR**. 그룹 서브헤더 컴포넌트 추출.

**검증**. `pnpm test swimlane-group BoardColumn.test KanbanBoard.test && pnpm typecheck`. ★드래그 회귀 0 — droppable/draggable id 불변.

### Task 6. 스윔레인 셀렉터 + 권한 게이팅 + BoardPage 통합

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/board/SwimlaneSelector.tsx`, `apps/web/src/components/board/SwimlaneSelector.test.tsx`, `apps/web/src/routes/projects.$projectKey.board.tsx`, `apps/web/src/routes/__tests__/projects.board.test.tsx`, `apps/web/src/i18n/board-labels.ts`]
- depends-on: [3, 5]

**RED**. `SwimlaneSelector.test` — 3 옵션(없음/담당자/우선순위) 렌더, 선택 시 onChange 호출. `projects.board.test` — CREATE 권한 있으면 셀렉터 노출, 없으면 미노출(S6). 변경 시 useUpdateSwimlane.mutate, 실패 시 토스트+롤백(S7).

**GREEN**. `SwimlaneSelector`(shadcn Select 재사용, value=swimlaneField). `BoardPage`에 `fetchProjectPermissions(projectKey)` 쿼리(use-project-permissions 훅 있으면 재사용) → `permissions.CREATE`로 셀렉터 게이팅. onChange → useUpdateSwimlane. i18n 라벨.

**REFACTOR**. 셀렉터 배치를 BoardSelectorDropdown 옆 헤더 행으로.

**검증**. `pnpm test SwimlaneSelector projects.board && pnpm typecheck`. ★메모리 [UI 권한게이팅 요약API 노출] resolver+요약API 둘 다, [mutation 부분응답 플리커] invalidate-only.

### Task 7. E2E (D7)

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/board-wip-swimlane.spec.ts`, `apps/web/src/mocks/board-fixtures.ts`(시드 보강만)]
- depends-on: [2, 3, 4, 5, 6]

**RED→GREEN**. Playwright happy path — S1(WIP 초과 컬럼 헤더 경고 표시), S3(스윔레인 ASSIGNEE 전환 → PATCH 라운드트립 → 컬럼내 담당자 그룹 표시, SPA 내부 이동으로 store 영속 검증), S4(PRIORITY), S6(권한 없는 사용자 셀렉터 미노출). 기존 보드 E2E 회귀 0.

**검증**. `pnpm test:e2e board-wip-swimlane` + 기존 board E2E 재실행. ★메모리 [MSW 파생동작 공유store E2E]·[E2E MSW 시나리오 토글] — reload 금지(store 리셋 가짜그린), 셀렉터 게이팅은 권한 핸들러 시드.

### Task 8. 문서 동기화 — product D6/D7 마킹 + 대시보드

**메타**.
- agent: `frontend-engineer`
- files: [`docs/plan/product/agile-planning.md`, `docs/progress.html`, `docs/plan/progress.html`]
- depends-on: [1, 2, 3, 4, 5, 6, 7]

**작업**. `agile-planning.md` §2.3 FR-BD-03 D6/D7 `[ ]`→`[x]`(PR 번호 #173 기재). 백엔드 priority 재노출 deviation은 product 인라인 메모. `node scripts/build-dashboard.mjs` 재생성. `bash scripts/verify-master-plan.sh` PASS 확인.

**검증**. `bash scripts/verify-master-plan.sh` 종료 0. ★메모리 [Dashboard regen after FR marking] 같은 커밋 포함.

## Plan 메타

- task 수: 8 (백엔드 1 + 프론트 6 + E2E 1)
- 예상 wave: 4 — wave1 [T1, T2], wave2 [T3, T4], wave3 [T5, T6], wave4 [T7] → T8(문서, 끝)
- 파일 겹침 직렬화: boards.ts(T2→T3), BoardColumn.tsx(T4→T5), route(T6)
- TDD 강제: yes (각 task RED→GREEN→REFACTOR)
- BC 격리 예외: Task 1 = frontend PR 내 same-BC(agile-planning) view-layer patch(PR #13 옵션C 선례). plan 리스크에 명시.
- 추가 검증: pnpm typecheck/lint/test, ktlint/detekt(agile-planning), playwright(qa), verify-master-plan

## 리뷰 결과 (← /bts-review-plan 채움)
