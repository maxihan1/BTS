# 기술부채 전수 처리 — 마스터 계획 (0파 ~ 10파 · 등재 26건)

> 생성 2026-08-12 · 0파 PR #365 에서 등재
> 장부 정본은 `TODOS.md`. 이 문서는 **각 항목을 어느 PR 이 닫는가**의 정본이다.
> 초판 24건에 PR #375 게이트 2 리뷰가 발견한 2건이 더해져 **26건**이다.
> 진척 대시보드 `docs/progress.html` (`node scripts/build-dashboard.mjs` 재생성)

## 이 문서가 왜 있나

PR 묶음표가 **건수만 적고 어느 항목인지는 안 적으면** 두 목록(장부 24건 ↔ 묶음 11개)이
서로를 검사하지 못한다. 이 저장소의 지배적 결함 양식(`[[two-lists-never-check-each-other]]`)이
바로 그것이고, 처방은 **차집합 판별식**이다.

### ★키를 줄번호에서 제목으로 바꿨다 (2026-08-12 · PR #375)

초판은 `TODOS.md` **줄번호**를 키로 실었다. **그 키는 작성 시점부터 틀렸고, 그 뒤 더 벌어졌다.**

| 항목 | 초판이 적은 줄 | #365 머지 직후 실제 | #366 머지 후 실제 |
|---|---|---|---|
| OpenAPI required | `2023` | **2033** | **2098** |
| setupServer 59개 | `2285` | **2295** | **2360** |
| placeholder/aria-label | `3243` | **3429** | **3531** |

22건 중 **18건**이 어긋나 있었다. 원인은 둘이다.

1. **줄번호는 휘발성 키다.** 장부를 한 줄만 고쳐도 그 아래가 전부 밀린다 — PR #366 하나가
   110줄을 추가했다. 부채를 닫을 때마다 밀리므로 **매 PR 마다 깨지는 구조**다.
2. **대조가 자동화돼 있지 않았다.** 아래 스니펫이 문서 안에만 있었고 판별식이 아니었다.
   그래서 아무도 안 돌렸다 — 돌렸으면 즉시 걸렸다. **「가드는 있는데 배선이 없다」 양식.**

⇒ 키를 **항목 제목**으로 바꾸고, 대조를 `scripts/workflow/debt-ledger-mapping.test.ts` 로
승격했다. 제목도 바뀔 수 있지만 **바뀌면 판별식이 즉시 red** 다.
완벽한 키가 목표가 아니라 **조용히 깨지지 않는 것**이 목표다.

**대조 방법 (이제 CI 가 돌린다).**

```bash
node --experimental-strip-types --test scripts/workflow/debt-ledger-mapping.test.ts
```

키 정규화는 **제목 끝의 괄호 그룹 하나만** 떼는 것이다. 상태 표기(`미착수`·`착수`·`선재`·
`신규`)는 진행에 따라 바뀌지만 제목 본문은 안 바뀐다. ★「괄호 앞까지 자르기」는 안 된다 —
`apps/web(테스트 인프라) — …` 는 괄호가 **맨 앞**, `로딩 프레임 계약 — 임포트 페이지는
고정됨(2026-08-10), …` 은 **중간**에 있다.

## 묶음 기준 — 「종류」가 아니라 「충돌면」

같은 파일·같은 계약을 건드리는 항목을 한 PR 로 묶는다. 종류(ui / infra / test)로 묶으면
`apps/web` 12개 파일을 두 PR 이 동시에 건드려 머지 충돌과 반쪽 봉합을 만든다.

## 전수 매핑 — 26건 (초판 24 + PR #375 리뷰 발견 2)

**이 표가 정본이다.** 「항목」 열은 `TODOS.md` 의 `## <마커> <제목>` 에서 **끝 괄호를 뗀 것과
정확히 같아야** 한다 — 판별식이 그 일치를 강제한다.

| # | 상태 | 항목 | PR | 영역 |
|---|---|---|---|---|
| 1 | ⬜ | apps/web — 접힘 레일에서 「최근 항목」 접근 경로 | **보류** | apps/web |
| 2 | ✅ | 인프라 — 전체 스위트 실행에서 `pnpm test` 가 **간헐적으로 exit≠0** 이 된다 | **#375** | 테스트 인프라 |
| 3 | ✅ | apps/web(테스트 인프라) — MSW 리졸버가 **요청 1회에 2번 실행**된다 | **#375** | 테스트 인프라 |
| 4 | ⬜ | 인프라 — CI 벽시계가 실제 실행의 10배다 | **미배정** | 인프라 |
| 5 | ⬜ | issue-tracking — OpenAPI required 오표기 **잔여 26 프로퍼티** + 전수 판별식 부재 | **미배정** | issue-tracking |
| 6 | ⬜ | apps/web — 담당자/리드 후보가 검색 전에 전량 노출되는 곳이 **3군데 더** 있다 | **미배정** | apps/web |
| 7 | ✅ | apps/web(테스트 인프라) — 로컬 `setupServer` 59개 전면 이주 | **#375** | 테스트 인프라 |
| 8 | ✅ | apps/web — `IssueCreateForm` 컴포넌트가 줄수 상한을 넘는다 | **#385** | apps/web |
| 9 | ⬜ | issue-tracking — 마스킹된 required 커스텀 필드의 충족 여부를 응답이 알려 주지 않는다 | **미배정** | issue-tracking |
| 10 | ✅ | 인프라 — 낡은 `main` run 이 러너를 계속 점유한다 | **#366** | 인프라 |
| 11 | ✅ | apps/web — 이동 대상 프로젝트 키 **대소문자 정규화 부재** | **#367** | apps/web |
| 12 | ✅ | apps/web — 이동 `errorProjectNotFound` 도 **prod 도달 불가** 문구다 | **#367** | apps/web |
| 13 | ⬜ | issue-tracking — `MovePreviewServiceTest` 가 **prod 동시 성립 불가 조합**을 스텁한다 | **미배정** | issue-tracking |
| 14 | ✅ | apps/web(E2E) — 임포트 CREATE 게이트 E2E 시나리오 부재 | **#384** | apps/web |
| 15 | ✅ | 로딩 프레임 계약 — 임포트 페이지는 고정됨(2026-08-10), **게이트 훅 다른 소비처 2곳은 공백** | **#384** | apps/web |
| 16 | ✅ | apps/web — 권한 재조회로 **임포트 화면이 통째로 사라질 수 있다** | **#384** | apps/web |
| 17 | ✅ | apps/web — 이동 `errorPreview` 가 500·단절에도 「키를 확인해 주세요」라 말한다 | **#367** | apps/web |
| 18 | ✅ | apps/web — 이동 403 문구에서 「다른 대상 프로젝트를 시도」가 함께 사라졌다 | **#367** | apps/web |
| 19 | ✅ | 문서 — `DEVELOPMENT.md §2.2` 와 SDD §22.7.1 이 파일 300줄 상한을 놓고 갈린다 | **#365** | 문서 |
| 20 | ⬜ | 도구 — Obsidian 동기화가 「자동」이라 적혀 있으나 그 스크립트가 존재하지 않는다 | **미배정** | 도구 |
| 21 | ⬜ | apps/web — 한글 문자열이 i18n 밖에 있는 **잔여 3종** | **미배정** | apps/web |
| 22 | ✅ | apps/web — 줄수 래칫이 **장부를 낮추는 쪽은 강제하지 않는다** | **#385** | apps/web |
| 23 | ⬜ | 도구 — 대시보드 렌더러가 인용블록 안의 표를 평문으로 뭉갠다 | **미배정** | 도구 |
| 24 | ⬜ | apps/web — 같은 문구를 `placeholder` 와 `aria-label` 이 각자 만든다 | **미배정** | apps/web |
| **25** | ⬜ | 인프라 — worktree 에 `node_modules` 링크를 만드는 절차가 없다 | **미배정** | 인프라 |
| **26** | ⬜ | 인프라 — E2E(Playwright)가 CI 에 한 번도 배선된 적이 없다 | **미배정** | 인프라 |
| **27** | ✅ | 인프라 — `pnpm test:workflow` 가 **로컬에서만 15건 죽는다**(CI 는 초록) | **#383** | 인프라 |
| **28** | ⬜ | apps/web — `workflow-scheme-assignment` E2E-5 가 **전량 실행에서만** strict mode 위반으로 죽는다 | **미배정** | apps/web |
| **29** | ⬜ | apps/web — 이동 다이얼로그가 **진행 중인 preview 요청과 경합**한다 | **미배정** | apps/web |
| **30** | ✅ | 인프라 — self-hosted 러너가 **Docker 데몬 상태에 은밀히 의존**한다 | **#378** | 인프라 |
| **31** | ✅ | 인프라 — `backend-ci` 가 **바뀐 모듈을 고르지 않는다** (주석 1줄에 10모듈 37분) | **#379** | 인프라 |
| **32** | ⬜ | 인프라 — 살아 있는 정본이 `docs/plans/` 안에 있어 판별식 사각이 된다 | **미배정** | 인프라 |
| **33** | ✅ | 워크플로우 — `classify-task` 가 E2E 를 포함한 혼합 PR 을 `qa-engineer` 로 오배정한다 | **#387** | 워크플로우 |
| **34** | ✅ | 워크플로우 — `bts-review-plan` 분기 표에 `ui`@T2 조합이 정의돼 있지 않다 | **#388** | 워크플로우 |
| **35** | ⬜ | search-export-import — 매핑 마법사 `confirm` 경로에 CREATE 재검사가 없다 | **미배정** | search-export-import |
| **36** | ⬜ | 문서 — `DESIGN.md` 에 `aria-live` 정책이 없다 | **미배정** | 문서 |
| **37** | ✅ | apps/web — 로딩 프레임 계약 레지스트리의 **계약 값**을 아무 판정도 안 읽는다 | **#386** | apps/web |
| **38** | ⬜ | apps/web — 임포트 진행 중 신호에 언마운트 cleanup 이 없다 | **미배정** | apps/web |
| **39** | ⬜ | 워크플로우 — 리뷰 렌즈 2종이 **같은 worktree 를 동시에 뮤테이션**한다 | **미배정** | 워크플로우 |
| **40** | ✅ | apps/web — 로딩 프레임 계약 판별식이 **배럴 재export 에 샌다** | **#386** | apps/web |
| **41** | ✅ | apps/web — `CreateIssueDialog` 제출 완료 프레임 재단언이 없다 | **#386** | apps/web |
| **42** | ⬜ | 워크플로우 — 티어 표면이 `apps/web` 안에 사는 **강제 장치**를 T1 로 떨어뜨린다 | **미배정** | 워크플로우 |
| **43** | ✅ | apps/web — 제출 훅의 게이트 파라미터가 판별식 밖의 우회를 연다 | **#386** | apps/web |
| **44** | ✅ | 워크플로우 — `classify-task` 가 경로 뒤 슬래시가 없으면 `apps/web` 작업을 backend 로 오배정한다 | **#387** | 워크플로우 |
| **45** | ✅ | 워크플로우 — `classify-task` 의 ASCII 키워드가 다른 영단어 안의 부분문자열에 매치한다 | **#387** | 워크플로우 |
| **46** | ⬜ | 워크플로우 — `bts-impl` 이 dispatch 하는 `spec-compliance-verifier` 에 정본 파일이 없다 | **미배정** | 워크플로우 |
| **47** | ⬜ | 도구 — 장부 판별식이 마스터 §전수 매핑의 **✅ 행 삭제**를 못 본다 | **미배정** | 도구 |
| **48** | ⬜ | 문서 — `DEVELOPMENT.md` 가 「CI 판별식이 커밋 순서를 대조한다」고 적으나 그런 잡이 없다 | **미배정** | 문서 |
| **49** | ⬜ | 워크플로우 — `AUTH_KEYWORDS` 의 `pat`·`토큰` 줄을 통째로 지워도 판별식이 전부 초록이다 | **미배정** | 워크플로우 |
| **50** | ⬜ | 도구 — 「메모리는 저장소 밖이라 CI 가 볼 수 없다」고 적힌 두 곳이 거짓이다 | **미배정** | 도구 |
| **51** | ⬜ | 도구 — 마스터 계획 §PR 별 집계 표의 건수·항목 번호 열을 아무 판별식도 안 읽는다 | **미배정** | 도구 |
| **52** | ⬜ | 워크플로우 — `classify-task` 가 타입 이름을 언급한 제목을 그 타입의 작업으로 읽는다 | **미배정** | 워크플로우 |
| **53** | ⬜ | 문서 — `DEVELOPMENT.md §4` 의 PR 제목 규칙이 실제 관행과 두 축으로 어긋난다 | **미배정** | 문서 |
| **54** | ⬜ | 도구 — jOOQ 코드젠 미러가 마이그레이션과 갈라져도 4개 모듈은 아무도 안 본다 | **미배정** | 도구 |
| **55** | ⬜ | 도구 — 픽스처 헬퍼가 BC 마다 복제된다 | **미배정** | 도구 |
| **56** | ⬜ | 워크플로우 — 구형 `workflow_states` 가 신형 카탈로그와 나란히 살아 있다 | **미배정** | 워크플로우 |
| **57** | ⬜ | 인프라 — `runner-health` 의 Docker 점검이 run 시작 시점 1회뿐이다 | **미배정** | 인프라 |
| **58** | ⬜ | 인프라 — 러너 맥이 메모리 압박 끝에 커널 패닉으로 재부팅된다 | **미배정** | 인프라 |
| **59** | ⬜ | 인프라 — 리눅스 러너에서 자원 고갈 판정이 통째로 건너뛴다 | **미배정** | 인프라 |
| **60** | ⬜ | 워크플로우 — 복제 전환 복사가 **0행으로만** 실행돼 새 쿼리가 미검증이다 | **미배정** | 워크플로우 |
| **61** | ⬜ | 워크플로우 — 최초 전환 수정 경로가 미도달이라 「영구 409」가 잡히지 않는다 | **미배정** | 워크플로우 |
| **62** | ⬜ | 워크플로우 — 편집 잠금 409 가 세 곳 중 한 곳만 판정된다 | **미배정** | 워크플로우 |
| **63** | ⬜ | 워크플로우 — V207 백필 가드가 한 번도 발화하지 않는다 | **미배정** | 워크플로우 |
| **64** | ⬜ | 문서 — V207 롤백 주석이 되돌릴 수 없는 마이그레이션을 「데이터 온전」이라 적는다 | **미배정** | 문서 |
| **65** | ⬜ | 문서 — 파일 300줄 상한 초과가 실측 7건인데 계획 문서는 3건이라 적는다 | **미배정** | 문서 |
| **66** | ⬜ | 인프라 — 병렬 작업이 Gradle 결과 디렉터리를 공유해 서로의 검사 결과를 지운다 | **미배정** | 인프라 |
| **67** | ⬜ | 워크플로우 — worktree 용 `typecheck` 대체 명령이 없어 **아무것도 검사하지 않는 명령**이 지어졌다 | **미배정** | 워크플로우 |
| **68** | ⬜ | 문서 — 전환 하드 삭제가 `DATA.md §3` 예외 목록에 없다 | **미배정** | 문서 |
| **69** | ⬜ | 워크플로우 — 신규 설치와 기존 DB 의 전환 표시 순서가 갈린다 | **미배정** | 워크플로우 |
| **70** | ⬜ | 워크플로우 — 전환 CRUD 가 워크플로우의 수정 시각·버전을 안 올려 낙관적 잠금이 없다 | **미배정** | 워크플로우 |
| **71** | ⬜ | 워크플로우 — `key` 조회 2곳이 소프트 삭제를 안 걸러 재현 가능한 500 을 낸다 | **미배정** | 워크플로우 |
| **72** | ⬜ | 워크플로우 — 전환 계획 미리보기에 `transitionId` 필드가 없어 409 왕복이 구조적으로 막힌다 | **미배정** | 워크플로우 |
| **73** | ⬜ | 워크플로우 — `TransitionRequestDto.toDomain()` 이 죽고 같은 매핑이 두 벌이 됐다 | **미배정** | 워크플로우 |
| **74** | ⬜ | issue-tracking — 여러 이슈 한꺼번에 전환할 때 응답의 전환 ID 가 첫 이슈 대표값이다 | **미배정** | issue-tracking |
| **75** | ⬜ | apps/web — 목록 인라인 상태 편집이 React `key` 중복과 409 미처리를 함께 갖는다 | **미배정** | apps/web |
| **76** | ⬜ | apps/web — 전환 `key` 규칙 사본이 프론트에만 3벌이고 서로를 검사한다 | **미배정** | apps/web |
| **77** | ⬜ | issue-tracking — 같은 엔드포인트의 409 응답이 두 형식이다 | **미배정** | issue-tracking |
| **78** | ⬜ | 워크플로우 — 전환 이름이 무검증이라 빈 문자열이 저장되고 후보 다이얼로그를 막는다 | **미배정** | 워크플로우 |
| **79** | ⬜ | 도구 — `WorkflowController` KDoc 의 엔드포인트 목록을 실제 매핑과 대조하는 판별식이 없다 | **미배정** | 도구 |
| **80** | ⬜ | 도구 — `DATA.md §5`(「jOOQ 생성물은 커밋」)와 `.gitignore` 가 서로를 검사하지 않는다 | **미배정** | 도구 |
| **81** | ✅ | 워크플로우 — `infra/deploy/bts-deploy.sh` 가 티어 표면 카탈로그에 없다 | **#397** | 워크플로우 |
| **82** | ⬜ | 워크플로우 — `PNPM_WRAPPER` 가 두 벌이고 잡는 것이 서로 다르다 | **미배정** | 워크플로우 |
| **83** | ⬜ | 워크플로우 — `blockDepthAt` 이 자기 완결형 한 줄 조건 뒤에서 거짓 red 를 낸다 | **미배정** | 워크플로우 |
| **84** | ⬜ | 워크플로우 — git spawn 스윕이 못 보는 호출 형태가 있다 | **미배정** | 워크플로우 |
| **85** | ⬜ | 워크플로우 — `.husky/pre-commit`·`post-merge` 에는 `GIT_*` 스크럽이 없다 | **미배정** | 워크플로우 |
| **86** | ⬜ | 워크플로우 — `gitFixtureEnv` 라는 이름이 의무 범위를 가린다 | **미배정** | 워크플로우 |
| **87** | ⬜ | 워크플로우 — `stripComments` 의 `/` 판별이 휴리스틱이다 | **미배정** | 워크플로우 |
| **88** | ⬜ | 워크플로우 — `env` 명시 판정이 `process.env` 우회 형태를 정적으로 못 가른다 | **미배정** | 워크플로우 |
| **89** | ⬜ | 워크플로우 — 자식 백스톱이 **읽기 전용** git 호출의 오염을 못 본다 | **미배정** | 워크플로우 |
| **90** | ⬜ | 워크플로우 — 임포트 도달성이 **실행** 도달성이 아니다 | **미배정** | 워크플로우 |
| **91** | ⬜ | 워크플로우 — `V207` 의 CASCADE 근거 주석이 도달 가능성 변화를 감춘다 | **미배정** | 워크플로우 |
| **92** | ⬜ | apps/web — post-action 테스트의 단언 2건이 사라진 문자열을 검사한다 | **미배정** | apps/web |
| **93** | ⬜ | 워크플로우 — 시작 상태 규칙이 프로덕션과 테스트에 두 벌이고 서로를 안 검사한다 | **미배정** | 워크플로우 |
| **94** | ⬜ | 워크플로우 — INITIAL 전환에 붙인 규칙은 저장되지만 실행 경로가 없다 | **미배정** | 워크플로우 |
| **95** | ✅ | 인프라 — 배포 게이트의 의존성 확인이 **shim 존재**만 보고 모듈 실체를 안 본다 | **#397** | 인프라 |
| **96** | ⬜ | 인프라 — 배포 전량 검증이 첫 실패에서 멈춰 뒤 관문의 고장을 가린다 | **미배정** | 인프라 |
| **97** | ⬜ | 워크플로우 — worktree 배선 판별식이 스킬 본문만 읽고 실제 worktree 를 안 본다 | **미배정** | 워크플로우 |
| **98** | ⬜ | 워크플로우 — 순수 이동 판정의 항목 축 조건 3건을 아무 판정도 안 잡는다 | **미배정** | 워크플로우 |
| **99** | ⬜ | 인프라 — 프로덕션 이미지·리버스 프록시 설정이 티어 표면 카탈로그에 없다 | **미배정** | 인프라 |
| **100** | ⬜ | 문서 — PR 제목 규칙이 실제 관행과 어긋나고 그것을 보는 판정이 없다 | **미배정** | 문서 |
| **101** | ⬜ | 문서 — 워크플로우 편집기 로드맵의 PR 표가 FR 의 D 마커 전량을 덮지 않는다 | **미배정** | 문서 |
| **102** | ⬜ | 도구 — 줄수 래칫이 한 줄 욱여넣기를 수치로 보상한다 | **미배정** | 도구 |
| **103** | ⬜ | apps/web — 전환 목록 스키마가 서버 계약보다 엄격해 응답 하나에 드롭다운이 통째로 사라진다 | **미배정** | apps/web |
| **104** | ⬜ | apps/web — 페이지 컨테이너 관례가 둘로 갈려 형제 탭끼리 236px 어긋난다 | **미배정** | apps/web |
| **105** | ⬜ | apps/web — `ProjectTree` 2단 들여쓰기가 깊이를 구분하지 못한다 | **미배정** | apps/web |
| **106** | ⬜ | apps/web — 설정 화면 마감 5건 | **미배정** | apps/web |
| **107** | ⬜ | apps/web — 모바일 드로어에 완전한 포커스 트랩이 없다 | **미배정** | apps/web |
| **108** | ⬜ | identity-access — 응답 규약이 두 벌인데 한쪽이 `ResponseEntity<*>` 로 타입을 지운다 | **미배정** | identity-access |
| **109** | ⬜ | 인프라 — `/ws` 가 공개 엔드포인트인데 동시 연결 상한이 어느 층에도 없다 | **미배정** | 인프라 |
| **110** | ⬜ | 워크플로우 — 리뷰 렌즈 분기가 `type` 만 보고 **변경 대상**을 안 본다 | **미배정** | 워크플로우 |
| **111** | ⬜ | identity-access — permitAll 경로군 중 `/ws` 만 메서드 고정을 기계가 지킨다 | **미배정** | identity-access |
| **112** | ⬜ | 워크플로우 — 개발 오리진 판별식이 다섯 곳 중 한 곳만 읽는다 | **미배정** | 워크플로우 |
| **113** | ⬜ | apps/web — 판별식의 주석 제거 헬퍼가 3벌 복제돼 있고 후행 주석을 못 지운다 | **미배정** | apps/web |
| **114** | ⬜ | apps/web — 소비처 판별식의 모듈 지정자 정규식이 확장자 표기를 놓친다 | **미배정** | apps/web |
| **115** | ⬜ | apps/web — 브레이크포인트 판별식의 스캔 범위가 앱 전역이라 무관한 파일을 막는다 | **미배정** | apps/web |
| **116** | ⬜ | apps/web — 봉인·토스트 판정이 대표 1건만 태워 나머지가 지워져도 초록이다 | **미배정** | apps/web |
| **117** | ⬜ | apps/web — 전환 종류를 고를 수 없어 전역 전환을 만들 방법이 없다 | **미배정** | apps/web |
| **118** | ⬜ | apps/web — 관리 메뉴 계약 가드가 링크 개수를 안 세어 새 링크가 영원히 안 걸린다 | **미배정** | apps/web |
| **119** | ⬜ | apps/web — 상태 순서 변경이 되돌아갔다 오고 표시 순서가 겹칠 수 있다 | **미배정** | apps/web |
| **120** | ⬜ | 도구 — `staticData.requireAuth` 가 소비처도 판별식도 없는 장식이다 | **미배정** | 도구 |
| **121** | ⬜ | 도구 — CHANGELOG 의 BC 진척 표를 어떤 판별식도 읽지 않는다 | **미배정** | 도구 |
| **122** | ⬜ | 도구 — `/admin/workflows/new` 매칭을 고정하는 단언이 없고 코드 주석의 근거가 틀렸다 | **미배정** | 도구 |
| **123** | ⬜ | apps/web — nav 라벨 정본이 선언만 있고 리터럴이 5벌 따로 박혀 있다 | **미배정** | apps/web |
| **124** | ⬜ | apps/web — 어떤 서버 문구를 화면에 실을지가 판정으로 잠겨 있지 않다 | **미배정** | apps/web |
| **125** | ⬜ | 도구 — 문서 인덱스 승격이 전부-아니면-전무라 문서 하나가 링크 9건을 지운다 | **미배정** | 도구 |
| **126** | ⬜ | 도구 — 권한 코드 판별식이 백엔드의 일부 형태·일부 핸들러만 읽는다 | **미배정** | 도구 |
| **127** | ⬜ | 워크플로우 — 요청 DTO 의 검증 애노테이션이 `@Valid` 부재로 장식이다 | **미배정** | 워크플로우 |
| **128** | ✅ | identity-access — 테스트 소스에 ktlint 위반 128건이 잠복해 있다 | **#402** | identity-access |
| **129** | ⬜ | identity-access — 클래스마다 `@DynamicPropertySource` 를 가져 context 를 하나도 공유하지 못한다 | **미배정** | identity-access |
| **130** | ⬜ | project-workflow — 프레임워크 예외 3종이 도메인 에러 봉투 밖으로 샌다 | **미배정** | project-workflow |
| **131** | ⬜ | project-workflow — `ValidatorResponse` 에 편집 가능 여부가 없어 D6 가 목록을 손으로 든다 | **미배정** | project-workflow |
| **132** | ⬜ | project-workflow — 편집 불가 조건이 코드는 허용 목록, 계약 문서는 `CustomExpression` 한 값 | **미배정** | project-workflow |
| **133** | ⬜ | project-workflow — `config` JSONB 가 크기·키 제한 없이 저장된다 | **미배정** | project-workflow |
| **134** | ⬜ | project-workflow — 전환 규칙 변경 로그에 행위자가 없다 | **미배정** | project-workflow |
| **135** | ⬜ | project-workflow — 전환 규칙 표면에 남은 사본 5종 | **미배정** | project-workflow |

**★건수는 여기 적지 않는다.** `scripts/workflow/debt-ledger-mapping.test.ts` 가 `TODOS.md` 의
`## ⬜` 집합과 이 표의 ⬜ 집합이 **양방향으로 같음**을 강제한다 — 집합 일치는 건수 일치를 포함한다.
수를 적으면 그것이 **세 번째 목록**이 되어 또 갈린다. 실제로 갈렸다 — 2026-08-12 에 적은
`⬜ 22 · ✅ 9` 가 #378 머지(항목 `30` 해소) 이후 `⬜ 21 · ✅ 10` 이 됐는데 **2026-08-14 까지
아무도 몰랐다.** 두 판정 다 초록이었기 때문이다. 지금 세고 싶으면 위 판별식을 돌린다.

**★번호 `60`~`80` 은 비어 있다 — 구멍이지 누락이 아니다.** 이 브랜치(#396)가 등재하는 항목은 `81` 에서
시작한다. 그 사이 번호는 **아직 머지되지 않은 PR #395 가 이미 쓰고 있고**, #396 이 먼저 머지되므로
60번대를 재사용하면 #395 가 리베이스할 때 그 구간 전체를 밀어야 한다. **번호는 이 장부의 조인 키**다 —
§순서 제약 · §확정된 결정 · §PR 별 집계가 전부 이 번호로 항목을 가리키므로, 번호가 밀리면 인용한 모든
자리가 조용히 어긋난다. 위 §★키를 줄번호에서 제목으로 바꿨다 가 적은 것과 **같은 양식**이다 — 휘발성
키는 조용히 깨지고, 깨진 줄 아무도 모른다.

⇒ **규칙. 번호를 당겨 메우지 않는다.** 항목이 해소돼도, 다른 브랜치가 선점한 구간이 끝내 안 들어와도
번호는 그 자리에 그대로 둔다. ★이 규칙은 여기서 처음 글로 적는다 — 그전까지는 §순서 제약의
「제약을 PR 번호가 아니라 항목 번호로 적는다」에 암묵으로만 들어 있었다. 없는 규칙을 있었다고 적지 않는다.

**빈 번호는 결함이 아니다.** `debt-ledger-mapping.test.ts` 는 번호 열을 읽지 않는다 — 대조하는 것은
`TODOS.md` 의 `## ⬜` 제목 집합과 이 표의 ⬜ 제목 집합이다. 구멍이 있어도 초록이고, 구멍을 메우려고
제목을 옮기면 그때 red 가 난다.

**★2파(#367)에서 4건을 닫고 3건을 새로 등재했다** — 매핑 `27`(`test:workflow` 로컬↔CI 불일치) ·
매핑 `28`(E2E 전량 실행에서만 나는 strict mode 위반) · 매핑 `29`(이동 다이얼로그 preview 경합 2종).
셋 다 **main 대조군 또는 origin/main 코드로 선재를 확증**했다.
최종 검증 중 발견했고 **main 트리 대조군에서도 동일하게 재현**돼 선재로 판정했다.
닫은 4건만 세고 신규를 안 적으면 그게 바로 이 장부가 막으려는 것이다.

## PR 별 집계

**항목은 위 표의 `#` 번호로 가리킨다** — 제목을 두 번 적으면 그 둘이 또 갈라진다.

| PR | 묶음 | ⬜ 건 | 항목 # |
|---|---|---|---|
| **#365 (0파)** | 결정 기록 + 규율 정정 | ✅ 완료 | 19 |
| **#366 (1파)** | 러너 자원 회수 | ✅ 완료 (항목 `4` 는 미배정 복귀) | 4 · **10** |
| **#367 (2파)** | 이동 다이얼로그 | ✅ 완료 | 11 · 12 · 17 · 18 |
| **미배정** | MovePreviewServiceTest | 1 | 13 |
| **미배정** | 사용자 선택 UI | 3 | 6 · 21 · 24 |
| **#384** | 권한 게이트 (E2E · 로딩 프레임 계약 · 진행 중 동결) | ✅ 완료 | 14 · 15 · 16 |
| **#385** | IssueCreateForm (줄수 분할 + 래칫 엄격 일치) | ✅ 완료 | 8 · 22 |
| **미배정** | OpenAPI required | 1 | 5 |
| **미배정** | 마스킹 required | 1 | 9 |
| **미배정** | Obsidian + 대시보드 | 2 | 20 · 23 |
| **#375 (3파)** | setupServer 이주 | ✅ 완료 | **2** · **3** · **7** |
| — | personalization (보류) | 1 | 1 |
| **미배정** | worktree·E2E 배선 (신규) | 2 | **25** · **26** |
| **#383** | `test:workflow` 로컬↔CI 불일치 (2파 발견) | ✅ 완료 | **27** |
| **미배정** | E2E 전량 실행 strict mode 위반 (2파 발견) | 1 | **28** |
| **미배정** | 이동 다이얼로그 preview 경합 (2파 리뷰 발견) | 1 | **29** |
| **미배정** | 러너 Docker 의존 · backend-ci 모듈 미선별 (2파 머지 후 발견) | 2 | **30** · **31** |
| **미배정** | 살아 있는 정본이 판별식 사각 (#383 리뷰 발견) | 1 | **32** |
| **미배정** | #384 착수·게이트 2 리뷰가 발견 (하네스 · 백엔드 · 프론트 · 문서) | 4 | **35** · **36** · **38** · **39** |
| **#386** | 로딩 프레임 계약 판별식 4종 (계약 값 · 배럴 재export · 완료 프레임 · 제출 훅 파라미터) | ✅ 완료 | **37** · **40** · **41** · **43** |
| **미배정** | 티어 표면이 `apps/web` 안 강제 장치를 T1 로 떨어뜨린다 (#385 발견 · 선재 고아) | 1 | **42** |
| **#387** | classify-task 오배정 3종 (경로 경계 · qa 판정 순서 · ASCII 부분문자열) | ✅ 완료 | **33** · **44** · **45** |
| **미배정** | #387 착수·검증이 발견 (verifier 정본 부재 · 장부 ✅ 행 사각 · 헌법 거짓 주장 · 키워드 커버리지) | 4 | **46** · **47** · **48** · **49** |
| **#388** | bts-review-plan 분기 표 렌즈 무조건화 (티어 열 제거 + 판별식 3종) | ✅ 완료 | **34** |
| **미배정** | #388 착수·리뷰가 발견 (CI 의 메모리 가시성 오단정 · 집계 표 미검사 · 타입 이름 언급 오배정 · PR 제목 규칙 이탈) | 4 | **50** · **51** · **52** · **53** |
| **미배정** | #393 머지 후 발견 (러너 프리플라이트 시점 갭 · 메모리 압박발 커널 패닉) | 2 | **57** · **58** |
| **미배정** | 러너 증설(`ncp-linux-bts`) 착수가 발견 (리눅스에서 자원 판정 미동작) | 1 | **59** |
| **미배정** | #395 재리뷰 — 전환 CRUD 백엔드 (미도달 판정 · 낙관적 잠금 · 계약 공백) | 10 | **60** · **61** · **62** · **63** · **69** · **70** · **71** · **72** · **73** · **78** |
| **미배정** | #395 재리뷰 — 거짓·누락 서술 (V207 롤백 · 300줄 장부 · 하드 삭제 미등재) | 3 | **64** · **65** · **68** |
| **미배정** | #395 재리뷰 — 정본 대조 판별식 부재 (KDoc 엔드포인트 · jOOQ 커밋 정책) | 2 | **79** · **80** |
| **미배정** | #395 재리뷰 — 프론트 전환 표시 (React key 중복 · key 규칙 사본 3벌) | 2 | **75** · **76** |
| **미배정** | #395 재리뷰 — issue-tracking 전환 응답 (bulk 대표 id · 409 두 형식) | 2 | **74** · **77** |
| **미배정** | #395 재리뷰 — 병렬 실행 하네스 (Gradle 결과 공유 · worktree typecheck 공백) | 2 | **66** · **67** |
| **미배정** | #396 게이트 2 리뷰가 발견 (표면 카탈로그 · 훅 어휘 두 벌 · 깊이 계산 · 스윕 사각 · 훅 스크럽 범위 · 헬퍼 이름 · 주석 걷기) | 항목 # 열이 정본 | **81** · **82** · **83** · **84** · **85** · **86** · **87** · **88** · **89** · **90** |
| **미배정** | #395 라운드 3 리뷰가 발견 (V207 주석 체크섬 이연 · 죽은 단언 · 규칙 사본 · INITIAL 규칙 미실행) | 항목 # 열이 정본 | **91** · **92** · **93** · **94** |
| | **합** | **판별식이 강제** (§전수 매핑 각주) | |

**★이 행의 「⬜ 건」 열에 수를 적지 않았다.** 그 열을 읽는 판별식이 없고(항목 `51`), 수를 적으면
§전수 매핑 각주가 금지한 「세 번째 목록」이 그 자리에 생긴다. 항목 번호 전수 열거가 그 자리를 대신한다.

**★★2026-08-14 — 예약 draft PR 방식을 폐기했다.** `#368`~`#374` 7건은 **변경 0파일 예약 PR**
이었고, 이날 `--delete-branch` 로 닫았다. **일을 버린 것이 아니다** — 7건이 담당하던 부채는
하나도 안 닫혔고 전부 `미배정` 으로 장부에 남았다. 폐기 이유는 둘이다.
① `.claude/skills/bts/SKILL.md` A-0 이 draft PR 이 하나라도 있으면 **모든 `/bts` 를 정지**시킨다.
② `bts-start` Step 5 는 착수 시 Draft PR 을 **새로 만든다** — 기존 예약분을 재사용하지 않아
착수하는 순간 같은 항목에 PR 이 둘이 된다.

**★그래도 「묶음」 열은 남긴다.** 이 열은 PR 번호가 아니라 **충돌면**이다 — 예컨대
`사용자 선택 UI`(항목 `6`·`21`·`24`)는 `ComponentLeadSelect.tsx` 가 `6`∩`21`,
`LabelAutocompleteInput.tsx` 가 `21`∩`24` 로 겹쳐 **종류별로 쪼개면 두 PR 이 같은 파일을
동시에 건드린다**. 담당 PR 이 사라져도 이 제약은 그대로 유효하므로, 재착수 시 이 묶음을 그대로 쓴다.

**★#366 은 2건 중 1건만 닫았다.** `10`(낡은 main run)은 ✅ 지만 `4`(CI 벽시계)는 ⬜ 로 남았다 —
축 ②(Gradle 설정)의 처방이 스텝 실측으로 반증돼 제외됐고, 신규 후속 `maxParallelForks` 가
등재됐다. 자세히는 `2026-08-12-debt24-runner-reclaim.md` §Task 5·6·7.

**보류 1건의 근거.** `1`(personalization)은 FR-UX-08 PR-B 의 **의도된 대가**로 등재된 항목이라
부채 상환이 아니라 제품 결정이다. 여는 시점은 접힘 레일 UX 를 다시 다룰 때다.

## 순서 제약

앞 항목이 뒤 항목의 **측정 기준선 또는 계약 배치**를 흔드는 곳만 고정한다. 나머지는 순서 무관이다.
(2026-08-14 에 「계약 배치」를 넓혔다 — 근거는 아래 첫 행의 두 번째 이유다.)

**★제약을 PR 번호가 아니라 항목 번호로 적는다.** 2026-08-14 예약 PR 폐기로 `#368`~`#374` 가
사라졌는데, 제약이 그 번호로 적혀 있으면 **제약도 함께 증발한다.** 항목 번호는 장부의 키이고
판별식이 지키므로 담당자가 바뀌어도 살아남는다.

| 제약 | 상태 | 이유 |
|---|---|---|
| 항목 `2` 는 항목 `4`·`10` 다음 | **해제** (2026-08-12 #366·#375 머지) | 러너 자원 회수 전에는 `pnpm test` 간헐 실패(항목 `2`)가 **자원 경쟁인지 코드인지** 구분되지 않는다. `[[ci-slowdown-is-runner-memory-not-code]]` |
| 항목 `8`·`22` 는 항목 `14`·`15`·`16` 다음 | **해제** (2026-08-15 #385 머지 대기 · 두 항목 상환 완료) | **이유 둘.** ① 줄수 래칫 베이스라인(항목 `22`)은 게이트 변경이 끝난 뒤 재측정해야 값이 안 흔들린다. ② **계약 배치** — 항목 `15` 가 로딩 프레임 계약을 `IssueCreateForm.tsx` 에 넣는데, 항목 `8`(컴포넌트 분할)을 먼저 하면 **계약을 어느 조각에 넣을지가 다시 열린다.** 계약 고정 → 줄수 정리 순서다. ⚠️ 베이스라인은 `IssueCreateForm`(227) 과 `IssueMetaPanel`(317) **둘 다** 동결돼 있어 항목 `15` 의 계약 추가가 두 줄을 함께 흔든다. ★2026-08-15(#385) 후행 사실 — 앞의 `IssueCreateForm`(227) 은 **상환돼 베이스라인에서 사라졌다**(227 → 127). `IssueMetaPanel`(317)만 남아 있다. 위 문장을 현재 시제로 읽으면 없는 줄을 찾게 된다 |
| 항목 `9` 는 항목 `5` 와 **분리** | 유효 | 둘 다 required 표기를 다루지만 항목 `5` 는 **OpenAPI 문서**, `9` 는 **런타임 검증 정책**이다. 한 PR 로 묶으면 스키마 변경과 정책 변경이 섞여 되돌리기가 불가능해진다 |
| 항목 `9` 는 항목 `5` **다음**? | **미확정** | 폐기된 예약 PR #373 본문이 「선행 필수」라 주장했다 — 「`5` 가 『이건 required 가 아니다』로 스펙을 정정하고 `9` 가 『required 지만 검증은 건너뛴다』는 정책을 올린다. 순서가 뒤집히면 리뷰어가 **결국 required 가 뭔지** 판정할 수 없다」. 위 서문 기준(측정 기준선·계약 배치)에는 안 걸리므로 **고정하지 않았다.** 착수 시 Maxi 가 판정한다 |
| 항목 `34` 는 항목 `33` **다음** | **해제** (2026-08-17 · #388 이 항목 `34` 상환) | 항목 `33` 이 「E2E 가 섞인 혼합 작업」을 `qa`(분기 표 **미진입** 행)에서 `ui` 쪽으로 옮긴다. 그런데 항목 `34` 가 등재한 그대로 `ui`@T2 조합이 분기 표에 **정의돼 있지 않다** — 즉 `33` 을 먼저 닫으면 `34` 의 노출이 커진다. 순서를 뒤집으면 「고쳤더니 정의 안 된 곳으로 더 많이 간다」가 된다. ★#387 착수 중 **`chore`@T2 도 같은 미정의 조합**임을 실제로 밟았고(이 PR 자신이 그 조합이다) 항목 `34` 서술에 흡수했다 |

## 확정된 결정 6건 (2026-08-12)

정본은 `TODOS.md` 의 각 항목 안 `★Maxi 확정 (2026-08-12)` 블록이다. 아래는 색인이다.

**★여기도 줄번호를 쓰지 않는다** — 위 전수 매핑의 `#` 번호로 가리킨다(같은 휘발성 문제).

| # | 항목 | 매핑 # | 확정 | 담당 PR |
|---|---|---|---|---|
| 1 | 이동 키 소문자 처리 | `11` | **③** 기존 `PROJECT_KEY_PATTERN` 재사용 · 요청 전 차단 | ✅ #367 |
| 2 | 마스킹 required 필드 | `9` | **②** 검증 제외 | **미배정** |
| 3 | CI 러너 | `4` | **1대 유지** + 자원 회수 | #366 (자원 회수분만) · 나머지 **미배정** |
| 4 | Obsidian 동기화 | `20` | `sync-obsidian.ts` **실제 제작** | **미배정** |
| 5 | 파일 300줄 상한 | `19` | **TypeScript 무효** · SDD 행 분리 | **#365** |
| 6 | 이동 403 문구 | `18` | **③ 적용 후 PR-A 안에서 재판정** → **압축 복원 55자** | ✅ #367 |

### 결정 1·6 의 실행 결과 (2026-08-12 · PR #367)

**결정 1 은 처방은 유지, 재사용 방법이 바뀌었다.** 「기존 `PROJECT_KEY_PATTERN` 재사용」의
그 상수가 `routes/projects.new.tsx` **파일 지역 상수**였다. 라우트 파일을 import 하는 것은 잘못된
결합이고 복사하면 두 벌이 된다 — `apps/web/src/lib/project-key.ts` 로 **추출**해 두 화면이 같은
`isValidProjectKey` 를 호출한다. 정규식은 한 글자도 안 바꿨다.

**★결정 6 의 재판정 전제가 실측으로 거짓이 됐다.** 「③ 적용 후 403 원인이 3 → 2 로 준다」는
**틀렸다** — 형식 게이트는 **형식만** 보므로 `NOPE`(형식 OK · 미존재)는 그대로 403 이 된다.
원인은 셋 그대로이고 「키를 확인해 주세요」 앞부분을 뺄 근거는 없다.

**★그런데 반대쪽 전제(「70자를 넘기면 두세 줄」)도 절반이 거짓이었다.** 실브라우저 실측 결과
데스크톱(박스 464px)에서는 74자도 2줄이고, 3줄이 되는 것은 390px 이하뿐이다. 그래서 **55자로
압축해 다음 행동 안내를 되살렸다** — 모든 폭 2줄 유지. 데스크톱 한 폭만 쟀으면 반대로
결론 냈을 것이다. 상세는 `TODOS.md` 매핑 `18` 해소 블록.

### 결정 1 의 근거가 0파에서 뒤집혔다

매핑 `11`(이동 키 대소문자) 항목의 원 서술은 처방이 「프로젝트 키 생성 규칙
(`projects.new.tsx` 의 **대문자 강제**)에서 파생해야 한다」였다. 2026-08-12 실측은 다음과 같다.

| 대상 | 실측 |
|---|---|
| `apps/web/src/routes/projects.new.tsx:53` | `const PROJECT_KEY_PATTERN = /^[A-Z][A-Z0-9]{1,9}$/` — `:65` 에서 `refine` 으로 **거절만** 한다 |
| 같은 파일 안 `toUpperCase()` | **0건** (`apps/web/src` 전체로는 12건 — 범위를 안 적으면 오독된다) |
| `PROJECTS.KEY.eq(projectKey)` | 코드 **7곳** + KDoc 주석 1건(`UserCalendarLookupAdapter.kt:28`). grep 히트 8 을 그대로 쓰면 틀린다 |
| `ProjectDirectory.kt` | **identity-access BC** · jOOQ DSL 이 아니라 **raw SQL** `SQL_RESOLVE_KEY` = `WHERE key = :key` |

**⇒ 처방 ②(백엔드 대소문자 무시)는 cross-BC 다.** 그리고 `PROJECTS.KEY.eq(` 한 줄 grep 은
identity-access 의 raw SQL 을 **원리적으로 못 본다** — 일부만 고치면 「어떤 조회는 대소문자 무시,
어떤 건 아님」이라는 새 불일치가 남는다. 생성 화면이 **변환하지 않고 거절만 하므로**,
이동 화면도 같은 패턴을 재사용해 **요청 전에 차단**하면 두 화면의 동작이 통일된다. ⇒ **③ 확정**.

### 결정 5 — 행을 쪼갠다 (통째 태그 금지)

`docs/sdd/22-claude-code-env.md:209` 는 `함수 30줄 이내, 파일 300줄 이내` 로
**규칙 2개가 한 행**이다. 앞에 「Kotlin:」을 붙이면 `DEVELOPMENT.md:72` 가 TypeScript 에도
규정하는 **「함수 30줄」까지 Kotlin 전용**이 돼 반대 방향 drift 가 생긴다.
그래서 3행으로 분리하고, SDD 에 아예 없던 **TS 컴포넌트 200줄**도 함께 싣는다.

재발은 `scripts/verify-master-plan.sh` **룰 I** 가 막는다 — 두 정본의 줄수 규칙이 갈리면 EXIT 4.

## 0파(PR #365)가 닫는 것 / 안 닫는 것

| | 내용 |
|---|---|
| **닫힘** | `2967` 문서 충돌 · 결정 6건 등재 · 실측 전복 4건 정정 · 룰 I 신설 |
| **안 닫힘** | 200줄 초과 **18건의 상환**(정본은 매핑 `22` 항목과 `lint-ratchet-baseline.ts`) |
| **PR diff 밖** | `Maxi_wiki/BTS/_index.md:59` 주체 정정 1건 — 저장소 밖이라 CI 도 diff 도 못 본다 |

## 판별식 뮤테이션 실측 (2026-08-12 · PR #375)

`scripts/workflow/debt-ledger-mapping.test.ts` 가 실제로 배선됐는지 5가지로 흔들었다.
기준선 pass 5 / fail 0.

| 뮤테이션 | 결과 | 잡은 케이스 |
|---|---|---|
| M1. 마스터 계획에서 한 행 삭제 (담당 PR 없는 부채) | **fail 1** | 장부→마스터 차집합 |
| M2. 항목 제목을 한 글자 변경 (키 drift) | **fail 2** | 양방향 차집합 둘 다 |
| M3. 장부의 한 항목을 ✅ 로 변경 (마스터가 못 따라감) | **fail 1** | 마스터→장부 차집합 |
| M4. 표의 상태 열을 통째 제거 (파서 무력화) | **fail 2** | **비-공허 짝** + 장부→마스터 |
| M5. 마스터 계획에 중복 행 추가 | **fail 1** | 중복 검사 |

**★M4 가 비-공허 짝의 값을 보여 준다.** 파서가 0건을 훑으면 차집합이 「빈 집합 == 빈 집합」으로
**공허 통과**한다. 실제로 판별식 최초 실행(RED)에서 마스터 계획이 옛 형식이었을 때
「마스터→장부 차집합 0」이 **통과**했고, 비-공허 짝이 그것을 잡았다.

**측정 자체가 두 번 틀렸다 (기록).** M4·M5 의 1차 시도는 `perl -CSD` 로 돌려 **뮤테이션이
적용조차 안 됐는데** 그대로 넘어갈 뻔했다. 원인은 `-CSD` 가 파일을 UTF-8 문자로 디코드하는데
스크립트 소스의 `⬜` 리터럴은 `use utf8` 없이 **바이트**로 남아 서로 안 맞은 것이다.
`[⬜✅]` 문자 클래스도 바이트 모드에서 쪼개져 깨진다 — `(?:⬜|✅)` 를 쓸 것.
**그래서 모든 뮤테이션 뒤에 `git diff --quiet` 로 「적용됐는가」를 먼저 확인한다.**
확인이 없으면 「생존」과 「미적용」이 구분되지 않는다.
