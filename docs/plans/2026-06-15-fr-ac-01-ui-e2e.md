# FR-AC-01 D6/D7 — 첨부 프론트 UI + E2E

> slug: fr-ac-01-ui-e2e
> type: ui
> agent: frontend-engineer
> 생성: 2026-06-15

## Brief

FR-AC-01 첨부 기능의 D6(프론트 UI) + D7(E2E)를 구현한다.
백엔드 D1~D5는 PR #145로 완료(MinIO 서버 경유 멀티파트 스트리밍).

**deviation 반영(#145, ADR 2026-06-15-fr-ac-01-attachment-storage)**:
- plan 표기 "react-dropzone + 직접 PUT"은 stale. 실제는 **서버 경유 multipart 업로드**.
- scope: 업로드 + 다운로드 + 목록 + 삭제 4종.
- 삭제 = 하드삭제. 권한 = IssuePermission.UPDATE(업로드/삭제) / VIEW(다운로드/목록).

### 백엔드 API 계약 (실측, IssueAttachmentController.kt)
- POST   /api/v1/issues/{key}/attachments  (multipart, part명 `file`) → 201 { data: AttachmentResponse } + Location
- GET    /api/v1/issues/{key}/attachments → 200 { data: AttachmentResponse[] }
- GET    /api/v1/issues/{key}/attachments/{attachmentId} → 200 바이트스트림 (Content-Disposition: attachment)
- DELETE /api/v1/issues/{key}/attachments/{attachmentId} → 204
- 에러: 401(미인증) / 403(권한) / 404(이슈·첨부 없음) / 413(크기 초과)
- AttachmentResponse = { id: UUID, filename, contentType, sizeBytes: Long, uploadedBy: UUID, createdAt: Instant }

## 도메인 정리

- BC: issue-tracking
- 영향 엔티티: Attachment (백엔드 #145 확립, 신규 아님). glossary "어테처(Attachment)" 등재됨, domain/issue-tracking.md 핵심 엔티티 목록 포함.
- 새 용어: 없음 (프론트 UI는 기존 도메인 계약 소비만)
- 기존 결정 충돌: 없음
- 관련 ADR: docs/adr/2026-06-15-fr-ac-01-attachment-storage.md (백엔드 #145 생성, 프론트 영향 결정 확인)
- grill-with-docs: 도메인 변경 없는 프론트 UI 작업이라 대화형 검증 생략. 직접 정합성 확인(용어/ADR)으로 대체.

### 프론트에 영향을 주는 ADR 결정
1. 업로드 = **서버 경유 멀티파트**(`POST` multipart `file` part). presigned 직접 PUT 아님 → react-dropzone는 파일 선택 UX만, 전송은 서버로.
2. 범위 = 업로드/다운로드/목록/삭제 4종.
3. 권한 = UPDATE(업로드/삭제) / VIEW(목록/다운로드). 프론트 권한 게이팅은 서버 403 처리 + (가능 시) 권한 요약 API.
4. 삭제 = **하드삭제**(복구 불가) → UI에서 삭제 확인 필요.
5. 다운로드 = `Content-Disposition: attachment`(브라우저 강제 다운로드, 인라인 렌더 X). 미리보기는 FR-AC-02로 분리.

## 스펙

전체 스펙. [docs/specs/2026-06-15-fr-ac-01-ui-e2e.md](../specs/2026-06-15-fr-ac-01-ui-e2e.md)

핵심 결정(Maxi 확정 2026-06-15).
- 파일 선택 = **네이티브 HTML5**(`<input type=file multiple>` + drag/drop). react-dropzone 등 신규 의존성 0.
- 업로드 진행 = **단순 로딩 상태**(fetch 기반, apiFetch 재사용).

핵심 시나리오.
- 이슈 상세 좌측 본문 하단 "첨부" 섹션 — 업로드(드롭존)/목록/다운로드/삭제 4동작.
- 업로드는 `apiFetch`에 FormData 지원 확장(JSON 직렬화·Content-Type 강제를 FormData면 건너뜀, 401 refresh 인프라 재사용).
- 다운로드는 `lib/download.ts` triggerBlobDownload 재사용. 삭제는 하드삭제라 확인 필수.
- 권한 게이팅은 기존 `issue-permissions` UPDATE 플래그 재사용(신규 권한 API 노출 불필요).

## Brainstorming Check

✅ self-review 통과 (엣지 11건 점검). 완료된 기술 FR이라 대화형 brainstorming 대신 직접 점검 — office-hours/brainstorming mismatch 메모리 정신.

## Plan

### Task 1. apiFetch FormData 지원 확장

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/client.ts`, `apps/web/src/api/client.test.ts`]
- depends-on: []

**RED**: `client.test.ts` — `apiFetch(path, { method:'POST', body: formData })` 호출 시 (a) body가 `JSON.stringify`되지 않고 FormData 그대로 전달, (b) `Content-Type` 헤더가 자동 설정되지 않음(브라우저 boundary 위임)을 검증. 기존 JSON body 호출은 여전히 `application/json`.

**GREEN**: `client.ts`의 `buildHeaders`/`fetchOptions`에서 `body instanceof FormData` 분기 — FormData면 `JSON.stringify` 건너뛰고 Content-Type 자동설정 스킵. 401 refresh 인프라는 불변.

**REFACTOR**: FormData 분기 주석. `ApiFetchOptions.body`는 `unknown` 유지.

**검증**: `pnpm --filter web test -- client.test`

### Task 2. api/attachments.ts — CRUD 4함수 + Zod 스키마

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/attachments.ts`, `apps/web/src/api/attachments.test.ts`]
- depends-on: [1]

**RED**: `attachments.test.ts` — `fetchAttachments(key)`(목록, dataResponse 언랩), `uploadAttachment(key, file)`(FormData part `file` 구성 + 201 파싱), `downloadAttachment(key, id)`(blob 반환), `deleteAttachment(key, id)`(204) 4함수 + `attachmentResponseSchema` 파싱 테스트. ApiError 전파.

**GREEN**: 4함수 + `attachmentResponseSchema = z.object({ id, filename, contentType, sizeBytes, uploadedBy, createdAt })`. 업로드는 `FormData`에 `file` append 후 `apiFetch`(POST). 다운로드는 `downloadIssuePdf` 패턴(apiFetch GET → res.blob()). `MAX_ATTACHMENT_BYTES = 100*1024*1024` 상수 export.

**REFACTOR**: 타입 export(`AttachmentResponse = z.infer<...>`), KDoc.

**검증**: `pnpm --filter web test -- attachments.test`

### Task 3. useAttachments 훅 (react-query)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/useAttachments.ts`, `apps/web/src/api/useAttachments.test.ts`]
- depends-on: [2]

**RED**: `useAttachments.test.ts` — `useAttachmentList(key)`(query), `useUploadAttachment(key)`/`useDeleteAttachment(key)`(mutation, 성공 시 첨부 목록 쿼리 invalidate-only, 토스트). useCloneIssue 패턴.

**GREEN**: react-query 훅. queryKey `['attachments', key]`. mutation onSuccess → `invalidateQueries`. 에러 토스트(sonner).

**검증**: `pnpm --filter web test -- useAttachments.test`

### Task 4. AttachmentSection 컴포넌트 + i18n

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/issue/AttachmentSection.tsx`, `apps/web/src/components/issue/AttachmentSection.test.tsx`, `apps/web/src/i18n/attachment-labels.ts`]
- depends-on: [3]

**RED**: `AttachmentSection.test.tsx` — (a) 목록 렌더(파일명·크기 포맷·시각), (b) 빈 상태("첨부 없음"), (c) 드롭존 클릭/drop 업로드 트리거, (d) `canUpdate=false`면 드롭존·삭제버튼 미표시, (e) 삭제 시 확인 단계 후 호출, (f) 100MB 초과 파일 선택 시 토스트+업로드 안 함. i18n `ko.test`(콜론 종결 금지) 통과.

**GREEN**: `AttachmentSection({ issueKey, canUpdate })` — 네이티브 `<input type=file multiple>` + drag/drop 핸들러, 목록 map(다운로드/삭제 액션), 파일크기 포맷 헬퍼, **삭제 인라인 확인**(클릭 → 확인/취소 토글 + 하드삭제 경고 문구. 행별 반복이라 radix Dialog 대신 인라인 — FR-MF-02 모달부재 인라인 선례). `attachment-labels.ts`(component-labels 패턴) 문자열. 업로드/삭제는 Task 3 훅 사용.

**REFACTOR**: `formatFileSize` 헬퍼 분리, 드롭존 상태(dragover) 관리.

**검증**: `pnpm --filter web test -- AttachmentSection.test` + `pnpm --filter web test -- ko.test`

### Task 5. issues.$key.tsx 배선 + MSW 핸들러

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/issues.$key.tsx`, `apps/web/src/routes/issues.$key.test.tsx`, `apps/web/src/mocks/attachment-handlers.ts`, `apps/web/src/mocks/handlers.ts`]
- depends-on: [4]

**RED**: `issues.$key.test.tsx` — 상세 페이지 본문 하단에 `AttachmentSection` 렌더 + `canUpdate`(기존 `permissions.UPDATE`) 전달 검증. MSW `attachment-handlers`로 목록/업로드/삭제 stateful 동작.

**GREEN**: `IssueDescription` 아래 `AttachmentSection` 배선. `attachment-handlers.ts`(stateful in-memory store, component-handlers 패턴) + `handlers.ts` 등록.

**검증**: `pnpm --filter web test -- issues.\$key.test` + `pnpm --filter web typecheck` + `pnpm --filter web lint`

### Task 6. E2E — 첨부 라이프사이클 (qa-engineer)

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/issue-attachments.spec.ts`, `apps/web/src/mocks/attachment-handlers.ts`]
- depends-on: [5]

**RED→GREEN**: Playwright `issue-attachments.spec.ts` — S1 업로드(파일선택→목록 표시), S2 목록, S3 다운로드(waitForEvent download), S4 삭제(확인→목록 제거), S5 권한게이팅(UPDATE 없는 사용자 드롭존 미표시), S7 빈상태. MSW stateful(브라우저 시드). 기존 issue E2E 회귀 0.

**검증**: `pnpm --filter web test:e2e -- issue-attachments` + 기존 issue E2E 회귀 확인.

## Plan 메타

- task 수: 6 (각 TDD 사이클)
- 의존성 그래프: 1 → 2 → 3 → 4 → 5 → 6 (선형 직렬, 파일 의존 체인). wave ≈ 6.
- 예상 시간: 약 18~24분(직렬). 프론트 UI는 파일 의존이 선형이라 병렬 여지 적음.
- TDD 강제: yes
- 추가 검증: typecheck, eslint, vitest(단위), playwright(E2E, qa-engineer)
- 신규 의존성: 없음(네이티브 HTML5)

## 리뷰 결과

### plan-design-review (2026-06-15, 집중 직접 리뷰)

ui 타입이나 기존 이슈 상세 화면에 섹션 추가 + 기존 디자인 패턴 재사용이라 인터랙티브 design-shotgun/review 대신 design 관점 직접 점검(autoplan overkill 메모리 정신).

- ✅ 섹션 위치: 좌측 본문(IssueDescription) 하단 — 댓글/이력 섹션 패턴과 일관.
- ✅ i18n 콜론 종결 검증: Task 4가 ko.test 포함.
- ✅ 접근성/strict mode: Task 6 E2E가 컨테이너 한정 셀렉터(메모리 다수 선례) 적용 예정.
- ⚠️ C1(반영됨): 삭제 확인 UX — 행별 반복이라 radix Dialog 대신 **인라인 확인 + 하드삭제 경고**(FR-MF-02 선례). Task 4 GREEN에 명시 보강.
- ⚠️ C2: 드롭존 드래그오버 시각 피드백 + 빈 상태 메시지 — Task 4 REFACTOR(dragover 상태)/RED(빈 상태)에 이미 포함.
- ℹ️ C3(후속): 업로더 이름 표시는 uploadedBy(UUID)→이름 조회 비용. spec에서 우선순위 낮음. 본 작업은 파일명·크기·시각만. 필요 시 후속.
- **BLOCKER: 없음**

신규 의존성 0(네이티브 HTML5) → devex/의존성 리스크 없음.
