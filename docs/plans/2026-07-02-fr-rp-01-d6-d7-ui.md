# FR-RP-01 D6/D7 — 번다운/번업 차트 프론트 UI + E2E

> slug: fr-rp-01-d6-d7-ui
> type: ui
> agent: frontend-engineer
> primary_bc: agile-planning
> 생성: 2026-07-02

## Brief

FR-RP-01의 프론트엔드 마무리 (D6/D7). 백엔드 D1~D5는 **PR #219로 완료·머지**됨.

- **백엔드 API (완료)**. `GET /api/v1/sprints/{id}/burndown` — 스프린트 번다운/번업 시계열. 권한. 프로젝트 BROWSE + 이슈별 가시성 필터(뷰어 스코프). 에러. 401/403/404(SprintNotFound)/422(SPRINT_DATES_REQUIRED).
- **응답 형식 (BurndownResponse)**. `points: [{ date, remainingSeconds, idealSeconds, completedSeconds, scopeSeconds }]` — remainingSeconds/completedSeconds는 미래일 null 가능. 지표=잔여 추정시간(초). (정확한 필드는 D6 착수 시 백엔드 DTO grep으로 계약 확인 — frontend-zod-backend-dto-contract-gap)
- **D6**. recharts 라인 차트 컴포넌트 — 번다운(remaining) + ideal + scope, 번업(completed) 라인. 스프린트 화면/리포트 탭에 배치.
- **D7**. Playwright E2E — 차트 렌더 happy path.

**핵심 함정 (선행 인지)**.
- recharts는 jsdom에서 width 0 → 렌더 안 됨 → 단위테스트는 순수 데이터 변환 함수, 실제 렌더는 실브라우저 E2E (FR-TT-02 D6/D7 선례).
- 초(seconds) → 시간(h) 축 변환은 순수 함수로 분리·단위테스트.
- 백엔드 nullable(remaining/completed) → Zod `.nullish()` (@JsonInclude↔Zod 정합).
- MSW 계약 drift 주의(api ↔ MSW 핸들러 응답 형식 일치, 적대 리뷰서 표면화 선례).

## 도메인 정리

- **BC**. agile-planning (프론트는 `apps/web`에서 이 BC의 번다운 API 소비).
- **새 도메인 용어**. 없음 — 번다운/번업/ideal 라인/scope 라인/잔여 추정시간(초)은 백엔드 ADR에서 이미 정의. 프론트는 렌더링만.
- **영향 엔티티/read-model**. 신규 0. 기존 `BurndownResponse`/`BurndownPointResponse`(백엔드 #219) 소비.
- **기존 결정 충돌**. 없음.
- **관련 ADR**. [2026-07-02-fr-rp-01-burndown-burnup](../decisions/2026-07-02-fr-rp-01-burndown-burnup.md) (백엔드 #219에서 확정, 프론트는 그 계약 준수).
- **백엔드 응답 계약 (Zod 기준, DTO grep 확정)**.
  - `BurndownResponse`. `sprintId`(uuid), `projectKey`(string), `status`(SprintStatus enum), `startDate`/`endDate`(LocalDate→ISO "YYYY-MM-DD"), `totalScopeSeconds`(number), `points`(배열).
  - `BurndownPointResponse`. `date`(ISO date), `remainingSeconds`(**nullable**→`.nullish()`), `idealSeconds`(number), `completedSeconds`(**nullable**→`.nullish()`), `scopeSeconds`(number).
- **glossary 갱신 후보**. "번다운/번업" 미등재 — glossary는 Maxi 수동 영역이라 자동 추가 안 함 (필요 시 Maxi 확인).
- **grill-with-docs 생략 사유**. 도메인·ADR을 같은 세션 백엔드 PR #219에서 확정. 새로 모델링할 도메인 0 → 심층 그릴링 부적합(완료 FR office-hours 부적합 선례와 동일 판단). 경량 도메인 확인으로 대체.

## 스펙

전체 스펙. [docs/specs/2026-07-02-fr-rp-01-d6-d7-ui.md](../specs/2026-07-02-fr-rp-01-d6-d7-ui.md)

핵심 결정·시나리오 요약.
- 배치=전용 라우트 `/projects/$key/sprints/$id/burndown`, 진입=SprintColumn "번다운" 버튼 (Maxi 확정).
- 구성=단일 차트 + 번다운/번업 토글, 토글은 URL search param `?view=`(공유 URL 정합, brainstorming 발견).
- 번다운=잔여+ideal+scope, 번업=완료+scope. Y축 초→시간(formatSeconds), 미래 null 끊김.
- 상태별 화면. 로딩/403/404/422(dates-required 안내).
- 선례 재사용. WorklogAggregateChart(recharts·순수변환 named export·i18n).

## Brainstorming Check

✅ 통과 (1회, 경량 자체검토 — 완료 FR 연장). gap 1건(토글 URL param) 반영.

## Plan

> 모든 파일 경로는 worktree 루트 기준. 테스트=`cd apps/web && pnpm vitest run <path>`, 타입=`cd apps/web && pnpm typecheck`(tsconfig.app), e2e=`cd apps/web && pnpm test:e2e <spec>`. 전 task frontend-engineer(T6만 qa-engineer).

### Task 1. api/burndown.ts — fetch + Zod 스키마

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/burndown.ts`, `apps/web/src/api/burndown.test.ts`]
- depends-on: []

**RED**. `apps/web/src/api/burndown.test.ts`
- `burndownResponseSchema`가 유효 응답(remaining/completed=null 포함) 파싱 성공.
- scopeSeconds 누락 등 잘못된 형식 파싱 실패(throw).
- `fetchSprintBurndown(sprintId)`가 `/api/v1/sprints/{id}/burndown` GET 호출 후 `.data` 반환(apiFetch mock).
- 실패(예상). `burndown` 모듈 없음.

**GREEN**. `apps/web/src/api/burndown.ts` (worklog-aggregate.ts 선례)
- `import { apiFetch } from './client'`, `import { dataResponseSchema } from ...`(기존 위치 grep).
- `burndownPointSchema` = { date: z.string(), remainingSeconds: z.number().nullish(), idealSeconds: z.number(), completedSeconds: z.number().nullish(), scopeSeconds: z.number() }.
- `burndownResponseSchema` = { sprintId, projectKey, status: z.string(), startDate, endDate, totalScopeSeconds: z.number(), points: z.array(burndownPointSchema) }.
- `export async function fetchSprintBurndown(sprintId: string)` → apiFetch GET → `dataResponseSchema(burndownResponseSchema).parse(raw).data`.

**REFACTOR**. 타입 export(`BurndownResponse`,`BurndownPoint`,`BurndownView='burndown'|'burnup'`), 파일 L1 한국어 헤더 주석.

**검증**. `cd apps/web && pnpm vitest run src/api/burndown.test.ts`

### Task 2. i18n/burndown-labels.ts — 라벨

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/i18n/burndown-labels.ts`, `apps/web/src/i18n/burndown-labels.test.ts`]
- depends-on: []

**RED**. 라벨 존재 단언 — 제목, 토글(번다운/번업), 시리즈명(잔여·이상선·범위·완료), 상태 메시지(로딩·권한없음·스프린트없음·날짜필요·빈데이터), **모든 문자열 문장끝 콜론 없음**(정규식 단언, i18n 콜론 선례).

**GREEN**. `burndownLabels` 객체(worklog-aggregate-labels.ts 구조).

**REFACTOR**. 그룹핑 + 헤더 주석.

**검증**. `cd apps/web && pnpm vitest run src/i18n/burndown-labels.test.ts`

### Task 3. BurndownChart 컴포넌트 + 순수 변환 toBurndownSeries

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/burndown/BurndownChart.tsx`, `apps/web/src/components/burndown/BurndownChart.test.tsx`]
- depends-on: [1, 2]

**RED**. `BurndownChart.test.tsx` — **순수 함수 `toBurndownSeries(response, view)` 단위 테스트**(named export, recharts 미렌더):
- view='burndown' → 각 point가 { date, remaining, ideal, scope } (remaining=remainingSeconds, null 보존).
- view='burnup' → { date, completed, scope } (completed=completedSeconds, null 보존).
- 실패(예상). 함수/컴포넌트 없음.

**GREEN**. `BurndownChart.tsx` (WorklogAggregateChart.tsx 선례)
- `export function toBurndownSeries(response: BurndownResponse, view: BurndownView)` — 순수 변환.
- 컴포넌트 `BurndownChart({ response, view })` — ResponsiveContainer + LineChart, 고정 높이. burndown=Line(remaining, `connectNulls={false}`)+Line(ideal)+Line(scope); burnup=Line(completed,connectNulls=false)+Line(scope). YAxis tickFormatter=`formatSeconds`, XAxis=date, Tooltip(시간 표기), 시리즈명 i18n.

**REFACTOR**. 색/상수 추출, KDoc, `react-refresh/only-export-components` 주석(named export 사유, 선례).

**검증**. `cd apps/web && pnpm vitest run src/components/burndown/BurndownChart.test.tsx`

### Task 4. 번다운 라우트 페이지 + MSW + router 배선 (fetch·상태별·토글)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/projects.$projectKey.sprints.$sprintId.burndown.tsx`, `apps/web/src/routes/__tests__/sprint-burndown.test.tsx`, `apps/web/src/router.ts`, `apps/web/src/router.test.tsx`, `apps/web/src/mocks/burndown-handlers.ts`, `apps/web/src/mocks/handlers.ts`]
- depends-on: [1, 3]

**RED**. `__tests__/sprint-burndown.test.tsx` (MSW 시드)
- 200 → 차트 컨테이너 렌더 + 번다운 시리즈. `?view=burnup` → 번업 전환.
- 422(dates-required) → 날짜필요 안내(차트 미렌더).
- 403 → 권한없음 안내, 404 → 스프린트없음 안내, 로딩 상태.
- 실패(예상). 라우트/페이지 없음.

**GREEN**.
- `burndown-handlers.ts` — MSW GET `/api/v1/sprints/:id/burndown`, 시드 가능 store(파생동작 공유 store 선례), 200/403/404/422 시나리오. `handlers.ts`에 등록.
- 페이지 컴포넌트 — `useQuery(['sprint-burndown', sprintId], fetchSprintBurndown)`, `useSearch`로 view, 토글 UI(Link/setSearch로 `?view=` 갱신), 상태별 분기(로딩/에러/403/404/422), `<BurndownChart response view />`.
- `router.ts` — `createRoute({ getParentRoute: ()=>rootRoute, path: '/projects/$projectKey/sprints/$sprintId/burndown', component, beforeLoad: requireAuth, validateSearch: (s)=>({ view: s.view==='burnup'?'burnup':'burndown' }) })` + `addChildren`에 추가. `router.test.tsx` 라우트 등록 단언 갱신.

**REFACTOR**. 상태별 안내 컴포넌트 추출, KDoc.

**검증**. `cd apps/web && pnpm vitest run src/routes/__tests__/sprint-burndown.test.tsx src/router.test.tsx`

### Task 5. SprintColumn 번다운 진입 버튼

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/backlog/SprintColumn.tsx`, `apps/web/src/components/backlog/SprintColumn.test.tsx`, `apps/web/src/routes/projects.$projectKey.backlog.tsx`]
- depends-on: [4]

**RED**. `SprintColumn.test.tsx` — "번다운" 링크가 `to='/projects/$projectKey/sprints/$sprintId/burndown'`(projectKey/sprintId params) 로 렌더. 기존 테스트는 projectKey prop 추가에 맞춰 갱신.

**GREEN**. `SprintColumn`에 `projectKey` prop 추가 + `<Link>` 버튼. `projects.$projectKey.backlog.tsx`가 projectKey 전달.

**REFACTOR**. 버튼 스타일 토큰, aria-label.

**검증**. `cd apps/web && pnpm vitest run src/components/backlog/SprintColumn.test.tsx`

### Task 6. E2E — 번다운 happy path

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/tests/e2e/sprint-burndown.spec.ts`]
- depends-on: [4, 5]

**RED→GREEN**. Playwright — 백로그 진입 → 스프린트 칸 "번다운" 클릭 → 라우트 이동 → 차트 SVG(실브라우저 렌더) 표시 → 번업 토글 → URL `?view=burnup` 반영. MSW 시드(시나리오 토글 localStorage+addInitScript 선례). SVG E2E 함정(bbox 폭0/hit-path) 유의 — 컨테이너 가시성 위주 단언.

**검증**. `cd apps/web && pnpm test:e2e sprint-burndown`

## Plan 메타

- task 수: 6
- 예상 wave: 5 (W1=[1,2] · W2=[3] · W3=[4] · W4=[5] · W5=[6]). api→chart→route→button→e2e 계층 의존이라 대부분 직렬, W1만 병렬.
- TDD 강제: yes (T1~T5 red→green→refactor, T6 E2E)
- 추가 검증: typecheck(tsconfig.app), lint(eslint src), test(vitest), test:e2e(playwright)
- 신규 스키마/백엔드: 0 (읽기 전용 소비)

## 리뷰 결과 (← /bts-review-plan 채움)
