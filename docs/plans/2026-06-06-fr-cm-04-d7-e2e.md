# FR-CM-04 D7 — 프로젝트 리드 E2E

> slug: fr-cm-04-d7-e2e
> type: qa
> agent: qa-engineer
> 생성: 2026-06-06

## Brief

FR-CM-04 D7 — 프로젝트 리드 지정/해제 + (가능 시) 폴백 자동배정 Playwright E2E 추가.
D6(PR #91) 머지 완료 — UI 라우트 `/projects/$projectKey/settings/project-lead`, 백엔드 GET/PATCH /lead, MSW `project-lead-handlers`(X-MSW-Seed-ProjectLead) 존재.
범위: E2E + (필요 시) MSW 시드/핸들러 확장만. 구현 코드(src 컴포넌트/api/hook) 수정 금지.

## 도메인 정리 (fast-track 스킵)

## 스펙 (fast-track 스킵)

## Brainstorming Check (fast-track 스킵)

## Plan (/bts-plan 산출)

### 범위 (Maxi 확정)
- UI 시나리오(지정/해제/권한/삭제리드) + **폴백 자동배정 E2E**. 폴백은 MSW createIssueHandler 확장으로 미러(FR-CM-03 D7 동형).
- qa-engineer는 src 구현코드(컴포넌트/api/hook) 수정 금지. e2e/ + mocks/(테스트 인프라)만 허용.

### ground-truth (Explore 조사)
| 대상 | 경로 | 비고 |
|---|---|---|
| E2E 패턴 | `e2e/component-management.spec.ts`, `e2e/issue-component-default-assignee.spec.ts` | loginAsAlice, page.goto, getByRole/Label, X-MSW-Seed 헤더 |
| 헬퍼 | `e2e/fixtures/issue-fixtures.ts` | **loginAsAlice(L26)·loginAsBob(L43) 둘 다 기존재** — import만, 신규 작성 금지(리뷰 BLOCKER2). session-fixtures엔 alice만 |
| UI 셀렉터 | `ProjectLeadSelect.tsx` | data-testid `lead-current-name`, aria-label "리드 검색"·"미지정", "알 수 없는 사용자 (앞8자)" |
| 라우트 | `/projects/ATLAS/settings/project-lead` | heading "프로젝트 리드" |
| MSW 시드 | `project-lead-handlers.ts` | seedProjectLead, X-MSW-Seed-ProjectLead 헤더, 422 플래그 `msw-project-lead-422` |
| 폴백 미러 갭 | `issue-handlers.ts:221-229` createIssueHandler | componentStore 리드만 참조, **projectLeadStore 미참조** → 폴백 E2E 위해 확장 필요 |
| 권한 fixture | alice=admin(MANAGE_COMPONENTS true) / bob=member(false) | S3 disabled는 bob 로그인 |
| 사용자 fixture | alice=김앨리스(...001? 실제 c3d4...), carol=캐럴 | 검색 "캐럴"→선택 패턴 |

### E2E 시나리오 (`e2e/project-lead.spec.ts` 신규)
- S1. **리드 지정** — alice 로그인 → settings/project-lead → "리드 검색"에 "캐럴" 입력 → 드롭다운 "캐럴" 버튼 클릭 → PATCH /lead → `lead-current-name`이 "캐럴" 표시 + 성공 토스트.
- S2. **리드 해제** — S1 상태(또는 시드된 리드)에서 "미지정" 버튼 클릭 → PATCH null → `lead-current-name` "미지정".
- S3. **권한 disabled** — bob 로그인(MANAGE_COMPONENTS false) → "리드 검색" input·미지정 버튼 disabled.
- S4. **삭제된 리드 표시** — X-MSW-Seed-ProjectLead로 user fixture에 없는 UUID 시드 → `lead-current-name` "알 수 없는 사용자 (앞8자)" + 해제 버튼 노출.
- S5. **폴백 자동배정** — 프로젝트 리드(김앨리스, userAliceFixture.id) 시드 + **리드 없는 컴포넌트 1개**(COMP_NO_LEAD, leadUserId:null) 지정한 이슈 생성 → 이슈 상세 담당자 "김앨리스"(프로젝트 리드 폴백). "컴포넌트 미지정"(componentIds 0개) 케이스는 미러 가드(componentIds.length>0) 때문에 제외 — 리드 없는 컴포넌트 1개로 단일화(리뷰 CONCERN2). createIssueHandler 폴백 미러 필요.

### TDD task

- **T1 (qa-engineer)** — MSW 폴백 미러 확장 (mocks/, 테스트 인프라 — src 무수정 범위).
  - **선행(리뷰 BLOCKER1)**: `apps/web/src/mocks/project-lead-handlers.ts`에 읽기 getter export 추가 — `getStoredProjectLead(projectIdOrKey): string | null`(`projectLeadStore.get(...)?.leadUserId ?? null`). `getStoredComponentsByIds`(component-handlers.ts:59) 동형. projectLeadStore는 모듈-private라 직접 import 불가하므로 getter 필수. 미시드 프로젝트는 null 반환(기존 자동배정 spec 회귀 0).
  - files: `apps/web/src/mocks/project-lead-handlers.ts`(getter export), `apps/web/src/mocks/issue-handlers.ts`(폴백 분기 + getter import).
  - 동작: createIssueHandler 폴백 분기(`issue-handlers.ts:220` 컴포넌트 리드 미해결 시) → `getStoredProjectLead(body.projectKey)`로 폴백 자동배정. 우선순위 = 컴포넌트 리드 → 프로젝트 리드 → 미할당. store 키 = projectKey 원본('ATLAS', GET 시드 키와 동일).
  - **범위 한정(리뷰 CONCERN1)**: createIssue는 assigneeId를 받지 않으므로 "명시 담당자 보존(current!=null)" 케이스는 createIssue 미러에 존재하지 않음 → MSW는 "컴포넌트 리드 없음→프로젝트 리드" 한 갈래만 미러. current 보존 검증 책임은 백엔드 DefaultAssigneeResolverTest(PR #89).
  - RED: S5 E2E가 폴백 미적용(미할당)으로 red → getter+분기 추가 green.
- **T2 (qa-engineer)** — E2E spec. depends T1(S5).
  - files: `apps/web/e2e/project-lead.spec.ts`(신규).
  - 헬퍼는 `e2e/fixtures/issue-fixtures.ts`에서 loginAsAlice/loginAsBob import(기존재, 신규 작성 금지). S1~S5 작성. data-testid/aria-label 셀렉터, X-MSW-Seed-ProjectLead/Components 시드. 검색 300ms debounce는 버튼 노출 대기로 흡수. strict mode 안전(컨테이너 한정·exact).

### Plan 메타
- 2 task(T1 MSW → T2 E2E). qa-engineer 단일, T2가 T1 의존(S5).
- 구현 src 무수정(mocks/e2e만 — mocks는 테스트 인프라).
- **기존 `issue-component-default-assignee.spec.ts` 4건 함께 실행해 회귀 0 확인**(ui-pr-defer-e2e-regression-latent). 미시드 프로젝트는 getter null 반환이라 기존 spec 영향 없음.
- 가짜그린 주의: 폴백 ground-truth는 D5 백엔드 통합(PR #89). E2E는 UI+MSW 미러 회귀가드.

## 리뷰 결과 (/bts-review-plan 산출)

### code-reviewer ground-truth 리뷰 (2026-06-06, 구현 전)

BLOCKER 2 / CONCERN 2 적발 → 전부 plan 반영.
- **BLOCKER1(반영)** — projectLeadStore 모듈-private라 직접 참조 컴파일 불가 → `getStoredProjectLead` getter export 선행(getStoredComponentsByIds 동형) T1에 추가.
- **BLOCKER2(반영)** — loginAsBob 이미 `issue-fixtures.ts:43` 존재(환각) → 거기서 import, 신규 작성 금지로 정정.
- **CONCERN1(반영)** — createIssue가 assigneeId 미수신 → current 보존은 MSW 검증 불가, 백엔드 통합 책임 명시. MSW는 폴백 한 갈래만.
- **CONCERN2(반영)** — "컴포넌트 미지정" 폴백은 가드(componentIds.length>0)로 안 탐 → S5를 "리드 없는 컴포넌트 1개"로 단일화.
- **OK(검증됨)** — D6 셀렉터 전부 실재(lead-current-name/리드 검색/미지정/알 수 없는 사용자), bob=MANAGE_COMPONENTS false, 이슈 생성 E2E 경로(issue-component-default-assignee 동형), store 키 'ATLAS' 정합, 폴백 우선순위 백엔드 일치.

판정: 정정 반영 후 **구현 진입 가능**.
