# 에디터 마감 — 컨텍스트 노트

결정과 그 근거를 시간 순으로 덧붙인다. 계획 `../2026-09-04-editor-polish.md` · 진행 `checklist.md`

---

## 2026-09-04 — PR 을 둘로 가른다

**결정.** 남은 에디터 작업 5건을 PR A(이 문서, 항목 A~D)와 PR B(모달↔사이드패널 토글)로 가른다.

**근거.** A~D 는 에디터·첨부 컴포넌트 안에서 끝난다. 토글은 `routes/issues.index.tsx`
(목록 split view)를 건드리고 e2e 의존이 많다. 섞으면 토글이 막힐 때 나머지 4건이 함께 발이
묶이고, e2e 가 깨졌을 때 원인 분리에 시간이 든다. **직전 4-PR 캠페인에서 가장 비쌌던 것이
e2e 디버깅이었다** — 이건 추측이 아니라 그 캠페인의 실측이다.

## 2026-09-04 — 계획 문서를 첫 커밋으로 둔다

**결정.** 구현 전에 계획 3종을 먼저 커밋하고 draft PR 을 연다.

**근거 둘.** ① 빈 브랜치로는 PR 을 만들 수 없다. ② `CLAUDE.md` §7 이 plan + checklist +
context-notes 3종을 비-사소 작업의 선행 조건으로 요구한다.

**문서 규약** — `docs/plans/YYYY-MM-DD-slug.md` · `# H1` 필수 · 본문에 FR ID ·
spec 과 plan 은 같은 slug · 커밋 후 `node scripts/build-doc-index.mjs` 재실행.
형식은 직전 캠페인 `2026-09-04-issue-detail-jira-parity.md` 를 따랐다(frontmatter 없이
`# H1` + 인용 블록 메타).

## 2026-09-04 — 체크포인트의 항목 (3) 기술이 틀렸다. 실측이 더 나쁘다

**기록된 것.** 「본문·댓글 길이 카운터 미구현 — 지금은 `maxLength` 만 걸려 있다」

**실측.** 프론트에 본문·댓글 상한이 **아예 없다**.

| 층 | 제목 | 본문 | 댓글 |
|---|---|---|---|
| 서버 정본 | 255 | 32767 (`IssueTextConstraints.kt:45`) | 32767 (`:48`) |
| 프론트 상수 | `SUMMARY_MAX_LENGTH = 255` | **없음** | **없음** |
| 프론트 노출 | `maxLength` 속성 | **없음** | **없음** |

32767 은 `api/issues.ts:423`·`:430` **주석에만** 있다. TipTap `CharacterCount` 확장도
미설치다(`package.json` 조회 0건). 본문이 contenteditable 이라 `maxLength` 속성 자체를
걸 수 없으므로, 「`maxLength` 만 걸려 있다」는 상태는 애초에 성립할 수 없다.

**함의.** 사용자는 상한을 넘길 때까지 신호를 못 받고 **저장 시 서버 400 으로 처음 안다**.
32767자를 날리는 경로다. 항목 B 의 우선순위가 「있으면 좋은 카운터」가 아니라 **데이터 손실
경로 차단**으로 올라간다.

**따라오는 위험.** 프론트에 상수를 두는 순간 서버 상수와 **이중 목록**이 생긴다.
`[[two-lists-never-check-each-other]]` 의 지배 결함 양식 그대로다. 처방도 같다 —
차집합 판별식 + 비-공허 짝. 값만 복사하고 짝을 안 두면 다음 조정 때 조용히 어긋난다.

## 2026-09-04 — A1 은 「동작하지만 판별자가 없는」 형태다

`isEditableTarget`(`shortcuts.ts:212`)이 `isContentEditable` 을 이미 본다. `RichTextEditor.tsx:63`
주석이 그 사실에 명시적으로 기대고 있다. 그래서 **지금 동작은 옳다.**

문제는 그 분기를 지워도 red 가 되는 테스트가 없다는 것. TipTap 전환(#448) 이전에는 본문이
`textarea` 라 `tagName` 분기로 막혔고 `contentEditable` 분기는 관측면이 없었다. 전환이
그 분기를 **활성 경로로 바꿨는데 판별자는 따라오지 않았다.**

→ 가드를 쓸 때 `CLAUDE.md` §함정을 그대로 적용한다. **일부러 끊어 red 1회를 본다.**
안 보면 판별식이 헬프텍스트에 만족하는 형태(`[[invariant-satisfied-by-helptext-not-logic]]`)로
들어갈 수 있다.

## 2026-09-04 — C2 는 #446 결함의 짝까지 봐야 한다

`attachment:` 스킴 판별식을 `img[src]` 쪽만 쓰면 #446 에서 실제로 난 결함을 못 잡는다.
그 결함은 `allowUrlProtocols` 가 **정책 전역**이라 `img` 용으로 연 스킴이 `a[href]` 에도
열렸는데 UUID 술어는 `img[src]` 에만 있었던 것이다. 판별식이 한쪽만 보면 나머지 한쪽은
조용히 썩는다 — `[[partial-column-parser-lets-unread-column-rot]]` 와 같은 양식이다.

→ C2 는 `img` 통과와 `a` 차단을 **함께** 본다.

## 2026-09-04 — 워크트리 락이 살아 있다

`.claude/worktrees/editor-polish` 는 pid 10079(`claude bg-spare`, 2026-09-04 01:19 시작)가
잡고 있다. 커밋 0건 · `git status` clean 이라 편집 중인 파일은 없다.

**대응.** 인덱스가 워크트리 간 공유이므로 `git add` 를 자기 파일로 좁혀도 커밋 시점에 옆
세션이 스테이지해 둔 파일이 딸려 들어간다(`[[shared-worktree-git-index-defeats-narrow-git-add]]`
— 실측 사고 있음). **경로 한정 커밋 `git commit -- <경로>` 를 쓴다.**

## 2026-09-04 — 착수 시 미해결이던 4건, 전부 결정했다

- **B 표시 규칙 → 임계(90%) 노출.** Jira Cloud 사용자 문서에 카운터의 표시 규칙 서술이
  없다(조회 2026-09-04). 상한이 32,767자라 일상적 글쓰기가 닿지 않으므로 상시 노출은
  모든 에디터 아래에 쓸모없는 숫자를 붙이고 정작 필요한 순간의 신호를 소음에 묻는다.
  「숫자가 보인다는 것 자체가 경고」로 설계했다. 편차 X6.
- **B 저장 차단 → 막는다.** 버튼과 ⌘Enter 양쪽. 안 막으면 32,767자를 다 쓴 뒤 서버 400 으로
  처음 알게 되고, 그 시점에는 무엇을 지워야 할지 알 방법이 없다. 편차 X7.
- **D 경계 처리 → 분기를 없앴다.** 호출부가 미리보기 가능한 첨부만 걸러 넘긴다.
  건너뛸 것이 목록에 없으므로 「건너뛸까」라는 질문 자체가 사라진다.
- **A2 재현 → 못 했다.** 좌표 API 를 채워 넣고도 팝업이 안 뜬다(2회 시도). 아래 §참조.

## 2026-09-04 — A1 의 진짜 구멍은 술어가 아니라 배선이었다

착수 전 계획은 「`shouldIgnoreEvent` 술어에 판별자가 없다」였다. **틀렸다.**
`shortcuts.test.ts:106` 에 contenteditable 술어 테스트가 이미 있었다.

없던 것은 **가드 호출 배선**이다. `useKeyboardShortcuts.ts:156` 의 `shouldIgnoreEvent(e)`
한 줄이 전역·컨텍스트 단축키를 함께 막는데, 그 호출을 지워도 술어 테스트는 그대로 초록이다.
기존 배선 테스트(E4 `c` · E10 `j`)는 `<input>` 만 썼다 — TipTap 전환 전에는 본문이 textarea 라
그것으로 충분했고, contenteditable 분기가 활성 경로가 된 뒤에도 판별자는 안 따라왔다.

→ 판별식의 자리를 `shortcuts.test.ts` 가 아니라 `useKeyboardShortcuts.test.tsx` 로 옮겼다.
뮤테이션 2종(가드 호출 제거 · contenteditable 속성 분기 제거)이 각각 5건·4건 red 로 확인.

## 2026-09-04 — A2 는 팝업이 아니라 「결함이 났던 분기」를 겨눈다

jsdom 에서 suggestion 팝업이 안 뜬다. 이번에 좌표 API(`getClientRects`·
`getBoundingClientRect`)를 Text/Element/Range 프로토타입에 채워 넣어 봤다 — 크래시는
사라졌지만 팝업은 여전히 안 뜬다. 2회 시도 후 재현 불가로 확정했다.

그래서 팝업 렌더가 아니라 #448 에서 **실제로 결함이 났던 한 분기**를 겨눈다.
그 결함은 팝업이 아니라 `RichTextEditor.handleKeyDown` 의 Escape 처리였다 —
ProseMirror `someProp` 이 view props 를 플러그인보다 먼저 훑으므로, 여기서 Escape 를
삼키면 suggestion 의 `onKeyDown` 이 영영 안 불리고 작성 중인 본문이 통째로 날아갔다.

신호원(`isMentionSuggestionActive`)만 모킹하면 좌표 없이 그 분기만 정확히 잰다.
분기를 지우면 red 다. 팝업 층의 증인은 e2e 4건이 계속 맡는다.

## 2026-09-04 — 뮤테이션이 두 번 「가짜로」 통과했다

**M9(C2).** `sed` 가 BSD 문법 때문에 미매치해 **뮤테이션이 안 걸린 채** 초록이 나왔다.
`grep -c MUTATION` 으로 적용 여부를 되재 잡았고 python 으로 다시 걸어 red 를 봤다.
→ 뮤테이션은 「걸었다」가 아니라 **「걸렸다」를 확인**해야 한다.

**M20(D).** 섹션이 전체 목록을 갤러리에 넘기도록 바꿔도 **전부 초록**이었다. 모달의 이동
판별식은 넘겨받은 목록 안에서만 재기 때문에, 목록을 만드는 쪽이 틀려도 못 잡는다.
→ 목록 계약을 재는 describe (h) 2건을 새로 세우고, 테스트 안의 모달 대역이
`attachments`·`startIndex` 를 **실제로 쓰도록** 고쳤다
(`mock-swallowed-prop-is-invisible-to-unit-tests` 의 처방 그대로).

## 2026-09-04 — 워크트리에 node_modules 가 아예 없었다

`.bin/vitest` 부재로 시작해, main 체크아웃의 `node_modules` 를 심볼릭으로 걸었더니
`@tiptap/react` 미해결로 죽었다 — main 의 설치본이 9/3 것이라 TipTap(#448, 9/4) 보다 앞선다.
`pnpm install --frozen-lockfile` 을 워크트리에서 직접 돌려 해결했다.

→ 워크트리에서 `.bin` 부재를 만나면 **심볼릭으로 때우지 말 것.** 옆 체크아웃의 설치본이
언제 것인지 모른다. 링크는 성공하고 임포트만 실패해 원인이 멀어진다.

## 2026-09-04 — 하네스가 계획 문서에서 3건을 잡았다

`pnpm test:workflow` 가 red 3건. 전부 내 계획 문서가 원인이었다.

1. `jira-research-guard` — `## Jira 대조` 절이 없다. 계약 §1 이 도입일(2026-08-27) 이후
   plan/spec 에 실물 조회 근거를 요구한다. 편차 X6~X9 와 함께 채웠다.
2. `transition-term-guard` — 「push 前이면」의 한글 두 글자가 구 표기와 겹쳐 잡혔다.
   판별식은 텍스트로만 보므로 낱말 경계를 가르지 못한다. 가드 머리주석이 「이전」+조사 같은
   부분문자열을 정당한 예외로 이미 적어 두고 동결 파일 갱신을 지시하지만, 동결 파일은
   250건 넘는 4열 교차검증 구조라 T1 PR 에서 건드릴 자리가 아니다. **내 문장을 고치는 쪽이
   훨씬 싸다.** 실제로 이 노트 자체가 같은 함정을 한 번 더 밟아 red 를 냈다 — 고친 뒤에
   새로 쓴 문장이 다시 그 두 글자를 만들었다.
3. `git-fixture-isolation` — 2번의 **연쇄**다. 자식 판별식이 죽으면 이쪽 전제가 깨진다.
   독립 결함이 아니었다.

→ 계획 문서도 하네스의 판정 대상이다. 「문서니까 안전」이 아니다.

## 2026-09-04 — 눈확인은 Playwright 로 했다 (Chrome 확장 미연결)

`mcp__claude-in-chrome__tabs_context_mcp` 가 `Browser extension is not connected` 로 거부했다.
저장소 자체 도구인 Playwright 로 대체했다 — 스크린샷을 실제로 눈으로 확인했고, 툴바 종수·
카운터 문구·갤러리 위치는 DOM 에서 직접 읽어 수치로 남겼다.

**눈으로만 알 수 있었던 것 하나** — 갤러리 미리보기 영역이 비어 보인다. 결함이 아니다.
MSW `buildResponseBlob` 이 업로드 바이트와 무관하게 **1×1 최소 PNG** 를 돌려준다
(`naturalWidth=1` 로 확인). 스크린샷만 보고 「이미지가 안 뜬다」로 보고했으면 없는 결함을
쫓을 뻔했다.

## 2026-09-04 — 리뷰가 잡은 것 4건, 전부 실측으로 재현했다

**BLOCKER-1. ←/→ 가 자식의 기본동작을 빼앗는다.** `handleKeyDown` 을 `DialogContent` 에 걸었다는
것은 **모달 안의 모든 키가 거기로 버블한다**는 뜻인데, 조건 없이 `preventDefault` 했다.
`<video controls>` 의 ←/→ 는 5초 되감기다 — 가로채면 되감기가 죽고, 갤러리가 여럿이면
되감기 대신 다른 첨부로 넘어가면서 blob 이 revoke 되어 **재생 위치를 통째로 잃는다.**
재현 테스트 4건(비-공허 짝 포함)을 세워 red 3건을 본 뒤 고쳤다.
→ 컨테이너 레벨 키 핸들러는 **누가 그 키를 이미 쓰는지**를 먼저 물어야 한다.

**CONCERNS-2. 「HTML 을 잰다」를 지키는 판별식이 없었다.** 본문 카운터를 평문 길이로 바꿔도
6건이 **전부 초록**이었다(실측). 원인은 픽스처 — `htmlOfLength` 가 평문과 HTML 을 7자밖에
안 벌려 어느 쪽으로 재도 판정이 같은 쪽에 떨어졌다. 댓글에는 반대 방향 짝(`noisyHtml`)이
있었는데 본문에만 없었다.
★고치는 과정에서 **또 하나를 배웠다** — 처음 픽스처로 `<strong>가</strong>` 반복을 썼더니
카운터가 안 떴다. TipTap 이 마운트하며 **인접한 같은 mark 를 하나로 병합**해 HTML 이 짧아진
것이다. 문단 반복으로 바꿔야 정규화를 견딘다. 「HTML 길이」는 내가 넣은 문자열이 아니라
**에디터가 직렬화한 결과**의 길이다.

**CONCERNS-4. 갤러리 위치가 스크린리더에 안 알려진다.** Radix `DialogTitle` 은 내용이 바뀌어도
재알림되지 않는다. 위치 span 에 `aria-live="polite"` + 파일명 sr-only 를 더했다 —
없으면 시각 사용자만 쓸 수 있는 이동이 된다.

**CONCERNS-3. 체크리스트 게이트 칸이 `[ ]` 인 채 커밋됐다.** 실제로는 돈 항목이 최소 3개인데
증거가 산출물과 어긋났다. 다음 세션이 「어느 게이트가 실제로 돌았나」를 알 수 없다.
→ CLAUDE.md §7 의 「tick as you go」는 장식이 아니다.

**리뷰가 확인해 준 것** — 카운터가 재는 문자열(본문 HTML · 댓글 평문)이 백엔드와 정확히
맞는다(`UpdateIssueRequest.kt:86-89` 양쪽 `@Size` · `CommentApplicationService.kt:382-387` 은
`body` 만 · `bodyHtml` 에는 `@Size` 없음). `useEffect` 의존성도 무한 재실행이 아니다 —
TanStack Query v5 의 구조적 공유가 원소 참조를 유지한다.

## 미해결 — 다음 세션으로

- **e2e 미실행** — 155 spec 병렬이 이 기계 메모리를 넘는다. 워커 1개로 변경 표면 직격만
  돌려야 한다
- **잔여 위험(리뷰 지적)** — 미리보기 목록의 **순서가 바뀌면** `startIndex` 가 변해 열려 있던
  갤러리가 그 행의 첨부로 되돌아간다. 업로드는 뒤에 붙으므로 확률은 낮다
- **PR B** — 모달↔사이드패널 토글(항목 5). 브랜치 미생성
