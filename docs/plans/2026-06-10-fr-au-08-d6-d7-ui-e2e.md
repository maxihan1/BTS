# FR-AU-08 D6/D7 — 계정 통합 프론트 UI + E2E

> slug: fr-au-08-d6-d7-ui-e2e
> type: ui
> agent: frontend-engineer (+ qa-engineer E2E, + security-engineer 리뷰)
> primary_bc: apps/web (identity-access 프론트짝)
> 생성: 2026-06-10

## Brief

사용자 원문. "fr-au-08 d6 d7 진행하자"

백엔드 완료 — FR-AU-08 1차 LDAP 동기 연결(#103), FR-AU-08b SSO(SAML/OIDC) 리다이렉트 연결(#104). 남은 작업은 D6(프론트 UI)·D7(E2E).

classify 결과 (보정 적용).
- type: ui (classify 원판정 qa → 보정. "E2E" 키워드가 qa로 끌었으나 주작업은 D6 프론트 UI 구현)
- agent: frontend-engineer (D6 UI 구현) + qa-engineer (D7 E2E) + security-engineer (인증 플로우 리뷰)
- primary_bc: apps/web (identity-access BC 프론트짝)
- 보정 근거: 인증 UI는 CLAUDE.md상 frontend-engineer가 디자인 확인 후 구현. step-up 재인증·SSO 리다이렉트 보안 흐름은 security-engineer 리뷰.

대상 페이지. `/settings/account-links` 설정 페이지 (연결된 외부 계정 목록 + LDAP 연결 + SSO 연결 + 해제 + 재인증/step-up).

소비 API (백엔드 완료, #103/#104).
- `GET /api/v1/auth/account/links` — 연결 목록 (hasLocalPassword·externalSubject 마스킹·linkedAt·lastLoginAt·provider name/type/enabled)
- `POST /api/v1/auth/account/reauth` — LOCAL/LDAP 재인증 → step-up (응답=만료시각만)
- `POST /api/v1/auth/account/links` — LDAP 동기 연결 (step-up 필요, 신규201/멱등200)
- `DELETE /api/v1/auth/account/links/{id}` — 해제 (step-up 필요, 204, 마지막수단 409)
- `POST /api/v1/auth/account/links/sso/start` — SSO 연결 시작 (step-up 필요, XHR → authorizeUrl + JSESSIONID)
- `POST /api/v1/auth/account/reauth/sso/start` — SSO 재인증 시작 (XHR → authorizeUrl)
- SSO 콜백 복귀 → `/settings/account-links?link=success|already_linked|conflict|error` 또는 `?reauth=success|failed`

## 도메인 정리

- **BC**. apps/web (identity-access BC의 프론트짝). 백엔드는 #103/#104로 완료 — **백엔드 무변경이 원칙**(단, 아래 contract gap 1건은 예외 후보).
- **작업 성격**. 순수 프론트(D6) + Playwright E2E(D7). grill-with-docs 스킵 — 도메인은 ADR `account-linking-policy`(D1~D5)·`sso-account-linking`로 이미 확정. 신규 백엔드 도메인 용어 0. 새 개념은 프론트/UX 한정.
- **새 프론트 개념**. (1) 계정 연결 설정 페이지(`/settings/account-links`) (2) step-up 재인증 흐름(민감동작 전 재인증→5분 윈도우) (3) SSO 리다이렉트 2단계(XHR start→`window.location.assign`) (4) SSO 콜백 복귀 쿼리 처리.
- **기존 결정 충돌**. 없음. 정책 ADR 2종 계승(프론트 구현). 신규 ADR 0.

### 백엔드 API 계약 (실측 — Zod 1:1 정합 대상, frontend-zod-backend-dto-contract-gap)

| 메서드·경로 | 요청 | 성공 | 에러코드 |
|---|---|---|---|
| `GET /api/v1/auth/account/links` | — | 200 `{links:[{id,providerId,providerName?,providerType?,providerEnabled,externalSubjectMasked,linkedAt,lastLoginAt?}], hasLocalPassword}` | 403 `account_linking_requires_interactive_login`(PAT) |
| `POST /api/v1/auth/account/reauth` | `{method:"LOCAL"\|"LDAP", password, providerId?, username?}` | 200 `{stepUpExpiresAt}` | 400 `reauth_fields_required` / 401 `reauth_failed` / 403 PAT |
| `POST /api/v1/auth/account/links` | `{providerId(UUID), username, password}` | 201 신규 / 200 멱등 `AccountLinkResponse` | 403 PAT\|`step_up_required` / 401 `link_authentication_failed` / 409 `account_already_linked` / 503 `provider_unavailable` |
| `DELETE /api/v1/auth/account/links/{id}` | — | 204 | 403 PAT\|`step_up_required` / 409 `last_login_method` / 404 |
| `POST /api/v1/auth/account/links/sso/start` | `{registrationId, providerType:"SAML"\|"OIDC"}` | 200 `{authorizeUrl}` | 403 PAT\|`step_up_required` / 400 `invalid_sso_request` / 404 `provider_not_found` |
| `POST /api/v1/auth/account/reauth/sso/start` | 동일 | 200 `{authorizeUrl}` | 403 PAT\|sid부재 / 400 / 404 |

- **ProviderType enum**. `LOCAL` / `LDAP` / `SAML` / `OIDC` / `PAT` / `OAUTH`.
- **SSO 콜백 복귀(백엔드 고정 경로 실측)**. `/settings/account-links` + `?link=success\|already_linked\|conflict\|error` 또는 `?reauth=success\|failed`. ⟹ **프론트 라우트는 반드시 `/settings/account-links`** (SsoLinkingCallbackProcessor `SETTINGS_PATH` 상수).
- **CSRF**. mutation은 `X-XSRF-TOKEN` 수동(identity-access self-service 관례, `sessions.ts` 선례). JWT는 apiFetch가 Authorization 자동.

### 🔴 Contract gap (Maxi 결정 필요) — 연결 추가 UI의 provider 출처

"계정 추가" UI를 만들려면 프론트가 연결 가능한 provider 식별자를 알아야 함. 실측 결과.
- **SSO(SAML/OIDC)** — ✅ 해결됨. `GET /api/v1/auth/saml/idps`·`GET /api/v1/auth/oidc/providers`가 `{registrationId, displayName}` 반환(permitAll, 인증 사용자도 호출 가능). SSO 연결 = registrationId+type로 `/links/sso/start` 호출 가능.
- **LDAP** — ❌ gap. `POST /links`는 `providerId: UUID` 필요. 그런데 인증 사용자에게 LDAP provider UUID를 주는 엔드포인트가 **전무**(로그인용 `/providers`는 `id="ldap"` 토큰만·UUID 아님·LOCAL/LDAP만). ⟹ LDAP 연결 추가 UI 불가.

**✅ Maxi 결정 (2026-06-10) — 옵션 A**. 백엔드 엔드포인트 `GET /api/v1/auth/account/linkable-providers`(인증, security-engineer) 추가. LDAP(UUID+displayName)+SAML/OIDC(registrationId+displayName+type) 통합 목록. D6 = 프론트 + 백엔드 view-layer 엔드포인트 1개. **same-BC view layer 예외**(learnings 2026-05-22 — frontend PR에서 same BC backend view layer patch 자연스러움). 단일 "계정 추가" 피커로 모든 provider 타입 커버.

- **작업 구성 확정**. 백엔드 1 task(security-engineer, linkable-providers 엔드포인트) + 프론트 다수(frontend-engineer, 페이지/api/hooks/MSW/i18n) + E2E(qa-engineer, Playwright). 한 PR(identity-access BC view layer + 그 프론트 소비자).
- **응답 형태(결정)**. `{ linkable: [{ kind:"LDAP", providerId:UUID, displayName } | { kind:"SAML"|"OIDC", registrationId, displayName }] }`. enabled provider만. spec 단계에서 정확 필드 확정.

## 도메인 정리 끝 ──

## 스펙

전체 스펙. [docs/specs/2026-06-10-fr-au-08-d6-d7-ui-e2e.md](../specs/2026-06-10-fr-au-08-d6-d7-ui-e2e.md)

핵심 요약.
- `/settings/account-links` 페이지 — 연결 목록 + 계정 추가(LDAP 인라인 폼 / SSO 리다이렉트) + 해제(확인) + step-up 재인증(LOCAL/LDAP 동기 모달 + SSO 리다이렉트) + SSO 콜백 쿼리 처리.
- 백엔드 신규 1 — `GET /api/v1/auth/account/linkable-providers`(JWT 전용, enabled LDAP UUID + SAML/OIDC registrationId 통합). same-BC view layer.
- step-up = 403 `step_up_required` 진실출처 + `stepUpExpiresAt` 사전게이팅. CSRF 수동 토큰. Zod↔DTO 1:1. 토스트=sonner. 라우트 `/settings/account-links` 고정(백엔드 SETTINGS_PATH).
- E2E = S1 목록·S2 LDAP 연결·S4 SSO(start+콜백쿼리 분리)·S5 해제·S6 마지막수단 409. 진짜 IdP 왕복 비목표.

## Brainstorming Check

✅ 통과 (1회 iteration, 적대적 기술 갭 분석). 3건 발견 전부 스펙 반영(기술 갭, Maxi 결정 불요).
- G1 E2E SSO 왕복 불가→start/콜백쿼리 분리(EC9). G2 step-up 403 진실출처 명시(FR6). G3 토스트=sonner·null fallback·재인증 providerId 출처.

## Plan

> 경로 약어. `IA = backend/modules/identity-access/src` (패키지 `com.atlas.bts.identity`), `W = apps/web/src`, `E = apps/web/e2e`.
> TDD red→green→refactor 강제. 백엔드 2 task는 단일 모듈 test 컴파일 공유라 직렬(`bts-plan-wave-gradle-module-compile`). 프론트 task는 worktree 커밋 race 회피 위해 직렬 dispatch(`worktree-lint-staged-shared-git-stash-collision`).
> Zod는 백엔드 DTO 1:1 미러(`frontend-zod-backend-dto-contract-gap`). MSW mutation은 stateful 영속(`msw-mutation-stateful-refetch`). hooks는 invalidate-only(`mutation-setquerydata-partial-response-flicker`).

### Task 1. AuthnProviderConfigRepository.findEnabledByType (LDAP 행 UUID+name)

**메타**.
- agent: `security-engineer`
- files: [`IA/main/kotlin/com/atlas/bts/identity/provider/AuthnProviderConfigRepository.kt`, `IA/test/kotlin/com/atlas/bts/identity/provider/AuthnProviderConfigRepositoryTest.kt`]
- depends-on: []

**RED**. Testcontainers repo 테스트 — `findEnabledByType(ProviderType.LDAP)`가 enabled=true LDAP 행만 `AuthnProviderInfo(id,name,type,enabled)` 리스트로 반환. 비활성 LDAP·타 type 제외. 기존 테스트(findByIds/listEnabledByTypes) green 유지.
**GREEN**. `findEnabledByType(type): List<AuthnProviderInfo>` — `SELECT id,name,type,enabled FROM authn_providers WHERE type=:type AND enabled=TRUE`. 기존 `AuthnProviderInfo` 재사용. NamedParameterJdbcTemplate.
**REFACTOR**. SQL 상수화 + KDoc(linkable-providers 소비 명시).
**검증**. `./gradlew :backend:modules:identity-access:test --tests "*AuthnProviderConfigRepositoryTest"`

### Task 2. LinkableProvidersController + DTO — GET /linkable-providers

**메타**.
- agent: `security-engineer`
- files: [`IA/main/kotlin/com/atlas/bts/identity/web/LinkableProvidersController.kt`, `IA/main/kotlin/com/atlas/bts/identity/web/dto/LinkableProvidersDtos.kt`, `IA/test/kotlin/com/atlas/bts/identity/web/LinkableProvidersControllerTest.kt`]
- depends-on: [1]

**RED**. MockMvc 테스트(repo mock) — `GET /api/v1/auth/account/linkable-providers`: JWT→200, 응답 `{linkable:[...]}` 에 LDAP(kind=LDAP, providerId=UUID, displayName)·SAML(kind=SAML, registrationId, displayName)·OIDC(kind=OIDC, registrationId, displayName) 통합. PAT(Jwt 아님)→403 `account_linking_requires_interactive_login`. enabled만(비활성 provider 0건). 빈 결과 `{linkable:[]}`.
**GREEN**. `@RestController @RequestMapping("/api/v1/auth/account")`, `@GetMapping("/linkable-providers")`. deps — `AuthnProviderConfigRepository.findEnabledByType(LDAP)`·`SamlIdpConfigRepository.findAllEnabled()`·**구현체 `OidcProviderConfigRepository.findEnabled()`**(⚠️ 리뷰 — `findEnabled()`는 `OidcProviderConfigReader` 인터페이스가 아니라 **구현체에만** 존재. SsoAccountLinkController는 Reader 주입이지만 본 컨트롤러는 **구현체 `OidcProviderConfigRepository`를 주입**해야 호출 가능)·`AccountLinkJwtSupport`(resolveClaims null→PAT 403). DTO 평면형 `LinkableProviderDto(kind:String, providerId:UUID?, registrationId:String?, displayName:String)` + **`@JsonInclude(JsonInclude.Include.NON_NULL)`**(⚠️ 리뷰 C1 — null 필드를 응답서 제거해 LDAP 응답엔 registrationId 키 부재 → Zod `discriminatedUnion`이 깨끗이 파싱. 미적용 시 모든 kind가 두 키 다 직렬화해 drift). 읽기전용·step-up 불요.
**REFACTOR**. kind 상수 + KDoc(enabled 공개정보·per-user 필터 없음 / PAT는 필터 `authenticated()` 통과하므로 `resolveClaims` null이 유일 가드 — `@PreAuthorize` 불요 사유).
**검증**. `./gradlew :backend:modules:identity-access:test --tests "*LinkableProvidersControllerTest"` + 모듈 ktlint/detekt `--rerun-tasks`.

### Task 3. W/api/account-links.ts — Zod 스키마 + API 함수 (CSRF 수동)

**메타**.
- agent: `frontend-engineer`
- files: [`W/api/account-links.ts`, `W/api/account-links.test.ts`]
- depends-on: [2]

**RED**. api 단위 테스트(MSW) — `fetchAccountLinks()`→`{links,hasLocalPassword}` 언래핑, `fetchLinkableProviders()`→discriminatedUnion 파싱(NON_NULL 응답이라 LDAP엔 registrationId 키 부재), `reauth(body)`/`linkAccount(body)`/`unlinkAccount(id)`/`ssoLinkStart(body)`/`ssoReauthStart(body)` 각 메서드·경로·**X-XSRF-TOKEN 헤더(POST/DELETE 전부 — `ssoReauthStart` 포함, ⚠️ 리뷰 C2: `/reauth/sso/start`도 CSRF 면제 목록 밖이라 토큰 필수)**·DataResponse 처리. 에러코드(403/401/409/503) `ApiError`로 throw.
**GREEN**. Zod 스키마 5종 — `accountLinkSchema`(id/providerId/providerName.nullable/providerType.nullable/providerEnabled/externalSubjectMasked/linkedAt/lastLoginAt.nullable), `accountLinksResponseSchema`({links,hasLocalPassword}), `reauthResponseSchema`({stepUpExpiresAt}), `ssoLinkStartResponseSchema`({authorizeUrl}), `linkableProvidersResponseSchema`(z.discriminatedUnion('kind', [LDAP/SAML/OIDC])). api 함수 — `apiGet`/`apiFetch`+`readXsrfToken`(sessions.ts 선례). providerType은 `z.enum(['LOCAL','LDAP','SAML','OIDC','PAT','OAUTH'])`.
**REFACTOR**. JSDoc(에러코드 매핑) + 타입 z.infer.
**검증**. `pnpm --filter web test -- account-links` + `pnpm --filter web typecheck`.

### Task 4. W/i18n/account-link-labels.ts — 한국어 라벨

**메타**.
- agent: `frontend-engineer`
- files: [`W/i18n/account-link-labels.ts`, `W/i18n/account-link-labels.test.ts`]
- depends-on: []

**RED**. 라벨 키 존재/비어있지 않음 테스트(component-labels.test.ts 패턴) — 페이지 제목·빈상태·추가/해제/재인증 버튼·LDAP 폼·SSO 안내·에러코드별 메시지(account_already_linked/last_login_method/provider_unavailable/reauth_failed/step_up_required 등)·콜백 status(success/already_linked/conflict/error/reauth success·failed)·provider null fallback.
**GREEN**. `accountLinkLabels` 객체(per-feature 라벨 파일 관례). 계정 열거 0 일반 문구.
**REFACTOR**. 그룹 주석 + 콜론 종결 금지(한국어 마침표).
**검증**. `pnpm --filter web test -- account-link-labels`.

### Task 5. W/mocks/account-link-handlers.ts + fixtures — MSW (stateful)

**메타**.
- agent: `frontend-engineer`
- files: [`W/mocks/account-link-handlers.ts`, `W/mocks/account-link-fixtures.ts`, `W/mocks/handlers.ts`, `W/mocks/account-link-handlers.test.ts`]
- depends-on: [3]

**RED**. 핸들러 테스트 — GET /links(stateful store), GET /linkable-providers, POST /reauth(200 stepUpExpiresAt), POST /links(201 신규/200 멱등/409 conflict 시나리오 토글), DELETE /links/{id}(204/409 last_method/404), POST /links/sso/start(200 authorizeUrl), POST /reauth/sso/start(200). step-up·시나리오는 localStorage 플래그 토글(`e2e-msw-scenario-toggle-localstorage-flag`). 브라우저 시드가능 공유 store(`msw-derived-behavior-shared-store-e2e`).
**GREEN**. `accountLinkHandlers` + fixtures. **store 타입(⚠️ 리뷰 C4 명시)** — 링크 목록 상태는 모듈 인메모리 `Map`/배열(session-handlers.ts 선례), 시나리오 분기(409 last_method·conflict·503)는 localStorage 플래그 토글(`e2e-msw-scenario-toggle-localstorage-flag`). 둘 다 브라우저 메인스레드 MSW가 읽음(`msw-derived-behavior-shared-store-e2e`). handlers.ts에 `...accountLinkHandlers` 등록(알파벳순 그룹, **자기 import만 stage** `parallel-dispatch-precommit-hook-race`).
**REFACTOR**. store reset 헬퍼 + 시나리오 키 상수.
**검증**. `pnpm --filter web test -- account-link-handlers`.

### Task 6. W/auth/ hooks — query + mutations (invalidate-only)

**메타**.
- agent: `frontend-engineer`
- files: [`W/auth/useAccountLinksQuery.ts`, `W/auth/useLinkableProvidersQuery.ts`, `W/auth/useAccountLinkMutations.ts`, `W/auth/useAccountLinksQuery.test.tsx`, `W/auth/useAccountLinkMutations.test.tsx`]
- depends-on: [3]

**RED**. hook 테스트(QueryClient+MSW) — `useAccountLinksQuery`(목록), `useLinkableProvidersQuery`(추가 피커), `useAccountLinkMutations`(reauth/link/unlink/ssoLinkStart/ssoReauthStart). mutation 성공 시 links 쿼리 **invalidate-only**(setQueryData 금지). 에러는 호출자에 전파(토스트는 컴포넌트).
**GREEN**. `useSessionsQuery`/`useRevokeSessionMutation` 선례 패턴(`@/auth/`). queryKey 상수. reauth는 `stepUpExpiresAt` 반환.
**REFACTOR**. queryKey 모듈 상수 + JSDoc.
**검증**. `pnpm --filter web test -- useAccountLink useLinkableProviders`.

### Task 7. W/components/auth/AccountLinkList + AccountLinkCard

**메타**.
- agent: `frontend-engineer`
- files: [`W/components/auth/AccountLinkList.tsx`, `W/components/auth/AccountLinkCard.tsx`, `W/components/auth/AccountLinkList.test.tsx`, `W/lib/datetime.ts`, `W/lib/datetime.test.ts`]
- depends-on: [3, 4, 6]

**RED**. 컴포넌트 테스트(RTL+MSW) — 목록 렌더(provider 이름·타입 배지·마스킹 식별자·연결시각·마지막로그인/없음), `providerEnabled=false`→비활성 배지, null providerName→fallback, 빈 목록→빈상태+추가 CTA, "해제" 클릭→확인 다이얼로그(AlertDialog)→확인 시 `onUnlink(id)` 호출. 진행 중 버튼 disabled.
**GREEN**. SessionList Card 패턴. 해제 확인 = **`radix-ui` 직접 import `AlertDialog`**(⚠️ 리뷰 B1 — `components/ui/alert-dialog.tsx` 래퍼 부재. `import { AlertDialog } from 'radix-ui'`, SchemeInUseModal.tsx 선례). 날짜 포맷 = **신규 `W/lib/datetime.ts`의 `formatDateTime`**(⚠️ 리뷰 C1 — SessionList의 `formatDateTime`은 private non-export. 공유 util 신설해 신규 코드가 import, SessionList는 무변경). 라벨은 T4. 마지막수단 409·404는 부모가 토스트.
**REFACTOR**. AccountLinkCard 서브컴포넌트 분리 + JSDoc.
**검증 파일 추가**. `W/lib/datetime.ts`도 files에 포함(신규).
**검증**. `pnpm --filter web test -- AccountLinkList`.

### Task 8. W/components/auth/ReauthDialog — step-up 재인증 모달

**메타**.
- agent: `frontend-engineer`
- files: [`W/components/auth/ReauthDialog.tsx`, `W/components/auth/ReauthDialog.test.tsx`]
- depends-on: [3, 4, 6]

**RED**. 컴포넌트 테스트 — 수단 결정(hasLocalPassword→LOCAL 비번 폼 / 아니고 LDAP 링크 있음→LDAP username/password+providerId / 둘 다 없음(SSO 전용)→"SSO로 재인증" 버튼). LOCAL/LDAP 제출→`reauth` 호출→성공 시 `onStepUpGranted(stepUpExpiresAt)` 콜백. SSO 버튼→`ssoReauthStart`→`window.location.assign`(mock). 401 reauth_failed→에러 메시지(계정 열거 0). 400 reauth_fields_required→입력 검증.
**GREEN**. **`radix-ui` 직접 import `Dialog`**(⚠️ 리뷰 B1 — `components/ui/dialog.tsx` 부재. `import { Dialog } from 'radix-ui'`, AddMemberDialog.tsx 선례). providerId는 기존 LDAP 링크에서 취득(props). `window.location.assign` 주입 가능하게(테스트). 비번 제출 후 폼 초기화.
**REFACTOR**. 수단 분기 헬퍼 + JSDoc(step-up 윈도우 의미).
**검증**. `pnpm --filter web test -- ReauthDialog`.

### Task 9. W/components/auth/AddAccountDialog — linkable 피커 + LDAP 폼 / SSO 리다이렉트

**메타**.
- agent: `frontend-engineer`
- files: [`W/components/auth/AddAccountDialog.tsx`, `W/components/auth/AddAccountDialog.test.tsx`]
- depends-on: [3, 4, 6]

**RED**. 컴포넌트 테스트 — `useLinkableProvidersQuery` 결과로 피커 렌더(LDAP/SAML/OIDC, displayName). linkable 0건→비활성+안내(EC2). LDAP 선택→username/password 인라인 폼→제출→`onLink({providerId,username,password})`. SSO(SAML/OIDC) 선택→`onSsoStart({registrationId,providerType})`(부모가 step-up 게이팅+리다이렉트). 진행 중 disabled.
**GREEN**. **`radix-ui` 직접 import `Dialog`·`RadioGroup`**(⚠️ 리뷰 B1 — 래퍼 부재) + **`Select`는 `components/ui/select.tsx` 래퍼 실재**(그대로 사용). LDAP만 인라인 폼, SSO는 선택 즉시 부모 콜백. 멱등/충돌/503 메시지는 부모 토스트.
**REFACTOR**. 항목 렌더 헬퍼 + JSDoc.
**검증**. `pnpm --filter web test -- AddAccountDialog`.

### Task 10. W/routes/settings.account-links.tsx — 페이지 오케스트레이션 + 콜백 쿼리 + router 등록

**메타**.
- agent: `frontend-engineer`
- files: [`W/routes/settings.account-links.tsx`, `W/router.ts`, `W/routes/__tests__/settings.account-links.test.tsx`]
- depends-on: [6, 7, 8, 9]

**RED**. 라우트 테스트 — Page가 List+Add+Reauth 조립. step-up 게이팅 — mutating 동작 시 `stepUpExpiresAt` 미래면 진행, 아니면 ReauthDialog 오픈→성공 후 대기 동작 재개(SPA 내 LDAP 연결·해제). **403 `step_up_required` 응답도 ReauthDialog 트리거 — 음성 케이스 필수(⚠️ 리뷰 C3 — 사전게이팅 통과 후 서버 403 → 재인증. 클라 시각을 보안 1차 근거로 쓰지 않음을 테스트로 못박아 가짜그린 차단)**. SSO 연결=`ssoLinkStart`→`window.location.assign`. 콜백 쿼리(`?link=success|already_linked|conflict|error`·`?reauth=success|failed`) 마운트 시 토스트(sonner)+쿼리 제거(EC7). `?reauth=success`→step-up 부여 안내. router.ts에 `settingsAccountLinksRoute`(path `/settings/account-links`, `staticData:{requireAuth:true}, beforeLoad: requireAuthAndPasswordChanged`) 등록.
**GREEN**. Page+RouteAdapter(settings.sessions 패턴). `useStepUp` 소형 헬퍼(stepUpExpiresAt 상태 + requireStepUp(action) — 클라 시각은 round-trip 절감용, 403이 진실출처 주석). sonner `toast`. **콜백 쿼리는 TanStack `validateSearch`로 `?link`/`?reauth` 타입 선언(⚠️ 리뷰 C3 — `window.location.search` 직접 파싱 대신 타입 안전, issuesIndexRoute 선례)** 후 처리 + `navigate({search:{}})`/replaceState로 제거. **가드 = `requireAuthAndPasswordChanged`(⚠️ 리뷰 B3 — settings.sessions 동급 보안. mustChangePassword 사용자는 비번 변경 먼저)**. **router.ts 자기 변경만 stage**(병렬 race 회피).
**REFACTOR**. 콜백 status→메시지 매핑 헬퍼 + JSDoc. **router.ts L1·L212 주석의 라우트 카운트/목록 동기화(⚠️ 리뷰 B2 — 신규 라우트 추가분 반영, 기존 20/19 불일치도 함께 정정)**.
**검증**. `pnpm --filter web test -- settings.account-links` + `pnpm --filter web typecheck`.

### Task 11. E/account-links.spec.ts — Playwright E2E (D7)

**메타**.
- agent: `qa-engineer`
- files: [`E/account-links.spec.ts`, `E/fixtures/account-link-fixtures.ts`]
- depends-on: [5, 10]

**RED→GREEN**. Playwright(MSW 브라우저, `serviceWorkers:'block' 금지`). 시나리오 —
- S1 목록 표시(loginAsAlice→`/settings/account-links`, 링크 카드).
- S2 LDAP 연결(step-up 보유 시드→추가→LDAP 선택→폼→성공 토스트+목록 갱신).
- S4 SSO 연결 **분리**(EC9) — (a) SSO 선택→`/links/sso/start` 호출+`window.location.assign` 시도 검증, (b) `/settings/account-links?link=success` 직접 진입→토스트+쿼리 제거 검증.
- S5 해제(확인 다이얼로그→204→제거).
- S6 마지막수단 해제 409(시나리오 토글→"마지막 로그인 수단" 안내, 목록 불변).
- 콜백 `?reauth=success`/`?link=conflict` 메시지.
CSRF 쿠키 수동 시드(`worktree-stale-base-rebase-and-e2e-msw-traps`). 고유 이름 접두사 격리. **fixture userId는 RED 착수 전 `auth-handlers.ts` whoami userId 실측(grep) 후 동일 값 사용(⚠️ 리뷰 C5 — `e2e-fixture-whoami-userid-alignment`, 과거 반복 사고)**. 텍스트 중복 시 컨테이너 한정/exact(`playwright-getbyrole-exact-strict-mode`). 기존 auth E2E 회귀 0(`ui-pr-defer-e2e-regression-latent`).
**검증**. `pnpm --filter web test:e2e -- account-links` + 기존 auth E2E(login·session·signup) 회귀 0.

## Plan 메타

- task 수: 11 (백엔드 2 + 프론트 8 + E2E 1)
- 예상 wave: 백엔드 직렬(T1→T2, 모듈 test 컴파일 공유) → 프론트 직렬(T3→…→T10, worktree 커밋 race) → E2E(T11). 의존: T1·T4 무의존, T2←1, T3←2, T5←3·T6←3, T7/T8/T9←[3,4,6], T10←[6,7,8,9], T11←[5,10].
- TDD 강제: yes (각 task RED→GREEN→REFACTOR)
- 병렬 dispatch: 사실상 직렬(단일 백엔드 모듈 + 단일 프론트 worktree). bts-impl이 wave 계산하되 파일 겹침/모듈 결합으로 자연 직렬화.
- 추가 검증: 백엔드 ktlint+detekt(`--rerun-tasks`) + `:modules:identity-access:test`. 프론트 `pnpm verify`(lint+typecheck+test+build) + `pnpm test:e2e`.
- 마이그레이션 0. 신규 권한코드 0. cross-BC 0(identity-access view layer + 그 프론트).
- FR 카운트: D6/D7 완료 시 FR-AU-08 최종 완료 → product §2.8 마커 갱신(카운트는 머지 시 동기화 — 본 plan은 UI/E2E라 신규 FR 0).

## 리뷰 결과

타입=ui이나 신규 인증 엔드포인트+step-up/SSO 보안 흐름 포함 → design-review(설정페이지 관례추종 저위험) 스킵, **security + 프론트 eng 독립 ground-truth 리뷰**(`bts-review-plan-autoplan-overkill` 관례). 둘 다 실코드 1:1 대조.

### security-engineer 독립 적대적 리뷰 (2026-06-10)

**BLOCKER 0** — 인증/권한 불변식이 기존 컨트롤러와 일관, 참조 자산 전부 실재. **CONCERN 4 → 전부 plan 반영**.
- C1(반영, Task 2/3). Zod `discriminatedUnion('kind')` ↔ 백엔드 평면 nullable DTO drift 위험 → DTO에 `@JsonInclude(NON_NULL)`로 null 키 제거(LDAP 응답에 registrationId 키 부재). 가짜그린/계약 drift 핵심 지점.
- C2(반영, Task 3). `ssoReauthStart`도 POST mutation·CSRF 면제 밖 → `X-XSRF-TOKEN` 동반 명시(`sessions.ts` 일관).
- C3(반영, Task 10). step-up 사전게이팅 클라 시각 신뢰 경계 → 403 진실출처 유지, EC3 음성 테스트 필수.
- C4(반영, Task 2). linkable-providers는 필터 `authenticated()`라 PAT도 통과 → 컨트롤러 `resolveClaims` null이 유일 PAT 가드(기존 컨트롤러 동일 관례), KDoc 기록.
- **환각 점검**. `OidcProviderConfigRepository.findEnabled()`는 **구현체에만** 존재(Reader 인터페이스 아님) → Task 2 구현체 주입으로 정정. 그 외 `findEnabledByType`(신규 명확)·`findAllEnabled`·`SETTINGS_PATH`·콜백 쿼리셋·`StepUpService.isValid` 전부 실재.
- **OK 확인**. JWT전용/PAT403, sid JWT클레임전용·미노출, open-redirect 0(고정패턴 authorizeUrl), 콜백경로 일치, 계정열거 0, PII 마스킹, LDAP UUID 노출 안전(기존 surface), CSRF 선례 정합.

### frontend-engineer 독립 ground-truth 리뷰 (2026-06-10)

**BLOCKER 3 → 전부 plan 반영(auth라 무시 옵션 없음)**. **CONCERN 5 → 반영/문서화**.
- B1(반영, Task 7/8/9). `shadcn Dialog/AlertDialog/RadioGroup` 래퍼 **부재** → `radix-ui` 직접 import 패턴 정정(SchemeInUseModal/AddMemberDialog 선례). `Select`는 ui 래퍼 실재(유지).
- B2(반영, Task 10). router.ts 주석 라우트 카운트 동기화(기존 20/19 불일치도 정정).
- B3(반영, Task 10). 가드 `requireAuth`→`requireAuthAndPasswordChanged`(settings.sessions 동급, mustChangePassword 선차단).
- C1(반영, Task 7). `formatDateTime`은 SessionList private → 공유 util `W/lib/datetime.ts` 신설.
- C2(문서화). `useAccountLinkMutations.ts` 단일파일 5 mutation = 계정연결 응집성 위한 의식적 선택(선례 mutation당 1파일이나 본 기능은 묶음 유지가 명확). 허용.
- C3(반영, Task 10). 콜백 쿼리 `validateSearch` 타입선언(issuesIndexRoute 선례).
- C4(반영, Task 5). MSW store = 인메모리 Map(목록) + localStorage 플래그(시나리오) 명시.
- C5(반영, Task 11). E2E fixture userId는 whoami 실측 grep 후 정합.
- **OK 확인**. testDir `./e2e`, readXsrfToken/apiGet/apiFetch 실재, hooks `src/auth/`·컴포넌트 `src/components/auth/` 경로 정확, RouteAdapter·handlers spread·sonner·invalidate-only·i18n 파일관례 전부 정합.

종합. 양측 실코드 대조 — BLOCKER 3(프론트) + CONCERN 9 전부 plan 반영 완료(환각 2건 정정). 게이트 1 진입 가능.
