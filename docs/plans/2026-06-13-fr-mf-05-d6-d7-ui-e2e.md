# FR-MF-05 신뢰 디바이스 D6/D7 — 프론트엔드 UI + E2E

> slug: fr-mf-05-d6-d7-ui-e2e
> type: auth
> agent: security-engineer (impl 시 frontend-engineer D6 + qa-engineer D7 dispatch)
> 생성: 2026-06-13

## Brief

**사용자 원문**. FR-MF-05 신뢰 디바이스 D6/D7 진행 — 프론트엔드 UI(설정 화면 신뢰 디바이스 목록/취소, MFA verify 시 "이 디바이스 신뢰" 옵션) + E2E. 이것만 완료되면 계정 권한쪽(identity-access) BC 완료.

**classify**. type=auth, agent=security-engineer, primary_bc=identity-access, slug=fr-mf-05-d6-d7-ui-e2e.

**컨텍스트**. 백엔드 D1~D5는 PR #131로 완료. 본 작업은 프론트 전용(apps/web), 백엔드 변경 0(계약 소비). 선행 FR-MF-01~04 D6/D7과 동형 분할.

**백엔드 계약 요약**(PR #131, product §3.5).
- MFA verify 성공 + `trust_device=true` 동의 → `Set-Cookie trusted_device`(HttpOnly·Secure·SameSite=Strict·Path=/api/v1/auth·30일). DB는 SHA-256 token_hash만.
- 다음 로그인 시 `completeLogin`이 쿠키를 `verifyAndTouch` 검증 → 챌린지 생략. 실패 시 기존 `mfa_required` 폴백(fail-safe).
- 클라이언트 핑거프린트 기각(서버 불투명 토큰).
- 취소 = 수동(`GET` 목록 / `DELETE {id}`(IDOR 404) / `DELETE` 전체, JWT 전용·PAT 403) + 자동(비번변경·TOTP 비활성 시 revokeAll).
- 고정 30일(sliding 아님). 감사 TRUSTED_DEVICE_ADDED / REVOKED.

## 도메인 정리

- **BC**. identity-access (단일, cross-BC 0).
- **영향 엔티티**. `TrustedDevice`(백엔드 기존, #131). 프론트는 계약 소비만 — 신규 도메인/엔티티 0, 백엔드 변경 0.
- **새 용어**. "신뢰 디바이스"(Trusted Device) — glossary 미등록. ADR에 정의됨(MFA verify 동의 시 30일 챌린지 면제 디바이스, 식별=서버 불투명 토큰). glossary 추가는 게이트1에서 Maxi 일괄 확인.
- **기존 결정 충돌**. 없음. ADR `2026-06-13-trusted-device-mfa-exemption` 재확인 — D6/D7은 그 계약을 프론트로 노출하는 작업.
- **관련 ADR**. [docs/decisions/2026-06-13-trusted-device-mfa-exemption.md](../decisions/2026-06-13-trusted-device-mfa-exemption.md) (기존, 신규 ADR 생성 없음 — 선행 FR-MF D6/D7 동형).

### 소비할 백엔드 계약 (ground-truth, AuthController/TrustedDeviceController 실측)

| 용도 | 메서드 · 경로 | 응답 |
|---|---|---|
| 목록 | `GET /api/v1/auth/mfa/trusted-devices` | 200 `{devices:[{id:UUID, label:string?, createdAt:Instant, lastUsedAt:Instant?, expiresAt:Instant}]}` |
| 단건 취소 | `DELETE /api/v1/auth/mfa/trusted-devices/{id}` | 204 / 404 `{error:"not_found"}` / 403 `{error:"session_management_requires_interactive_login"}` / 401 |
| 전체 취소 | `DELETE /api/v1/auth/mfa/trusted-devices` | 204 (멱등) |
| 신뢰 동의 | `POST /api/v1/auth/mfa/verify` body에 `trust_device: boolean`(기본 false) | 성공 시 trusted_device 쿠키 발급, 응답 body는 기존 `TokenResponse` 그대로 |

- **PAT 차단**. 목록/취소는 JWT 전용(PAT는 403). 프론트는 대화형 세션이라 항상 JWT.
- **계약 주의**. `label` nullable, `lastUsedAt` nullable(미사용 시 null). 시각은 ISO Instant 문자열(직렬화 키 camelCase: createdAt/lastUsedAt/expiresAt).

## 스펙

전체 스펙. [docs/specs/2026-06-13-fr-mf-05-d6-d7-ui-e2e.md](../specs/2026-06-13-fr-mf-05-d6-d7-ui-e2e.md)

핵심 3줄 요약.
- 설정 `/settings/mfa`에 신뢰 디바이스 섹션(목록/단건취소/전체취소, `WebauthnSection` 동형 항상 노출).
- 로그인 MFA verify 단계 "이 디바이스 신뢰(30일)" 공통 체크박스 → TOTP·백업코드·보안키 **모든 수단**(Maxi 확정) verify body에 `trust_device`.
- 백엔드 변경 0(계약 소비). `sessions.ts`(목록/취소) + `verifyMfa`/`webauthn.ts`(trustDevice 인자) 동형 확장.

**Maxi 결정**. 체크박스 = 모든 MFA 수단 적용(보안키 포함 → `webauthn.ts` `authenticateWithSecurityKey` trustDevice 인자 동반).

## Brainstorming Check

✅ 통과 (자체 엣지 케이스 점검 10건, gap 0). 정의된 FR D6/D7이라 office-hours/brainstorming 풀 호출 스킵, 직접 sanity check.
핵심 함정 **E6 — 체크박스 상태가 `MfaCodeInput` key={mode} 재마운트에 리셋되지 않도록 부모 `LoginMfaStep`에 보관**(react-usestate-stale-key-prop · FR-MF-02 resolver 재마운트 교훈 동류). plan task에 명시.

## Plan

> 모든 task TDD red→green→refactor. 프론트 전용. **직렬 dispatch 권장** — worktree lint-staged 공유 git stash race(FR-MF-03/04 D6/D7 선례, `worktree-lint-staged-shared-git-stash-collision`·`parallel-dispatch-precommit-hook-race`). depends-on/files는 정확히 기재하되 bts-impl은 직렬 처리.

### Task 1. `api/trusted-devices.ts` — 목록/단건취소/전체취소 + Zod

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/trusted-devices.ts`, `apps/web/src/api/trusted-devices.test.ts`]
- depends-on: []

**RED**. `trusted-devices.test.ts` — `listTrustedDevices()` apiGet 언래핑 + Zod parse(label/lastUsedAt nullable), `revokeTrustedDevice(id)` DELETE + `X-XSRF-TOKEN` 헤더, `revokeAllTrustedDevices()` DELETE. 실패: 모듈 없음.
**GREEN**. `api/trusted-devices.ts` — `sessions.ts` 동형. `trustedDeviceSchema`(id uuid, label nullable, createdAt/expiresAt string, lastUsedAt nullable) + `trustedDevicesResponseSchema`. `readXsrfToken`은 `sessions.ts`에서 import.
**REFACTOR**. KDoc(경로/에러코드/IDOR 404·PAT 403 주석), 타입 `z.infer` 추론.
**검증**. `pnpm vitest run apps/web/src/api/trusted-devices.test.ts`

### Task 2. `verifyMfa` + `authenticateWithSecurityKey` — `trustDevice` 인자

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/mfa.ts`, `apps/web/src/api/mfa.test.ts`, `apps/web/src/api/webauthn.ts`, `apps/web/src/api/webauthn.test.ts`]
- depends-on: []

**RED**. `mfa.test.ts` — `verifyMfa(token, code, method, trustDevice=true)` → body `trust_device:true`, 기본 호출 `trust_device:false`(하위호환·기존 호출 무변경). `webauthn.test.ts` — `authenticateWithSecurityKey(token, trustDevice=true)` → verify body `trust_device:true`. refreshCallCount=0(raw fetch 유지).
**GREEN**. `verifyMfa` 4번째 인자 `trustDevice=false` → body에 `trust_device`. `authenticateWithSecurityKey` 인자 추가 → 내부 verify 호출 body 전달.
**REFACTOR**. KDoc에 trust_device(FR-MF-05) 한 줄.
**검증**. `pnpm vitest run apps/web/src/api/mfa.test.ts apps/web/src/api/webauthn.test.ts`

### Task 3. i18n 문자열 + MSW 핸들러

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/i18n/ko.ts`, `apps/web/src/mocks/trusted-devices-handlers.ts`, `apps/web/src/mocks/mfa-handlers.ts`, `apps/web/src/mocks/handlers.ts`]
- depends-on: []

**RED**. (a) `mfaStrings`에 신뢰 디바이스 키(섹션 제목·빈 상태·취소 라벨·확인·체크박스 라벨·기기명 fallback·사용기록 없음) — 컴포넌트/E2E가 참조. (b) `trusted-devices-handlers.ts` stateful(목록 store + DELETE 반영, `X-MSW-Reset` 헤더 리셋 — `e2e-msw-scenario-toggle-localstorage-flag`/`msw-derived-behavior-shared-store-e2e`). (c) `mfa-handlers.ts` verify 분기에서 `trust_device` 읽어 store 반영.
**GREEN**. 핸들러 작성 + `handlers.ts` 등록.
**REFACTOR**. 시드 헬퍼 정리.
**검증**. `pnpm vitest run apps/web/src/mocks` (해당 시) + typecheck.

### Task 4. `TrustedDevicesSection` 컴포넌트

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/auth/TrustedDevicesSection.tsx`, `apps/web/src/components/auth/TrustedDevicesSection.test.tsx`]
- depends-on: [1, 3]

**RED**. `TrustedDevicesSection.test.tsx` — 목록 2행 렌더(기기명/등록일/마지막사용/만료), label null→"알 수 없는 기기"(E1), lastUsedAt null→"사용 기록 없음"(E2), 빈 목록 안내+취소버튼 숨김(E3·S4), 단건 취소 인라인 확인→204→refetch(S2·E4), 전체 취소 인라인 확인→빈 상태(S3), 로딩/에러 분기.
**GREEN**. `TrustedDevicesSection.tsx` — useQuery `['mfa','trusted-devices']` + 단건/전체 취소 mutation(invalidate-only). 인라인 확인 state(모달 부재, `BackupCodesSection` 동형). 200줄 초과 시 Row 분리.
**REFACTOR**. 시각 포맷 헬퍼(InvalidDate 방어·E10), KDoc.
**검증**. `pnpm vitest run apps/web/src/components/auth/TrustedDevicesSection.test.tsx`

### Task 5. `MfaSettings` 통합

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/auth/MfaSettings.tsx`, `apps/web/src/components/auth/MfaSettings.test.tsx`]
- depends-on: [4]

**RED**. `MfaSettings.test.tsx` — `TrustedDevicesSection`이 TOTP 활성/비활성 무관하게 항상 노출(FR-5·`WebauthnSection` 동형).
**GREEN**. `<TrustedDevicesSection />`을 `<WebauthnSection />` 뒤에 추가.
**REFACTOR**. 주석 한 줄(항상 노출 사유).
**검증**. `pnpm vitest run apps/web/src/components/auth/MfaSettings.test.tsx`

### Task 6. 로그인 MFA "이 디바이스 신뢰" 체크박스

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/auth/LoginForm.tsx`, `apps/web/src/auth/LoginForm.test.tsx`]
- depends-on: [2, 3]

**RED**. `LoginForm.test.tsx` — 체크 후 코드 검증 → `verifyMfa` trustDevice=true(S5), 미체크 → false(S6), **mode 토글(TOTP↔백업코드) 후에도 체크 상태 보존(E6 — 핵심)**, 보안키 "인증" → `authenticateWithSecurityKey` trustDevice=true(S7).
**GREEN**. `LoginMfaStep`에 체크박스 state(부모 보관 — `MfaCodeInput` key={mode} 재마운트 무관). `MfaCodeInput`에 trustDevice prop 전달(verifyMutation에서 verifyMfa 4번째 인자), `handleWebauthnVerify`에 전달. 체크박스 UI는 `MfaCodeInput` 위 공통 위치(코드입력·보안키 버튼 공통 상단).
**REFACTOR**. 체크박스 라벨 i18n, KDoc(E6 보존 사유).
**검증**. `pnpm vitest run apps/web/src/auth/LoginForm.test.tsx`

### Task 7. E2E (Playwright)

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/trusted-devices.spec.ts`]
- depends-on: [5, 6]

**RED→GREEN**. `trusted-devices.spec.ts` — S1 목록 표시, S2 단건 취소, S3 전체 취소, S4 빈 상태, S5 로그인 신뢰 체크→다음 로그인 MFA 생략(MSW 우회 시드), S6 미체크 기본. 기존 mfa/login/webauthn E2E 회귀 0.
**검증**. `pnpm test:e2e --grep trusted-devices` + 기존 MFA/login/webauthn 스펙 회귀 실행.

## Plan 메타

- task 수: 7 (각 TDD 사이클)
- dispatch: **직렬**(worktree lint-staged stash race 회피, FR-MF-03/04 D6/D7 선례). depends-on 그래프상 논리 순서 1·2·3 → 4 → 5 → 6 → 7.
- 예상 시간: 약 25~30분(직렬)
- TDD 강제: yes
- 추가 검증: typecheck(tsconfig.app.json CI동일·`ci-typecheck-tsconfig-app-vs-local`), ESLint, vitest 전체 회귀, playwright(qa-engineer)
- 백엔드 변경 0 / 신규 의존성 0(절대규칙 #17)

## 리뷰 결과 (← /bts-review-plan 채움)
