# context-notes — pending mutation 누수 봉합 + 전역 가드 승격 (R2)

작업 중 내린 결정과 그 근거. 계속 덧붙인다.

## D1. 대상 집합을 정적 grep 이 아니라 1회 실측으로 만든다 (TODOS 처방 승계)

원 처방은 「34개 파일을 디렉토리 단위로 나눠 이주」였다. **실측이 그 전제를 뒤집었다.**

| 기준 | 값 |
|---|---|
| 34-set 중 mutation 을 아예 안 만드는 파일 | **13** (원천적으로 무관) |
| 34-set 중 mutation 은 만들지만 누수 0 | **14** (이미 안전) |
| 34-set 중 실제 누수 | **7** |
| 34-set **밖**인데 실제 누수 | **3** |
| 34-set·22-set **둘 다 밖**인데 실제 누수 | **2** |

즉 정적 목록은 **거짓양성 27/34(79%) · 거짓음성 3/10(30%)** 로 양방향 모두 틀렸다.
대상이 10파일 13건으로 줄어 **디렉토리 분할이 불필요**해졌고 한 PR 로 닫는다.

## D2. ★제3 관용구 — 「수동 게이트」가 두 정의 모두를 빠져나갔다

`RuleExecutionTraceRow.test.tsx` · `CreateIssueDialog.test.tsx` 는 `setTimeout` · `delay()` ·
무한 Promise 가 **전부 0건**인데 pending 을 남긴다. 실제 형태는 이렇다.

```ts
let release: () => void = () => {}
const held = new Promise<void>((resolve) => { release = resolve })   // ← executor 인자 1개
server.use(http.post('/api/v1/issues', async () => { await held; return HttpResponse.json(...) }))
// ... 검증 ...
release()   // 풀어주긴 한다. 그러나 정착을 기다리지 않고 테스트가 끝난다
```

- executor 인자가 **1개**(`resolve`)라 22-set 정의(`\(\(\) *=>` — 인자 0개)에 안 걸린다
- `setTimeout` · `delay()` 를 쓰지 않아 34-set 에도 안 걸린다
- **그런데 응답이 실제로 도착한다.** 무한 Promise(영원히 안 옴)보다 위험하다 —
  `release()` 이후 응답이 다음 파일이 도는 동안 도착해 `onSuccess` 가 실행된다

⇒ **정적 grep 기반 가드는 원리적으로 불가능**하다는 실증. 가드는 런타임이어야 한다.

## D3. 추적은 `MutationCache.prototype.build` 래핑으로 한다

선례 `AutomationYamlImportDialog.test.tsx:107` 은 파일 로컬 `lastQueryClient` 를 렌더 헬퍼가
채우는 방식이다. 전역화하려면 **모든 테스트 파일이 등록해 줘야** 하므로 쓸 수 없다.

`build` 를 감싸면 **mutation 을 실제로 만든 캐시만** 레지스트리에 들어온다. 그래서
「레지스트리가 비었다」가 「이 파일은 mutation 을 아예 안 만들었다」와 **동치**가 되고,
TODOS ②가 경고한 `?? []` 항진명제(레지스트리가 비면 무조건 통과)가 구조적으로 사라진다.

## D4. `process.on('unhandledRejection')` 경로는 쓰지 않는다

`setup.ts:19-45` 가 절대 금지로 못박았다. 리스너가 1개를 넘으면 vitest 가 물러나
**종료 코드가 1 → 0 으로 뒤집힌다**. `MutationCache.prototype` 래핑은 그 경로가 아니라 무관하다.

## D5. 측정을 2회 돌렸다 — 1회로 확정하지 않는다

체크포인트에 「재측정 자체가 4회 틀렸다」가 있고, pending 누수는 타이밍 의존일 수 있다.
2회차가 **파일::테스트 단위로 차이 0건**이라 대상집합이 안정적임을 확인한 뒤 확정했다.
(메모리 `flaky-determination-needs-repeat-not-single-contrast` — 단일 대조로는 flaky 판정 불가.)

## D6. 전역 `afterEach` 는 수집 → 레지스트리 비우기 → 단언 순서로 한다

단언이 throw 하면 그 뒤 코드가 안 돌기 때문에, 레지스트리 정리를 **단언 앞**에 둔다.
정리하지 않으면 같은 pending mutation 이 뒤따르는 모든 테스트에서 다시 잡혀 **연쇄 실패**한다.
`build` 가 mutation 마다 호출되므로 캐시는 다음 mutation 때 재등록되고 추적 손실은 없다.

## D7. ★★가드가 자기 자신을 부풀렸다 — 봉합이 새 회귀

가드를 켠 첫 실행에서 `FavoriteButton` 이 **5건** red 였다. 측정값은 3건이다.
추가 2건(S6a·S7b)의 실패 사유는 pending 단언이 **아니라**
`TestingLibraryElementError: Found multiple elements with the role "button"` 이었다.

**기전.** 전역 `afterEach` 의 `expect` 가 throw 하면 **같은 root 레벨에 등록된 RTL 자동 cleanup 이
실행되지 않는다.** DOM 이 남고 다음 테스트가 연쇄 실패한다. 소요 시간이 20ms 대(진짜 위반) 대
1000ms 대(waitFor 타임아웃)로 갈린 것이 서명이었다.

**처방.** `afterEach` 맨 앞에서 `cleanup()` 을 **직접** 부른다. 멱등이라 RTL 이 나중에 또 불러도
무해하고, unmount 뒤에 재는 편이 판정도 엄밀하다(컴포넌트가 사라졌는데도 남은 것만 누수로 잡힌다).

**교훈.** 전역 훅에 throw 하는 단언을 넣으면 **그 뒤에 등록된 정리 훅을 전부 인질로 잡는다.**
같은 형태(전역 afterEach 단언)를 추가할 때마다 재발한다.

## D8. ★★측정조차 2건을 놓쳤다 — 목록을 정본으로 믿지 말 것

가드를 켠 전량 RED 는 16건이었고, `WatchersSection.test.tsx` S9a·S9c **2건이 측정 목록에 없었다.**
소스를 보면 `FavoriteButton` S7a·S7c 와 **구조가 완전히 동일**하다 — 같은 패턴을 복사한 형제 파일이다.

**기전.** 프로브의 `afterEach` 는 `import` 위치 때문에 `setup.ts` 의 `afterEach` 보다 **늦게** 실행됐고,
훅 사이의 await 경계에서 마이크로태스크가 flush 되며 그 사이 정착한 것을 놓쳤다.
가드는 `setup.ts` 안에서 더 이른 시점에 재므로 더 엄격하다.

**어느 쪽이 옳은가.** 엄격한 쪽이다. 선례 `AutomationYamlImportDialog.test.tsx:107` 도 파일 레벨
`afterEach` 에서 **가장 이른 시점**에 판정하며, 주석이 그 의도를 「파일 안에서 즉시 드러낸다 —
옆 파일로 번지기 전에」라고 적었다. 그리고 「의도적으로 붙잡은 것은 테스트가 명시적으로 정리한다」가
더 좋은 규율이다.

**교훈.** 측정 목록은 **하한**이지 정본이 아니다. 같은 관용구를 쓰는 형제 파일이 있으면 목록에
없어도 의심하고, **가드를 켠 red 목록**을 정본으로 삼는다.

## D9. 가드 주석이 기존 판별식을 건드렸다

`msw-single-setupserver.test.ts` 가 `pending-mutation-guard.ts` 를 위반으로 지목했다.
내가 주석에 34-set 정의를 **문자 그대로** 옮겨 적었고 거기에 그 판별식이 감시하는 문자열이 있었다.

선례가 이미 같은 문제를 `NEEDLE = 'setupServer' + '('` 런타임 조립으로 풀어 뒀는데, 그건 판별식
**자신**의 회피책이고 **새로 들어오는 파일**은 각자 피해야 한다. 주석에서 정의를 풀어쓰는 것으로 해결했다.

**교훈.** 소스를 훑는 판별식이 있는 저장소에서는 **주석도 소스다.** 정의를 인용할 때 원문 복사는
판별식을 깨운다.

## D10. ★★★「60% 느려졌다」는 내가 대조군을 잘못 잡아 만든 유령이었다

**결론부터.** 전량 기준 성능 회귀는 **없다.** 그런데 나는 있다고 진단했고, 그 근거가 틀렸다.

**틀린 비교.**

| 측정 | 값 | 무엇인가 |
|---|---|---|
| 프로브 1·2회차 | 226.11s · 225.19s | **프로브가 켜진 상태** (main 아님) |
| 가드 1차 (cleanup 방식) | 360.10s | |
| 가드 2차 (expect.soft) | 332.32s | |
| main baseline (오염) | 1646.07s | 워커 시작 실패 다수 · 563/577 만 실행 |
| **main baseline (깨끗)** | **340.03s** | 577/577 · 워커 실패 0 |

**332.32s(가드) vs 340.03s(main).** 차이가 없다. 오히려 가드 쪽이 근소하게 빠르다.
내가 회귀로 읽은 226 → 360 은 **main 과 비교한 것이 아니라** 프로브 켜진 상태와 비교한 것이고,
226 과 340 은 **세션 초반과 후반의 머신 상태 차이**다. 같은 코드가 조건에 따라 226~340 을 오간다.

**★이 실수의 이름.** 메모리 `fr-ux-14-f14-card-density-done` 의 「선재 판정 대조군은 반드시 main」과
`measured-the-wrong-thing-twice` 의 「무엇을 고쳤나가 아니라 무엇을 안 쟀나를 물어라」가
**그대로 재현됐다.** 대조군을 손에 있던 값(226s)으로 잡은 것이 원인이다.
**대조군은 반드시 main 에서, 같은 시간대에, 워커 실패 0 을 확인하고 잰다.**

**그럼에도 처방 2가지는 유효하다 — 근거가 다르다.**
`src/mocks` 49파일 대조 실험은 **같은 시점에 연속으로** `setup.ts` 만 바꿔 잰 통제 실험이다.

| 상태 | Duration | setup |
|---|---|---|
| 배선 없음 | 15.73s | 24.42s |
| 배선 있음 | 20.94s | **43.01s** (+76%) |
| RTL import 제거 후 | 22.03s | **36.63s** |

`setup.ts` 는 **모든 테스트 파일**에서 실행되므로, 거기서 `@testing-library/react` 를 static import
하면 RTL 을 쓰지 않는 순수 유닛 테스트가 그 로드 비용을 문다. 이 비용은 실재하고 제거하는 편이 옳다.
전량에서 상쇄돼 보이지 않을 뿐이다(대부분의 파일이 이미 RTL 을 쓴다).
`expect.soft` 는 성능과 무관하게도 **D7 의 함정을 더 싸게 푸는 해법**이라 그대로 둔다.

**처방 두 가지.**
1. `cleanup()` 을 부르지 않는다 → 대신 **`expect.soft`**. D7 의 「단언이 throw 해서 RTL cleanup 을
   인질로 잡는다」 문제를 cleanup 을 부르지 않고 푼다. 실측으로 확인했다 —
   soft 는 테스트를 실패로 표시하면서 afterEach 를 끝까지 실행시킨다.
   ⇒ **D7 의 처방이 D10 때문에 교체됐다.** 같은 문제의 더 싼 해법이다.
2. `settlePendingMutations` 안의 RTL 을 **동적 import** 로 내린다. 이 함수를 부르는 쪽은
   어차피 RTL 로 렌더한 테스트라 거기서 로드해도 새 비용이 아니다.

**교훈 둘.**
1. `setup.ts` 에 무엇을 import 하는지는 **전 스위트 비용**이다. 전역 훅을 추가할 때는 초록만 보지 말고
   소요 시간도 본다.
2. **더 중요한 쪽** — 시간을 볼 때 대조군을 아무 값이나 쓰면 없는 회귀를 만들어낸다.
   벽시계는 머신 상태로 ±50% 흔들린다. 대조군은 **main 에서 · 같은 시간대에 · 워커 실패 0 확인**
   세 조건을 모두 만족해야 한다. 하나라도 빠지면 그 수치로 진단하지 않는다.

## D11. ★★★게이트 2 — 내가 「비-공허 짝」이라 부른 것이 정작 자기 목적을 못 지켰다

리뷰 5종(절대규칙 · testing · security · maintainability · performance)에서 BLOCKER 0 · 지적 15건.
**가장 아픈 것은 겹친 지적이다.** 서로 모르는 두 리뷰어가 각자 다른 실험으로 같은 결론에 도달했다.

**결함.** `setup.ts` 의 전역 `afterEach` 단언을 통째로 죽여도 계약 테스트 4/4 와 봉합 파일 9/9 가
**전부 초록**이었다. 이유는 단순하다 — ①~④가 전부 라이브러리 함수(`collectPendingMutations` ·
`trackedCacheCount` · `settlePendingMutations`)를 **직접** 부르고 그 `afterEach` 를 거치지 않는다.
①마저 약했다. 플래그만 `true` 로 두고 감싸기를 건너뛰어도(뮤테이션 M-B) ①은 통과했다.

⇒ 메모리 `fr-ux-13-f16-f7-epic-control-test-contract-done`(봉인 자체가 영구초록) ·
`two-lists-never-check-each-other` 와 **같은 양식**이다. 내가 그 교훈을 인용하면서 같은 함정을 팠다.

**처방.** 소스 훑기 계약 ⑦⑧⑨ 신설 (`msw-single-setupserver.test.ts` 관례).
⑦ 수집+단언 존재 · ⑧ 수집이 정리보다 앞 · ⑨ RTL static import 부재.

**★그리고 그 처방 자신을 뮤테이션으로 검증했다** (코드리뷰가 요구한 절차).

| 뮤테이션 | 잡은 계약 |
|---|---|
| `expect.soft` → `expect` | ⑦ |
| 수집/정리 순서 뒤바꿈 | ⑧ |
| RTL static import 추가 | ⑨ |
| 수집 호출 제거 | ⑦ + ⑧ |

각 뮤테이션이 **정확히 대응하는 계약만** red 를 냈다 — 무차별 red 가 아니다.

## D12. waitFor 기본 상한이 flaky 를 심고 있었다

`settlePendingMutations` 가 RTL 기본 `asyncUtilTimeout` 1000ms 를 그대로 물려받았다.
호출부 6곳이 실제 지연(50~200ms)을 기다리는데, **이 저장소의 `vitest.config.ts` 주석이 바로 그
위험을 이미 기록해 뒀다** — 전량 실행 시 워커 기아로 53ms 작업이 벽시계 5000ms 를 넘는다(약 94배).
그래서 `testTimeout` 을 15초로 올렸는데, **config 의 `testTimeout` 은 `waitFor` 내부 예산에 도달하지
못한다.** 200ms 가 5배만 밀려도 상한을 친다.

더 나쁜 것은 그때 뜨는 메시지다 — 「정착하지 않은 mutation N건」. **스케줄러 기아를 진짜 누수로
오진**하게 만든다. ⇒ `{ timeout: 10_000, interval: 25 }` 명시.

**교훈.** 라이브러리 기본값을 물려받을 때는 **그 기본값이 이 저장소의 알려진 지연 특성과 맞는지**
확인한다. 답이 이미 옆 파일(`vitest.config.ts`) 주석에 적혀 있었다.

## D13. 진단 정보와 비밀 노출이 같은 자리에서 충돌했다

security 는 「`variables` 값을 단언 페이로드에 담지 마라」(실패 시 `currentPassword` 가 CI 로그에
그대로 찍힌다 — 프로브로 재현), maintainability 는 「`settlePendingMutations` 가 개수만 알려주고
어느 mutation 인지 버린다」를 각각 지적했다. **둘은 같은 자리의 반대 요구다.**

⇒ **키 이름만 남기는 것**으로 동시에 해소했다. 값은 어떤 경로로도 나가지 않고, 어느 테스트가
범인인지는 키 이름으로 찾을 수 있다. 계약 ⑥이 이 성질을 봉인한다(값 문자열이 직렬화 결과에
없고 키 이름은 있어야 한다).
