<!-- FR-PJ PR-2 plan — R6 백필 러너 + R6-B 재시드 생존 + FR-PJ-01 프로젝트 생성 백엔드. 스펙 §9.2 6분할 중 둘째 -->

# FR-PJ PR-2 — R6 백필 러너 + R6-B 재시드 생존 + FR-PJ-01 프로젝트 생성 백엔드

> slug: fr-pj-pr-2-r6-r6-b-fr-pj-01
> type: backend
> agent: backend-engineer
> 생성: 2026-07-17

## Brief

**사용자 원문.**

```
FR-PJ PR-2 — R6 백필 러너 + R6-B 재시드 생존 + FR-PJ-01 프로젝트 생성 백엔드
```

**classify 결과.** `type=backend` · `agent=backend-engineer` · `slug=fr-pj-pr-2-r6-r6-b-fr-pj-01` · `primary_bc=null`

**진실의 출처 = 기존 스펙.** `docs/specs/2026-07-17-project-management-crud.md` (826줄).
이 PR 은 그 스펙 §9.2 PR 분할표의 **둘째 PR** 이다. 스펙이 이미 있으므로 `/bts-domain` · `/bts-spec` 은 건너뛴다.

**선행 = PR-1 (#277, `6c4d2861a`).** FR 123→128 (`FR-PJ-01~04` + `FR-PM-10`, 첫 프리픽스 신설).
기존 plan `docs/plans/2026-07-17-project-management-crud.md` 는 **PR-1 전용**이라 재사용하지 않는다.
단 그 파일 `## 구현 진행` 절의 **인계 사실**은 이 PR 도 물려받는다.

**범위 (스펙 §9.2 기준 — plan 단계에서 전수 대조할 것).**

| 항목 | 내용 |
|---|---|
| R6 | 백필 러너 — 빈 DB 부팅 시 워크플로우 스킴 기본 매핑 생성 (멱등) |
| R6-B | 재시드 생존 — 매핑 존재 상태에서 워크플로우 YAML 구조 변경해도 부팅 성공 |
| FR-PJ-01 | 프로젝트 생성 백엔드 (`POST /projects`) |

## 미결 결정 (plan 단계 게이트 — 스펙이 "plan 단계에서 확정 필요"라 명시)

**D-A. 재시드 생존 전략 — ✅ (a) 확정 (Maxi, 2026-07-17).**

**(a) 매핑 정리 후 재연결.** `deleteWorkflow` 전에 매핑을 DELETE, `insertWorkflow` 후 key 기준 재INSERT. **마이그레이션 0.**
- 근거 — 매핑이 비는 창이 없어 백필 실패 시 무증상 422(R6 재발)를 원천 차단. `seedAll` 이 단일 tx(REQUIRED)라 "같은 tx 면 무해"가 실제 성립(실측). `seed-project.sql:57` 이 이미 *"workflow_id 는 재시드 시 UUID 가 바뀌므로 key 로 조회"* 를 관례로 못박음 — (a)의 "key 기준 재INSERT"는 신규 발명이 아님.
- 기각 — (b) CASCADE 는 매핑 조용히 소멸 → 백필 실패 시 무증상 422 = R6 재발(스펙이 "위험"). (c) UPSERT 는 `YamlSeedService` 핵심 재설계 = 폭발 반경 최대.

**D-A' 재연결 위치 — ✅ `YamlSeedService` 가 같은 tx 에서 직접 (Maxi, 2026-07-17).**
- 지우기 전 매핑 기억 → 재적재 → 즉시 재연결까지 **한 트랜잭션**. 매핑이 비는 창 0.
- 백필 러너(R6)는 **빈 DB 초기화 전용**으로 남아 이중 안전망. R6(빈 DB 백필)와 R6-B(재시드 생존)가 서로 다른 시점을 지킨다.
- DRY 부채 — 재삽입 로직이 백필 러너 + 서비스 재연결 두 곳. **공통 헬퍼 1개로 뽑는다**(예: `SchemeIssueTypeMappingRepository` 에 key 기준 재연결 메서드). 착시 방지.

> **★ (a)의 실측 근거.** `V201:137-145` 에 스킴 4종 CASE 매핑 INSERT 가 **이미 있다**(부팅 시 workflows 0행이라 0건 삽입될 뿐). 백필/재연결 SQL 은 이 원본을 그대로 물려받는다.

**D-B. BC 표기 방식.** 스펙 `:576` 이 "plan 단계에서 정한다"고 지시.
`bc:<context>` 라벨이 **단수 전제**인데 이 PR 은 BC 를 여럿 건드린다 (D10 명시 고지 예외).
→ **`bc:project-workflow` + `bc:issue-tracking` 라벨 2개 부착** + PR 본문에 D10 근거(§9.2 `:575`) 첨부. (라벨이 복수 부착을 막지 않으므로 표기 자체는 가능. codereview 가 D10 근거 확인.)

## ★ 개수 검증 (직접 셈 — 인용 안 함)

이 저장소가 5연속 데인 패턴 ([[spec-stated-count-becomes-blindfold]] · [[orchestrator-instruction-counts-are-blindfolds]]).

| 숫자 | 출처 A | 출처 B | 판정 |
|---|---|---|---|
| 이 PR 의 BC 수 | 체크포인트 "2개" | 스펙 `:8` "**3개 BC**" | ✅ **2개 확정** — 둘 다 맞고 범위가 다름 |
| FR 총수 (DoD-8) | 스펙 DoD-8 "**128**" | `#281` 이 129 로 올림 | ✅ **129** — 스펙 숫자가 stale |

**BC 2개 확정 — 근거 3중 (전부 스펙 직접 인용).**
- `:563` §9.2 표 — PR-2 행이 `project-workflow + issue-tracking` **(2개 — D10 예외)**
- `:573` — *"PR-1 에 두면 PR-2 는 **소비만** 하므로 PR-2 의 BC 수가 **2개로 유지**된다. 포트를 PR-2 에 두면 **3 BC**"*
- `:575` — *"**PR-2 만 BC 2개다.**"*

> `:8` 의 "3개 BC" 는 **스펙 전체(PR 6개) 범위**다 (issue-tracking + identity-access + project-workflow).
> 두 숫자는 모순이 아니라 **가리키는 범위가 다르다.** 어느 쪽도 인용하지 않고 §9.2 를 직접 읽어 판정했다.

> **★ 2개 유지의 전제 조건.** PR-1(#277)이 `ProjectMembershipWritePort` + 어댑터를 이미 실었기 때문에 성립한다.
> 만약 이 PR 이 포트나 어댑터를 **건드리면 그 순간 3 BC 가 되어 D10 고지 범위를 넘는다.** 소비만 한다.

**DoD-8 정정.** 스펙은 `128` 이라 적었으나 `#281`(FR-UX-06 등록)이 **129** 로 올렸다.
스펙 자신이 *"기존 숫자 복사 금지"* 라 적어둔 덕에 막힌 함정이다. **이 PR 은 카운트를 바꾸지 않으므로 129 통과가 기준**이다.
(FR-PJ-01 **완료 마킹은 이 PR 몫이 아니다** — D6 UI 가 PR-5 라 automation 선례 DEC-18 동형. 진척 122/129 불변.)

## 이 PR 에 걸리는 것만 추림 (스펙 §7 제약 · §8 DoD 에서 선별)

**시나리오 — 6/12.** S1(생성 해피) · S2(권한없음 403) · S3(미인증 401) · S4(중복키 409) · **S10**(빈 DB → 이슈 201, R6) · **S11**(YAML 변경 후 재부팅, R6-B).
나머지 — S5~S9 는 PR-3/PR-4(목록·아카이브), S12 는 PR-1 에서 완료.

**DoD — 6/11 이 이 PR 몫.**

| DoD | 내용 | 비고 |
|---|---|---|
| DoD-2 | 빈 DB 부팅 → 프로젝트 생성 → **이슈 생성 201** | S10 · Testcontainers |
| DoD-3 | YAML 구조 변경 후 **재부팅 성공** | S11 · Testcontainers |
| DoD-4 | 권한 테스트가 `@ActiveProfiles("prod")` 에서 통과 | C1 |
| **DoD-9** | **`CREATE_PROJECT` grant 를 가진 비-SYSTEM_ADMIN 이 만들 수 있다** (`@ActiveProfiles("prod")`) | **B8 — 없으면 override 누락이 초록불 통과하고 D7 이 조용히 뒤집힌다** |
| **DoD-11** | 생성 도중 예외 주입 시 `projects` · `project_memberships` **둘 다 롤백** | I1 실증 · **C10 필수** |
| DoD-6 | `:modules:app:test` 통과 (9 BC prod 조립) | C7 |
| DoD-7 | `verify-master-plan.sh` 통과 | 129 (위 정정) |

제외 — DoD-5/DoD-10 은 아카이브(PR-4), DoD-1 은 전 시나리오라 PR 분할상 부분 충족, DoD-8 은 카운트 불변.

**제약 — 이 PR 에 실제로 걸리는 것.**

| C | 내용 | PR-2 적용 |
|---|---|---|
| **C1** | `NonProdAllowSystemAdminResolver` 가 `@Profile("!prod")` + **항상 true** | 🔴 권한 테스트를 기본 프로파일로 짜면 **무의미하게 통과**. `@ActiveProfiles("prod")` + 실제 grant 시드 필수 |
| **C2** | 음성 가드는 **본문 판별자** 필요 | 🔴 *"여전히 403"* 은 vacuous — 컨트롤러가 없어도 403 |
| **C6** | 새 cross-BC 의존 소비 → OpenApi 테스트 `@MockBean` 동반 | 🔴 issue-tracking 이 `ProjectMembershipWritePort` 를 **처음 소비**한다 |
| **C7** | prod 조립 부팅 재검증 (머지 전 rebase + `:modules:app:test`) | 🔴 |
| **C8** | Flyway V번호 동시 브랜치 충돌 — 머지 직전 재확인 | 🟡 (a) 선택 시 마이그레이션 0 이라 무관, (b)/(c) 면 해당 |
| **C9** | 백엔드 CI 부재 — 로컬 검증 필수 | 🔴 |
| **C10** | 🛑 **I1 롤백 테스트를 표준 베이스에 얹으면 거짓 red** | 🔴 아래 별도 항목 |
| C3 | `init_codegen.sql` 미러 (jOOQ) | 🟡 전략 확정 + 실측 후 판정 |
| C4/C5 | 권한 시드 카운트 가드 · default 메서드 | ⚪ PR-1 에서 처리됨 |

### 🛑 C10 — DoD-11 이 거짓 red 를 내는 기전 (착수 전 필독)

`IssueTestcontainersBase.kt:112` 가 `DSL.using(dataSource, SQLDialect.POSTGRES)` — **tx-aware 가 아니다**. 동형 10곳 이상.
tx-aware 로 감싼 것은 **`IssueImportAdapterTest.kt:135` 단 1곳**이고 `:126-130` 이 함정을 자인한다 —
*"createIssue 의 INSERT 가 즉시 커밋돼버려 이후 `setRollbackOnly()` 가 무력화된다 … 다른 기존 통합 테스트는 **실패 지점이 항상 insert 이전(validate)이라 이 gap 을 드러낸 적이 없었다**"*.

→ DoD-11 컨텍스트를 **`DSL.using(TransactionAwareDataSourceProxy(dataSource), ...)` 로 명시 고정**한다.
안 하면 `projects` INSERT 가 tx 밖에서 자동 커밋돼 *"멤버십만 롤백, projects 잔존"* 이라는 **거짓 red** 가 뜨고,
**원인을 포트 설계로 오진**하게 된다. 선례 — `IssueImportAdapterTest.kt:135` · `AgilePlanningTestcontainersConfig.kt:121`.

관련 메모리 [[transaction-aware-dslcontext-rollback-test-gap]] — 거짓 red 기전이 **2개**다 (손수 배선 `DSLContext` · `@JdbcTest` 가 `@Transactional` 메타).
**main 코드는 무관하다** — `DSL.using` main 히트 0건으로 전부 주입된 tx-aware 빈.

## ★ 실측이 뒤집은 스펙 사실 (구현 에이전트 필독 — 스펙을 그대로 믿지 말 것)

두 Explore 에이전트 + controller 직접 검증으로 확인. 스펙/에이전트 서술 중 **틀린 것**만 추림.

**S-1. 스펙 §4.4 포트 시그니처가 낡았다 (머지된 코드가 진실).**
스펙은 `addMember(projectId, userId, role: String)` 이라 적었으나 PR-1 이 실제로 만든 건 **`addCreatorAsAdmin(projectId, userId)`** — 역할 파라미터 없음(의도적).
포트 KDoc — *"역할을 열어두면 '임의 사용자를 임의 프로젝트의 관리자로' 만드는 무가드 프리미티브의 표면만 넓어진다"*.
→ FR-PJ-01 은 `addCreatorAsAdmin(projectId, creatorId)` 를 호출한다. 멱등 아님(UNIQUE), `@Transactional` 없음(호출자 tx 참여가 존재 이유).
**스펙 §4.4 코드블록은 codereview 단계에서 정정 대상**(전수 동기화). 단 이 PR 은 포트를 **소비만** 하므로 포트 파일 자체는 안 건드린다.

**S-2. R6-B 진단의 절반이 틀렸다 — 원인은 CASCADE 가 아니라 RESTRICT.**
`YamlSeedService` KDoc 3곳(`:143-145`·`:325-328`·`:462-466`)이 *"CASCADE 로 post-action 소실 — 알려진 한계"* 라 적지만,
**실제 부팅 차단 원인은 `workflow_scheme_issue_type_mappings.workflow_id → workflows(id) ON DELETE RESTRICT`**(`V201:84`)다.
매핑이 있으면 `deleteWorkflow`(`YamlSeedService.kt:467-472`, jOOQ typed DSL — 평문 아님)가 **FK 위반으로 먼저 죽어** CASCADE 시나리오엔 도달조차 못 한다.
→ (a) 구현 시 **매핑 DELETE 를 `deleteWorkflow` 앞에** 둬야 하는 이유가 바로 이 RESTRICT. `deleteWorkflow` KDoc 도 RESTRICT 를 언급 안 해 정정 후보.

**S-3. 기존 테스트가 R6 를 은폐한다 — 회귀 테스트가 이걸 뚫어야 한다.**
`WorkflowSchemesMigrationIntegrationTest.kt:331` 이 *"V004 가 매핑 4행을 만든다"* 로 통과하는 **유일한 이유는 `:148` 헬퍼가 손수 심기 때문**. 실제 Flyway 는 0건.
issue-tracking 통합 테스트 **22개도 전부 매핑을 fixture 로 손수 INSERT**한다 — R6 가 테스트에서 안 잡혀온 구조적 이유.
→ **S10 회귀 테스트(DoD-2)는 매핑을 손수 심지 않고** 백필 러너가 채우는지 봐야 한다. 손수 심으면 R6 를 또 은폐한다.

**S-4. issue-tracking 인증 관례는 `@PreAuthorize` 가 아니라 `CurrentActor`다 (에이전트 오판 정정).**
에이전트가 "issue-tracking 은 `@PreAuthorize` 0건이라 인증 선례 없음" 이라 했으나 **틀렸다** — `CurrentActor.kt`(`issue-tracking/.../rest/CurrentActor.kt`)를 컨트롤러 10곳+가 이미 쓴다(Comment·Watcher·Attachment·Worklog·Issue·IssueMove·CycleTime·Epic·Require2fa).
FR-PJ-01 컨트롤러는 **`CurrentActor.current()` 로 actor 추출**(스펙 PJ1-2·§2.2 정본, placeholder `SYSTEM_ACTOR_UUID` 금지) + **`CREATE_PROJECT` 명시 호출 게이트**(`@PreAuthorize hasRole` 금지 — PAT 탈락).
권한 판정은 **`SystemPermissionResolver.hasGlobalPermission` 으로만**(§인계 1). `GlobalPermissionGrantRepository.hasGrant` 직접 호출 금지(SYSTEM_ADMIN 탈락).

**S-5. `SystemPermissionResolver` 빈 충돌 위험은 해소돼 있다 (에이전트 "추측" 반증).**
`ProjectRequire2faApplicationService` 가 **이미 issue-tracking 에서 이 타입을 주입**받아 잘 돈다. `IdentityAccess...`(@Profile 없음)가 조립·prod 에서 유일 실 구현, `NonProd...`(@Profile("!prod"))는 issue-tracking 단독 부팅 fallback.
→ FR-PJ-01 도 같은 패턴을 그대로 따르면 된다. **신규 빈 배선 불필요.**

**S-6. `CREATE_PROJECT` 는 공용 상수가 아니다 — grant 행도 0건.**
DB CHECK(`V036:26`)와 서비스 private 화이트리스트(`GlobalPermissionGrantService.kt:129`)만 존재. issue-tracking 에서 쓸 공용 상수 없음.
→ FR-PJ-01 이 `hasGlobalPermission(actor, "CREATE_PROJECT")` 를 부르려면 **문자열 리터럴 또는 신규 상수**가 필요. (DB grant 는 0건이라 DoD-9 테스트가 grant 를 직접 시드해야 함 — C1.)

**S-7. issue-tracking 다음 마이그레이션은 V037.** (에이전트 실측 — main 최신 V036=pgmq_queue_automation_events.) **단 (a) 전략이라 이 PR 은 마이그레이션 0.** project-workflow 최신 V202, V203 free 확인.

## 도메인 정리 (← /bts-domain 채움)

**스킵.** 기존 스펙이 도메인 정리를 포함한다 (`docs/specs/2026-07-17-project-management-crud.md`).

## 스펙 (← /bts-spec Phase A 채움)

**스킵.** 기존 스펙 `docs/specs/2026-07-17-project-management-crud.md` 가 정본.
이 PR 이 참조할 절 — §9.2 (PR 분할표) · §2.5 (R6/R6-B) · S10 (`:127`) · S11 (`:137`) · §4.3 · `:576` (BC 표기 지시).

## Brainstorming Check (← /bts-spec Phase B 채움)

**스킵** (위와 동일 사유).

## Plan

> 파일 경로 접두사 생략 표기 — `PW/` = `backend/modules/project-workflow/src/`, `IT/` = `backend/modules/issue-tracking/src/`, `APP/` = `backend/modules/app/src/`.
> 모든 통합 테스트는 tembo pgmq 이미지 필수(§H 실측 — V201 이 pgmq 요구). project-workflow 는 공유 base 부재라 컨테이너 선언 개별 복제.

### 세 갈래 — R6(백필) · R6-B(재시드 생존) · FR-PJ-01(생성)

- **R6-B 재연결 헬퍼가 R6 백필과 재사용되므로 헬퍼를 먼저** (T1). DRY 부채 해소(D-A').
- FR-PJ-01(T7~T11)은 issue-tracking 파일이라 R6/R6-B(T1~T6, project-workflow)와 **파일 무교집합 → 병렬 가능**.
- 최종 통합 S10(T12)만 양쪽 다 필요 → `depends-on` 으로 직렬.

---

### Task 1. 매핑 복구 헬퍼 — dangling 수리 + 없는 것만 insert (R6/R6-B 공용)

> **★ 리뷰 결정 (MEDIUM-HIGH, outside voice + Maxi 2026-07-18).** 무조건 전체 UPSERT `DO UPDATE SET workflow_id = EXCLUDED.workflow_id` 는 **admin 이 REST 로 바꾼 default 매핑을 매 부팅마다 시스템 기본값으로 되돌린다.**
> `WorkflowSchemeController:198 addMapping` 이 `issueTypeKey=null`(default 매핑) 변경을 허용하고 `V201:21` 주석이 *"mapping 은 admin 이 변경 가능 (D11 결정)"* 이라 명시. 무조건 UPSERT 는 D11 계약 위반.
> → **dangling 수리 + 없는 것만 insert** 로 좁힌다. admin 설정 보존하면서 R6(없는 매핑 채움)·R6-B(재적재 후 dangling 수리) 둘 다 해결.

**메타**.
- agent: `backend-engineer`
- files: [`PW/main/kotlin/com/bts/workflow/scheme/repository/SchemeIssueTypeMappingRepository.kt`, `PW/test/kotlin/com/bts/workflow/scheme/repository/SchemeIssueTypeMappingRepositoryIntegrationTest.kt`]
- depends-on: []

**RED**.
- 파일. `SchemeIssueTypeMappingRepositoryIntegrationTest.kt`
- 테스트 1. `repairDefaultMappings 가 없는 스킴에 default 매핑을 만든다 (R6 백필)` — 매핑 없는 상태 → 호출 → 4 스킴 default 매핑 생성.
- 테스트 2. `dangling(옛 UUID) 매핑을 유효한 workflow 로 수리한다 (R6-B)` — workflow 를 delete/reinsert 해 UUID 변경(옛 매핑은 dangling) → 호출 → 매핑이 **새 UUID** 를 가리킴.
- **🛑 테스트 3 (admin 보존 판별자 — MEDIUM-HIGH 회귀 가드)**. `admin 이 바꾼 유효한 default 매핑은 건드리지 않는다` — software-scheme 의 default 를 **다른 유효한 workflow**(예: bug-tracking)로 바꿔 심은 뒤 호출 → **여전히 그 workflow 를 가리킴**(시스템 기본 software-default 로 안 되돌아감). **이 테스트가 무조건 UPSERT 를 금지하는 가드** — UPSERT 로 구현하면 이 테스트가 fail.
- 실패 메시지 (예상). `repairDefaultMappings` 메서드 없음

**GREEN**.
- 파일. `SchemeIssueTypeMappingRepository.kt`
- `@Transactional fun repairDefaultMappings()` — **두 부분**. (1) **dangling 수리** — `UPDATE ... SET workflow_id = <key 조인 새 UUID> WHERE issue_type_id IS NULL AND workflow_id NOT IN (SELECT id FROM workflows)`(옛 UUID 만). (2) **없는 것 insert** — `INSERT ... SELECT ... WHERE NOT EXISTS(default 매핑)`(스킴 4종 CASE, `V201:137-145`·`seed-project.sql:58-67` 패턴). **유효한 기존 매핑은 두 조건 다 안 걸려 무변경** → admin 보존.
- **★ 부분 인덱스 실재 (outside voice 검증).** `V201:106` `ix_scheme_default_mapping (scheme_id) WHERE issue_type_id IS NULL` 실재 — insert 분기의 `WHERE NOT EXISTS` 가 이 스킴당 1개 불변식을 지킴(멱등 R6-3). 일반 제약 `uq_scheme_issue_type(:86)` 은 `NULL≠NULL` 이라 default 중복 못 막음(`:104` 주석 자인).
- 매핑 테이블은 untyped `DSL.table()/DSL.field()`(실측 `:43-47`, jOOQ codegen 없음) — 손수 작성. **emitted SQL 검증 필수**(arbiter/술어 오류 쉬움).
- 스킴 4종은 상수 맵(`software-scheme→software-default` 등). `standardWorkflowKeys` 와 정합.

**REFACTOR**.
- CASE 매핑을 `companion object` 상수로. KDoc — *"R6 백필(없는 매핑 채움)·R6-B(dangling 수리) 둘 다 처리. admin 이 설정한 유효 매핑은 보존(D11) — 무조건 UPSERT 금지"* + `seed-project.sql:57` 관례 인용.

**검증**. `./gradlew :backend:project-workflow:test --tests SchemeIssueTypeMappingRepositoryIntegrationTest`

---

### Task 2. R6-B — YamlSeedService 재적재 시 매핑 정리→재연결 (같은 tx)

**메타**.
- agent: `backend-engineer`
- files: [`PW/main/kotlin/com/bts/workflow/seed/YamlSeedService.kt`, `PW/test/kotlin/com/bts/workflow/seed/YamlSeedServiceTest.kt`]
- depends-on: [1]

> **★★ 리뷰 결정 (verifier 발견 + Maxi 2026-07-18) — 전체 매핑 기록→재연결.**
> plan 원안(workflow_id 기준 DELETE + default 만 복구)은 **admin 이 건 특정타입 매핑(`issue_type_id NOT NULL`)을 조용히 소실**시킨다.
> 실측 — `addMappingByKeys`(`WorkflowSchemeApplicationService.kt:319`)에 **is_default 스킴 가드가 없어** admin 이 표준 스킴/표준 workflow 에 특정타입 매핑을 걸 수 있다. 재적재 시 그 매핑이 dangling 이 되는데 default 만 복구하면 소실.
> → **재적재 전 그 workflow 를 가리키는 전체 매핑을 (scheme_id, issue_type_id) 튜플로 기록 → DELETE → insertWorkflow(새 UUID) → 기록한 튜플을 새 UUID 로 재INSERT.** default·admin 특정타입 둘 다 보존.
> **T1 의 `repairDefaultMappings` 는 R6(빈 DB 백필) 전용으로 역할 정리** — dangling 수리 분기는 정상 흐름 도달 불가(FK RESTRICT 가 막음)라 defense-in-depth. **실제 R6-B 처방 = 이 T2 의 기록→재연결.**

**메타** (수정).
- files: [`PW/main/kotlin/com/bts/workflow/seed/YamlSeedService.kt`, `PW/main/kotlin/com/bts/workflow/scheme/repository/SchemeIssueTypeMappingRepository.kt`, `PW/test/kotlin/com/bts/workflow/seed/YamlSeedServiceTest.kt`]
  # T1 이 만든 SchemeIssueTypeMappingRepository 에 기록/복원 메서드 추가 필요 → T1 파일 재편집(depends-on 1 이라 순서 안전)

**RED**.
- 파일. `YamlSeedServiceTest.kt`
- 테스트 1. `매핑이 존재해도 YAML 구조 변경 재적재가 FK RESTRICT 로 죽지 않는다 (R6-B / S11)` — 워크플로우 4종 + default 매핑 심은 뒤, software-default YAML 에 state 추가(structural dirty) → `seedAll()` 호출. **기대** — 예외 없이 성공 + 재적재 후 default 매핑이 **유효한 새 workflow UUID** 를 가리킴(R6B-2).
- **🛑 테스트 2 (admin 특정타입 매핑 보존 — verifier 발견 회귀 가드)**. 재적재 전 admin 이 software-scheme 에 `issue_type=<특정타입> → software-default` 특정타입 매핑을 심는다 → software-default YAML structural 변경 → `seedAll()`. **기대** — 그 특정타입 매핑이 **살아 있고 새 UUID 를 가리킴**(소실 금지). **이 테스트가 "default 만 복구" 구현을 fail 시키는 가드.**
- **판별자 (C2 — vacuous 방지)**. 기록→재연결 로직을 제거하면 (a) `deleteWorkflow` 가 FK RESTRICT 로 죽거나 (b) 매핑만 지우고 복원 안 하면 테스트 2 가 소실로 fail. GREEN 후 mutation 실증.
- 실패 메시지 (예상). 현재는 매핑 존재 시 `deleteWorkflow` 가 FK 위반 → 재적재 실패

**GREEN**.
- 파일. `SchemeIssueTypeMappingRepository.kt` — 두 메서드 추가.
  - `fun findMappingTuplesByWorkflowId(workflowId: UUID): List<Pair<Long, IssueTypeId?>>` — 그 workflow 를 가리키는 (scheme_id, issue_type_id) 전부 기록.
  - `fun reinsertMappings(tuples: List<...>, newWorkflowId: UUID)` — 기록한 튜플을 새 UUID 로 재INSERT.
  (또는 단일 `fun reconnectWorkflowMappings(oldWorkflowId, newWorkflowId)` 로 묶어도 됨 — 구현자 판단.)
- 파일. `YamlSeedService.kt` — `applyIfChanged`(`:289-305`) 재적재 분기.
  - `deleteWorkflow(dto.key)` **앞에** — `findByKey(dto.key)?.id` 로 옛 UUID 획득 → `findMappingTuplesByWorkflowId(옛 UUID)` 로 전체 매핑 기록 → 그 workflow 가리키는 매핑 DELETE(FK RESTRICT 회피).
  - `insertWorkflow(dto)` 는 새 UUID 반환(실측 `:486-493` `returningResult(WORKFLOWS.ID)`) → `reinsertMappings(기록, 새 UUID)`.
  - **같은 tx**(`seedAll` `@Transactional`) 라 원자성. 실패 시 전체 롤백.
- `SchemeIssueTypeMappingRepository` 를 생성자 주입(현재 미주입 — 추가).
- **R6 백필은 seedAll 말미 `repairDefaultMappings()`(T1) 유지** — 빈 DB 최초 부팅용. 재적재 매핑 보존과 **직교**(재적재는 기존 매핑 보존, 백필은 없는 default 생성). 위치 load-bearing(EC-9, `:211` 루프 밖).

**REFACTOR**.
- `deleteWorkflow` KDoc `:462-466` 정정 — RESTRICT FK(`workflow_scheme_issue_type_mappings`) 존재 명시. 클래스 KDoc `:143-145` "CASCADE 로 소실"이 **RESTRICT 라 도달 불가**임을 정정(S-2). 재적재 매핑 보존 흐름 KDoc.

**검증**. `cd backend && ./gradlew :modules:project-workflow:test --tests YamlSeedServiceTest --tests SchemeIssueTypeMappingRepositoryIntegrationTest`

---

### Task 3. R6 — 빈 DB 백필 = seedAll 말미 재연결 (별도 러너 없음)

> **★ 리뷰 결정 (아키텍처 이슈 1, Maxi 2026-07-18).** 별도 `DefaultMappingBackfillRunner`(ApplicationReadyEvent 리스너)를 **만들지 않는다.**
> `seedAll` 도 `@Order` 가 없어(실측 `YamlSeedService.kt:196`) 두 리스너의 순서가 비결정적 → 백필이 seedAll 보다 먼저 돌면 workflows 가 비어 매핑 0건(R6-2 위반).
> **대신 seedAll 말미에서 `reconnectDefaultMappings()` 를 직접 호출**한다. workflows 가 채워진 것이 코드로 보장(R6-2)되고, 같은 tx 원자성 공짜, 신규 클래스 5→4 감소, 순서 버그 원천 소거.
> **이로써 T2(재시드 재연결)와 T3(빈 DB 백필)가 같은 한 줄로 통합된다** — R6·R6-B 가 단일 경로. T2 GREEN 이 이 호출을 넣으면 T3 는 그것이 빈 DB 도 커버함을 **검증만** 한다.

**메타**.
- agent: `backend-engineer`
- files: [`PW/test/kotlin/com/bts/workflow/seed/YamlSeedServiceTest.kt`]  # T2 와 같은 파일 — 직렬(depends-on 2)
- depends-on: [2]

**RED**.
- 파일. `YamlSeedServiceTest.kt`
- 테스트. `빈 DB 부팅(seedAll 최초 실행)이 4 스킴 기본 매핑을 만든다 (R6-1)` + `seedAll 재실행은 멱등 — 4행 유지 (R6-3)`.
- **판별자 (C2)**. T2 가 넣은 seedAll 말미 `repairDefaultMappings()` 호출을 제거하면 **빈 DB 최초 부팅 후 매핑 0행** → `findDefaultMapping` null → (통합에서) resolveStart 422. mutation 실증.
- 실패 메시지 (예상). 통합 전이면 매핑 0행

**GREEN**. (프로덕션은 T2 에서 완료 — seedAll 말미 호출)
- T2 가 이미 seedAll 말미에 `repairDefaultMappings()` 를 호출하므로 **빈 DB 도 자동 커버**. 이 task 는 그 사실을 R6-1/R6-3 각도로 검증하는 테스트만 추가.
- 멱등(R6-3) = `repairDefaultMappings` 의 insert 분기 `WHERE NOT EXISTS`(T1)가 보장.

**REFACTOR**.
- seedAll KDoc 에 *"말미 재연결이 R6(빈 DB 백필)·R6-B(재시드 생존)를 단일 경로로 커버한다"* 명시. 별도 러너 부재 이유(순서 비결정성) 기록.

**검증**. `./gradlew :backend:project-workflow:test --tests YamlSeedServiceTest`

---

### Task 4. R6/R6-B 마이그레이션 통합 검증 — 기존 은폐 테스트 뚫기 (S10 하위 · project-workflow 층)

**메타**.
- agent: `backend-engineer`
- files: [`PW/test/kotlin/com/bts/workflow/scheme/migration/WorkflowSchemesMigrationIntegrationTest.kt`]
- depends-on: [3]

**RED**.
- 파일. `WorkflowSchemesMigrationIntegrationTest.kt`
- 테스트. `Flyway + 백필 후 기본 매핑 4행이 실재한다 (R6 — 손수 심지 않음)` — **핵심: `seedWorkflowsAndMappings()` 헬퍼(`:148`)를 쓰지 않는다**(S-3 은폐 차단). 대신 Flyway migrate → 워크플로우 seed(seedAll 경로 또는 직접) → **백필 러너 실행** → 매핑 4행 확인.
- **판별자**. 백필을 실행하지 않으면 매핑 0행(현재 상태) → fail. 백필 실행 후에만 4행 → pass. 이게 R6 를 실제로 잡는 유일한 형태.
- 실패 메시지 (예상). 백필 미실행 시 0행

**GREEN**. (프로덕션은 T3 에서 완료 — 이 task 는 검증 배선만)
- 테스트에서 백필 러너를 명시 호출하거나 Spring 컨텍스트 부팅으로 `ApplicationReadyEvent` 발화. 기존 `:329-332` "4 row" 단언이 **손수 심음 없이** 통과하도록 재작성.

**REFACTOR**.
- 기존 `:96-99` 주석(*"통합 테스트는 Spring 컨텍스트 없이 실행되므로 seed 직접 INSERT"*)을 백필 경유로 정정.

**검증**. `./gradlew :backend:project-workflow:test --tests WorkflowSchemesMigrationIntegrationTest`

---

### Task 5. CREATE_PROJECT 권한 상수 + 프로젝트 생성 도메인/리포지토리 (FR-PJ-01 기반)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/shared-kernel/src/main/kotlin/com/bts/shared/permission/GlobalPermissionCodes.kt`, `IT/main/kotlin/com/bts/issue/project/domain/Project.kt`, `IT/main/kotlin/com/bts/issue/project/repository/ProjectCreateRepository.kt`, `IT/test/kotlin/com/bts/issue/project/repository/ProjectCreateRepositoryIntegrationTest.kt`]
- depends-on: []

**RED**.
- 파일. `ProjectCreateRepositoryIntegrationTest.kt`(신규)
- 테스트. `insert 가 projects 1행을 만들고 생성된 id/key/name 을 반환한다` + `key 중복 시 도메인 예외(409 매핑) (PJ1-7)` + `key 정규식 위반은 DB CHECK 로 거부 (PJ1-6)`.
- **🛑 EC-1 (리뷰 이슈 3)**. `소프트 삭제된(deleted_at NOT NULL) 프로젝트의 key 로 생성 시 409` — 이슈키 영속성(DATA.md §1.1). `deleted_at` 세팅한 프로젝트를 심고 같은 key insert → UNIQUE 위반. **판별자** — 중복 조회가 `WHERE deleted_at IS NULL` 로 필터하면 삭제된 key 를 놓쳐 201 이 나옴(그게 잘못). DB UNIQUE 는 `deleted_at` 무관하게 점유(V001 `key VARCHAR(10) UNIQUE`)라 이게 진실원천.
- **🛑 EC-6 (리뷰 이슈 3)**. `동시에 같은 key 로 2건 insert → 1건 201 + 1건 409` — DB UNIQUE 가 진실원천([[jooq-exception-translator-409-dependency]]). **판별자** — 서비스층 선-조회 가드만으로는 TOCTOU 로 2건 다 통과할 수 있음. UNIQUE 위반 → `translateUniqueViolation` → 409 경로가 실제로 도는지 실증(단순 순차 중복이 아니라 변환 경로 검증).
- 실패 메시지 (예상). `ProjectCreateRepository`/`Project` 없음

**GREEN**.
- `Project.kt`(신규) — 최소 애그리거트(id·key·name). 기존은 도메인 객체 부재(S-6 실측)라 신설.
- `ProjectCreateRepository.kt`(신규) — `@Transactional fun insert(key, name): Project`. `projects` INSERT `returningResult`. UNIQUE 위반 → 도메인 예외(`SchemeIssueTypeMappingRepository.translateUniqueViolation` 패턴 참고). **`archived_at` 은 이 PR 범위 밖**(PR-4) — 컬럼 없이 진행(§5.1 실측 9컬럼).
- **`CREATE_PROJECT` 상수 — shared-kernel 에 신설**(리뷰 이슈 2, Maxi 2026-07-18). `com.bts.shared.permission` 에 `const val CREATE_PROJECT = "CREATE_PROJECT"`(그 패키지의 권한 enum `toPermissionCode()` 관례와 정합). issue-tracking 게이트(T7)가 이걸 import. **identity-access 의 기존 `ALLOWED_GLOBAL_PERMISSIONS`(`GlobalPermissionGrantService.kt:129`) 를 이 상수로 바꾸는 것은 후속 TODO** — PR-1 파일 변경 = surgical 위반이라 이 PR 범위 밖. shared-kernel 은 공유 커널이라 별도 BC 로 세지 않음(BC 2개 유지).

**REFACTOR**.
- 예외 → 409 매핑은 T8 컨트롤러/핸들러에서. 여기선 도메인 예외만.

**검증**. `./gradlew :backend:issue-tracking:test --tests ProjectCreateRepositoryIntegrationTest`

---

### Task 6. 프로젝트 생성 서비스 — cross-BC 멤버십 쓰기 (프로덕션 코드 + 단위 검증)

> **★ 리뷰 결정 (CRITICAL, outside voice + Maxi 2026-07-18).** DoD-11(원자성 롤백 실증)은 **여기서 못 한다.**
> issue-tracking 격리 테스트(`IssueTestcontainersBase:89`)는 `classpath:db/migration/issue-tracking` **만** 로드 → `project_memberships`(identity **V007**)·실제 `ProjectMembershipWriteAdapter`(identity-access)가 **부재**. issue-tracking 은 identity-access 를 의존 안 함(실측 build.gradle).
> 격리 테스트에서 포트를 fake 로 주면 (a) DB 무접촉이라 롤백 단언이 vacuous(projects 롤백만 증명) 또는 (b) 없는 테이블 타겟. **C10 tx-aware 컨텍스트로도 못 고침** — 테이블·어댑터 부재가 근본 원인.
> → **DoD-11 은 T12(:modules:app 조립 부팅)로 이동.** 거기엔 9 BC 전부 있어 실제 어댑터+project_memberships 로 진짜 롤백 실증 가능. 이 task 는 **프로덕션 서비스 코드 + 포트 호출을 단위(mock) 검증**만.

**메타**.
- agent: `backend-engineer`
- files: [`IT/main/kotlin/com/bts/issue/project/application/ProjectCreateApplicationService.kt`, `IT/test/kotlin/com/bts/issue/project/application/ProjectCreateApplicationServiceTest.kt`]
- depends-on: [5]

**RED**.
- 파일. `ProjectCreateApplicationServiceTest.kt`(신규, mock 단위 — Testcontainers 아님)
- 테스트. `create 가 projectCreateRepo.insert 후 membershipWritePort.addCreatorAsAdmin 을 호출한다 (S1 협력)` — 두 협력자 mock, 호출 순서·인자 검증(생성된 project.id 가 addCreatorAsAdmin 에 전달). + `insert 예외 시 addCreatorAsAdmin 미호출`(단락).
- **여기서 안 하는 것**. 실제 DB 롤백 원자성(DoD-11) = T12. 이 단위 테스트는 **협력 계약**만(mock 이라 tx 실증 불가 — 정직하게 분리).
- 실패 메시지 (예상). `ProjectCreateApplicationService` 없음

**GREEN**.
- `ProjectCreateApplicationService.kt`(신규) — 클래스 레벨 `@Transactional`(선례 `IssueApplicationService.kt:125-127`). `projectCreateRepo.insert(...)` → `membershipWritePort.addCreatorAsAdmin(project.id, creatorId)`(S-1 실측 시그니처). **둘 다 같은 tx** → I1(원자성은 T12 가 실증).
- `ProjectMembershipWritePort` 주입(소비만 — 포트/어댑터는 PR-1 산출물, 안 건드림 → BC 2개 유지).

**REFACTOR**.
- KDoc — *"원자성(I1) 실증은 조립 부팅(T12)에서. 여기선 협력 계약만"* 명시(테스트 의도 정직).

**검증**. `./gradlew :backend:issue-tracking:test --tests ProjectCreateApplicationServiceTest`

---

### Task 7. 프로젝트 생성 컨트롤러 — CurrentActor + CREATE_PROJECT 명시 게이트

**메타**.
- agent: `security-engineer`  # 권한 게이트가 폭발반경. S-4 인증관례·PAT·DoD-9
- files: [`IT/main/kotlin/com/bts/issue/project/web/ProjectCreateController.kt`, `IT/main/kotlin/com/bts/issue/project/web/ProjectCreateExceptionHandler.kt`, `IT/main/kotlin/com/bts/issue/project/web/dto/CreateProjectRequest.kt`, `IT/test/kotlin/com/bts/issue/project/web/ProjectCreateControllerTest.kt`]
- depends-on: [6]

**RED**.
- 파일. `ProjectCreateControllerTest.kt`(신규, `@WebMvcTest` 슬라이스)
- 테스트. `POST /api/v1/projects 201 (해피 S1)` + `미인증 401 (S3)` + `CREATE_PROJECT 없으면 403 + 내부구조 미노출 (S2/PJ1-8)` + `중복 key 409 (S4)` + `key 정규식 위반 400 (PJ1-6)`.
- **판별자 (C2)**. 403 음성 테스트는 **본문 판별자** 필요 — 가드 제거 시 실제로 통과해버리는지 mutation 실증(가드 없어도 401 나오는 vacuous 회피). **actor 추출을 key 중복조회보다 먼저**(PJ1-3, [[auth-extraction-before-resource-lookup]]).
- 실패 메시지 (예상). `ProjectCreateController` 없음

**GREEN**.
- `ProjectCreateController.kt`(신규) — `@PreAuthorize("isAuthenticated()")`(클래스) + 핸들러에서 `CurrentActor.current()`(S-4) 로 actor 추출 + **`systemPermissionResolver.hasGlobalPermission(actorId, CREATE_PROJECT)`** 명시 호출(S-1·§인계 1 — `hasGrant` 직접 호출 금지). **`@PreAuthorize hasRole` 금지**(PAT 탈락 — GlobalPermissionGrantController KDoc 선례).
- `CreateProjectRequest`(key·name Zod-동형 검증), `ProjectCreateExceptionHandler`(409·400·403 매핑, message 내부구조 미노출 [[fr-pm-04-guard-exception-message-http-leak]]).
- **🛑 @WebMvcTest mock 배선 (outside voice)**. 컨트롤러 → 서비스 → `ProjectMembershipWritePort`(PR-1, issue-tracking 미보유 빈) 체인이라 슬라이스에 **`@MockBean ProjectMembershipWritePort` + `@MockBean SystemPermissionResolver`** 동반 필요(C6, [[new-crossbc-dep-openapi-mockbean-regression]]). 없으면 NoSuchBean.

**REFACTOR**.
- actor 추출 순서 KDoc 명시. `SystemPermissionResolver` 주입은 S-5 선례(ProjectRequire2fa) 동형.

**검증**. `./gradlew :backend:issue-tracking:test --tests ProjectCreateControllerTest`

---

### Task 8. DoD-9 — 비-SYSTEM_ADMIN grant 보유자 생성 (:modules:app 조립 부팅, prod 프로파일)

> **★ 리뷰 결정 (CRITICAL, outside voice + Maxi 2026-07-18).** issue-tracking 격리 테스트로는 **부팅 불가.**
> `@ActiveProfiles("prod")` 면 issue-tracking 의 `NonProdAllowSystemAdminResolver`(@Profile("!prod"))가 **꺼지고**, 실제 `IdentityAccessSystemPermissionResolver`(identity-access)는 클래스패스에 **없음** → `SystemPermissionResolver` 빈 부재 → **NoSuchBean 부팅 실패**. grant 시드가 필요로 하는 `global_permission_grants`(identity **V036**)도 issue-tracking 마이그레이션에 없음.
> → **DoD-9 를 `:modules:app` 조립 부팅으로 이동.** 9 BC 전부 조립돼 실제 resolver+테이블+어댑터 존재. S-5 의 "issue-tracking 에서 잘 돈다"는 **non-prod fallback 빈 덕**이라 prod 케이스엔 틀림(outside voice 정정).

**메타**.
- agent: `security-engineer`
- files: [`APP/test/kotlin/com/bts/app/ProjectCreatePermissionProdBootTest.kt`]
- depends-on: [7]

**RED**.
- 파일. `ProjectCreatePermissionProdBootTest.kt`(신규, `:modules:app`, `@SpringBootTest(webEnvironment=RANDOM_PORT)` + `@ActiveProfiles("prod")` — C1)
- 테스트. **🛑 DoD-9** `CREATE_PROJECT grant 를 가진 비-SYSTEM_ADMIN 이 프로젝트를 만든다` — grant 를 `global_permission_grants` 에 직접 시드(S-6 — grant 0건이라 필수) 후 그 사용자로 201. + `grant 없는 비-admin 403` + `SYSTEM_ADMIN 은 grant 없이 201`(판정식 `grant OR isSystemAdmin` 뒷항).
- **🛑 C1**. 조립 부팅이라도 `@ActiveProfiles("prod")` 필수 — 아니면 `NonProdAllowSystemAdminResolver`(항상 true)가 **무의미 통과**.
- **판별자 (B8)**. prod 어댑터가 `hasGlobalPermission` override 를 잊으면 `CREATE_PROJECT` 가 SYSTEM_ADMIN 전용으로 되돌아감 → **비-admin grant 보유자가 403**. 이 테스트만이 그 회귀를 잡음.
- 실패 메시지 (예상). prod 조립 부팅 배선
- **부팅 레시피** [[identity-access-prod-randomport-boot-recipe]] — prod 프로파일 필수 env(암호화 키 등) 주입([[use-time-validated-env-passes-boot-fails-on-use]] — 스모크가 실기능 경로 밟게).

**GREEN**. (프로덕션은 T7 완료 — 이 task 는 조립 부팅 검증)
- grant 시드 fixture + prod env.

**REFACTOR**. grant 시드 헬퍼 재사용화(T12 와 공유 가능).

**검증**. `./gradlew :backend:app:test --tests ProjectCreatePermissionProdBootTest`

---

### Task 9. OpenApi/컨텍스트 부팅 — 새 cross-BC 의존 @MockBean 동반 (C6)

**메타**.
- agent: `backend-engineer`
- files: [`IT/test/kotlin/com/bts/issue/**/OpenApi*Test.kt`(실측 후 확정), `IT/test/kotlin/...`(whoami/슬라이스 전수 mock)]
- depends-on: [7]

**RED**.
- issue-tracking 이 `ProjectMembershipWritePort` 를 **처음 소비**(S-5) → `ProjectCreateApplicationService` 를 component-scan 하는 **모든 issue-tracking `@SpringBootTest`** 가 `NoSuchBeanDefinitionException`(포트 빈 부재, 어댑터는 identity-access) — **OpenApi 만이 아님**(outside voice 정정, [[new-crossbc-dep-openapi-mockbean-regression]]). 먼저 **`./gradlew :backend:issue-tracking:test` 전체를 실행해 깨지는 테스트를 전수 열거**(실측 우선 — 개수 추정 금지).
- 테스트. 깨지는 모든 슬라이스/full-boot 에 `@MockBean ProjectMembershipWritePort`(+ 필요 시 `SystemPermissionResolver`) 동반.

**GREEN**. 실측으로 깨진 테스트 전수에 mock 배선.

**REFACTOR**. mock 배선을 공통 TestConfig 로(반복 제거).

**검증**. `./gradlew :backend:issue-tracking:test` (전체 — 회귀 확인, 깨진 개수→0)

---

### Task 10. init_codegen / jOOQ 미러 영향 판정 (C3 — 실측 후 조건부)

**메타**.
- agent: `db-engineer`
- files: [(실측 후 확정 — 변경 없을 수 있음)]
- depends-on: [3]

**RED/GREEN**.
- **C3 는 이 PR 에 적용 안 될 가능성 높음** — project-workflow 는 `init_codegen.sql` 부재(S 실측), V201 테이블 전부 `DSL.table("문자열")` 동적 참조. R6/R6-B 가 **새 테이블/컬럼을 안 만들고**(전략 a, 마이그레이션 0) 기존 매핑 테이블만 쓰므로 codegen 재생성 불필요.
- **판정만** — `git diff` 로 마이그레이션·스키마 변경 0 확인 → C3 무관 확정. 변경 있으면 issue-tracking `init_codegen.sql` 미러([[jooq-init-codegen-mirror]]).

**검증**. 변경 0이면 no-op 문서화. 있으면 codegen diff 확인.

---

### Task 11. 프론트 계약 — 해당 없음 (백엔드 전용 PR)

**메타**.
- agent: `backend-engineer`
- files: []
- depends-on: []

- **no-op**. D6 UI 는 PR-5. 이 PR 은 REST 계약만. E2E 없음([[ui-pr-defer-e2e-regression-latent]] 경고 대상은 PR-5/6). Zod 계약도 PR-5.
- 이 task 는 **plan 완결성 표시용** — 프론트 미변경을 명시 기록(리뷰어가 "왜 프론트 0" 즉시 파악).

---

### Task 12. 최종 통합 — S10/S11 + DoD-11 원자성 + prod 조립 (DoD-2·3·6·11)

> **★ DoD-11 이 T6 에서 여기로 이동(CRITICAL 결정).** 실제 `ProjectMembershipWriteAdapter` + `project_memberships`(identity V007)가 있는 **유일한 곳**이 조립 앱이다. issue-tracking 격리 테스트로는 원자성 실증 불가.

**메타**.
- agent: `qa-engineer`
- files: [`APP/test/kotlin/com/bts/app/ProjectCreateEndToEndBootTest.kt`(신규 — 조립 앱), `APP/test/kotlin/com/bts/app/WorkflowBackfillBootTest.kt`(신규 — S10/S11 조립)]
- depends-on: [4, 8, 9]

**RED**.
- **🛑 DoD-2 (S10)** `빈 DB 부팅 → 프로젝트 생성 → 그 프로젝트에 이슈 생성 201 (422 아님)` — 마이그레이션만 적용, 시드 SQL 손수 실행 없음, seedAll 말미 복구가 매핑 채움. **매핑 손수 안 심음**(S-3 은폐 차단).
- **🛑 DoD-3 (S11)** 매핑 존재 상태에서 YAML 구조 변경 후 재부팅 성공(dangling 수리).
- **🛑 DoD-11 (T6 에서 이동)** `프로젝트 생성 도중 예외 주입 시 projects·project_memberships 둘 다 롤백` — **실제 어댑터 경유**. 예외 주입은 실제 실패 경로(예: 멤버십 UNIQUE 위반, 또는 이미 존재하는 멤버십)로 유도. 조립 앱의 tx-aware DataSource 라 **C10 의 수동 tx-aware 컨텍스트 배선 불필요**(auto-config 가 이미 tx-aware — outside voice Q2 확인). 롤백 후 `projects`·`project_memberships` **둘 다 0행** 단언.
- 실패 메시지 (예상). 통합 전이면 422 / 한쪽만 롤백

**GREEN**. (프로덕션 T1~T7 완료 — 조립 검증)
- `./gradlew :backend:app:test`(9 BC prod 조립 — C7·DoD-6). rebase 후 재검증([[prod-assembly-boot-verification-required]]).

**REFACTOR**. S10/S11 이 매핑을 손수 안 심는지 최종 확인(S-3 은폐 재발 방지).

**검증**. `./gradlew :backend:app:test`

---

### Task 13. auto-assign SYSTEM_ACTOR 권한 우회 hot-fix (S10 blocker — T12 발견)

> **★★ T12 가 진짜 프로덕션 버그 발견 (Maxi 2026-07-18 — 이 PR hot-fix).**
> 새 프로젝트 첫 이슈 생성 시 EC-1 D10 auto-assign 이 `assignToProject(SYSTEM_ACTOR, ...)`(`WorkflowSchemeApplicationService.kt:445`)를 부르는데,
> prod 판정기 `IdentityAccessWorkflowSchemePermissionResolver.hasProjectPermission`(`:77`)이 actor 의 **프로젝트 멤버십**을 요구한다. SYSTEM_ACTOR(nil UUID)는 어떤 프로젝트 멤버도 아니라 **항상** `WorkflowSchemeAccessDeniedException`(500).
> **FR-PJ-01 이 프로젝트 생성을 열면서 노출한 선재 결함** — `seed-project.sql:10-12` 가 이미 문서화("미리 배정해 auto-assign 을 회피"). R6-B 와 **동형 패턴**(경로를 열면 선재 결함 노출, [[permitall-opens-preexisting-body-buffer-dos]]). R6(매핑)와는 **다른 층위**(스킴 배정 + 권한).
> 안 고치면 S10(DoD-2) 미충족 = FR-PJ-01 "프로젝트는 만들어지는데 이슈는 안 만들어지는" 반쪽 머지(불변식 I3 동형).

**메타**.
- agent: `security-engineer`  # 권한 우회 = 폭발반경
- files: [`PW/main/kotlin/com/bts/workflow/scheme/application/WorkflowSchemeApplicationService.kt`, `PW/test/kotlin/com/bts/workflow/scheme/application/WorkflowSchemeApplicationServiceTest.kt`(또는 관련 단위 테스트)]
- depends-on: [12]  # T12 S10 test 가 GREEN 되는 게 최종 검증

**★ BC 범위 — project-workflow 내 완결 (identity-access 안 건드림).**
resolver(`IdentityAccessWorkflowSchemePermissionResolver`) 수정은 **BC 3개(D10 초과)**. auto-assign 은 project-workflow 라 **여기서 우회하면 BC 2개 유지**. (실측: `assignToProject` 가 project-workflow, resolver 가 identity-access.)

**★ 보안 — 외부 악용 표면 없음 (실측).**
SYSTEM_ACTOR(nil UUID)는 **외부 요청으로 못 온다** — issue-tracking `CurrentActor`/`ActorId` 가 nil UUID 를 `require`/401 로 거부(`ActorId.kt:19`). SYSTEM_ACTOR 는 auto-assign 내부에서만 생성(project-workflow ActorId 는 nil 허용, `:512`). auto-assign 은 **software-scheme(표준)만** 배정 → 위험 낮음.

**RED**.
- 파일. `WorkflowSchemeApplicationServiceTest.kt`(project-workflow 단위)
- 테스트. `assignment 없는 프로젝트에 findAssignedScheme 호출 시 auto-assign 이 권한거부 없이 software-scheme 배정한다` — prod 판정기(멤버십 요구) 하에서 SYSTEM_ACTOR auto-assign 이 500 아니라 성공. **판별자** — 우회 로직 제거 시 `WorkflowSchemeAccessDeniedException`(현재 버그) 재현.
- 실패 메시지 (예상). 현재 SYSTEM_ACTOR 가 멤버십 없어 AccessDenied.

**GREEN**.
- 파일. `WorkflowSchemeApplicationService.kt`
- auto-assign 전용 경로가 `requirePermission` 을 **우회**하도록 분리. 방식은 security-engineer 판단 — 후보 (i) `assignToProject` 가 SYSTEM_ACTOR 면 requirePermission 스킵(명시 분기), (ii) auto-assign 전용 `private fun autoAssignSystemScheme(...)` 신설(권한 검사 없이 배정 로직만, `findAssignedScheme` 이 이걸 호출). **public `assignToProject`(사용자 명시 배정)는 권한 검사 유지** — 우회는 auto-assign 에만.
- KDoc `:377` "SYSTEM_ACTOR 도 허용"의 **의도를 실제로 구현**(현재는 의도만 있고 코드가 안 지킴). 우회가 auto-assign(software-scheme만)에 국한됨을 명시.

**REFACTOR**.
- 우회 경로 KDoc — 왜 안전한가(외부 nil UUID 불가·software-scheme 한정). `seed-project.sql:10-12` 가 우회하던 걸 코드로 해소했으니 그 시드 주석도 후속 정리 대상(별건).

**검증**. `cd backend && ./gradlew :modules:project-workflow:test --tests WorkflowSchemeApplicationServiceTest` + **T12 S10 GREEN 재확인** `./gradlew :modules:app:test --tests WorkflowBackfillBootTest`.

---

## Plan 메타

- task 수: 12 (T10/T11 은 조건부/no-op — 실질 구현 9)
- TDD 강제: yes (T4/T8/T9/T12 는 검증 배선 — 기존 실패를 RED 로 간주 또는 신규 판별자)
- 예상 wave: 3~4 (T1→{T2,T3,T5}→… 파일 무교집합 병렬. project-workflow 축 T1~T4 직렬성 있음)
- **병렬 주의** — R6/R6-B(project-workflow)와 FR-PJ-01(issue-tracking)은 다른 모듈이라 컴파일 직렬화 유의([[bts-plan-wave-gradle-module-compile]]). 리뷰 뮤테이션 격리([[parallel-review-mutation-contaminates-peers]]).
- 추가 검증: `:modules:app:test`(조립) · verify-master-plan(129 불변) · ktlint/detekt(`--rerun-tasks`)

## 리뷰 결과

### plan-eng-review (2026-07-18)

**스코프**: 그대로 진행(Maxi). 신규 클래스 5개는 과설계 아님 — FR-PJ-01 생성이 도메인부터 REST까지 전무라 수직 슬라이스가 기존 관례(ProjectLead*/Require2fa*). R6/R6-B 는 불변식 I3 로 같은 PR 강제.

**4 섹션 발견 + 결정 (전부 Maxi 승인).**

| # | 심각도 | 발견 | 결정 |
|---|---|---|---|
| 아키1 | 아키텍처 | 별도 백필 러너(ApplicationReadyEvent)는 seedAll 과 순서 비결정 → R6-2 위반 위험 | **seedAll 말미 직접 호출**. 신규 클래스 5→4, T2/T3 통합 |
| 코드2 | 코드품질(DRY) | CREATE_PROJECT 3번째 사본 | **shared-kernel 상수**. identity 화이트리스트 이전은 후속 TODO |
| 테3 | 테스트 | EC-1(소프트삭제 key)·EC-6(동시성) 판별자 부재 | **T5 에 명시 추가** |
| 성능 | — | 이슈 없음(전부 저빈도) | — |

**★ outside voice (Claude subagent)가 section 리뷰가 못 잡은 CRITICAL 발견 — 전부 실측 확인 후 반영.**

| # | 심각도 | 발견 (실측 근거) | 결정 |
|---|---|---|---|
| **CRITICAL** | 🚨 | **DoD-9(T8)·DoD-11(T6)이 issue-tracking 격리 테스트에 배정됐으나 실행 불가.** issue-tracking 은 identity-access 미의존(build.gradle 실측), `IssueTestcontainersBase:89` 가 issue-tracking 마이그레이션만 로드 → `project_memberships`(V007)·`global_permission_grants`(V036)·실제 resolver/어댑터 부재. prod 프로파일이면 `SystemPermissionResolver` 빈 부재로 NoSuchBean 부팅실패 | **DoD-9→T8(:modules:app 조립), DoD-11→T12(조립)로 이동.** T6 은 서비스 프로덕션+협력 mock 검증만. section 리뷰는 각 task 내부논리만 봐서 "타겟 모듈이 테스트를 호스팅 가능한가"를 구조적으로 못 물음 |
| **MEDIUM-HIGH** | 🟠 | **무조건 UPSERT 재연결이 admin 이 REST 로 바꾼 default 매핑을 매 부팅 되돌림.** `WorkflowSchemeController:198` + `V201:21` "admin 변경 가능 D11" | **dangling 수리 + 없는 것만 insert 로 축소**(T1). admin 보존 회귀 가드 테스트 추가 |
| 부수1 | 정정 | T2 매핑 DELETE predicate 가 스킴 기준 → FK 는 workflow_id 기준 | **workflow_id 기준으로 정정** |
| 부수2 | 정정 | T9 는 OpenApi 만이 아니라 서비스 scan 하는 모든 @SpringBootTest 깨짐 | **전수 열거로 확장** |

**검증한 것 (outside voice 4 질문 — 전부 실측).**
- EC-9(기존 DB 재연결 skip) — **문제 없음**. 복구를 `applyIfChanged` 밖(seedAll 말미)에 둬서 skip 무관하게 매 부팅 실행. 위치가 load-bearing(plan 에 못박음).
- 부분 UNIQUE 인덱스 — **실재**(`V201:106 ix_scheme_default_mapping`). insert 분기 `WHERE NOT EXISTS` 가 겨냥.
- cross-BC 원자성 — **prod 건전**. 단일 DataSource + auto-config tx-aware DSLContext ↔ NamedParameterJdbcTemplate 같은 커넥션. 어댑터 tx 경계 미선언(참여자). **단 조립 앱에서만 실증 가능**(T12).

**cross-model tension**: 없음(outside voice 발견 전부 실측 확인 후 채택 — Claude section 리뷰와 outside voice 가 상충 아니라 **보완**. section 은 task 내부, outside voice 는 모듈 호스팅 가능성).

**BLOCKER**: 없음(CRITICAL 은 발견 시점엔 blocker 였으나 T6/T8/T12 재배치로 해소).

### NOT in scope

- **아카이브(FR-PJ-04)** — S5~S10 시나리오·EC-2/3/5/10·`archived_at` 컬럼·issue-tracking 17곳 잠금 = **PR-4**.
- **목록/설정(FR-PJ-02/03)** — S5·§4.2 lead/require_2fa 충돌 = **PR-3**.
- **OCC version 컬럼(EC-7)** — `projects` 에 version 없음. 설정 동시변경 = PR-3 확정.
- **D6 프론트 UI** — PR-5. E2E·Zod 계약 포함.
- **FR-PM-10 관리화면** — PR-6.
- **identity-access 화이트리스트를 shared-kernel 상수로 이전** — PR-1 파일 변경(surgical 위반) = 후속 TODO.
- **FR-PJ-01 완료 마킹** — D6 UI(PR-5)가 자연 완료 시점(automation DEC-18 동형). 진척 122/129 불변.

### What already exists (재사용 vs 재구축)

| 기존 코드 | plan 활용 |
|---|---|
| `V201:137-145` default 매핑 CASE INSERT | T1 insert 분기가 이식(재구축 아님) |
| `seed-project.sql:58-67` key 기준 매핑 심기 | T1 패턴 선례 |
| `CurrentActor.kt`(컨트롤러 10곳+) | T7 actor 추출 재사용 |
| `SystemPermissionResolver.hasGlobalPermission`(PR-1) | T7 권한 게이트 소비 |
| `ProjectMembershipWritePort.addCreatorAsAdmin`(PR-1) | T6 소비만(어댑터 안 건드림) |
| `translateUniqueViolation` 패턴 | T5 409 변환 |
| `ProjectLead*`/`Require2fa*` 수직 슬라이스 | T5~T7 구조 선례 |
| `ix_scheme_default_mapping` 부분 인덱스 | T1 멱등 보장 |

**재구축 없음** — 병렬 코드 신설 0.

### 병렬화 전략 (worktree)

| 갈래 | 모듈 | task | 의존 |
|---|---|---|---|
| A. R6/R6-B | project-workflow | T1→T2→T3→T4 | 직렬(같은 파일) |
| B. FR-PJ-01 | shared-kernel+issue-tracking | T5→T6→T7→T9 | 직렬(계층 의존) |
| C. 조립 검증 | :modules:app | T8, T12 | A+B 후 |

- **Lane A ∥ Lane B** — project-workflow ↔ issue-tracking 파일 무교집합. 단 Gradle 모듈 컴파일 직렬화 유의([[bts-plan-wave-gradle-module-compile]]).
- **Lane C 는 A+B 완료 후** — 조립이 양쪽 산출물 필요(T8 depends-on 7, T12 depends-on 4·8·9).
- 실행: A + B 병렬 → 둘 다 머지 → C. 리뷰 뮤테이션 격리([[parallel-review-mutation-contaminates-peers]] — 뮤테이션 검증 에이전트와 읽기전용 리뷰어 같은 worktree 동시 금지).

### 실패 모드 (신규 코드경로)

| 경로 | 실패 방식 | 테스트 | 에러 처리 | 사용자 경험 |
|---|---|---|---|---|
| repairDefaultMappings | dangling 미수리 | T1 테2 | seedAll tx 롤백→부팅실패(fail-fast) | 부팅 로그(운영자) |
| seedAll 말미 복구 | 위치 오배치(applyIfChanged 안) | T3 판별자 | — | EC-9 매핑 누락→422 |
| cross-BC 생성 | 한쪽만 커밋 | T12 DoD-11 | @Transactional 롤백 | 500(부분생성 없음) |
| CREATE_PROJECT 게이트 | override 누락 | T8 DoD-9 | fail-closed(403) | **조용한 기능소실**(B8 — DoD-9 만 잡음) |
| 403 게이트 | vacuous(본문판별자 없음) | T7 C2 | — | 권한없어도 통과 |

**critical gap 없음** — 모든 실패모드에 테스트 또는 fail-fast 부팅차단. B8(조용한 소실)은 DoD-9(T8)이 전담.

## ★ 인계 — PR-2 가 반드시 지킬 것

**1. `CREATE_PROJECT` 판정은 `SystemPermissionResolver.hasGlobalPermission` 으로만 한다.**
`GlobalPermissionGrantRepository.hasGrant` 직접 호출은 **SYSTEM_ADMIN 이 탈락**한다 —
판정식 `grant OR isSystemAdmin` (ADR `2026-07-17-global-permission-grants` D-2) 의 뒷항이 소실된다.
리포지토리 KDoc 최상단에 경고가 박혀 있다.

**2. R6 와 R6-B 는 서로를 가려온 결함이다.**
R6 때문에 매핑이 늘 0행이라 FK RESTRICT 가 발동할 일이 없었고, **R6 를 고치는 순간 R6-B 가 활성화**된다 (스펙 S11 이 그 회귀 가드).
**로컬은 이미 이 지뢰 위에 있다** — `seed-project.sql:58-67` 이 매핑을 심어서 워크플로우 YAML 구조 수정 시 부팅 실패.
아직 아무도 안 밟았을 뿐. 패턴 동형 [[permitall-opens-preexisting-body-buffer-dos]].

**3. 병렬 진행 중인 PR 이 있다.**
#280 (FR-AT-07 PR-D — automation/프론트). 이 PR 과 파일 무충돌이나 **머지 직전 마이그레이션 V번호 재확인**은 관례.
백엔드 CI 부재 → 머지 전 로컬 검증 필수 ([[no-backend-ci-and-assembly-merge-verification-traps]]).

**4. 리뷰 단계 — 뮤테이션 검증 에이전트와 읽기전용 리뷰어를 같은 worktree 에서 동시에 돌리지 않는다.**
#278 에서 오탐 3건 발생 (리뷰어가 남의 임시 뮤테이션을 실제 코드로 인용).
`git show HEAD:` 대조가 유일하게 통한 방어 ([[parallel-review-mutation-contaminates-peers]]).

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR | 6 issues, 0 critical gaps (1 CRITICAL 발견→해소) |
| Outside Voice | Claude subagent | Independent 2nd opinion | 1 | issues_found | CRITICAL 1 + MEDIUM-HIGH 1 + 정정 2 — 전부 실측 확인 후 반영 |

- **CODEX:** Codex CLI 미설치 → Claude subagent 로 outside voice 실행. section 리뷰가 구조적으로 못 잡은 CRITICAL(DoD-9/DoD-11 모듈 호스팅 불가) 발견 — 가장 큰 성과.
- **CROSS-MODEL:** tension 없음. section 리뷰(task 내부논리)와 outside voice(모듈 호스팅 가능성)가 상충 아니라 보완. outside voice 4개 검증질문 전부 실측 확인(EC-9·부분인덱스·cross-BC원자성 건전, DoD 배치만 결함).
- **VERDICT:** ENG CLEARED — ready to implement. 6개 발견 전부 Maxi 승인 후 plan 반영(백필순서·DRY상수·EC판별자·DoD재배치·UPSERT축소·predicate정정). 신규 클래스 5→4, DoD-9/11 을 :modules:app 으로 이동.

NO UNRESOLVED DECISIONS
