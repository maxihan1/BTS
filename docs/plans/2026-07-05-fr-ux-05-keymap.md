# FR-UX-05 키보드 단축키

> slug: fr-ux-05-keymap
> type: ui
> agent: frontend-engineer
> primary_bc: personalization (논리 BC = apps/web)
> 생성: 2026-07-05

## Brief

FR-UX-05 키보드 단축키 구현. FR-UX-04(Cmd+K 팔레트)와 인접한 프론트 전용 기능.

기본 단축키 매핑(SDD 20.9, Gmail/Linear 스타일 참조):
- `?` — 단축키 도움말
- `c` — 이슈 생성
- `g i` — 내 이슈
- `g d` — 대시보드
- `/` — 검색
- `j / k` — 다음/이전 (보드, 리스트)
- `e` — 편집
- `m` — 담당자 변경
- `s` — 상태 변경
- `cmd+k` — Command Palette (FR-UX-04에서 이미 구현)

구현: react-hotkeys-hook + 도움말 모달(`?`).

백엔드 D3~D5 해당 없음(product §4.3). 커스터마이즈(FR-PF-03, `user_keymap` 테이블)는 별도 FR로 이 작업 범위 밖.

classify: type=ui, agent=frontend-engineer (primary_bc는 agile-planning으로 오판→personalization 정정).

## 도메인 정리

- BC: personalization (논리 BC = apps/web, 물리 백엔드 모듈 없음). FR-UX-04와 동일 패턴.
- 영향 엔티티: 없음 (프론트 코드 상수 단축키 레지스트리). 백엔드 도메인/DTO/마이그레이션 0.
- 새 용어: 없음 (glossary 갱신 불필요 — "단축키"는 일반 어휘, 새 유비쿼터스 언어 아님).
- 기존 결정 충돌: 없음. FR-UX-04(Cmd+K 팔레트)와 상보 — `cmd+k`는 이미 구현됨.
- **Maxi 결정 (2026-07-05, /bts-domain 게이트)**:
  1. **범위** — 전역 네비게이션 5종 + 도움말(`?` `c` `/` `g i` `g d`). `j/k/e/m/s`는 후속 FR로 제외.
  2. **구현** — 커스텀 훅(의존성 0), FR-UX-04 `useCommandPalette` 선례. react-hotkeys-hook 미도입.
- 관련 ADR: [docs/decisions/2026-07-05-fr-ux-05-keymap.md](../decisions/2026-07-05-fr-ux-05-keymap.md) (생성됨)
  - D1 프론트 전용 / D2 커스텀 훅(product D6 react-hotkeys-hook deviation) / D3 MVP 범위 5종 / D4 논리≠물리
- product 문서 deviation: `personalization.md §4.3` D6를 커스텀 훅 + 전역 5종으로 조정 (구현 PR서 전수 동기화). fr-index 카운트 불변.

## 스펙

전체 스펙. [docs/specs/2026-07-05-fr-ux-05-keymap.md](../specs/2026-07-05-fr-ux-05-keymap.md)

핵심 3줄 요약.
- 커스텀 훅 `useKeyboardShortcuts`(native keydown, 의존성 0)가 전역 5종 처리 — `c`→`/issues/new`, `/`→`/search`, `g i`→`/issues`, `g d`→`/dashboards`, `?`→도움말 모달.
- 가드 4종(enabled/IME/입력포커스/수정자) + leader-key 시퀀스(타임아웃 1초). 단일 진실 출처 레지스트리 `SHORTCUTS`가 훅 dispatch와 도움말 모달을 함께 구동.
- `__root.tsx`에서 인증 시에만 렌더(FR-UX-04 useCommandPalette 패턴). 백엔드/DB/API 0.

## Brainstorming Check

✅ 통과 (office-hours 스킵 — 범위/구현 Maxi 확정 + 라우트 실재 확인. 설계 명확화 4건 접음: e.key 매칭 / SHORTCUTS 단일 진실 출처 / 가드 순서 early-return / 다이얼로그 스택. Maxi 결정 필요 gap 0.)

## Plan

파일 배치 (command-palette/ 미러). `apps/web/src/components/keyboard-shortcuts/`.
- `shortcuts.ts` — SHORTCUTS 레지스트리 + 순수 `resolveKeydown` 리듀서 + 가드 술어
- `useKeyboardShortcuts.ts` — keydown 리스너 훅 (가드/leader 상태/타이머/enabled/dispatch)
- `ShortcutsHelpDialog.tsx` — radix-ui Dialog 도움말 모달 (SHORTCUTS 구동)
- `__root.tsx` — 결선 (인증 시에만, useCommandPalette 패턴)

프리미티브 사실. `radix-ui` v1.4.3(`import { Dialog as DialogPrimitive } from 'radix-ui'`, ResolutionPickerModal 선례) · `useNavigate`(@tanstack/react-router) 사용.

### Task 1. shortcuts 레지스트리 + 순수 resolveKeydown 리듀서

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/keyboard-shortcuts/shortcuts.ts`, `apps/web/src/components/keyboard-shortcuts/shortcuts.test.ts`]
- depends-on: []

**RED**:
- 파일: `shortcuts.test.ts`
- 테스트(순수 함수라 jsdom 불필요, KeyboardEvent-유사 객체 주입):
  - `resolveKeydown`가 `c`→`{kind:'navigate', to:'/issues/new'}`, `/`→`{kind:'navigate', to:'/search'}` 반환
  - `?`(e.key==='?')→`{kind:'toggle-help'}`, `/`(e.key==='/')와 구분(★e.key 매칭, code/keyCode 금지)
  - leader: `g`(pending 없음)→`{kind:'set-leader'}`; 직후 `i`(pending='g')→`{kind:'navigate', to:'/issues'}`; `d`(pending='g')→`{kind:'navigate', to:'/dashboards'}`
  - leader 리셋: `g` 후 `x`(무효)→`{kind:'reset'}`(E2); pending='g'에서 `g`→`{kind:'set-leader'}`(E3 재시작)
  - ★도움말 열림 가드: `helpOpen=true`이면 `?`→`{kind:'toggle-help'}`(닫기)만 활성, `c`/`/`/`g` 등은 `{kind:'none'}`(E7 — 모달 이탈 방지, 리뷰 발견)
  - 가드 술어 `shouldIgnoreEvent`: isComposing=true→true(E5), target이 input/textarea/select/contentEditable→true(E4), meta/ctrl/alt→true(E6·FR6, 단 `?`의 shift 단독은 허용)
- 실패(예상): `shortcuts.ts` 없음

**GREEN**:
- 파일: `shortcuts.ts`
- `SHORTCUTS` 상수(단일 진실 출처: keys 표기·라벨·설명·action). `resolveKeydown(e, pendingLeader, helpOpen)` 판별 유니온 반환(navigate/toggle-help/set-leader/reset/none). helpOpen=true면 toggle-help만 활성. `shouldIgnoreEvent(e)` 가드 술어. 라우트는 FR-UX-04 QUICK_LINKS와 동일.

**REFACTOR**:
- 액션 유니온 타입 export(훅·테스트 공유). KDoc(파일 L1 한글 주석 포함).

**검증**: `pnpm --filter web test shortcuts.test`

### Task 2. useKeyboardShortcuts 훅 (리스너·leader 타이머·enabled·dispatch)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/keyboard-shortcuts/useKeyboardShortcuts.ts`, `apps/web/src/components/keyboard-shortcuts/useKeyboardShortcuts.test.tsx`]
- depends-on: [1]

**RED**:
- 파일: `useKeyboardShortcuts.test.tsx` (renderHook + document keydown dispatch)
- 테스트:
  - enabled=false → document keydown 리스너 미등록, `c` 눌러도 navigate 미호출(E8·FR7, FR-UX-04 E5 선례)
  - enabled=true, `c` keydown → navigate('/issues/new') 1회(useNavigate mock)
  - `g`→`i` 순차 keydown(타이머 내) → navigate('/issues'); `g` 후 fake timer 1초 경과 후 `i` → navigate 미호출(E1)
  - `?` keydown → helpOpen true; 재 `?` → false(토글, E9). setHelpOpen 노출
  - ★helpOpen=true에서 `c` keydown → navigate 미호출(도움말 열림 중 이탈 방지, E7 리뷰 발견); `?` → 닫힘
  - 입력창 target에서 `c` → navigate 미호출(가드가 preventDefault 이전 early-return, FR9 순서)
  - 언마운트 시 리스너/타이머 cleanup(NFR4)
- 실패(예상): `useKeyboardShortcuts` 없음

**GREEN**:
- 파일: `useKeyboardShortcuts.ts`
- `useKeyboardShortcuts(enabled): { helpOpen, setHelpOpen }`. `useNavigate()` + `useState(helpOpen)` + `useRef(pendingLeader)` + `useRef(timerId)` + `useRef(helpOpen)`(리스너 클로저 stale 방지). useEffect([enabled])에서 리스너 등록/해제, enabled=false면 미등록+pending 리셋. handleKeyDown: shouldIgnoreEvent 가드(early return, preventDefault 전) → resolveKeydown(e, pending, helpOpenRef.current) → 액션 dispatch(navigate/setHelpOpen/leader ref 갱신+타이머). 가드 순서 FR-UX-05 스펙 §설계 명확화 준수.

**REFACTOR**:
- leader 타이머 상수(1000ms) 추출. KDoc.

**검증**: `pnpm --filter web test useKeyboardShortcuts.test`

### Task 3. ShortcutsHelpDialog (radix-ui Dialog, SHORTCUTS 구동)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/keyboard-shortcuts/ShortcutsHelpDialog.tsx`, `apps/web/src/components/keyboard-shortcuts/ShortcutsHelpDialog.test.tsx`]
- depends-on: [1]

**RED**:
- 파일: `ShortcutsHelpDialog.test.tsx`
- 테스트:
  - open=true → role=dialog + Title("키보드 단축키") 렌더, SHORTCUTS 5종 + `Cmd+K` 항목 각각 표시(FR8, 단일 진실 출처 구동)
  - 비구현 `j/k/e/m/s` 미표시(FR8 제약)
  - onOpenChange(false)(Esc/오버레이) → 콜백 호출
- 실패(예상): `ShortcutsHelpDialog` 없음

**GREEN**:
- 파일: `ShortcutsHelpDialog.tsx`
- Props `{ open, onOpenChange }`. `DialogPrimitive.Root/Portal/Overlay/Content/Title`(ResolutionPickerModal 선례 클래스). SHORTCUTS.map으로 kbd 표기+설명 렌더. `Cmd+K`(팔레트, FR-UX-04 이미 구현) 별도 표기 항목 포함.

**REFACTOR**:
- kbd 표기 렌더 소컴포넌트/헬퍼. KDoc.

**검증**: `pnpm --filter web test ShortcutsHelpDialog.test`

### Task 4. __root.tsx 결선 (인증 시 훅+도움말 모달)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/__root.tsx`, `apps/web/src/routes/__root.test.tsx`]
- depends-on: [2, 3]

**RED**:
- 파일: `__root.test.tsx` (없으면 신규; 있으면 케이스 추가)
- 테스트:
  - 인증 상태 → ShortcutsHelpDialog 렌더 결선 존재 + `?` keydown 시 도움말 노출(useKeyboardShortcuts 결합)
  - 비인증 → 훅 enabled=false(단축키 무반응), 모달 미렌더
  - 기존 CommandPalette/Header 결선 회귀 없음
- 실패(예상): __root에 결선 부재

**GREEN**:
- 파일: `__root.tsx`
- `const { helpOpen, setHelpOpen } = useKeyboardShortcuts(isAuthenticated)` 추가. `{isAuthenticated && <ShortcutsHelpDialog open={helpOpen} onOpenChange={setHelpOpen} />}` 렌더(CommandPalette 인접). useNotificationStream/useCommandPalette 패턴(항상 호출 + 인증가드 조건렌더) 유지.

**REFACTOR**:
- import 정렬. 주석.

**검증**: `pnpm --filter web test __root.test && pnpm --filter web typecheck`

### Task 5. E2E (Playwright) — 5 단축키 + 가드

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/keyboard-shortcuts.spec.ts`, `apps/web/e2e/support/*` (필요 시 MSW 핸들러 재사용)]
- depends-on: [4]

**RED→GREEN** (E2E는 시나리오 작성 = RED, 통과 = GREEN):
- S1 `c`→URL `/issues/new`
- S2 `/`→URL `/search`
- S3 `g i`→`/issues`, `g d`→`/dashboards`
- S4 `?`→도움말 모달 노출, `Esc`→닫힘 / 재 `?`→토글 닫힘(E9)
- S5 입력창 포커스 후 `c` 타이핑 → URL 불변(가드)
- (E8 비로그인은 단위테스트 커버, E2E는 인증 세션 기준)
- MSW: `/search`·`/dashboards`·`/issues` 렌더에 필요한 기존 핸들러 재사용(신규 최소화). getByRole exact 주의(playwright-getbyrole-exact-strict-mode). orphan 5173 청소.

**검증**: `pnpm --filter web exec playwright test keyboard-shortcuts`

## Plan 메타

- task 수: 5
- 예상 wave: 4 (W1=T1 / W2=T2·T3 병렬 / W3=T4 / W4=T5). FR-UX-04와 동형.
- TDD 강제: yes (test 커밋 먼저)
- 병렬 dispatch: bts-impl이 depends-on+files로 wave 계산. W2는 T2·T3 파일 무교집합 → 병렬.
- 추가 검증: typecheck(tsconfig.app.json) · lint · vitest 전량 · playwright · verify-master-plan 123/123
- sub-agent: frontend-engineer(T1~T4) + qa-engineer(T5). backend/security/db 0.
- 공유 worktree 병렬 커밋 위생: `--no-verify` + 자기 pathspec만 add(lint-staged 전역 stash 충돌 회피, parallel-dispatch-precommit-hook-race 선례).

## 리뷰 결과

**리뷰 방식**. type=ui이나 프론트 전용 저위험(mutation 0·의존성 0·전부 관용 패턴 재사용)이라 `/plan-design-review` 대신 eng+design 집중 자체 리뷰로 규모 맞춤(learnings [[bts-review-plan-autoplan-overkill]] + FR-UX-04 선례).

### Eng 리뷰 (2026-07-05)
- ✅ `?`/`/` 동일 물리키 → `e.key` 매칭으로 구분(plan §설계 명확화). code/keyCode 금지 명시.
- ✅ 가드 순서 early-return(preventDefault 전) → 입력창 `/` 정상 타이핑 보존.
- ✅ 타이머/리스너 cleanup(NFR4), enabled=false 시 pending 리셋.
- ✅ vitest는 타입 무시 → typecheck(tsconfig.app.json) 동반 명시(learnings zod-schema-strengthen-inline-mock-fanout).
- ✅ __root.tsx 단일 task(T4)만 수정 → 공유 파일 충돌 없음.
- **★발견 (수정 반영)**: 스펙 E7("도움말 열림 중 `c`/`/` 억제")이 입력 포커스 가드만으로는 미충족 — Radix Dialog 내용은 `<div>`(input 아님)라 `c`가 누출돼 모달 열린 채 `/issues/new`로 이탈. → `resolveKeydown(e, pending, helpOpen)`에 helpOpen 인자 추가, 열림 중엔 toggle-help만 활성(T1/T2 수정). 리스너 클로저 stale 방지용 `helpOpenRef` 동기화도 T2에 명시.
- ⚠️ 주의(비차단): 명령 팔레트는 cmdk `<input>` 포커스로 가드에 걸려 안전(S7/E6). 팔레트 내 비-input 요소 포커스 시 누출은 FR-UX-04와 동일한 기존 동작 — 범위 밖.
- BLOCKER: 없음.

### Design 리뷰 (2026-07-05)
- ✅ 도움말 모달은 ResolutionPickerModal(radix Dialog) 선례 재사용 → 포커스 트랩/Esc/포커스 복원/오버레이 일관.
- ✅ 단일 진실 출처 `SHORTCUTS`가 도움말 목록 구동 → 표기 drift 0. 비구현 `j/k/e/m/s` 미표시(FR8).
- ⚠️ 주의(비차단): `?` 발견성 — 사용자가 `?` 존재를 모를 수 있음. MVP는 `?` 관용(universal) + Cmd+K 팔레트가 보조. 상시 힌트(footer "press ? for shortcuts")는 후속 개선 후보(scope 밖).
- BLOCKER: 없음.

### 종합
BLOCKER 0. 실 발견 1건은 plan(T1/T2)에 반영 완료. 게이트 1 진입 가능.
