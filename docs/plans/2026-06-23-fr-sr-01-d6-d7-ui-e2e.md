# FR-SR-01 D6/D7 — 이슈 필터(다중 필드 조합) 프론트엔드 UI + E2E

> slug: fr-sr-01-d6-d7-ui-e2e
> type: ui
> agent: frontend-engineer (E2E task는 qa-engineer)
> 생성: 2026-06-23

## Brief

FR-SR-01 이슈 필터(다중 필드 조합)의 D6(프론트 UI) + D7(E2E) 단계.
백엔드 D1~D5 완료(#180, 72/123). `GET /api/v1/issues`에 status/assignee/label/component
필터 파라미터 추가됨(필드 내 OR + 필드 간 AND, BoardCardFilter 동형). visibility는
buildActiveSecureWhere 보안술어 위 AND결합. V029 부분인덱스.

classify 원판정 type=qa(E2E 키워드 오판) → ui로 교정(FR-NT-03/FR-TT-02 동일 선례).
primary_bc=issue-tracking. FR-BL-01 backlog-lexorank worktree/draft PR #179는 유지(별개).

## 도메인 정리

- **BC**: issue-tracking (물리 구현). 논리 FR 소속은 search-export-import 유지 (논리 ≠ 물리, ADR D1/D2).
- **영향 영역**: 프론트엔드 이슈 목록 화면 (apps/web). 백엔드 0 변경(이미 #180 완료).
- **새 용어**: 없음. 필터 4종(status/assignee/label/component)은 기존 개념. status=워크플로우 상태,
  assignee=담당자(unassigned 센티널 포함), label=라벨 이름, component=컴포넌트.
- **API 계약 (소비 대상, #180)**:
  - `GET /api/v1/issues?projectKey=&status=&assignee=&label=&component=&page=&size=`
  - 같은 파라미터 다중 값 = OR, 다른 파라미터 간 = AND (괄호/중첩 미지원 — 그건 FR-SR-02 AQL).
  - assignee: UUID 또는 `"unassigned"`(대소문자 구분 센티널). label: 이름 문자열. component: UUID.
  - 잘못된 UUID → 400 `ISSUE_FILTER_INVALID_UUID`.
  - 응답: `Page<IssueResponse>` (기존 목록 응답과 동일 스키마).
- **기존 결정 충돌**: 없음.
- **관련 ADR**: [docs/decisions/2026-06-23-fr-sr-01-issue-filter-bc.md](../decisions/2026-06-23-fr-sr-01-issue-filter-bc.md)
  (백엔드 PR #180에서 생성, D6/D7은 신규 ADR 불필요).
- **참고**: 보드 필터(FR-BD-02)가 동일 4필드 필터 UI를 이미 보유 가능 → 재사용/패턴 정렬 확인 필요(spec에서).

## 스펙

전체 스펙. [docs/specs/2026-06-23-fr-sr-01-d6-d7-ui-e2e.md](../specs/2026-06-23-fr-sr-01-d6-d7-ui-e2e.md)

핵심 결정 (Maxi 확정).
- **재사용 = 이슈 전용 신규 + 공유부품 재사용**. `IssueFilterBar` + `lib/issue-filter.ts` 신규, 보드 코드 0 변경.
  공유부품(`LabelAutocompleteInput`/`ComponentMultiSelect`/`useUsers`·`useLabels`·`useComponents`·`useDebounce`) 재사용.
- **status 옵션 = 전체 워크플로우 수집**. `GET /api/v1/workflows` states flatten + key dedupe, displayOrder 정렬. 신규 `useWorkflows`.
- **필터 상태 = URL search params** (`?status=&assignee=&label=&component=&page=`), 새로고침/공유 보존.
- **queryKey filter-aware** `['issues', projectKey, page, filter]`, bulk invalidate `['issues', projectKey]` prefix 정합.
- **클라 필터링 0** — 백엔드 SQL 위임. 필터 변경 시 page=0 리셋 + 일괄 선택 클리어(FR10).

## Brainstorming Check

✅ 통과 (1회 iteration). gap 4건 발견·반영 — G1 필터 변경 시 선택 클리어(FR10, 핵심)·G2 status 표시명 노출·G3 MSW workflows 핸들러·G4 필터 바 위치.

## Plan

레이어별 TDD task 7개. 재사용 자산: `LabelAutocompleteInput`/`ComponentMultiSelect` 컴포넌트,
`useUsers`/`useUsersByIds`/`useLabels`/`useComponents`/`useDebounce` 훅, `fetchWorkflows` API,
`board-filter.ts`·`BoardFilterBar` 패턴(미러, 보드 코드 0 변경), `workflow-handlers.ts` MSW(status 옵션). 백엔드 변경 0.

### Task 1. api/issues.ts — `fetchIssues` 필터 확장 + `IssueFilterParams` + query 조립

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/issues.ts`, `apps/web/src/api/issues.test.ts`]
- depends-on: []

**RED**: `issues.test.ts` — `fetchIssues({ projectKey, page, size, filter })`가 올바른 query string 생성(fetch mock URL 단언).
- `{ statusKeys:['open','in_progress'] }` → `?...&status=open&status=in_progress`
- `{ assigneeIds:['a1'], includeUnassigned:true }` → `?...&assignee=a1&assignee=unassigned`
- `{ labels:['bug'] }` → `?...&label=bug`, `{ componentIds:['c1'] }` → `?...&component=c1`
- 빈 필터 / filter 미전달 → 필터 param 없음(기존 `projectKey&page&size`만, 하위호환).

**GREEN**: `IssueFilterParams = { statusKeys: string[]; assigneeIds: string[]; includeUnassigned: boolean; labels: string[]; componentIds: string[] }` export + `buildIssueFilterQuery(params, filter?)` 헬퍼(URLSearchParams append, `unassigned` 센티널, 빈 배열 생략) + `FetchIssuesParams`에 `filter?: IssueFilterParams` 추가. 응답 스키마 불변.

**REFACTOR**: query 조립 KDoc, 빈-필터 단축 경로. 기존 `fetchIssues({projectKey,page,size})` 호출 호환 명시.

**검증**: `pnpm --filter web test issues.test`

### Task 2. lib/issue-filter.ts — URL search ↔ `IssueFilterParams` 매핑 (순수 함수, 신규)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/lib/issue-filter.ts`, `apps/web/src/lib/issue-filter.test.ts`]
- depends-on: [1]

**RED**: `issue-filter.test.ts` — `board-filter.ts` 동형 + status.
- `searchToIssueFilter({ status:['open'], assignee:['a1','unassigned'], label:['bug'], component:['c1'] })` → `{ statusKeys:['open'], assigneeIds:['a1'], includeUnassigned:true, labels:['bug'], componentIds:['c1'] }`.
- 단일 문자열도 배열 정규화(`toArray`), undefined → 빈 배열.
- `issueFilterToSearch(filter)` → URL search 객체, 빈 배열/false는 키 생략(`?status=` 잔존 금지), `includeUnassigned` → assignee 배열에 `unassigned`.
- `isEmptyIssueFilter(filter)` 헬퍼.

**GREEN**: `IssueFilterSearch` 인터페이스(`status/assignee/label/component?: string|string[]`) + `searchToIssueFilter`/`issueFilterToSearch`/`isEmptyIssueFilter` 순수 함수. `UNASSIGNED='unassigned'` 상수. (board-filter.ts 미러, 보드 파일 import 금지 — 독립.)
- **★B1 — `normalizeIssueFilter(filter)` 추가**: `statusKeys`/`assigneeIds`/`labels`/`componentIds`를 각각 `.sort()`한 새 객체 반환. queryKey 안정성용(`use-boards.ts:20-32` normalizeFilter 미러). RED에 "키 순서만 다른 동일 필터 → 동일 정규화 결과" 단언 추가.

**REFACTOR**: KDoc, 상수 추출.

**검증**: `pnpm --filter web test issue-filter`

### Task 3. hooks/use-workflows.ts — `useWorkflows` + `extractStatusOptions` (신규)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/hooks/use-workflows.ts`, `apps/web/src/hooks/use-workflows.test.tsx`]
- depends-on: []

**RED**: `use-workflows.test.tsx` —
- `useWorkflows()` queryFn이 `fetchWorkflows` 호출, queryKey `['workflows']`, staleTime 설정.
- `extractStatusOptions(workflows)` → 모든 워크플로우 `states` flatten + `key` 중복 제거 + 정렬 → `{ key, name }[]`.
- **★B4 — 결정적 정렬 규칙(비결정/flaky 차단)**: 같은 key가 워크플로우마다 다른 displayOrder/name을 가질 수 있음(workflow-fixtures 실측: in_progress=2/3/3 등). dedupe는 **첫 등장 항목(key,name,displayOrder) 채택**, 최종 정렬은 **(displayOrder asc, key asc) 튜플**로 안정 정렬. RED에 displayOrder 충돌 fixture(동일 key 다른 order)로 결정적 순서 단언.
- 참고(Maxi 확정 트레이드오프): 전체 워크플로우 수집이라 ATLAS 미사용 상태도 노출될 수 있음(spec D2에서 단순성 위해 수용). 본 task는 정렬 결정성만 보장.

**GREEN**: `useWorkflows()` react-query 훅 + `extractStatusOptions(workflows: WorkflowView[]): StatusOption[]` 순수 함수(첫 등장 dedupe + (displayOrder,key) 정렬).

**REFACTOR**: KDoc, `StatusOption` 타입 export.

**검증**: `pnpm --filter web test use-workflows`

### Task 4. components/issues/IssueFilterBar.tsx — 필터 바 UI (status 포함, 제어형, 신규)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/issues/IssueFilterBar.tsx`, `apps/web/src/components/issues/IssueFilterBar.test.tsx`, `apps/web/src/i18n/issue-filter-labels.ts`]
- depends-on: [1, 3]

**RED**: `IssueFilterBar.test.tsx` —
- status 멀티셀렉트 선택/해제 → `onChange`(statusKeys 갱신), 옵션·칩에 표시명(name) 노출(키 아님).
- 담당자 선택/해제 → assigneeIds, "미배정" 토글 → includeUnassigned.
- 라벨 자동완성 선택 → labels + 칩, 컴포넌트 체크 → componentIds.
- 칩 개별 ✕ → 해당 조건만 제거, "초기화" → 빈 filter onChange.
- 활성 개수 표시, 접근성(label 연결·칩 aria-label).

**GREEN**: `IssueFilterBar({ projectKey, value, onChange })` 제어형. status=`useWorkflows`+`extractStatusOptions` 멀티셀렉트(name 표시, key 전송), 담당자=`useUsers` typeahead+미배정, 라벨=`LabelAutocompleteInput`, 컴포넌트=`ComponentMultiSelect`(`useComponents`). 칩 + 초기화 + 활성 개수. 컨테이너 `flex flex-wrap items-end gap-3 rounded-lg border bg-muted/30 p-4`(보드 미러).
- **★C4 — status 셀렉터는 신규 서브컴포넌트** `StatusMultiSelect`(이 파일 내 비공개). `ComponentMultiSelect`는 `Component{id,name}` props라 `StatusOption{key,name}`과 형태가 달라 그대로 재사용 불가. 보드 0변경 제약상 ComponentMultiSelect 제네릭화 금지 → status 전용 체크박스 멀티셀렉트 신규(옵션·칩에 name, value엔 key).
- **★C1 — fail-safe(spec EC7)**: `useWorkflows` 로딩/에러 시 status 섹션은 빈 목록·비활성으로 렌더(throw 금지). 다른 필터는 정상 동작.

**REFACTOR**: `issue-filter-labels.ts` i18n(콜론 종결 금지), status/담당자/칩 서브컴포넌트, aria 정리. 반응형 flex-wrap·터치 44px.

**검증**: `pnpm --filter web test IssueFilterBar`

### Task 5. routes/issues.index.tsx + router.ts — 통합 결선

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/issues.index.tsx`, `apps/web/src/routes/issues.index.test.tsx`, `apps/web/src/router.ts`]
- depends-on: [1, 2, 4]

**RED**: `issues.index.test.tsx` —
- URL search filter가 `searchToIssueFilter` → `fetchIssues(filter)` 전달, queryKey `['issues',projectKey,page, normalizeIssueFilter(filter)]`(★B1 정규화된 filter).
- `IssueFilterBar` 변경 → `navigate`로 URL search 갱신(빈 필터 시 param 제거), **page=0 리셋 + clearAll**(FR10).
- **★C3 — clearAll 트리거 정합**: filter가 **실제로 바뀐 경우에만** clearAll(이전 filter와 비교). 단순 page 이동(filter 동일)은 선택 누적 유지(FR10 단서) — RED에 "page만 변경 시 selection 유지" 회귀 단언.
- 필터 결과 0건 → 빈 상태 + **"필터 초기화" CTA**(EC1).
- bulk invalidate `['issues',projectKey]` prefix가 filter 캐시 무효화(EC8 회귀 가드 — 4-요소 queryKey prefix 매칭 단언).
- **★C1 — 기존 T1~T16 회귀 무손상**: `renderPage` 헬퍼에 workflow/user/component/label MSW 핸들러 등록 추가(IssueFilterBar 새 네트워크 호출). status 배지 `role="status"`(issues.index.tsx:104)와 필터 바 상태 텍스트의 셀렉터 충돌 회피(컨테이너 한정).

**GREEN**:
- `router.ts` issues 라우트 `validateSearch`에 `status/assignee/label/component` 배열 스키마 추가(단일값→배열 정규화, 기존 `page` 보존, 타 search 충돌 0 확인).
- `IssueListRouteAdapter`가 `useSearch`로 filter 추출 → `searchToIssueFilter` → IssueListPage prop. onChange → `issueFilterToSearch` → navigate(page:0).
- `IssueListPage`가 filter prop 수신 → queryKey `normalizeIssueFilter` 적용 + `fetchIssues({...,filter})` + `IssueFilterBar` 렌더(헤더 아래·일괄 액션 바 위, G4) + 0건 빈상태 초기화 CTA + filter 실변경 시 `clearAll`.
- **★C6 — EC9(직접 URL 조작 400)**: `retry:false`라 잘못된 UUID는 error 상태. 본 PR 범위는 정상 옵션 선택만 다루므로 EC9는 일반 에러 표시로 처리(필터 초기화 CTA는 0건 빈상태 전용). plan에 "EC9 = 일반 에러 경로, 전용 회복 UI 범위 외" 명시.

**REFACTOR**: filter useMemo 안정화(참조)+normalizeIssueFilter(값) 별개 명시, 주석.

**검증**: `pnpm --filter web test issues.index && pnpm --filter web typecheck`

### Task 6. mocks/issue-handlers.ts — query param stateful 필터링 (status 포함)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/mocks/issue-handlers.ts`, `apps/web/src/mocks/issue-handlers.test.ts`, `apps/web/src/mocks/issue-fixtures.ts`]
- depends-on: []

**RED**: `issue-handlers.test.ts` — `GET /api/v1/issues`가 `status`/`assignee`/`label`/`component` query param을 읽어 이슈 필터링.
- 필드내 OR(status=open&status=done), 필드간 AND(status=open&assignee=a1), `assignee=unassigned`(미배정), 빈 param → 전체(소프트삭제 제외 유지).

**GREEN**:
- **★B2 — `listIssuesHandler`를 `({ request })` 시그니처로 재작성**(현재 `() =>`라 query param을 전혀 안 읽음 — "강화" 아닌 신규 파싱). `new URL(request.url).searchParams.getAll(...)` 로 status/assignee/label/component 추출 → `matchesIssueFilter(issue, params)`(board-handlers `matchesFilter` 패턴 + `status=params.getAll('status').includes(issue.currentStateKey)` 분기)로 `buildFilteredPage` 필터링.
- **★B3 — fixture 분별 시드(E2E vacuous 차단)**: 현 `issuePageFixture` 4개 이슈는 assigneeId=null·labels=[]·componentIds=[] **전부 균일**(필터 무효). 이슈별로 `assigneeId`(alice/bob/null 혼합), `labels`(['bug'] 등), `componentIds`(실제 component fixture UUID) 분포를 명시 부여. ★주의: `userAliceFixture.id`가 `issueAtlas2Fixture.id`와 동일 UUID라 assignee에 쓰면 혼란 — 별도 UUID 사용 또는 명확히 분리. status는 기존 다양(open/in_progress/done/in_review) 활용.
- status 옵션 MSW는 기존 `workflow-handlers.ts` 재사용(GET /api/v1/workflows states 응답 확인). currentStateKey 값과 workflow states.key 매칭 확인.

**REFACTOR**: 필터 술어 헬퍼(`matchesIssueFilter`), 메모리 교훈(stateful 영속·shared store 시드) 반영.

**검증**: `pnpm --filter web test issue-handlers`

### Task 7. E2E — issue-filter.spec.ts

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/issue-filter.spec.ts`]
- depends-on: [5, 6]

**시나리오(MSW 실필터 관통, vacuous 금지, board-filter.spec.ts 참고)**: S1 status 필터로 이슈 감소 · S2 담당자 · S3 미배정 · S4 라벨 · S5 컴포넌트 · S6 복합 AND(status+assignee) · S8 초기화(URL param 제거) · S9 새로고침 후 필터 보존(URL search).
- **★B3 — 각 시나리오는 "필터 후 건수 < 전체"를 숫자로 단언**(board-filter.spec S1 "alice 카드 2개만" 패턴). vacuous 통과 차단. Task 6 분별 시드와 정합.
- strict-mode 셀렉터는 이슈 목록 컨테이너(`getByRole('list',{name:'이슈 목록'})`) within 한정(텍스트 중복 회피).

**검증**: `pnpm --filter web test:e2e issue-filter`

## Plan 메타

- task 수: 7
- wave (depends-on + files 교집합 기준):
  - **W1**: T1(api/issues), T3(use-workflows), T6(mocks) — 독립 병렬
  - **W2**: T2(lib/issue-filter, dep[1]), T4(IssueFilterBar, dep[1,3]) — 파일 비겹침 병렬
  - **W3**: T5(routes+router, dep[1,2,4])
  - **W4**: T7(E2E, qa-engineer, dep[5,6])
- 예상: 7 task, 4 wave
- TDD 강제: yes (test: 커밋이 feat: 보다 선행)
- 추가 검증: typecheck, lint, vitest, playwright(qa), `pnpm build`

## 리뷰 결과

### plan-design-review (2026-06-23) — 집중 디자인 리뷰 (직접 적용)

무거운 mockup/외부보이스 절차는 이 작업(기존 BoardFilterBar 패턴 재사용 + DESIGN.md 확립)에 과해 생략(선례 [[bts-review-plan-autoplan-overkill]]).
- ✅ 빈 상태: EC1 0건 안내 + "필터 초기화" CTA(회복 경로) 명시.
- ✅ 활성 필터 가시성: 활성 개수 표시(Task 4)로 "왜 이슈 적지?" 혼란 차단.
- ✅ 반응형(WCAG AA): flex-wrap 세로 적층, 터치 44px, label 연결·칩 aria-label·키보드(Task 4).
- ✅ AI slop 없음: 보드 필터 패턴 미러.
- **BLOCKER: 없음.**

### eng 적대적 독립 리뷰 (2026-06-23, Plan 에이전트) — 실코드 12파일 대조

**BLOCKER 4건 — 전부 plan 반영 완료.**
- ✅ **B1** queryKey 직렬화 안정성: `normalizeIssueFilter`(배열 sort) 누락 → Task 2 GREEN 추가(`use-boards.ts:20-32` 미러), Task 5 queryKey가 정규화 filter 사용. useMemo(참조)와 정규화(값)는 별개.
- ✅ **B2** MSW 미필터링: `listIssuesHandler`가 현재 `() =>`로 query param 미수신("강화" 과소평가) → Task 6 GREEN `({request})` 재작성 명시.
- ✅ **B3** E2E vacuous: issue-fixtures 4개가 assignee/label/component 전부 균일 → Task 6 분별 시드(alice/bob/null·labels·componentIds 분포) + Task 7 건수 숫자 단언. (alice id가 issueAtlas2 id와 동일 UUID 충돌 주의 반영.)
- ✅ **B4** status 비결정 정렬: 동일 key의 displayOrder가 워크플로우마다 상이 → Task 3 첫 등장 dedupe + (displayOrder,key) 안정 정렬 규칙 명시. 전체 수집 트레이드오프는 Maxi 확정(spec D2) 수용.

**CONCERN 6건 처리.**
- ✅ C1 기존 테스트 회귀: renderPage 헬퍼에 workflow/user/component/label 핸들러 등록 + 셀렉터 충돌 회피 → Task 5 RED.
- ✅ C3 clearAll 경합: filter 실변경 시만 clearAll, page-only 유지 → Task 5 RED 회귀 단언.
- ✅ C4 status 셀렉터 미명세: `StatusMultiSelect` 신규 서브컴포넌트(ComponentMultiSelect 제네릭화 금지=보드 0변경) → Task 4.
- ✅ C6 EC9 400: 일반 에러 경로로 처리(전용 회복 UI 범위 외) → Task 5 명시.
- ✅ C2 fetchIssues URL 단언 mock: request.url 캡처 방식 → Task 1 RED(기존 T1-3 격리).
- ✅ C5 wave 의존성: Task4가 dep[2] 미포함 정확(매핑은 Adapter 책임). 그래프 정확·순환 없음.

**NIT**: N1 CSRF 무근거(GET, 해소) · N2 issue-handlers.test 신규(정합) · N3 i18n 콜론 종결 금지 반영 · N4 validateSearch 타 search 충돌 0 확인(Task 5).

**머지 차단 BLOCKER: 0건 (4건 모두 plan 반영 후 해소).**
