<!-- FR-WF-02 D6 워크플로우 스킴 관리 UI plan — 채워가는 진행 문서 -->

# FR-WF-02 D6 — 워크플로우 스킴(Workflow Scheme) 관리 UI

> slug: fr-wf-02-d6-ui-crud
> type: ui
> agent: frontend-engineer
> primary_bc: **project-workflow** (classify-task 의 issue-tracking 분류는 키워드 오인. plan 단계에서 정정)
> 생성: 2026-05-28

## Brief

PR #18 (FR-WF-02 backend D1~D5) 가 main 에 머지된 상태. backend REST 10 endpoint (Scheme CRUD 5 + Mapping 2 + Project assignment 2 + IssueType read 1 + RFC 7807 ExceptionHandler 11건) 의 frontend side 를 구현. 프로젝트 설정 화면에서 워크플로우 스킴 CRUD + 이슈 타입 별 워크플로우 매핑 + 프로젝트에 스킴 할당.

### 선례

- PR #10→#13 (FR-WF-01 backend→frontend) — workflows.$key 라우트 패턴
- PR #26 (FR-IS-01 D6 이슈 UI) — TanStack Router code-based + RouteAdapter + MSW + Zod + 낙관적 업데이트 + sonner 토스트
- PR #13/#16 (FR-WF-01 frontend) — mermaid stateDiagram-v2 다이어그램

### 핵심 함정 (learnings 인계)

- mermaid 사용 시 `vi.mock('mermaid', ...)` 필수 (jsdom getBBox 미구현)
- lint-staged race 재발 위험 (공유 파일 병렬 수정) → 공유 mock/i18n 은 단독 task 또는 명시 공유 task
- 서브에이전트 scoped typecheck 맹점 → wave 마지막 controller 가 전체 `pnpm typecheck` 필수
- vi.mock factory 시그니처는 `import type { ComponentProps }` 명시 (contract drift 본질 차단)
- §NFR silent 영구 보류 차단 — Deferred trigger blockquote 명시

## 도메인 정리

- **BC**: project-workflow (classify-task 의 issue-tracking 분류는 키워드 오인 — "이슈 타입"/"프로젝트" 단어로 매핑됨. plan 본문에서 정정)
- **영향 엔티티** (backend, main 에 이미 존재 — PR #18 머지)
  - `Workflow` (FR-WF-01)
  - `WorkflowScheme` (FR-WF-02 D1, 신규)
  - `SchemeIssueTypeMapping` (FR-WF-02 D2, 신규)
  - `ProjectWorkflowSchemeMap` (FR-WF-02 D3, 신규)
  - `IssueType` (shared-kernel, PR #25 도입)
- **D6 frontend 영역 신규 entity**: 없음 (backend 호출만)
- **신규 용어 후보 4건** (Maxi 승인 시 [[../../../Maxi_wiki/BTS/glossary]] 추가)
  1. **워크플로우 스킴** (Workflow Scheme) — 이슈 타입 별 워크플로우 매핑을 묶은 단위. 프로젝트에 할당하는 행정 단위
  2. **이슈 타입** (Issue Type) — Bug / Story / Task / Epic 등. PR #25 에서 shared-kernel 로 통일
  3. **스킴 할당** (Scheme Assignment) — 프로젝트 ↔ 워크플로우 스킴 binding
  4. **기본 매핑** (Default Mapping) — 스킴에서 명시 매핑이 없는 이슈 타입에 적용되는 기본 워크플로우
- **기존 결정 충돌**: 없음. workflow 관련 ADR 6건 모두 backend 영역 (transition identity policy / port result sealed / yaml vs db storage 등), D6 frontend UI 와 직접 충돌 없음
- **관련 ADR**: 신규 추가 후보 (UI 결정사항 — 일부 후속 plan/spec 단계에서 도출. 예. "Mapping 행 추가 UX 패턴" 또는 "기본 매핑 노출 방식")
- **domain/project-workflow.md 갱신 대기**: WorkflowScheme / SchemeIssueTypeMapping / ProjectWorkflowSchemeMap 3 entity + 4 신규 용어 (Maxi 승인 후 수동 갱신)

## 스펙

전체 스펙 — [docs/specs/2026-05-28-fr-wf-02-d6-ui-crud.md](../specs/2026-05-28-fr-wf-02-d6-ui-crud.md).

핵심 시나리오 5줄 요약.
- 스킴 목록 (`/admin/workflow-schemes`) — 좌 네비 표준 4 + 커스텀 N, 행 클릭 → 상세
- 스킴 상세 + 매핑 편집 (`/admin/workflow-schemes/$schemeKey`) — 중앙 매핑 테이블 + 우 메타패널, 인라인 매핑 추가/삭제 + default mapping 강조 + 낙관적 업데이트
- 스킴 생성 (`/admin/workflow-schemes/new`) — key/name/description 입력
- 프로젝트 스킴 할당 (`/projects/$projectKey/settings/workflow-scheme`) — 현재 스킴 + 변경 + 적용 (UPSERT)
- 표준 스킴 보호 (D11) — key/is_default lock, mapping 자유 변경, 「삭제 불가」 disabled

### 디자인 시안

선택. **시안 1 — Multi-pane admin console** (3-컬럼, PR #26 사이드 메타패널 taste 직접 계승).
목업. `apps/web/public/mockups/fr-wf-02-d6-1.html` + 비교 페이지 `apps/web/public/mockups/fr-wf-02-d6-index.html`.
대안. 시안 2/3/4 미채택.

## Brainstorming Check

✅ 통과 (1 iteration, BLOCKER 3건 옵션 (A) 채택 + 보강 4건 inline 반영).

### BLOCKER 결정 (Maxi 2026-05-28)

옵션 (A) — **backend view layer patch 동반** (PR #13 D2 옵션 C 선례, BC 격리 예외 패턴). 본 PR scope 확장.

| ID | backend patch | 정당화 |
|---|---|---|
| B1 | `WorkflowSchemeResponse` 카운트 2 필드 추가 | 좌 네비/카드 카운트 표시 |
| B2 | 단건 조회 응답에 mappings 동봉 | PR #18 controller 주석 약속 이행 (잠재 결함 정상화) |
| B3 | `MappingResponse` key/name 4 필드 추가 | frontend 가 ID 변환 없이 표시 |
| B4 | ApplicationService cross-BC lookup | mappings + IssueType/Workflow 정보 동봉 |
| B5 | `IssueTypeLookupPort` outbound port | issue-tracking BC adapter 가 구현, shared-kernel 정본 (PR #25 패턴) |
| B6 | ArchUnit BC 격리 예외 ADR | `docs/adr/2026-05-28-workflow-scheme-frontend-view-layer-cross-bc-lookup.md` |

### 보강 4건 (게이트 1 inline)

| ID | 위치 | 보강 |
|---|---|---|
| G1 | spec §FR4 | 매핑 추가 select 옵션에서 이미 매핑된 이슈 타입 제외 (round-trip 절약) |
| G2 | spec §S6 | default mapping sentinel `"__default__"` → POST 시 null 직렬화 utility |
| G3 | (Maxi) | 표준 스킴 description 편집 시 추가 확인 모달 — 자유 편집 유지 (D11 결정 따름, 모달 없음) |
| G4 | spec §FR11 | dashboard/header 에 「관리 → 워크플로우 스킴」 link 1개 추가 |

## Plan (← /bts-plan 채움)

## 리뷰 결과 (← /bts-review-plan 채움)
