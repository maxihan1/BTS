# FR-SL-05 인터랙티브 메시지 PR2 — 담당자 변경 + 코멘트

> slug: fr-sl-05-pr2
> type: backend
> agent: backend-engineer
> primary BC: slack-integration (cross-BC 쓰기 → issue-tracking via IssueMutationPort)
> 생성: 2026-07-12

## Brief

FR-SL-05(Slack 인터랙티브 메시지)의 2번째이자 마지막 PR. slack-integration BC의 6번째이자 마지막 FR을 완결한다.

- **PR1(#261) 이미 머지됨** — 인바운드 인터랙티브 인프라(`POST /slack/interactions` + 서명 검증) + `완료로 표시`(resolution 선택 모달) + `상세보기`(URL) + V703 감사 마이그레이션.
- **이 PR2 범위** — 두 인터랙티브 액션 추가.
  1. **담당자 변경** — Slack `block_actions` users_select → cross-BC `IssueMutationPort.assign` 위임 → ASSIGN 감사 기록.
  2. **코멘트** — Slack `view_submission`(모달 제출) → cross-BC `IssueMutationPort.addComment` 위임 → COMMENT 감사 기록.
- PR2 완료 시 D4~D7 채워지고 slack-integration이 **5/6 → 6/6 (BC 완료)**. FR 총수 123 불변.

### 재사용 (PR1에서 이미 착지)

- 인바운드 엔드포인트 `POST /slack/interactions` + Slack 서명 검증 (신규 action_id/callback_id 분기만 추가).
- V703 감사 인프라 (ASSIGN·COMMENT 종류 레코드만 추가 기록. **V703 파일 편집 금지** — 이미 적용된 마이그레이션, 스키마 변경 필요 시 V704 신설).
- cross-BC `IssueMutationPort` (assign/addComment 메서드 존재 여부는 domain/spec에서 grep 검증).

### 착수 전 알려진 함정 (learnings + memory)

1. **★Flyway 체크섬 함정** — `:modules:app:test`(prod 조립 부팅 검증)는 Testcontainers가 아니라 영속 로컬 postgres(localhost:5433). 이미 적용된 마이그레이션(V703 등) 편집 시 체크섬 드리프트로 부팅 실패. 새 스키마는 V704.
2. **★test-boot 가짜그린** — `SlackTestcontainersConfig`의 공유 mock `MethodsClient`(relaxed)는 viewsOpen에 isOk=false 반환. 모달 API 결과 무시 시 실패 침묵. `stubSlackModalApisOk`로 ok 스텁 필요.
3. **★후처리 격리** — cross-BC 커밋 후 장식용 side-effect(chat.update)는 best-effort try로 격리.
4. **form-body @RequestParam 병용금지** (SL-04 선례) — interactions는 `payload` form-field JSON 파싱.
5. **cross-BC 권한 전량 포트 위임** — 담당자/코멘트 권한 판단은 issue-tracking 어댑터에 위임(직접 role 조회 금지).

## 도메인 정리

- **BC**: slack-integration (cross-BC 쓰기 위임 → issue-tracking via `IssueMutationPort`)
- **새 용어**: 없음 (담당자/Assignee = glossary L46, 코멘트/Comment 기존)
- **기존 결정 충돌**: 없음. `renderUnfurlCard`는 액션 버튼 미포함(ADR D3) 유지 — 액션은 할당 DM(`renderAssignmentActionsMessage`)에만.
- **관련 ADR**: FR-SL-05 PR1 결정(메모리 `fr-sl-05-interactive-pr1-done`). 담당자/코멘트 흐름은 이 PR2에서 ADR 후보(bts-spec/plan에서 결정).

### 실재 검증 (phantom entity 방지 — learnings 2026-05-20)

| 대상 | 상태 | 위치 |
|---|---|---|
| `IssueMutationPort.assign(AssignCommand)` | ✅ 실재 (automation FR-AT-02가 도입, 소비 중) | `shared-kernel/.../issue/IssueMutationPort.kt:88` |
| `IssueMutationPort.addComment(AddCommentCommand)` | ✅ 실재 | 동 파일 :103 |
| `IssueMutationPermissionDeniedException` (타입 예외) | ✅ 실재 | `shared-kernel/.../issue/` |
| `AssignCommand(actorUserId, issueKey, assigneeId?, dryRun)` | ✅ 실재 (assigneeId null=해제) | `IssueMutationCommands.kt:55` |
| `AddCommentCommand(actorUserId, issueKey, body, dryRun)` | ✅ 실재 | `IssueMutationCommands.kt:76` |
| V703 `action_type` ASSIGN/COMMENT 허용 | ✅ CHECK 제약 없음, 원문 저장 → **마이그레이션 불필요** | `V703__slack_interaction_log.sql` |
| DM 액션 렌더 `renderAssignmentActionsMessage` | ✅ 실재 (현재 상세보기+완료 2액션) | `SlackBlockKitRenderer.kt:97` |
| `SlackInteractionService` handle 분기 | ✅ 실재 (block_actions/view_submission 디스패치) | `SlackInteractionService.kt:89` |
| `SlackModalBuilder` (완료 모달 패턴) | ✅ 실재 (코멘트 모달 미러 대상) | `interaction/SlackModalBuilder.kt` |

### 신규 필요 (PR2에서 추가)

- **`StubIssueMutationPort`** (test) — `SlackTestcontainersConfig`에 `@Bean` 추가. 안 하면 slack test-boot `NoSuchBeanDefinitionException`(new-crossbc-dep-testboot 회귀). automation의 `StubIssueMutationPort` 참고.
- **prod 어댑터 배선** — `IssueMutationPort`의 issue-tracking prod 어댑터는 automation FR-AT-02에서 이미 `@Profile prod`로 존재. slack이 새로 소비 → `:modules:app` prod 조립 부팅 재검증 필요(prod-assembly-boot-verification-required). 신규 어댑터 작성 아님, 소비처만 추가.

### 열린 스펙 질문 (bts-spec에서 확정 → 게이트1 Maxi 확인 후보)

1. **담당자 변경 UX 흐름** — (A) 메시지 인라인 `users_select`(block_actions 선택 즉시 배정) vs (B) `담당자 변경` 버튼→모달 `users_select`→view_submission. 완료 흐름은 모달(B형)이라 대칭성 vs 원탭 간결성의 trade-off.
2. **Slack users_select 반환값 = Slack 사용자 id** → `SlackUserMappingRepository`로 BTS 사용자 역매핑 필수. 미연결 대상 선택 시 거부(ephemeral 안내) + UNMAPPED 감사.

## 스펙

전체 스펙. [docs/specs/2026-07-12-fr-sl-05-pr2.md](../specs/2026-07-12-fr-sl-05-pr2.md)

**확정 설계 (Maxi B형)** — 세 액션(완료·담당자·코멘트) 모두 `버튼→모달→view_submission` 단일 패턴.

핵심 3줄 요약.
- `담당자 변경` 버튼 → `users_select` 모달 → 제출 시 actor+대상 Slack 사용자 둘 다 역매핑 → `IssueMutationPort.assign` → ASSIGN 감사.
- `코멘트` 버튼 → `plain_text_input(multiline)` 모달 → 제출 시 actor 역매핑 + 본문 → `IssueMutationPort.addComment` → COMMENT 감사.
- 신규 엔드포인트·마이그레이션 0. 대상 미연결=ERROR, 사전 권한조회 생략(모달 콘텐츠 비-파생), OCC 없음(CONFLICT 미발생), 장식용 chat.update 없음(비종료 액션).

## Brainstorming Check

✅ 통과 (자가 sanity-check — 명확한 연속 작업이라 office-hours 부적합, 메모리 `bts-spec-office-hours-mismatch`). 담당자 UX 흐름은 게이트 전 AskUserQuestion으로 B형 확정. gap 5건(대상미연결 outcome·피드백 채널·사전조회 생략 정당성·unassign 범위·OCC 부재) 명문화.

## Plan (← /bts-plan 채움)

## 리뷰 결과 (← /bts-review-plan 채움)
