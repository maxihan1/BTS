# FR-SL-05 인터랙티브 메시지 PR2 — 담당자 변경 + 코멘트

> slug: fr-sl-05-pr2
> type: backend
> agent: backend-engineer
> primary BC: slack-integration (cross-BC 쓰기 → issue-tracking via IssueMutationPort)
> 생성: 2026-07-12

## Brief

FR-SL-05(Slack 인터랙티브 메시지)의 2번째이자 마지막 PR. slack-integration BC의 6번째이자 마지막 FR을 완결한다.

- **PR1(#261) 이미 머지됨** — 인바운드 인터랙티브 인프라(`POST /slack/interactions` + 서명 검증) + `완료로 표시`(resolution 선택 모달) + `상세보기`(URL) + V703 감사 마이그레이션.
- **이 PR2 범위** — 두 인터랙티브 액션 추가.
  1. **담당자 변경** — Slack `block_actions` users_select → cross-BC `IssueMutationPort.assign` 위임 → ASSIGN 감사 기록.
  2. **코멘트** — Slack `view_submission`(모달 제출) → cross-BC `IssueMutationPort.addComment` 위임 → COMMENT 감사 기록.
- PR2 완료 시 D4~D7 채워지고 slack-integration이 **5/6 → 6/6 (BC 완료)**. FR 총수 123 불변.

### 재사용 (PR1에서 이미 착지)

- 인바운드 엔드포인트 `POST /slack/interactions` + Slack 서명 검증 (신규 action_id/callback_id 분기만 추가).
- V703 감사 인프라 (ASSIGN·COMMENT 종류 레코드만 추가 기록. **V703 파일 편집 금지** — 이미 적용된 마이그레이션, 스키마 변경 필요 시 V704 신설).
- cross-BC `IssueMutationPort` (assign/addComment 메서드 존재 여부는 domain/spec에서 grep 검증).

### 착수 전 알려진 함정 (learnings + memory)

1. **★Flyway 체크섬 함정** — `:modules:app:test`(prod 조립 부팅 검증)는 Testcontainers가 아니라 영속 로컬 postgres(localhost:5433). 이미 적용된 마이그레이션(V703 등) 편집 시 체크섬 드리프트로 부팅 실패. 새 스키마는 V704.
2. **★test-boot 가짜그린** — `SlackTestcontainersConfig`의 공유 mock `MethodsClient`(relaxed)는 viewsOpen에 isOk=false 반환. 모달 API 결과 무시 시 실패 침묵. `stubSlackModalApisOk`로 ok 스텁 필요.
3. **★후처리 격리** — cross-BC 커밋 후 장식용 side-effect(chat.update)는 best-effort try로 격리.
4. **form-body @RequestParam 병용금지** (SL-04 선례) — interactions는 `payload` form-field JSON 파싱.
5. **cross-BC 권한 전량 포트 위임** — 담당자/코멘트 권한 판단은 issue-tracking 어댑터에 위임(직접 role 조회 금지).

## 도메인 정리

- **BC**: slack-integration (cross-BC 쓰기 위임 → issue-tracking via `IssueMutationPort`)
- **새 용어**: 없음 (담당자/Assignee = glossary L46, 코멘트/Comment 기존)
- **기존 결정 충돌**: 없음. `renderUnfurlCard`는 액션 버튼 미포함(ADR D3) 유지 — 액션은 할당 DM(`renderAssignmentActionsMessage`)에만.
- **관련 ADR**: FR-SL-05 PR1 결정(메모리 `fr-sl-05-interactive-pr1-done`). 담당자/코멘트 흐름은 이 PR2에서 ADR 후보(bts-spec/plan에서 결정).

### 실재 검증 (phantom entity 방지 — learnings 2026-05-20)

| 대상 | 상태 | 위치 |
|---|---|---|
| `IssueMutationPort.assign(AssignCommand)` | ✅ 실재 (automation FR-AT-02가 도입, 소비 중) | `shared-kernel/.../issue/IssueMutationPort.kt:88` |
| `IssueMutationPort.addComment(AddCommentCommand)` | ✅ 실재 | 동 파일 :103 |
| `IssueMutationPermissionDeniedException` (타입 예외) | ✅ 실재 | `shared-kernel/.../issue/` |
| `AssignCommand(actorUserId, issueKey, assigneeId?, dryRun)` | ✅ 실재 (assigneeId null=해제) | `IssueMutationCommands.kt:55` |
| `AddCommentCommand(actorUserId, issueKey, body, dryRun)` | ✅ 실재 | `IssueMutationCommands.kt:76` |
| V703 `action_type` ASSIGN/COMMENT 허용 | ✅ CHECK 제약 없음, 원문 저장 → **마이그레이션 불필요** | `V703__slack_interaction_log.sql` |
| DM 액션 렌더 `renderAssignmentActionsMessage` | ✅ 실재 (현재 상세보기+완료 2액션) | `SlackBlockKitRenderer.kt:97` |
| `SlackInteractionService` handle 분기 | ✅ 실재 (block_actions/view_submission 디스패치) | `SlackInteractionService.kt:89` |
| `SlackModalBuilder` (완료 모달 패턴) | ✅ 실재 (코멘트 모달 미러 대상) | `interaction/SlackModalBuilder.kt` |

### 신규 필요 (PR2에서 추가)

- **`StubIssueMutationPort`** (test) — `SlackTestcontainersConfig`에 `@Bean` 추가. 안 하면 slack test-boot `NoSuchBeanDefinitionException`(new-crossbc-dep-testboot 회귀). automation의 `StubIssueMutationPort` 참고.
- **prod 어댑터 배선** — `IssueMutationPort`의 issue-tracking prod 어댑터는 automation FR-AT-02에서 이미 `@Profile prod`로 존재. slack이 새로 소비 → `:modules:app` prod 조립 부팅 재검증 필요(prod-assembly-boot-verification-required). 신규 어댑터 작성 아님, 소비처만 추가.

### 열린 스펙 질문 (bts-spec에서 확정 → 게이트1 Maxi 확인 후보)

1. **담당자 변경 UX 흐름** — (A) 메시지 인라인 `users_select`(block_actions 선택 즉시 배정) vs (B) `담당자 변경` 버튼→모달 `users_select`→view_submission. 완료 흐름은 모달(B형)이라 대칭성 vs 원탭 간결성의 trade-off.
2. **Slack users_select 반환값 = Slack 사용자 id** → `SlackUserMappingRepository`로 BTS 사용자 역매핑 필수. 미연결 대상 선택 시 거부(ephemeral 안내) + UNMAPPED 감사.

## 스펙

전체 스펙. [docs/specs/2026-07-12-fr-sl-05-pr2.md](../specs/2026-07-12-fr-sl-05-pr2.md)

**확정 설계 (Maxi B형)** — 세 액션(완료·담당자·코멘트) 모두 `버튼→모달→view_submission` 단일 패턴.

핵심 3줄 요약.
- `담당자 변경` 버튼 → `users_select` 모달 → 제출 시 actor+대상 Slack 사용자 둘 다 역매핑 → `IssueMutationPort.assign` → ASSIGN 감사.
- `코멘트` 버튼 → `plain_text_input(multiline)` 모달 → 제출 시 actor 역매핑 + 본문 → `IssueMutationPort.addComment` → COMMENT 감사.
- 신규 엔드포인트·마이그레이션 0. 대상 미연결=ERROR, 사전 권한조회 생략(모달 콘텐츠 비-파생), OCC 없음(CONFLICT 미발생), 장식용 chat.update 없음(비종료 액션).

## Brainstorming Check

✅ 통과 (자가 sanity-check — 명확한 연속 작업이라 office-hours 부적합, 메모리 `bts-spec-office-hours-mismatch`). 담당자 UX 흐름은 게이트 전 AskUserQuestion으로 B형 확정. gap 5건(대상미연결 outcome·피드백 채널·사전조회 생략 정당성·unassign 범위·OCC 부재) 명문화.

## Plan

> 모든 경로는 `backend/modules/slack-integration/` 기준. 완료 흐름(PR1) 코드를 미러링한다.
> 각 task 상단 action_id/callback_id/block_id 문자열은 렌더러↔모달↔서비스 세 곳에서 **반드시 일치**(PR1 `ATLAS_COMPLETE_ACTION_ID` 중복상수 선례 — import 아닌 값 일치).

### Task 1. 담당자·코멘트 버튼 렌더 (SlackBlockKitRenderer)

**메타**.
- agent: `backend-engineer`
- files: [`src/main/kotlin/com/bts/slack/message/SlackBlockKitRenderer.kt`, `src/test/kotlin/com/bts/slack/message/SlackBlockKitRendererTest.kt`]
- depends-on: []

**RED**: `renderAssignmentActionsMessage`가 이제 4개 액션 요소(상세보기 url · 완료로 표시 `atlas_complete` · **담당자 변경 `atlas_assign`** · **코멘트 `atlas_comment`**, 각 value=issueKey)를 담는지 검증. 신규 버튼 부재로 실패.
- 테스트: `renders assign and comment action buttons with issueKey value`
**GREEN**: `assignmentActionsBlock`에 `actionButtonElement(ASSIGN_BUTTON_TEXT, ATLAS_ASSIGN_ACTION_ID, issueKey)` + `actionButtonElement(COMMENT_BUTTON_TEXT, ATLAS_COMMENT_ACTION_ID, issueKey)` 추가 + companion 상수(`atlas_assign`/`atlas_comment`, 라벨 "담당자 변경"/"코멘트").
**REFACTOR**: 상수 그룹 주석 정리.
**검증**: `./gradlew :modules:slack-integration:test --tests '*SlackBlockKitRendererTest'`

### Task 2. 담당자·코멘트 모달 빌더 (SlackModalBuilder)

**메타**.
- agent: `backend-engineer`
- files: [`src/main/kotlin/com/bts/slack/interaction/SlackModalBuilder.kt`, `src/test/kotlin/com/bts/slack/interaction/SlackModalBuilderTest.kt`]
- depends-on: []

**RED**: 두 신규 메서드 검증 —
- `buildAssignModal(issueKey)`: `type=modal`, `callback_id=atlas_assign_modal`, `private_metadata={"issueKey":...}`, input 블록(`block_id=assignee_block`) + `element.type=users_select`(`action_id=assignee_select`). 제목/제출/취소 라벨.
- `buildCommentModal(issueKey)`: `callback_id=atlas_comment_modal`, `private_metadata={"issueKey":...}`, input 블록(`block_id=comment_block`) + `element.type=plain_text_input`(`action_id=comment_input`, `multiline=true`).
- 메서드 부재로 실패.
**GREEN**: 두 메서드 + `usersSelectElement`/`plainTextInputElement` 헬퍼 + companion 상수. private_metadata는 `{issueKey}`만(OCC 없음·비종료라 channel/ts 불필요 — YAGNI). `buildCompletionModal` 무회귀.
**REFACTOR**: input 블록 공통 헬퍼로 element 주입 파라미터화(기존 `inputBlock` 재사용 검토).
**검증**: `./gradlew :modules:slack-integration:test --tests '*SlackModalBuilderTest'`

### Task 3. StubIssueMutationPort + test-boot 배선 (테스트 인프라)

**메타**.
- agent: `backend-engineer`
- files: [`src/test/kotlin/com/bts/slack/StubIssueMutationPort.kt`, `src/test/kotlin/com/bts/slack/SlackTestcontainersConfig.kt`]
- depends-on: []

**RED**(변형 — 테스트 인프라 scaffolding): 자연 RED는 Task 4가 서비스에 `IssueMutationPort`를 주입하는 순간 slack test-boot가 `NoSuchBeanDefinitionException`으로 실패하는 것. 그 부팅 실패를 green으로 만드는 선행 배선.
**GREEN**: `StubIssueMutationPort`(settable — 다음 `MutationResult` 또는 던질 예외를 세팅, automation `StubIssueMutationPort` 참고, 기본 fail-closed) + `SlackTestcontainersConfig`에 `@Bean fun issueMutationPort(): StubIssueMutationPort`. 기존 `stubSlackModalApisOk`(있으면 재사용, 없으면 viewsOpen ok 스텁 헬퍼 추가 — 공유 mock isOk=false 가짜그린 회피).
**REFACTOR**: 스텁 KDoc(계약·기본 거부 사유).
**검증**: 컴파일 + Task 4/5 통합테스트가 주입 소비(단독 RED 없음 — scaffolding).

### Task 4. 서비스 오케스트레이션: 담당자·코멘트 (SlackInteractionService)

**메타**.
- agent: `backend-engineer`
- files: [`src/main/kotlin/com/bts/slack/interaction/SlackInteractionService.kt`, `src/test/kotlin/com/bts/slack/interaction/SlackInteractionServiceTest.kt`]
- depends-on: [2]   # modalBuilder.buildAssignModal/buildCommentModal 호출(컴파일 의존). Task 1과는 action_id 문자열 일치(값 계약).

**RED**(단위, mockk): 신규 분기 검증 —
- block_actions `atlas_assign`/`atlas_comment` → actor 역매핑 fail-closed → 봇토큰/trigger_id 게이트 → 해당 모달 openModal. 미연결 actor → response_url ephemeral + UNMAPPED 감사. openModal non-Sent → ephemeral + ERROR.
- view_submission `atlas_assign_modal` → actor 역매핑 + `selected_user` 역매핑(대상) → `IssueMutationPort.assign(dryRun=false)` → ASSIGN/SUCCESS. 대상 미연결 → response_action errors + ASSIGN/ERROR. 권한예외 → PERMISSION_DENIED.
- view_submission `atlas_comment_modal` → actor 역매핑 + 본문 추출 → `IssueMutationPort.addComment` → COMMENT/SUCCESS. 권한예외 → PERMISSION_DENIED.
- 신규 분기 부재로 실패.
**GREEN**: `IssueMutationPort` 생성자 주입. `handleBlockActions` when에 `atlas_assign`/`atlas_comment` 분기 + `handleAssignButton`/`handleCommentButton`(완료 버튼 게이트 미러, 단 사전 권한조회 없음). `handleViewSubmission` callback 분기 확장 + `executeAssign`/`executeComment`(타입예외 catch 순서: `IssueMutationPermissionDeniedException` → generic). `selectedUser` 헬퍼(state.values `selected_user` 추출). ASSIGN/COMMENT action_type + assign/comment 모달 상수. **완료 흐름 무회귀**(기존 테스트 green 유지).
**REFACTOR**: 세 액션 공통 "버튼 게이트(역매핑·봇토큰·모달오픈)" 헬퍼 추출 검토(응집 유지 범위 내). `@Suppress` 사유 갱신.
**검증**: `./gradlew :modules:slack-integration:test --tests '*SlackInteractionServiceTest'`

### Task 5. 통합 E2E: 담당자·코멘트 시나리오 (SlackInteractionEndToEndTest)

**메타**.
- agent: `qa-engineer`
- files: [`src/test/kotlin/com/bts/slack/SlackInteractionEndToEndTest.kt`]
- depends-on: [1, 2, 3, 4]

**RED**: 실 `POST /slack/interactions`(서명검증 포함)로 assign·comment 시나리오 — (a) 담당자 버튼→모달 오픈 검증, (b) assign 모달 제출→StubIssueMutationPort.assign 호출+ASSIGN/SUCCESS 감사, (c) 대상 미연결→ERROR, (d) 무권한→PERMISSION_DENIED, (e) 코멘트 버튼→모달, (f) comment 제출→addComment+COMMENT/SUCCESS, (g) actor 미연결→UNMAPPED. 미구현 경로라 실패(또는 endpoint no-op).
**GREEN**: (Task 1~4로 이미 구현됨) 시나리오가 통과하도록 스텁 세팅(`stubSlackModalApisOk` + StubIssueMutationPort 결과/예외 세팅) + 감사 조회 assert. `clearMocks` 후 재스텁.
**REFACTOR**: 시나리오 헬퍼 공통화.
**검증**: `./gradlew :modules:slack-integration:test --tests '*SlackInteractionEndToEndTest'`

### Task 6. 전수 동기화: FR-SL-05 D4~D7 완료 · slack 6/6 (docs)

**메타**.
- agent: `backend-engineer`
- files: [`docs/plan/product/slack-integration.md`, `docs/plan/fr-index.md`, `docs/plan/README.md`, `docs/sdd/` 해당 챕터, `CLAUDE.md`(카운트), `DATA.md`(필요시)]
- depends-on: [5]   # 검증 green 후 완료 표기(성급한 완료 주장 방지)

**RED**(변형 — 문서 동기화): `bash scripts/verify-master-plan.sh`가 통과하되 FR-SL-05 D4~D7·slack BC 완료 게이트가 반영돼야 함.
**GREEN**: product/slack-integration.md의 FR-SL-05 D4~D7 체크박스 + §헤더 카운트 + BC 완료 게이트(6/6) 갱신. fr-index·README·SDD·CLAUDE 카운트 정합(FR 총수 123 **불변** — 신규 FR 없음, 상태만 변경). `docs/progress.html`은 머지 후 post-merge 훅이 재생성(수동 재생성 금지 — dashboard regen).
**REFACTOR**: 없음.
**검증**: `bash scripts/verify-master-plan.sh` (종료 0)

## Plan 메타

- **task 수: 6**
- **예상 wave: 3** — Wave1 = {T1, T2, T3}(독립·파일 무겹침 병렬) · Wave2 = {T4}(depends 2) · Wave3 = {T5}(depends 1·2·3·4) · 이후 {T6}(depends 5). 실제 wave는 Gradle 모듈 컴파일 직렬화로 다소 합쳐질 수 있음(메모리 `bts-plan-wave-gradle-module-compile`).
- **예상 시간: 약 18분**(직렬), 병렬 wave 적용 시 약 10분
- **TDD 강제: yes** (T3은 테스트 인프라 scaffolding·T6은 문서 동기화 — TDD 변형 명시)
- **마이그레이션: 0** (V703 재사용, 편집 금지)
- **cross-BC prod 어댑터: 신규 없음** — `IssueMutationPort` prod 어댑터는 issue-tracking에 이미 존재(automation FR-AT-02). slack은 소비만 추가. bts-codereview/merge에서 `:modules:app:test`(9BC prod 조립 부팅) 재검증 필수(prod-assembly-boot-verification-required).
- **.bts-cache/classify.json 미갱신** — 동시 세션(FR-AT-03) 공유 캐시 충돌 회피. task_count 진실출처는 이 plan(메모리 `bts-cache-multisession-collision`).
- 추가 검증: ktlint/detekt(모듈 baseline·`--rerun-tasks` false-green 주의), `:modules:app:test` prod 조립.

## 리뷰 결과

### plan-eng-review (2026-07-12)

**Step 0 스코프**: 프로덕션 파일 3개(렌더러·모달·서비스), 신규 프로덕션 클래스 0(테스트 스텁 1만). 8파일/2클래스 스멜 미달 → 축소 게이트 불발. PR1 패턴 재사용(innovation token 0, boring by default).

**아키텍처 — ✅ SOUND (BLOCKER 0)**. 실코드 검증 결과.
- `IssueMutationPort` prod 어댑터 `AutomationIssueMutationAdapter`(`@Component @Profile("prod")`) **실재** + `:modules:app`에 issue-tracking·slack 둘 다 조립 → prod에서 slack이 빈 주입 가능(부팅 OK 예상).
- 어댑터가 **권한 거부를 shared-kernel 타입 예외로 번역**(`catch IssueAccessDeniedException → throw IssueMutationPermissionDeniedException`, adapter L186-189). assign(OCC 재시도)·addComment 모두 이 경로 → slack의 PERMISSION_DENIED 분류가 올바르게 작동(`crossbc-failure-classification-typed` 함정 회피 확인).
- 어댑터는 automation 전용 커플링 없음(표준 `IssueApplicationService.changeAssignee`/`CommentApplicationService.create` 위임). slack actor=역매핑 BTS 사용자로 동일 권한 경로 적용.

**⚠️ 주의 / 권장 (비차단)**.
1. **[DRY]** 세 버튼 게이트(완료·담당자·코멘트)가 역매핑→봇토큰/trigger_id 게이트→openModal→감사 패턴 공유. Task 4 REFACTOR의 "공통 헬퍼 추출"을 **필수 반영** 권장 — `openActionModal(actionType, buildModal)` 헬퍼. 완료의 `getCompletionOptions` 사전조회만 완료 고유로 유지(모달 옵션 파생 때문). DRY 강조(Maxi 선호).
2. **[Test]** 방어 케이스 추가 — assign 제출에 `selected_user` 없음(malformed state) → ERROR. Task 4 RED에 명시.
3. **[Test/멱등]** 코멘트는 OCC/dedup 없음 → 이론상 중복 코멘트 가능. 단 Slack은 view_submission을 자동 재시도하지 않음(3초 타임아웃=사용자에 오류 표시, 재제출 아님) → 중복은 사용자 수동 재제출 한정(허용). 코드리뷰 노트로만.
4. **[Test]** 지속 OCC 충돌 시 어댑터가 1회 재시도 후 `IssueVersionConflictException`(generic) throw → slack generic catch → **ERROR**(CONFLICT 아님). 스펙의 "CONFLICT 미발생" 확인됨. slack 단위테스트로 트리거 어려움(어댑터 내부) → 저우선.
5. **[네이밍, 범위 밖]** prod 어댑터명이 `AutomationIssueMutationAdapter`지만 이제 automation+slack 공용. 오해 소지. 이름 변경은 issue-tracking(타 BC) 수정이라 이 PR 범위 밖 → 후속 후보(KDoc 보강도 타 BC 편집이라 defer).
6. **[Security, low]** users_select 대상 역매핑이 오류 메시지로 BTS 계정 연결 여부를 노출. 연결 상태는 민감도 낮음 → 허용.

**🔑 verification 하드게이트**: slack이 `IssueMutationPort` **신규 소비** → 머지 전 `:modules:app:test`(9BC prod 조립 부팅) 필수 통과(`prod-assembly-boot-verification-required`). 빈 해석은 정적 확인됐으나 실부팅 테스트로 확정.

**BLOCKER: 없음.** taste decision: 없음(담당자 UX는 스펙 단계서 B형 확정).
