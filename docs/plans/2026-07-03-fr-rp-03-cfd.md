# FR-RP-03 — CFD (Cumulative Flow Diagram)

> slug: fr-rp-03-cfd
> type: backend
> agent: backend-engineer
> primary_bc: issue-tracking (issue_history 활용 — domain 단계 확정)
> 생성: 2026-07-03

## Brief

FR-RP-03 CFD (Cumulative Flow Diagram, 누적 흐름 다이어그램). 시간축 위에서 각
워크플로우 상태에 머문 이슈 개수를 누적 영역으로 쌓아 병목을 시각화하는 애자일 리포트.

- 선행. FR-RP-01(번다운) 완료 ✅ (§4.1)
- product 정본. docs/plan/product/notification-dashboard.md §4.3
- D1 도메인 / D2 명세(상태별 누적) / D3 데이터모델(issue_history 활용 + 인덱스, db)
  / D4 백엔드(GET /api/v1/projects/{id}/cfd?from=&to=) / D5 백엔드 테스트
  / D6 프론트(recharts 영역 차트, designer→frontend) / D7 E2E(qa)
- 스코프. FR-RP-01/02 선례 = 백엔드 D1~D5 먼저 + 프론트 D6/D7 후속 PR (domain/spec 확정)

## 도메인 정리

- **BC**: issue-tracking (물리 모듈) / 논리 BC 라벨 notification-dashboard 유지 (fr-index, 카운트 불변)
- **영향 엔티티/테이블 (활용, 신규 0)**: `issues`(created_at·deleted_at·current_state_key·type_id) +
  `issue_change_group`/`issue_change_item`(V018, field='status' 전이) + `WorkflowStateCatalog`(상태→카테고리 SPI)
- **새 용어**: **CFD** (Cumulative Flow Diagram, 누적 흐름 다이어그램), **CfdPoint** (VO, read-model)
- **Maxi 확정 결정 2건**:
  - D1. 누적 띠 = **카테고리 3띠** (TODO/IN_PROGRESS/DONE), 워크플로우 상태별 N띠 아님
  - D2. 시계열 = **on-the-fly 역산** (issue_change 이력), 스냅샷 테이블/스케줄러 0. V018 이전 이슈 한계 수용
- **설계 (선례 계승)**:
  - D3. 모듈 = issue-tracking (데이터 로컬, cross-BC 포트 불필요 — 번다운과 달리 스프린트 미참조)
  - D4. 보안 = 이슈별 가시성 필터 `filterVisibleIssueKeys`(buildActiveSecureWhere 재사용, 복제 금지) — 번다운 D6·벨로시티 동형
  - D5. 창 = from/to UTC 날짜 + 일 단위(utcDateField) + 상한, 잘못된 범위 422
- **상태 타임라인 역산 알고리즘**: 초기상태(첫 status 전이 from_value, 없으면 current_state_key) →
  전이들 created_at 오름차순 적용. day D 상태 = created_at::date ≤ D 마지막 전이 to_value(없으면 초기상태) → 카테고리 → 띠 +1.
  존재 판정 = created_at::date ≤ D AND (deleted_at NULL OR deleted_at::date > D).
- **기존 결정 충돌**: 없음
- **관련 ADR**: [docs/decisions/2026-07-03-fr-rp-03-cfd.md](../decisions/2026-07-03-fr-rp-03-cfd.md) (생성됨)
- **참조 코드 (실재 확인)**:
  - `SprintVelocityLookupAdapter.kt` — 가시성 필터 + IsolatedWorkflowStateLookup(REQUIRES_NEW) + 타입별 category 캐시 + WorkflowSchemeNoDefaultException 폴백 (전부 재사용)
  - `SprintBurndownQueryRepository.kt` — utcDateField(UTC 날짜 결정성) 패턴
  - `V018__issue_change_history.sql` — issue_change_group(issue_id, created_at)/item(field, from_value, to_value)
  - `EpicProgress.kt` — NormalizedCategory 폴백(TODO)

## 스펙

전체 스펙. [docs/specs/2026-07-03-fr-rp-03-cfd.md](../specs/2026-07-03-fr-rp-03-cfd.md)

핵심 시나리오 3줄 요약.
- `GET /api/v1/projects/{projectKey}/cfd?from=&to=` — 창 내 각 UTC 날짜마다 `{date, todoCount, inProgressCount, doneCount}` 반환(카테고리 3띠, 날짜 오름차순).
- 각 날짜 카운트 = 존재(created≤D, 미삭제) 가시 이슈를 그 날 상태(이력 역산, end-of-day)의 카테고리로 분류·합산.
- 보안 = 프로젝트 BROWSE + 이슈별 가시성 필터(filterVisibleIssueKeys). 잘못된 창 422, 스킴 미할당 TODO 폴백(500 차단).

핵심 설계.
- 신규 스키마 0, cross-BC 포트 0(스프린트 미참조). WorkflowStateCatalog SPI + IsolatedWorkflowStateLookup 재사용.
- 기본 창 = 최근 30일(Clock 주입), 상한 180일. CfdResult/CfdPoint VO(read-model), 순수 계산기 분리.

## Brainstorming Check

✅ 통과 (1회 iteration). 자체 적대 검토 4건 보강(전이 tie-break·Clock 주입·누적 semantics 명시·조건부 인덱스) + plan 위임 3건(BROWSE 진입점/404 vs 403·델타 알고리즘·인덱스 실필요).

## Plan

> 모듈=issue-tracking, 패키지=`com.bts.issue.cfd`(worklog.aggregate 선례 미러). TDD red→green→refactor 강제.
> 참조 재사용: `SprintVelocityLookupAdapter`(가시성 필터·IsolatedWorkflowStateLookup·타입별 category 캐시·WorkflowSchemeNoDefault 폴백),
> `WorklogAggregateController/Service/ExceptionHandler`(actor 401·param 400·BROWSE 403·basePackages advice),
> `SprintBurndownQueryRepository`(utcDateField UTC 날짜 결정성), `EpicProgress`(category TODO 폴백).

### Task 1. CFD 순수 계산기 + read-model VO (CfdCalculator, CfdPoint, CfdResult, CfdCategory)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/cfd/domain/CfdCategory.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/cfd/domain/CfdPoint.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/cfd/domain/CfdResult.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/cfd/domain/CfdCalculator.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/cfd/domain/CfdCalculatorTest.kt`]
- depends-on: []

**RED**: `CfdCalculatorTest` — 순수 함수 `CfdCalculator.calculate(from, to, issues: List<CfdIssueTimeline>): List<CfdPoint>`.
- `CfdIssueTimeline(createdDate: LocalDate, segments: List<CfdSegment>)`, `CfdSegment(startDate: LocalDate, category: CfdCategory)`.
  segments 오름차순·distinct 날짜, `segments[0].startDate == createdDate`(초기 카테고리). day D 카테고리 = `startDate ≤ D`인 마지막 segment.
- 테스트 케이스(비-vacuous 단언):
  ```kotlin
  @Test fun `single always-TODO issue counts 1 in todo every day`()
  @Test fun `issue transitions TODO to IN_PROGRESS to DONE mid-window shifts bands`()
  @Test fun `issue created before window uses reconstructed status at from`()
  @Test fun `issue created after to is excluded`()
  @Test fun `reopen DONE to IN_PROGRESS decreases doneCount`()   // monotonic 아님
  @Test fun `empty issue list yields all-zero points across window`()
  @Test fun `window bounds inclusive both ends`()
  @Test fun `points sorted ascending by date`()
  ```
- 실패 메시지(예상): `CfdCalculator`/`CfdPoint`/`CfdCategory` 클래스 없음.

**GREEN**:
- `enum class CfdCategory { TODO, IN_PROGRESS, DONE }` + `companion fun fromCategoryString(s: String?): CfdCategory`(비표준/null → TODO 폴백, EpicProgress EC2/EC3 동형).
- `data class CfdPoint(date, todoCount: Int, inProgressCount: Int, doneCount: Int)`, `data class CfdResult(projectKey, from, to, points)`.
- `CfdCalculator.calculate` — **델타-누적**: 창 날짜 배열 3개(todo/inProgress/done) 델타. 이슈별 effectiveStart=max(createdDate,from),
  createdDate>to면 skip. effectiveStart 카테고리 +1, 이후 segment 변화마다 prevCat−1/newCat+1. 카테고리별 prefix-sum → 날짜별 카운트.

**REFACTOR**: 델타 인덱싱 헬퍼 추출 + KDoc(알고리즘 O(이슈×segment+날짜×3) 명시).

**검증**: `./gradlew :backend:modules:issue-tracking:test --tests '*CfdCalculatorTest'`

### Task 2. 가시 활성 이슈 메타 조회 (IssueRepository.fetchActiveVisibleIssuesForCfd)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/repository/IssueRepository.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/repository/IssueRepositoryCfdSourceTest.kt`]
- depends-on: []

**RED**: `IssueRepositoryCfdSourceTest`(Testcontainers) — 신규 메서드
`fetchActiveVisibleIssuesForCfd(projectKey, viewerUserId, access): List<CfdIssueSourceRow>`.
- `CfdIssueSourceRow(issueId: UUID, typeId: Long, currentStateKey: String, createdAt: Instant)`.
- 케이스: (a) 프로젝트 이슈 메타 반환, (b) `deleted_at IS NOT NULL` 이슈 제외(활성만), (c) 보안 등급 차단 이슈 제외(기밀 누출0), (d) 타 프로젝트 이슈 제외.
- **정본 보안 술어 `buildActiveSecureWhere` 재사용**(복제 금지 — isomorphic-clone 회귀 방지).

**GREEN**: `IssueRepository`에 메서드 추가 — `dsl.select(ID, TYPE_ID, CURRENT_STATE_KEY, CREATED_AT).from(ISSUES).join(PROJECTS)
.where(buildActiveSecureWhere(projectKey, viewer, access))`. `@Transactional(readOnly=true)`.
- **CONCERN-1 반영**: `CfdIssueSourceRow`는 **`com.bts.issue.repository`** 패키지(IssueRepository 이웃)에 정의 — core repo가 feature(cfd) 패키지에 의존하지 않도록(결합 역전 방지). repository-layer read-model.

**REFACTOR**: 로우 매핑 헬퍼 + KDoc(filterVisibleIssueKeys 형제·같은 secure-where 재사용 명시).

**검증**: `./gradlew :backend:modules:issue-tracking:test --tests '*IssueRepositoryCfdSourceTest'`

### Task 3. status 전이 이력 조회 리포지토리 (CfdStatusHistoryRepository)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/cfd/repository/CfdStatusHistoryRepository.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/cfd/repository/CfdStatusChangeRow.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/cfd/repository/CfdStatusHistoryRepositoryTest.kt`]
- depends-on: []

**RED**: `CfdStatusHistoryRepositoryTest`(Testcontainers) — `fetchStatusChanges(issueIds: Set<UUID>): List<CfdStatusChangeRow>`.
- `CfdStatusChangeRow(issueId: UUID, changedAt: Instant, groupId: Long, fromValue: String?, toValue: String?)`.
- 케이스: (a) `field='status'` 항목만 반환(assignee/summary 변경 제외), (b) `(issueId, changedAt ASC, groupId ASC)` 정렬,
  (c) 여러 이슈 배치 조회, (d) 빈 issueIds → 빈 결과(jOOQ 빈 IN 방어).
- **신규 인덱스 0** — 기존 `idx_issue_change_group_issue`·`idx_issue_change_item_group`·`idx_issue_change_item_field` 커버.

**GREEN**: jOOQ — `issue_change_group g JOIN issue_change_item i ON i.group_id=g.id
WHERE g.issue_id IN (:ids) AND i.field='status' ORDER BY g.issue_id, g.created_at, g.id`. `@Repository`, `.repository` 패키지(ArchUnit 룰2 정합).

**REFACTOR**: 상수(`FIELD_STATUS="status"`) 추출 + KDoc.

**검증**: `./gradlew :backend:modules:issue-tracking:test --tests '*CfdStatusHistoryRepositoryTest'`

### Task 4. CFD 유스케이스 서비스 (CfdService — BROWSE 체크 + 타임라인 조립 + 집계)

**메타**.
- agent: `backend-engineer` (권한 가드 포함 — security-engineer 게이트2 검토)
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/cfd/application/CfdService.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/cfd/application/CfdServiceTest.kt`]
- depends-on: [1, 2, 3]

**RED**: `CfdServiceTest`(mockk) — `getCfd(actorId, projectKey, from: LocalDate, to: LocalDate): CfdResult`.
- 케이스: (a) BROWSE 미보유 → `IssueAccessDeniedException`(repo 조회 前), (b) 가시 이슈 없음 → 0점 창,
  (c) 타입별 category 맵 캐싱(N+1 차단, listStates 타입당 1회), (d) `WorkflowSchemeNoDefaultException` → 해당 타입 TODO 폴백(500 차단),
  (e) 초기 상태 역산(전이 없으면 currentStateKey, 있으면 첫 전이 from_value), (f) 같은 날 다중 전이 → end-of-day.

**GREEN**: 오케스트레이션 —
1. `checkPermission`: `permissionResolver.hasPermission(actor.value, IssuePermission.BROWSE, IssueScope.Project(projectKey))` 실패 시 `IssueAccessDeniedException`(WorklogAggregateService 선례).
2. `accessibleLevels(viewer, projectKey)` → `issueRepository.fetchActiveVisibleIssuesForCfd(...)`.
3. 가시 이슈 없으면 zero-point 창 즉시 반환.
4. `cfdStatusHistoryRepository.fetchStatusChanges(issueIds)` → issueId별 그룹핑.
5. **type_id → IssueTypeKey 해석**(`issueTypeRepository.findAll` 1쿼리 = velocity `loadTypeIdToKeyMap` 미러, N+1 차단) →
   타입별 category 맵 = **기존** `IsolatedWorkflowStateLookup`(velocity가 정의한 @Component 재사용, 중복 생성 금지)
   `.listStates(projectKey, typeKey)` 캐싱 + `WorkflowSchemeNoDefaultException`(simpleName) 폴백(빈 맵→TODO).
6. 이슈별 `CfdIssueTimeline` 조립 — 초기 상태(첫 전이 from_value 또는 currentStateKey) → `byDate` LinkedHashMap(createdDate 먼저 + 전이 chronological, 같은 날 overwrite=end-of-day) → segments 날짜 오름차순. category는 `CfdCategory.fromCategoryString`.
7. `CfdCalculator.calculate(from, to, timelines)` → `CfdResult`.
- `@Transactional(readOnly=true)` — `listStates` MANDATORY/REQUIRES_NEW 전파 충족.

**REFACTOR**: 타임라인 조립을 private 헬퍼로 추출 + KDoc(velocity 재사용 지점 명시).

**검증**: `./gradlew :backend:modules:issue-tracking:test --tests '*CfdServiceTest'`

### Task 5. 컨트롤러 + 예외 핸들러 + DTO (CfdController, CfdExceptionHandler, CfdResponse)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/cfd/web/CfdController.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/cfd/web/CfdExceptionHandler.kt`, `backend/modules/issue-tracking/src/main/kotlin/com/bts/issue/cfd/web/dto/CfdResponse.kt`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/cfd/web/CfdControllerTest.kt`]
- depends-on: [1, 4]

**RED**: `CfdControllerTest`(mockk service, standalone MockMvc) — `GET /api/v1/projects/{projectKey}/cfd?from=&to=`.
- 케이스: (a) 미인증 → 401(actor 최상단), (b) `from`/`to` 파싱 불가 → 400, (c) `from > to` → 400, (d) 창 > 180일 → 400,
  (e) from/to 생략 → 기본 최근 30일(주입 `Clock` 고정 검증), (f) 정상 → 200 + DTO 형태(from/to echo, points), (g) `IssueAccessDeniedException` → 403.

**GREEN**:
- `CfdController(service, clock: Clock)` — `CurrentActor.current()`(401) → `parseLocalDate`(400, WorklogAggregate 헬퍼 미러) → 창 해석(생략 시 Clock 기준 default, both-null=최근30일) → `from>to`/`>180일` 400 → `service.getCfd(...)` → `CfdResponse.from(result)` → `DataResponse`.
- `CfdResponse(projectKey, from: LocalDate, to: LocalDate, points: List<CfdPointResponse>)` + `CfdPointResponse(date, todoCount, inProgressCount, doneCount)` + `companion from(result)`.
- `CfdExceptionHandler` `@RestControllerAdvice(basePackages=["com.bts.issue.cfd.web"])` — `ResponseStatusException` passthrough(401/400) + `IssueAccessDeniedException`→403. **catch-all 미포함**(ResponseStatusException 삼킴 방지).

**REFACTOR**: 창 해석/검증을 private 헬퍼로 + Swagger 어노테이션 + KDoc.

**검증**: `./gradlew :backend:modules:issue-tracking:test --tests '*CfdControllerTest'`

### Task 6. HTTP 통합 테스트 (S1~S9, Testcontainers 실 부팅)

**메타**.
- agent: `backend-engineer`
- files: [`backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/cfd/CfdIntegrationTest.kt`]
- depends-on: [5]

**RED/GREEN**: 실 부팅 + 실 repo + 시드(이슈 + issue_change 상태 전이 이력) — S1~S9 전 시나리오.
- S1 이력 있는 프로젝트 정상 점, S2 빈 프로젝트 0점, S3 기밀 이슈 제외(비-vacuous 건수 단언), S4 403, S5 401, S6 스킴 미할당 TODO 폴백(200),
  S7 잘못된 창 400, S8 전이 없는 이슈 current_state_key, S9 삭제 이슈 제외.
- **CONCERN-2 반영 (필수)**: status 이력 시드는 `issue_change_group.created_at`를 **통제된 과거 타임스탬프로 직접 INSERT**한다.
  실 전이 경로(IssueHistoryRecorder/전이 서비스)는 `created_at=NOW()`로 박혀 **30일 과거 CFD를 시드할 수 없다**. 실 DB·실 read repo는
  사용하되(mock 금지 — issue-tracking-transition-test 선례 정신 유지), 이력 행 자체는 명시적 타임스탬프로 insert(jOOQ/SQL). Clock는 컨트롤러 고정 주입.

**REFACTOR**: 시드 헬퍼 추출 + KDoc.

**검증**: `./gradlew :backend:modules:issue-tracking:test --tests '*CfdIntegrationTest'`

## Plan 메타

- task 수: 6 (각 TDD 사이클)
- 예상 wave: 4 (Wave1=T1·T2·T3 병렬[동일 모듈 컴파일 직렬화 유의], Wave2=T4, Wave3=T5, Wave4=T6)
- TDD 강제: yes (test 커밋이 feat 커밋보다 먼저)
- 신규 스키마/인덱스/포트: 0. DB 마이그레이션 없음 → db-engineer 불요.
- 추가 검증: ktlint/detekt(--rerun-tasks, 캐시 false-green 방지), verify-master-plan(카운트 불변·D단계 마킹).
- product 동기화(머지 전): `notification-dashboard.md §4.3` D1~D5 체크 + `구현 범위` deviation 박스, SDD §14 CFD 카테고리 3띠 명시.

## 리뷰 결과

### plan-eng-review (2026-07-03) — backend 집중 적대 리뷰

**BLOCKER: 없음.** 트랜잭션 경계·보안 술어 재사용·N+1 차단·vacuous 방지 모두 plan에 반영됨.

CONCERN (plan 수정 완료).
- **C-1 결합 역전**: `CfdIssueSourceRow`를 `cfd.repository`에 두면 core `IssueRepository`가 feature 패키지 의존 →
  `com.bts.issue.repository`(repository-layer read-model)로 이동. **Task 2 수정 반영.**
- **C-2 과거 타임스탬프 시드 (중요)**: 통합 테스트가 실 전이 경로로 이력 시드 시 `created_at=NOW()`로 박혀
  과거 CFD 시나리오(30일) 검증 불가 → issue_change_group.created_at 명시적 과거값 직접 INSERT. **Task 6 수정 반영.**
- **C-3 IsolatedWorkflowStateLookup 재사용**: velocity가 정의한 기존 @Component 재사용(중복 생성 금지, self-invocation REQUIRES_NEW 함정 회피). **Task 4 수정 반영.**
- **C-4 type_id→typeKey 해석 명시**: category 매핑 전 `issueTypeRepository.findAll` 1쿼리로 역매핑(velocity loadTypeIdToKeyMap 미러, N+1 차단). **Task 4 수정 반영.**

CONCERN (구현 시 유의 — 코드리뷰서 재검증).
- **C-5 LocalDate JSON 직렬화**: `CfdControllerTest`(standalone MockMvc)는 JavaTimeModule 등록 필요(LocalDate ISO). 미등록 시
  배열 직렬화로 가짜 실패 → JSON 날짜 형식은 **통합 테스트(실 부팅)가 authoritative**(memory enablewebmvc-slice-localdate-array).
- **C-6 Clock 빈 배선**: 컨트롤러 `Clock` 주입 — 모듈에 @Bean Clock 있는지 확인, 없으면 export/velocity 선례처럼 default `Clock.systemUTC()`.
- **C-7 null from_value 방어**: 첫 status 전이 `from_value`가 null이면 `CfdCategory.fromCategoryString(null)`→TODO 폴백(NPE 금지).

검증된 강점(적대 리뷰 통과).
- 상태 타임라인 역산(초기상태·end-of-day·창 경계 이전 전이 폴딩)이 델타-누적으로 정확 — 순수 계산기 TDD가 pin.
- 보안: `buildActiveSecureWhere` 재사용(복제 0), BROWSE→403(probe 차단, 404 없음), 기밀 이슈 비-vacuous 단언.
- 신규 스키마/인덱스/포트 0 — 마이그레이션 리스크 없음, on-the-fly 선례(번다운/벨로시티) 정합.
