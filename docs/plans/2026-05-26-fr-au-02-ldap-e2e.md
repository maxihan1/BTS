<!-- FR-AU-02 D7 E2E (Playwright LDAP 시나리오) 마무리 — fast-track qa-only PR -->
# FR-AU-02 D7 E2E (Playwright LDAP 시나리오) 마무리

> slug. fr-au-02-ldap-e2e
> type. qa (E2E only)
> agent. qa-engineer
> primary_bc. identity-access
> 생성. 2026-05-26

## Brief

FR-AU-02 (LDAP/AD 연동) 의 D7 E2E 마무리. D1~D6 PR #8 (`fr-au-09-securityfilterchain-local-provider-pr-6-7`) 머지 시점 완료. D7 만 미진행 — Playwright LDAP 시나리오 1~2건 추가. PR #19 fast-track 패턴 일관 (qa-only, scope 작음).

## 도메인 정리

- **BC**. identity-access (LDAP/AD 연동, FR-AU-02 D7).
- **영향 엔티티**. 0 — User / UserExternalAccount / LdapAuthFlow / AutoProvisionService 모두 PR #8 시점 도입 + 안정. 본 PR 은 E2E 시나리오 (Playwright) 추가 only.
- **새 용어**. 0건.
- **기존 결정 충돌**. 없음.
- **관련 ADR**. PR #8 시점 도입된 `docs/decisions/2026-05-20-authentication-provider-spi-naming.md`, `docs/decisions/2026-05-20-stored-password-credential-schema.md`, `docs/decisions/2026-05-20-jwt-issuer-strategy.md`, `docs/decisions/2026-05-20-jwt-key-rotation-policy.md`, `docs/decisions/2026-05-20-session-pat-schema.md`. 본 PR 신규 ADR 0.
- **관련 learnings**. L#8 (Testcontainers 라이프사이클 — LDAP integration), L#9 (prod 단일 LdapTemplate vs test dead URL 가정 — DB serverUrl 동적 wiring 부재 — 단일 Provider 가정). 본 PR 의 E2E 가 같은 Provider 가정 기반.
- **grill-with-docs**. skip — qa-only E2E 추가, 도메인 모델 영향 0, PR #19 (`workflow-detail-404-e2e`) fast-track 패턴 일관.
- **본 작업의 위치**. FR-AU-02 의 D1~D6 (PR #8) 완료 후 D7 E2E 마무리. BC 완료 카운트 1건 추가 (1 / 117 → 2 / 117).

## 스펙

### 사용자 시나리오 (Given-When-Then)

- **S1-ldap (외부 LDAP 사용자, dev 환경)**. Given LoginForm 의 provider 드롭다운 / When `LDAP-corp` 선택 + `alice` / `Test1234!` 입력 + 로그인 / Then /dashboard 리다이렉트 + 환영 메시지 + Header alice. 회귀 가드. local provider (PR #11 시점 검증) 와 같은 흐름이 LDAP 에서도 동작.
- **S2-ldap (잘못된 LDAP 비번)**. Given provider=LDAP-corp / When alice + wrong / Then 401 한국어 에러 + URL /login 유지. local 의 S2 (invalid, PR #11) 와 같은 흐름이 LDAP 에서도 동작.

### 기능 요구사항 (FR)

- **FR-1 (MSW Track 1)**. `apps/web/src/mocks/auth-handlers.ts` 의 loginHandler 가 `provider` 필드 검증 — `local` 또는 `ldap-corp` 분기. 다른 값은 401 (`unknown_provider`). 기존 local 분기 회귀 0.
- **FR-2 (MSW Track 1)**. `apps/web/src/mocks/auth-fixtures.ts` 에 `LDAP_VALID_PASSWORDS` map 신규 (alice/Test1234!, bob/Test1234!). backend `LdapAuthFlowIntegrationTest` fixture (uid=alice,Test1234!) 와 일관. AUTH_USERS 는 local 과 공유 (whoami 응답 동일).
- **FR-3 (MSW Track 1)**. `apps/web/src/mocks/auth-handlers.test.ts` 에 ldap-corp 분기 assert 3건 추가 (S3-ldap happy / S4-ldap invalid / S5-ldap-iso provider×사용자 mismatch). 기존 6 assert 변경 0.
- **FR-4 (E2E Track 2)**. `apps/web/e2e/login-ldap.spec.ts` 신규. Playwright **S1-ldap (happy) + S2-ldap (invalid)** 2건. PR #11 의 login-happy-path.spec.ts + login-invalid.spec.ts 패턴 일관.

### 비기능 요구사항 (NFR)

- **NFR-1**. 회귀 0 — `pnpm test` 17 file / **105 + 3 = 108 passed** / 0 skipped (기존 105 + auth-handlers.test.ts 신규 3 assert).
- **NFR-2**. typecheck / lint / build 0 issue.
- **NFR-3**. E2E playwright **9 + 2 = 11/11** 통과 (기존 9 + 신규 S1-ldap + S2-ldap).
- **NFR-4**. test duration 영향 ≤ +5s (mock 분기 추가는 단위 테스트 3건 / E2E 2건 + 영향 작음).

### 엣지 케이스

- **EC-1**. provider=ldap-corp + local 사용자 (alice 의 local password=`password`) → MSW 가 LDAP_VALID_PASSWORDS 만 검증 (`password` ≠ `Test1234!`) → 401. 의도적 provider isolation.
- **EC-2**. provider=local + ldap 사용자 (alice 의 ldap password=`Test1234!`) → MSW 가 VALID_PASSWORDS 만 검증 (`Test1234!` ≠ `password`) → 401. 의도적 isolation.
- **EC-3**. provider 미지정 / 알 수 없는 값 — frontend Zod (`z.enum(['local', 'ldap-corp'])`) 가 차단. MSW 도달 시 401 `unknown_provider` (방어 layer).
- **EC-4**. shadcn/ui Select component E2E 패턴 — `page.getByLabel('로그인 방식').click()` (또는 `getByRole('combobox')`) → `page.getByRole('option', { name: 'LDAP-corp' }).click()`. 기존 LoginForm 의 Select 마운트가 Radix Portal 사용 — popup 셀렉터가 portal root 에 렌더.

### 제약 조건

- backend 변경 0 / generated 변경 0.
- `apps/web/src/auth/LoginForm.tsx` 변경 0 (D6 LDAP UI 이미 PR #11 시점 도입).
- `apps/web/src/auth/useLoginMutation.ts` 변경 0 (provider 필드 이미 mutation body 에 포함).
- `apps/web/src/mocks/auth-handlers.test.ts` 의 기존 6 assert 변경 0 (회귀 가드).
- `apps/web/e2e/login-happy-path.spec.ts` / `login-invalid.spec.ts` / `already-authed.spec.ts` 변경 0 (회귀 0).

### 측정 가능한 완료 기준

1. auth-handlers.ts 가 provider 분기 (local / ldap-corp / 기타 401 unknown_provider).
2. auth-fixtures.ts 의 LDAP_VALID_PASSWORDS map 신규 (alice/bob = Test1234!).
3. auth-handlers.test.ts 의 ldap-corp 분기 assert 3건 통과.
4. login-ldap.spec.ts 의 S1-ldap + S2-ldap 모두 통과.
5. pnpm test 17 file / 108 passed / 0 skipped + typecheck/lint/build 0 issue + E2E 11/11.
6. plan §FR-AU-02 D7 `[x]` 마킹 + `(PR #22, 2026-05-26)` 명시.

## Brainstorming Check

self-brainstorming Phase B sanity check 1회 (PR #19/#20/#21 fast-track 패턴) — gap 2건 발견 + 모두 spec inline 반영.

- **G-1**. auth-handlers.test.ts 에 ldap-corp 분기 회귀 가드 없으면 silent break 위험 (MSW 분기 자체가 frontend dev / E2E 모두 영향). **반영 — FR-3 신규 신설 (auth-handlers.test.ts ldap-corp 분기 assert 3건)**.
- **G-2**. Playwright Select component 클릭 패턴 — shadcn/ui Select 가 Radix Portal 렌더. listbox + option 셀렉터 정확성 미검증 시 E2E flake 위험. **반영 — EC-4 의 셀렉터 패턴 명시 + Task 2 본문에 fallback 셀렉터 (`getByRole('combobox')` + `getByRole('option', { name: 'LDAP-corp' })`) 명시**.

✅ 통과 (1회 iteration, gap 2건 모두 inline 반영).

## Plan

### Task 1. MSW provider 분기 + LDAP fixture + 회귀 가드 (FR-1, FR-2, FR-3)

**메타**.
- agent. `frontend-engineer` (MSW + fixture, 단위 테스트)
- files. [`apps/web/src/mocks/auth-handlers.ts`, `apps/web/src/mocks/auth-fixtures.ts`, `apps/web/src/mocks/auth-handlers.test.ts`]
- depends-on. []

**RED**. `auth-handlers.test.ts` 에 ldap-corp 분기 assert 3건 추가 (실행 시 기존 handler 가 provider 무시라 ldap-corp 의 alice 가 local fixture 와 매칭 안 되어 실패 예상 — 정확 RED).
- 새 it. **S3-ldap happy**. POST `/api/v1/auth/login` with `{ provider: 'ldap-corp', username: 'alice', password: 'Test1234!' }` → 200 + access_token.
- 새 it. **S4-ldap invalid**. 같은 payload 의 password='wrong' → 401 invalid_credentials.
- 새 it. **S5-ldap-iso**. provider='ldap-corp' + alice + 'password' (local pw) → 401 (provider isolation).
- 커밋. `test: fr-au-02-ldap-e2e task-1 — auth-handlers.test.ts ldap-corp 분기 assert 3건 추가 (RED)`.

**GREEN**. auth-handlers.ts + auth-fixtures.ts 변경.
- `auth-fixtures.ts`. `LDAP_VALID_PASSWORDS: Readonly<Record<string, string>> = { alice: 'Test1234!', bob: 'Test1234!' }` 신규.
- `auth-handlers.ts`. loginHandler 가 `body.provider` 검증. `provider === 'ldap-corp'` 시 LDAP_VALID_PASSWORDS 검증, `provider === 'local'` 시 기존 VALID_PASSWORDS, 그 외 401 `unknown_provider`.
- 커밋. `feat: fr-au-02-ldap-e2e task-1 — MSW provider 분기 + LDAP fixture`.

**REFACTOR**. KDoc 갱신 — loginHandler 의 KDoc 에 provider 분기 명시 + LDAP_VALID_PASSWORDS fixture comment (backend LdapAuthFlowIntegrationTest 매칭 명시).
- 커밋. `refactor: fr-au-02-ldap-e2e task-1 — KDoc 갱신 (provider 분기 + backend LDAP fixture 매칭 명시)`.

**검증**. `pnpm --filter web test src/mocks/auth-handlers.test.ts` — 9 assert (기존 6 + 신규 3) 모두 통과.

---

### Task 2. E2E login-ldap.spec.ts 신규 (FR-4)

**메타**.
- agent. `qa-engineer` (E2E only, BTS §sub-agent qa 책임)
- files. [`apps/web/e2e/login-ldap.spec.ts`]
- depends-on. [1] (E2E 가 Task 1 의 MSW 분기 동작 의존)

**TDD 변형**. PR #19 패턴 — E2E 작성 자체가 RED + 통과 = GREEN, 단일 commit. 별도 RED commit 분리 불요 (E2E 가 처음 작성 + 통과 한 commit). 검증 = `pnpm exec playwright test --grep "S1-ldap|S2-ldap"` 통과.

**작업**.
- `apps/web/e2e/login-ldap.spec.ts` 신규. PR #11 의 login-happy-path.spec.ts + login-invalid.spec.ts 패턴 일관. 2 시나리오.
  - **S1-ldap (LDAP happy)**. `page.goto('/login')` → 헤딩 'BTS 로그인' 검증 → `page.getByLabel('로그인 방식').click()` → `page.getByRole('option', { name: 'LDAP-corp' }).click()` → `alice` / `Test1234!` 입력 → 로그인 → `page.waitForURL('**/dashboard')` → 환영 메시지 + Header alice 검증.
  - **S2-ldap (LDAP invalid)**. 같은 흐름이지만 wrong password → 401 한국어 에러 검증 + URL /login 유지.
- 커밋. `feat: fr-au-02-ldap-e2e task-2 — login-ldap.spec.ts 신규 (S1-ldap happy + S2-ldap invalid)`.

**검증**. `pnpm --filter web exec playwright test --grep "S1-ldap|S2-ldap"` — 2건 통과.

---

### Task 3. 통합 검증 (controller wave 종료 후)

**메타**.
- agent. controller (메인 agent)
- files. 없음 (실행만)
- depends-on. [1, 2]

**검증 단계**.
1. `pnpm --filter web typecheck` 0 issue.
2. `pnpm --filter web lint` 0 issue.
3. `pnpm --filter web test` 17 file / **108 passed** / 0 skipped (105 + 3 ldap-corp assert).
4. `pnpm --filter web build` 0 error.
5. `pnpm --filter web exec playwright test` **11/11 통과** (9 + S1-ldap + S2-ldap).
6. `git diff --stat origin/main -- backend/ apps/web/src/generated/` empty.

## Plan 메타

- task 수. 3 (Task 1 MSW + 회귀 가드 / Task 2 E2E / Task 3 통합 검증).
- 예상 wave 수. **2 wave 직렬**. wave 1 = Task 1 (MSW + handlers.test). wave 2 = Task 2 (E2E, depends-on Task 1). Task 3 = 통합 검증.
- TDD 강제. Task 1 = TDD red→green→refactor (3 commit). Task 2 = TDD 변형 (E2E 작성 = RED, 통과 = GREEN, 단일 commit). Task 3 = 통합 검증.
- 병렬 dispatch. 2 wave 직렬 — Task 2 가 Task 1 의 MSW 분기 의존이라 wave 분리. dispatch race 위험 0.
- **Task 1 implementer 의 git commit 시 `git add <file>` 단위 명시 stage 강제** (PR #20 learnings #1 race 예방).

## 리뷰 결과

### plan-design-review (skip, 2026-05-26)

**결정 — skip**. UI 변경 0 (LoginForm 의 LDAP UI 이미 PR #11 시점 도입). E2E + MSW 분기만.

### plan-eng-review (self, 2026-05-26)

self-eng-review 1회 (PR #19/#20/#21 fast-track 패턴). 검토 영역.

1. **MSW LDAP fixture 가 backend LdapAuthFlowIntegrationTest 와 일관**. ✅ alice/bob + Test1234! 매칭 (backend test 라인 151/179/198/207/208 확인). provider id `'ldap-corp'` 가 LoginForm Zod + backend Session.providerId + V004__sessions.sql comment 모두 일관.
2. **auth-handlers.test.ts 의 기존 assert 회귀 0**. ✅ ldap-corp 분기 3 assert 추가 only, 기존 6 assert 변경 0.
3. **LoginForm 의 provider 드롭다운 → useLoginMutation → MSW → E2E 흐름 정합**. ✅ `z.enum(['local', 'ldap-corp'])` + LoginFormValues 가 useLoginMutation mutate 에 그대로 전달 + MSW 분기 + Playwright Select 클릭 패턴. 모든 layer provider 값 일관.
4. **EC-1/EC-2 (provider × 사용자 mismatch isolation)**. ✅ MSW 가 provider 별 fixture 만 검증. local/ldap fixture 의 password 다른 점이 isolation 보장. S5-ldap-iso assert 가 이 isolation 검증.
5. **PR #19 fast-track 패턴 일관**. ✅ qa + frontend 2-track / 2 wave 직렬 / TDD 변형 (Task 1 = red→green→refactor, Task 2 = E2E 단일 commit) / docs inline 통합 / plan §리뷰 결과 self-eng-review 1회 PASS.
6. **PR #20 learnings #1 race 예방**. ✅ Task 1 implementer prompt 에 `git add <file>` 단위 명시 stage 강제.
7. **PR #20 learnings #2 (vi.mock worker scope leak) 회귀 위험**. ✅ 본 PR 은 vi.mock 신규 도입 0 — MSW handler 자체 변경 (vi.mock 무관).
8. **PR #21 learnings #1 (mock prop type 좁힘)**. ✅ 본 PR 은 vi.mock factory 변경 0 — 회귀 위험 0.

**결정 — ✅ PASS**. BLOCKER 0, 주의 0.
