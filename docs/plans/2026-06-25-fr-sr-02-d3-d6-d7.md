# FR-SR-02 D3/D6/D7 — AQL 텍스트 쿼리 인덱스 + 프론트엔드 + E2E

> slug: fr-sr-02-d3-d6-d7
> type: feature (복합 — migration + ui + qa)
> agent: 복합 (D3=db-engineer, D6=frontend-engineer, D7=qa-engineer)
> 생성: 2026-06-25

## Brief

FR-SR-02(AQL 텍스트 쿼리, JQL 호환)의 백엔드 코어 MVP는 PR #189로 완료. 본 작업은 남은 D단계 마무리.

- **D3** — `pg_trgm` 확장 + GIN 인덱스 (issues.summary 등). AQL `~` 연산자(ILIKE `%term%`) 가속 (db-engineer)
- **D6** — AQL 입력창 + syntax highlight 프론트 UI (frontend-engineer)
- **D7** — E2E (qa-engineer)

classify 결과(type=qa)는 D7 "E2E" 키워드 오판 — 실제는 migration+ui+qa 복합. plan에서 task별 agent 지정.

백엔드 계약(이미 머지됨). `POST /api/v1/search/aql`, 지원 필드 status/label/summary/priority, 연산자 `=`/`!=`/`~`/`IN`/`NOT IN`/`AND`/`OR`/`NOT`/괄호/`ORDER BY`. DoS 가드(깊이50/토큰1000/2000자). 입력 오류 400 매핑.

## 도메인 정리

- **BC**: search-export-import (검색 UI 논리 소속) + issue-tracking (D3 인덱스 물리 위치). SR-01/SR-02 백엔드와 동일한 "논리≠물리" 구조 ([[fr-sr-01-issue-filter-done]]). 프론트(D6/D7)는 apps/web — BC 중립.
- **새 용어**: 없음. AQL(Atlas Query Language)은 glossary §검색/AQL에 이미 등재. trigram/pg_trgm·syntax highlight는 기술 용어(도메인 용어 아님) → glossary 추가 불필요.
- **새 엔티티**: 없음. 프론트는 기존 계약 소비. D3는 성능 인덱스(스키마 구조/도메인 로직 변경 없음).
- **기존 결정 충돌**: 없음. ADR `docs/decisions/2026-06-25-fr-sr-02-aql-parser-and-bc.md` 준수.

### 백엔드 계약 (PR #189 머지됨 — 프론트 정본)

- `POST /api/v1/search/aql`, body `{ projectKey: string, query: string, page?: int=0(min0), size?: int=50(1..100, 초과 시 400 거부=클램프 아님) }`. **인증 필수**(401 SEARCH_UNAUTHENTICATED), BROWSE 권한(403 SEARCH_ACCESS_DENIED).
- 응답: Spring `Page<AqlSearchHit>` raw(봉투 없음). `content[]` 항목: `key, summary, typeKey, currentStateKey, assigneeId(UUID|null), priority(int 1..5), priorityName, projectKey, updatedAt(ISO Instant)`. 메타: `totalElements, numberOfElements, first, last, empty, pageable{pageNumber,pageSize}`.
- 지원 필드: `status, label, summary, priority` (status/priority는 `~` 불가). 후속 예정 필드(assignee/reporter/component/project)는 400 `SEARCH_FIELD_NOT_YET_SUPPORTED`.
- 키워드(대소문자 무시): `AND OR NOT IN`, `ORDER BY`, `ASC DESC`. 연산자: `= != ~ IN "NOT IN"`. 리터럴: `"따옴표문자열"` · bare word · 정수.
- 에러: RFC 7807 ProblemDetail + `errorCode` + `detail` + `position`(0-base, 문법오류 시) + `timestamp`. 코드: `SEARCH_SYNTAX_ERROR`/`SEARCH_UNKNOWN_FIELD`/`SEARCH_FIELD_NOT_YET_SUPPORTED`/`SEARCH_VALIDATION_FAILED`(400), `SEARCH_UNAUTHENTICATED`(401), `SEARCH_ACCESS_DENIED`(403), `SEARCH_INTERNAL_ERROR`(500).
- DoS 가드: query 2000자 / 토큰 1000 / 중첩 깊이 50 초과 시 400.

### D3 인덱스 단서

- `~`(CONTAINS): `summary` → `summary ILIKE '%term%' ESCAPE '\'` (IssueRepository.kt:2292). `label` → `EXISTS(unnest(labels) ILIKE ...)` (IssueRepository.kt:2337).
- `summary` VARCHAR(255) → `pg_trgm` GIN 인덱스(`gin(summary gin_trgm_ops)`)로 ILIKE `%term%` 가속 가능.
- **결정 필요**: label `~`는 unnest 후 ILIKE라 trigram 인덱스 직접 적용 까다로움 → D3 범위에서 summary만 가속 vs label도 포함 결정(spec). 다음 마이그레이션 V번호 = issue-tracking **V031**(머지 직전 재확인 — [[migration-vnumber-concurrent-branch-collision]]).

### 결정 포인트 (→ spec 이월)

1. **syntax highlight 구현 방식**: 신규 라이브러리(CodeMirror/Monaco/Prism) vs 경량 자체 토크나이저. **신규 의존성은 Maxi 승인 필요**(DEVELOPMENT.md §외부 의존성). 새 라이브러리 채택 시 ADR 후보.
2. **D3 인덱스 대상 범위**: summary만 vs label 포함. pg_trgm 확장 설치(SDD §1.1에 이미 계획됨)는 인프라 영향 — 마이그레이션에 `CREATE EXTENSION IF NOT EXISTS pg_trgm` 포함.

- 관련 ADR: `docs/decisions/2026-06-25-fr-sr-02-aql-parser-and-bc.md` (기존, 준수). 신규 ADR은 syntax highlight 라이브러리 채택 시에만.

## 스펙

전체 스펙. [docs/specs/2026-06-25-fr-sr-02-d3-d6-d7.md](../specs/2026-06-25-fr-sr-02-d3-d6-d7.md)

Maxi 결정(2026-06-25). ① syntax highlight = 자체 경량 토크나이저(textarea+overlay, 의존성 0) ② UI = 별도 라우트 `/search` ③ D3 = summary만 GIN trgm(label 후속).

핵심 시나리오.
- `/search`에서 AQL 쿼리 입력 → POST /api/v1/search/aql → 결과 목록(Page<AqlSearchHit>) + 페이지네이션
- 입력 중 키워드/필드/연산자/문자열/숫자 색상 구분(자체 토크나이저 overlay)
- 문법오류 시 detail+position 표시, 미지원필드/0건/401/403 분기
- D3 V031 — pg_trgm 확장 + issues.summary trigram GIN 인덱스로 `~` 검색 가속

## Brainstorming Check

✅ 통과 (직접 sanity check). gap 2건(G1 projectKey 선택 방식, G2 트리거/size/ORDER BY) → 기존 패턴으로 보강, Maxi 추가 결정 불필요.

## Plan

> 정본 스펙. [docs/specs/2026-06-25-fr-sr-02-d3-d6-d7.md](../specs/2026-06-25-fr-sr-02-d3-d6-d7.md). 백엔드 계약(#189) 불변 — 프론트는 소비만, 필드 invent 금지.

### Task 1. [D3] V031 — pg_trgm 확장 + issues.summary trigram GIN 인덱스

**메타**.
- agent: `db-engineer`
- files: [`backend/modules/issue-tracking/src/main/resources/db/migration/issue-tracking/V031__issues_summary_trgm_index.sql`, `backend/modules/issue-tracking/src/main/resources/db/codegen/init_codegen.sql`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/migration/SummaryTrgmIndexMigrationTest.kt`]
- depends-on: []

**RED**. `SummaryTrgmIndexMigrationTest.kt` (기존 `WatcherSchemaMigrationTest`/`AttachmentSchemaMigrationTest` 패턴 미러). Testcontainers로 마이그레이션 적용 후 `pg_indexes`/`pg_extension` 조회 — ① `pg_trgm` 확장 설치됨 ② `issues`에 `summary gin_trgm_ops` GIN 인덱스 존재. 실패 메시지(예상): 인덱스 미존재.

**GREEN**. V031 SQL — `CREATE EXTENSION IF NOT EXISTS pg_trgm;` + `CREATE INDEX IF NOT EXISTS idx_issues_summary_trgm ON issues USING gin (summary gin_trgm_ops);`. 다음 V번호(V031)는 머지 직전 재확인([[migration-vnumber-concurrent-branch-collision]] — 현재 최신 V030).

**REFACTOR**. SQL 주석(L1 역할 + 왜 trgm GIN인지: ILIKE `%term%` 가속). `init_codegen.sql` 미러 판단 — 인덱스/확장이 jOOQ 코드젠 산출물(Indexes 클래스/부팅)에 영향 주면 미러([[jooq-init-codegen-mirror]]), 아니면 사유 주석. **확장 설치가 codegen DB 부팅에 필요한지 반드시 확인**(필요 시 init_codegen에 `CREATE EXTENSION` 추가).

**검증**. `./gradlew :backend:issue-tracking:test --tests "*SummaryTrgmIndexMigrationTest*"` + `EXPLAIN`이 인덱스 사용함을 테스트나 수동으로 기록.

### Task 2. [D6] AQL 토크나이저 (lib/aql-tokenizer.ts)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/lib/aql-tokenizer.ts`, `apps/web/src/lib/aql-tokenizer.test.ts`]
- depends-on: []

**RED**. `aql-tokenizer.test.ts` — `tokenizeAql(query)`가 KEYWORD(`and/or/not/in/order/by/asc/desc` 대소문자무시)·FIELD(`status/label/summary/priority`)·OPERATOR(`= != ~`)·STRING(`"..."`)·NUMBER·PAREN·COMMA·PLAIN으로 분해. 각 토큰 `{type, value, start, end}`. **drift 가드** — 키워드/필드/연산자 상수가 백엔드 화이트리스트와 동일함을 단언(상수 배열 직접 비교). 실패: 함수 없음.

**GREEN**. 단일 패스 스캐너(정규식/문자 순회). 따옴표 문자열·이스케이프 처리. `ORDER`/`BY` 개별 KEYWORD. 상수 `AQL_KEYWORDS`/`AQL_FIELDS`/`AQL_OPERATORS` export.

**REFACTOR**. 상수를 파일 상단 단일 정본으로. L1 한국어 주석.

**검증**. `pnpm --filter web test aql-tokenizer`.

### Task 3. [D6] api/search.ts + Zod + MSW 핸들러/픽스처

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/search.ts`, `apps/web/src/api/search.test.ts`, `apps/web/src/mocks/search-handlers.ts`, `apps/web/src/mocks/search-fixtures.ts`, `apps/web/src/mocks/handlers.ts`]
- depends-on: []

**RED**. `search.test.ts` — `searchAql({projectKey, query, page, size})`가 ① 200 시 `Page<AqlSearchHit>` Zod 파싱(필드 정합: key/summary/typeKey/currentStateKey/assigneeId(nullable)/priority/priorityName/projectKey/updatedAt + totalElements/totalPages/first/last/empty/number) ② 400 ProblemDetail(errorCode/detail/position) → `ApiError` throw(봉투 보존) ③ 401/403 분기. MSW search-handlers로 시나리오 제공. 실패: 함수 없음.

**GREEN**. `apiPost`(client.ts) 재사용 — **POST+인증이라 CSRF 필요 여부를 client.ts/같은 패턴 grep으로 확정**([[frontend-api-convention-per-bc]]). Zod 스키마는 백엔드 DTO와 1:1([[frontend-zod-backend-dto-contract-gap]]). `search-handlers.ts`(정상/문법오류/0건/미지원필드/401/403 시나리오) + `search-fixtures.ts` + `handlers.ts`에 등록.

**REFACTOR**. Zod 스키마 export 정리, 에러코드 상수화. L1 주석.

**검증**. `pnpm --filter web test search`.

### Task 4. [D6] AqlHighlighter 컴포넌트 (textarea + overlay)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/search/AqlHighlighter.tsx`, `apps/web/src/components/search/AqlHighlighter.test.tsx`]
- depends-on: [2]

**RED**. `AqlHighlighter.test.tsx` — value 입력 시 토큰별 색상 span(`<pre aria-hidden>`) 렌더, textarea와 overlay 폰트/패딩/스크롤 정렬, `maxLength=2000`, caret 보존(mousedown preventDefault — [[jsdom-browser-textarea-selectionstart]]). 실패: 컴포넌트 없음.

**GREEN**. Task 2 `tokenizeAql` 사용. textarea(투명 텍스트/caret 보임) + 절대배치 overlay. 색상은 DESIGN.md 토큰(token별 클래스). `aria-hidden` overlay(스크린리더는 textarea만).

**REFACTOR**. 토큰→className 매핑 추출. L1 주석.

**검증**. `pnpm --filter web test AqlHighlighter`.

### Task 5. [D6] /search 페이지 — SearchPage + SearchRouteAdapter

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/search.tsx`, `apps/web/src/routes/search.test.tsx`]
- depends-on: [3, 4]

**RED**. `search.test.tsx` — ① 정상 쿼리 검색 → 결과 목록(key/summary/상태/우선순위) + 페이지네이션 ② 0건 빈 상태 ③ 문법오류 detail+position 표시·결과 미갱신 ④ 미지원필드 안내 ⑤ 401/403 분기 ⑥ 빈 쿼리 검색버튼 disable ⑦ Cmd/Ctrl+Enter 트리거 ⑧ filter-aware queryKey(쿼리 변경 시 page=0). MSW(Task 3) 사용. 실패: 컴포넌트 없음.

**GREEN**. `SearchPage`(props 기반, 라우터 비의존) + `SearchRouteAdapter`(useSearch/useNavigate로 `q`/`projectKey`/`page` ↔ URL, `DEFAULT_PROJECT_KEY='ATLAS'` 기본+override). useQuery(`['search-aql', projectKey, q, page]`). AqlHighlighter 입력창 + 검색버튼 + 결과목록 + IssuePagination 재사용/유사 + 에러 alert(role=alert). page size=20.

**REFACTOR**. 결과 카드/에러 매핑 헬퍼 추출. L1 주석.

**검증**. `pnpm --filter web test search`.

### Task 6. [D6] router.ts 등록 + Header 검색 진입점

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/router.ts`, `apps/web/src/components/Header.tsx`, `apps/web/src/components/Header.test.tsx`]
- depends-on: [5]

**RED**. `Header.test.tsx`에 `/search` 진입 링크 존재 단언 추가(기존 테스트 보존). 실패: 링크 없음.

**GREEN**. router.ts에 `searchRoute`(path `/search`, `SearchRouteAdapter`) 추가 + routeTree addChildren 등록. Header에 검색 링크/아이콘.

**REFACTOR**. 정리. 기존 Header 테스트 회귀 0 확인.

**검증**. `pnpm --filter web test Header` + `pnpm --filter web typecheck`(tsconfig.app — [[ci-typecheck-tsconfig-app-vs-local]]).

### Task 7. [D7] E2E — search.spec.ts (S1 정상 / S2 문법오류 / S4 0건)

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/search.spec.ts`]
- depends-on: [5, 6]

**RED/시나리오**. ① S1 — `/search` 진입, `status = open AND priority IN (1, 2)` 입력, 검색, 결과 목록 표시 + syntax highlight 실렌더(span 색상 클래스 확인) ② S2 — 잘못된 쿼리 → 에러 메시지 표시 ③ S4 — 0건 빈 상태. 로그인 fixture는 공유 auth-fixtures([[e2e-loginasalice-fixture-fr-au-07-regression]]) 2단계. MSW는 Task 3 글로벌 핸들러 — E2E는 SPA 내부이동(reload 금지 — [[fr-nt-03-d6-d7-done]]). getByRole exact/컨테이너 한정([[playwright-getbyrole-exact-strict-mode]]).

**GREEN**. Playwright spec 작성, 셀렉터 안정화.

**검증**. `pnpm --filter web test:e2e search`.

## Plan 메타

- task 수: 7 (D3 1 + D6 5 + D7 1)
- wave 예상: 5 — W1[T1·T2·T3 병렬] → W2[T4] → W3[T5] → W4[T6] → W5[T7]. 프론트 체인(토크나이저→하이라이터→페이지→라우터→E2E)은 본질적 직렬([[bts-plan-wave-gradle-module-compile]]).
- TDD 강제: yes (test 커밋 선행)
- 병렬 dispatch: bts-impl이 depends-on+files로 wave 계산. files 교집합 0 확인됨.
- 추가 검증: ktlint/detekt(issue-tracking, T1) · vitest(T2~T6) · typecheck tsconfig.app(T6) · playwright(T7) · pnpm verify 최종.
- BC 경계 메모: T1은 issue-tracking 마이그레이션(성능 인덱스, 도메인 무변경), T2~T7은 apps/web. 한 PR 공존 — 인덱스는 view/성능 레이어라 BC 격리 예외(spec §제약). 

## 리뷰 결과 (← /bts-review-plan 채움)
