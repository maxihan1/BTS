# FR-SR-02 D3/D6/D7 — AQL 텍스트 쿼리 인덱스 + 프론트엔드 + E2E

> slug: fr-sr-02-d3-d6-d7
> type: feature (복합 — migration + ui + qa)
> agent: 복합 (D3=db-engineer, D6=frontend-engineer, D7=qa-engineer)
> 생성: 2026-06-25

## Brief

FR-SR-02(AQL 텍스트 쿼리, JQL 호환)의 백엔드 코어 MVP는 PR #189로 완료. 본 작업은 남은 D단계 마무리.

- **D3** — `pg_trgm` 확장 + GIN 인덱스 (issues.summary 등). AQL `~` 연산자(ILIKE `%term%`) 가속 (db-engineer)
- **D6** — AQL 입력창 + syntax highlight 프론트 UI (frontend-engineer)
- **D7** — E2E (qa-engineer)

classify 결과(type=qa)는 D7 "E2E" 키워드 오판 — 실제는 migration+ui+qa 복합. plan에서 task별 agent 지정.

백엔드 계약(이미 머지됨). `POST /api/v1/search/aql`, 지원 필드 status/label/summary/priority, 연산자 `=`/`!=`/`~`/`IN`/`NOT IN`/`AND`/`OR`/`NOT`/괄호/`ORDER BY`. DoS 가드(깊이50/토큰1000/2000자). 입력 오류 400 매핑.

## 도메인 정리

- **BC**: search-export-import (검색 UI 논리 소속) + issue-tracking (D3 인덱스 물리 위치). SR-01/SR-02 백엔드와 동일한 "논리≠물리" 구조 ([[fr-sr-01-issue-filter-done]]). 프론트(D6/D7)는 apps/web — BC 중립.
- **새 용어**: 없음. AQL(Atlas Query Language)은 glossary §검색/AQL에 이미 등재. trigram/pg_trgm·syntax highlight는 기술 용어(도메인 용어 아님) → glossary 추가 불필요.
- **새 엔티티**: 없음. 프론트는 기존 계약 소비. D3는 성능 인덱스(스키마 구조/도메인 로직 변경 없음).
- **기존 결정 충돌**: 없음. ADR `docs/decisions/2026-06-25-fr-sr-02-aql-parser-and-bc.md` 준수.

### 백엔드 계약 (PR #189 머지됨 — 프론트 정본)

- `POST /api/v1/search/aql`, body `{ projectKey: string, query: string, page?: int=0(min0), size?: int=50(1..100, 초과 시 400 거부=클램프 아님) }`. **인증 필수**(401 SEARCH_UNAUTHENTICATED), BROWSE 권한(403 SEARCH_ACCESS_DENIED).
- 응답: Spring `Page<AqlSearchHit>` raw(봉투 없음). `content[]` 항목: `key, summary, typeKey, currentStateKey, assigneeId(UUID|null), priority(int 1..5), priorityName, projectKey, updatedAt(ISO Instant)`. 메타: `totalElements, numberOfElements, first, last, empty, pageable{pageNumber,pageSize}`.
- 지원 필드: `status, label, summary, priority` (status/priority는 `~` 불가). 후속 예정 필드(assignee/reporter/component/project)는 400 `SEARCH_FIELD_NOT_YET_SUPPORTED`.
- 키워드(대소문자 무시): `AND OR NOT IN`, `ORDER BY`, `ASC DESC`. 연산자: `= != ~ IN "NOT IN"`. 리터럴: `"따옴표문자열"` · bare word · 정수.
- 에러: RFC 7807 ProblemDetail + `errorCode` + `detail` + `position`(0-base, 문법오류 시) + `timestamp`. 코드: `SEARCH_SYNTAX_ERROR`/`SEARCH_UNKNOWN_FIELD`/`SEARCH_FIELD_NOT_YET_SUPPORTED`/`SEARCH_VALIDATION_FAILED`(400), `SEARCH_UNAUTHENTICATED`(401), `SEARCH_ACCESS_DENIED`(403), `SEARCH_INTERNAL_ERROR`(500).
- DoS 가드: query 2000자 / 토큰 1000 / 중첩 깊이 50 초과 시 400.

### D3 인덱스 단서

- `~`(CONTAINS): `summary` → `summary ILIKE '%term%' ESCAPE '\'` (IssueRepository.kt:2292). `label` → `EXISTS(unnest(labels) ILIKE ...)` (IssueRepository.kt:2337).
- `summary` VARCHAR(255) → `pg_trgm` GIN 인덱스(`gin(summary gin_trgm_ops)`)로 ILIKE `%term%` 가속 가능.
- **결정 필요**: label `~`는 unnest 후 ILIKE라 trigram 인덱스 직접 적용 까다로움 → D3 범위에서 summary만 가속 vs label도 포함 결정(spec). 다음 마이그레이션 V번호 = issue-tracking **V031**(머지 직전 재확인 — [[migration-vnumber-concurrent-branch-collision]]).

### 결정 포인트 (→ spec 이월)

1. **syntax highlight 구현 방식**: 신규 라이브러리(CodeMirror/Monaco/Prism) vs 경량 자체 토크나이저. **신규 의존성은 Maxi 승인 필요**(DEVELOPMENT.md §외부 의존성). 새 라이브러리 채택 시 ADR 후보.
2. **D3 인덱스 대상 범위**: summary만 vs label 포함. pg_trgm 확장 설치(SDD §1.1에 이미 계획됨)는 인프라 영향 — 마이그레이션에 `CREATE EXTENSION IF NOT EXISTS pg_trgm` 포함.

- 관련 ADR: `docs/decisions/2026-06-25-fr-sr-02-aql-parser-and-bc.md` (기존, 준수). 신규 ADR은 syntax highlight 라이브러리 채택 시에만.

## 스펙

전체 스펙. [docs/specs/2026-06-25-fr-sr-02-d3-d6-d7.md](../specs/2026-06-25-fr-sr-02-d3-d6-d7.md)

Maxi 결정(2026-06-25). ① syntax highlight = 자체 경량 토크나이저(textarea+overlay, 의존성 0) ② UI = 별도 라우트 `/search` ③ D3 = summary만 GIN trgm(label 후속).

핵심 시나리오.
- `/search`에서 AQL 쿼리 입력 → POST /api/v1/search/aql → 결과 목록(Page<AqlSearchHit>) + 페이지네이션
- 입력 중 키워드/필드/연산자/문자열/숫자 색상 구분(자체 토크나이저 overlay)
- 문법오류 시 detail+position 표시, 미지원필드/0건/401/403 분기
- D3 V031 — pg_trgm 확장 + issues.summary trigram GIN 인덱스로 `~` 검색 가속

## Brainstorming Check

✅ 통과 (직접 sanity check). gap 2건(G1 projectKey 선택 방식, G2 트리거/size/ORDER BY) → 기존 패턴으로 보강, Maxi 추가 결정 불필요.

## Plan

> 정본 스펙. [docs/specs/2026-06-25-fr-sr-02-d3-d6-d7.md](../specs/2026-06-25-fr-sr-02-d3-d6-d7.md). 백엔드 계약(#189) 불변 — 프론트는 소비만, 필드 invent 금지.

### Task 1. [D3] V031 — pg_trgm 확장 + issues.summary trigram GIN 인덱스

**메타**.
- agent: `db-engineer`
- files: [`backend/modules/issue-tracking/src/main/resources/db/migration/issue-tracking/V031__issues_summary_trgm_index.sql`, `backend/modules/issue-tracking/src/main/resources/db/codegen/init_codegen.sql`, `backend/modules/issue-tracking/src/test/kotlin/com/bts/issue/migration/SummaryTrgmIndexMigrationTest.kt`]
- depends-on: []

**RED**. `SummaryTrgmIndexMigrationTest.kt` (기존 `WatcherSchemaMigrationTest`/`CustomFieldsMigrationTest:100` 패턴 미러 — indexdef 검사로 비-vacuous). Testcontainers로 마이그레이션 적용 후 ① `pg_extension`에 `pg_trgm` 행 존재 ② `pg_indexes.indexdef`가 `using gin` **그리고** `gin_trgm_ops` **그리고** `lower(summary)`를 모두 포함(이름만 보는 `indexExists`는 vacuous → indexdef 단언 병행) ③ **EXPLAIN 인덱스 사용 검증** — N건 시드 후 `EXPLAIN`(또는 `EXPLAIN ANALYZE`)이 `idx_issues_summary_trgm`을 통한 `Bitmap Index Scan`을 포함(B1 핵심 — 죽은 인덱스 방지). 실패(예상): 인덱스 미존재.

**GREEN**. V031 SQL — `CREATE EXTENSION IF NOT EXISTS pg_trgm;` + `CREATE INDEX IF NOT EXISTS idx_issues_summary_trgm ON issues USING gin (lower(summary) gin_trgm_ops);`. **표현식 인덱스(B1)** — 백엔드 `~`가 jOOQ `likeIgnoreCase`(`IssueRepository.kt:2298`) = `lower(summary) LIKE lower(?)`라 bare `summary` 인덱스는 미사용. `lower(summary)` 표현식으로 매칭. 백엔드 코드 무변경. 다음 V번호(V031)는 머지 직전 재확인([[migration-vnumber-concurrent-branch-collision]] — 현재 최신 V030).

**REFACTOR**. SQL 주석(L1 역할 + 왜 `lower(summary)` trgm GIN인지: `likeIgnoreCase`의 lower() 매칭으로 ILIKE `%term%` 가속). **`init_codegen.sql` 미러 필수(B2)** — V001~V030 전부 미러하는 codegen 부트스트랩 컨벤션([[jooq-init-codegen-mirror]]). 순서. `CREATE EXTENSION IF NOT EXISTS pg_trgm;`(기존 pgcrypto/pgmq 확장 선언부 인접) → `CREATE INDEX ... gin (lower(summary) gin_trgm_ops)`. 확장 누락 시 `operator class "gin_trgm_ops" does not exist`로 codegen 빌드 BLOCKED.

**검증**. `./gradlew :backend:issue-tracking:test --tests "*SummaryTrgmIndexMigrationTest*"` (EXPLAIN 인덱스 사용 단언 포함 — 필수) + `./gradlew :backend:issue-tracking:ktlintCheck detekt`.

### Task 2. [D6] AQL 토크나이저 (lib/aql-tokenizer.ts)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/lib/aql-tokenizer.ts`, `apps/web/src/lib/aql-tokenizer.test.ts`]
- depends-on: []

**RED**. `aql-tokenizer.test.ts` — `tokenizeAql(query)`가 KEYWORD(`and/or/not/in/order/by/asc/desc` 대소문자무시)·FIELD(`status/label/summary/priority`)·OPERATOR(`= != ~`)·STRING(`"..."`)·NUMBER·PAREN·COMMA·PLAIN으로 분해. 각 토큰 `{type, value, start, end}`. **회귀 스냅샷 가드(B3 재정의)** — TS는 Kotlin 상수를 import할 수 없으므로 "백엔드 동일 단언"은 불가능(vacuous). 대신 ① 상수 배열 회귀 스냅샷(`AQL_KEYWORDS`/`AQL_FIELDS`/`AQL_OPERATORS` 값 고정 — 무단 변경 감지) ② 가드가 검증하는 건 "프론트 내부 일관성"이지 백엔드 동기화가 아님을 테스트 주석에 명시. 실패: 함수 없음.

**GREEN**. 단일 패스 스캐너(정규식/문자 순회). 따옴표 문자열·이스케이프 처리. `ORDER`/`BY` 개별 KEYWORD. 상수 `AQL_KEYWORDS`/`AQL_FIELDS`/`AQL_OPERATORS` export. **하이라이트는 best-effort, 판정 정본은 백엔드** — 백엔드 lexer가 식별자에 한글/하이픈 허용(`AqlLexer.kt:191`)하는 디테일까지 미러 불필요(하이라이트만 어긋남, 기능 무영향).

**REFACTOR**. 상수를 파일 상단 단일 정본 + 백엔드 정본 위치 주석(`// 백엔드 정본: shared-kernel AqlFields.MVP_FIELDS:41 / AqlToken.KEYWORDS:90 — 변경 시 동반 수정`). L1 한국어 주석.

**검증**. `pnpm --filter web test aql-tokenizer`.

### Task 3. [D6] api/search.ts + Zod + MSW 핸들러/픽스처

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/search.ts`, `apps/web/src/api/search.test.ts`, `apps/web/src/mocks/search-handlers.ts`, `apps/web/src/mocks/search-fixtures.ts`, `apps/web/src/mocks/handlers.ts`]
- depends-on: []

**RED**. `search.test.ts` — `searchAql({projectKey, query, page, size})`가 ① 200 시 `Page<AqlSearchHit>` Zod 파싱(필드 정합: key/summary/typeKey/currentStateKey/assigneeId(nullable)/priority/priorityName/projectKey/updatedAt + totalElements/totalPages/size/number/first/last/empty) ② 400 ProblemDetail(errorCode/detail/position) → `ApiError` throw(봉투 보존) ③ 401/403 분기. MSW search-handlers로 시나리오 제공. 실패: 함수 없음.

**GREEN**. `apiPost`(client.ts) 재사용 — **CSRF 불필요 확정(eng OK)**: SPA는 `Authorization: Bearer`(client.ts:98)를 보내고 `oauth2ResourceServer`가 Bearer 요청 CSRF skip(`SecurityConfig.kt:117`), 기존 boards/bulk POST도 apiPost만 씀. Zod는 issues.ts **`pageSchema`(봉투 없음 — `data` 래퍼 아님, C2)** 형태 미러 + AqlSearchHit 스키마(백엔드 DTO 1:1, `labels` 없음 — invent 금지 [[frontend-zod-backend-dto-contract-gap]]). `search-handlers.ts`(정상/문법오류/0건/미지원필드/**401 — refresh도 실패시켜 진짜 401 도달 C4**/403) + `search-fixtures.ts` + `handlers.ts`에 등록(spread-index 한 줄).

**REFACTOR**. Zod 스키마 export 정리, 에러코드 상수화. L1 주석.

**검증**. `pnpm --filter web test search`.

### Task 4. [D6] AqlHighlighter 컴포넌트 (textarea + overlay)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/search/AqlHighlighter.tsx`, `apps/web/src/components/search/AqlHighlighter.test.tsx`]
- depends-on: [2, 8]

**RED**. `AqlHighlighter.test.tsx` — value 입력 시 토큰별 색상 span(`<pre aria-hidden>`) 렌더, textarea와 overlay **computed font-family 동일**(B2 정렬), `maxLength=2000`, caret 보존(mousedown preventDefault — [[jsdom-browser-textarea-selectionstart]]), **IME composition(B3)** — compositionstart→update→compositionend 시 정렬 유지(`isComposingRef`로 조합 중 overlay 갱신 보류). 실패: 컴포넌트 없음.

**GREEN**. Task 2 `tokenizeAql` 사용. textarea(투명 텍스트/caret 보임) + 절대배치 overlay. **색상은 T8이 등록한 DESIGN.md syntax 토큰 클래스만 사용(B1 — 즉흥 색 발명 금지)** + `--font-mono`(B2). **IME** — `onCompositionStart/End` + `isComposingRef`로 조합 중 갱신 보류(`use-mention-autocomplete.ts:70` 패턴 미러). **정렬 체크리스트(C6)** — overlay·textarea 동일 `box-sizing/padding/border-width/letter-spacing/tab-size/line-height`. `aria-hidden` overlay(스크린리더는 textarea만).

**REFACTOR**. 토큰→className 매핑 추출. L1 주석.

**검증**. `pnpm --filter web test AqlHighlighter`.

### Task 5. [D6] /search 페이지 — SearchPage + SearchRouteAdapter

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/search.tsx`, `apps/web/src/routes/search.test.tsx`]
- depends-on: [3, 4]

**RED**. `search.test.tsx` — ① 정상 쿼리 검색 → 결과 목록(key/summary/상태/우선순위) + 페이지네이션 ② 0건 빈 상태(결과영역, `muted-foreground`) ③ 문법오류 detail+position 표시(**입력창 하단**, C4) · 결과 미갱신 · position은 overlay 내 destructive underline 시각화(C2 — 숫자 노출 대신) ④ 미지원필드 안내 ⑤ 401/403 분기(에러 alert) ⑥ 빈 쿼리 검색버튼 disable ⑦ Cmd/Ctrl+Enter 트리거 + 힌트 텍스트 노출(C1) ⑧ filter-aware queryKey(쿼리 변경 시 page=0) ⑨ 로딩 "검색 중..." + 입력 보존(C7). MSW(Task 3) 사용. 실패: 컴포넌트 없음.

**GREEN**. `SearchPage`(props 기반, 라우터 비의존) + `SearchRouteAdapter`(useSearch/useNavigate로 `q`/`projectKey`/`page` ↔ URL `(prev)=>` 머지, `DEFAULT_PROJECT_KEY='ATLAS'` 기본+override). useQuery(`['search-aql', projectKey, q, page]`, `placeholderData` keepPrevious로 깜빡임 방지 C7). AqlHighlighter 입력창 + 검색버튼(+키 힌트) + 결과목록 + IssuePagination 재사용. **결과 카드(C3)** — 기존 IssueCard에서 표시전용 코어 재사용 또는 동일 클래스(`rounded-lg border bg-card px-4 py-3`, 상태배지 `rounded-full bg-muted`). priorityName은 i18n `priorityNames`(ko.ts:122) 한글화. **에러 위치 분리(C4)** — 문법오류는 입력창 하단(`role=alert`), 0건은 결과영역 회색 안내. page size=20.

**REFACTOR**. 결과 카드/에러코드→메시지 매핑 헬퍼 추출. L1 주석.

**검증**. `pnpm --filter web test search`.

### Task 6. [D6] router.ts 등록 + Header 검색 진입점

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/router.ts`, `apps/web/src/components/Header.tsx`, `apps/web/src/components/Header.test.tsx`]
- depends-on: [5]

**RED**. `Header.test.tsx`에 `/search` 진입(우측 아이콘 클러스터, `aria-label="검색"`) 존재 단언 추가(기존 테스트 보존). 실패: 링크 없음.

**GREEN**. router.ts에 `searchRoute`(path `/search`, `SearchRouteAdapter`) 추가 + routeTree addChildren 등록. **Header 우측 아이콘 클러스터(InboxBell 옆)에 lucide `Search` 아이콘 버튼(C5)** — InboxBell 패턴 미러(`rounded-md p-1.5 hover:bg-accent` + `<Search className="size-4" />` + `aria-label="검색"`). lucide Search 신규 import(비용 0).

**REFACTOR**. 정리. 기존 Header 테스트 회귀 0 확인.

**검증**. `pnpm --filter web test Header` + `pnpm --filter web typecheck`(tsconfig.app — [[ci-typecheck-tsconfig-app-vs-local]]).

### Task 7. [D7] E2E — search.spec.ts (S1 정상 / S2 문법오류 / S4 0건)

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/search.spec.ts`]
- depends-on: [5, 6]

**RED/시나리오**. ① S1 — `/search` 진입, `status = open AND priority IN (1, 2)` 입력, 검색, 결과 목록 표시 + syntax highlight 실렌더(span 색상 클래스 확인) ② S2 — 잘못된 쿼리 → 에러 메시지 표시 ③ S4 — 0건 빈 상태 ④ **한글 입력(B3)** — `summary ~ "로그인"` IME 조합 입력 시 overlay 정렬 유지(jsdom이 composition 못 흉내 → E2E 필수). 로그인 fixture는 공유 auth-fixtures([[e2e-loginasalice-fixture-fr-au-07-regression]]) 2단계. MSW는 Task 3 글로벌 핸들러 — E2E는 SPA 내부이동(reload 금지 — [[fr-nt-03-d6-d7-done]]). getByRole exact/컨테이너 한정([[playwright-getbyrole-exact-strict-mode]]).

**GREEN**. Playwright spec 작성, 셀렉터 안정화.

**검증**. `pnpm --filter web test:e2e search`.

### Task 8. [D6-design] DESIGN.md syntax highlight 토큰 + mono 폰트 등록

**메타**.
- agent: `designer`
- files: [`DESIGN.md`, `apps/web/src/index.css`]
- depends-on: []

**작업(design BLOCKER B1/B2 해소)**. DESIGN.md는 스펙/토큰 정본 — 코드는 frontend가 소비.
- §2 컬러 — `--syntax-keyword`/`--syntax-field`/`--syntax-operator`/`--syntax-string`/`--syntax-number` 5종 신규 등록(OKLCH, 라이트/다크). `index.css @theme`에 동일 등록. 회색조 chart 토큰 재사용 금지(색상 구분 불가).
- §8 대비표 — 각 syntax 토큰 vs `--background`(거의 흰색) WCAG AA 4.5:1 검증 기록.
- §3 폰트 — `--font-mono` 토큰 신규(한글 글리프 fallback 포함 — Geist mono는 한글 없음). `index.css @theme` 등록.
- "임의 색상 추가 금지"(§2) 준수 — 토큰 먼저 등록 후 T4가 클래스로만 사용.

**검증**. `pnpm --filter web build`(토큰 빌드 반영) + DESIGN.md §8 대비표 수치 기재.

## Plan 메타

- task 수: 8 (D3 1 + D6 5 + D6-design 1 + D7 1)
- wave 예상: 5 — W1[T1·T2·T3·T8 병렬] → W2[T4(dep 2,8)] → W3[T5] → W4[T6] → W5[T7]. 프론트 체인(토크나이저/토큰→하이라이터→페이지→라우터→E2E)은 본질적 직렬([[bts-plan-wave-gradle-module-compile]]).
- TDD 강제: yes (test 커밋 선행). T8(designer 토큰)·T1 init_codegen은 비-test 산출물이라 chore/docs 커밋 허용.
- 병렬 dispatch: bts-impl이 depends-on+files로 wave 계산. files 교집합 0 확인됨(T8 index.css/DESIGN.md ↔ 타 task 무겹침).
- 추가 검증: ktlint/detekt(issue-tracking, T1) · vitest(T2~T6) · typecheck tsconfig.app([[ci-typecheck-tsconfig-app-vs-local]], T6) · playwright(T7, 한글 케이스 포함) · pnpm verify 최종.
- BC 경계 메모: T1은 issue-tracking 마이그레이션(**성능 인덱스, 도메인/스키마 구조 무변경** — 옵션 A 유지로 백엔드 코드 무변경 확정), T2~T8은 apps/web + DESIGN.md. 한 PR 공존 — 인덱스는 성능 레이어라 BC 격리 예외(spec §제약, [[fr-sr-01-issue-filter-done]] 선례).

## 리뷰 결과

타입(feature, task 7)상 autoplan 분기이나, 복합 작업에 맞춰 eng/design 2종 독립 적대 리뷰 집중([[bts-review-plan-autoplan-overkill]] 정신). 두 리뷰가 BLOCKER 6건 사전 차단(#189에서도 eng/devex가 BLOCKER 5건 차단 — 적대 리뷰 효과 재확인).

### plan-eng-review (2026-06-25)
- **BLOCKER B1 (반영)** — `~`는 jOOQ `likeIgnoreCase`(`IssueRepository.kt:2298`) → `lower(summary) LIKE lower(?)`. bare `summary` GIN trgm 인덱스는 표현식 불일치로 **미사용(죽은 인덱스)** → NFR 가짜충족. → **표현식 인덱스 `gin (lower(summary) gin_trgm_ops)`** 채택(백엔드 무변경, 범위 보존). EXPLAIN 인덱스 사용 통합테스트 필수화(T1). 옵션 B(repo 쿼리 변경)는 백엔드 계약 수정=범위 초과라 기각.
- **BLOCKER B2 (반영)** — `init_codegen.sql`은 V001~V030 전부 미러하는 codegen DB 전체 부트스트랩(`build.gradle.kts:307`). V031 미러 **필수**(조건부 아님). `pg_trgm` 확장이 `gin_trgm_ops` opclass에 필요 → init_codegen에 `CREATE EXTENSION` **인덱스보다 먼저**(누락 시 codegen 빌드 BLOCKED). T1 문구 단호화.
- **BLOCKER B3 (반영)** — 토크나이저 drift 가드는 TS가 Kotlin 상수 import 불가 → "백엔드 동일 단언"은 구조적 vacuous. → 회귀 스냅샷 + 백엔드 정본 file:line 주석으로 재정의(과대광고 제거). 가드 범위=프론트 내부 일관성 명시. T2 수정.
- **CONCERN (반영)** — C1 인덱스테스트 indexdef에 `gin_trgm_ops`+`lower(summary)`까지 단언(`CustomFieldsMigrationTest:100` 선례) · C2 Zod는 issues.ts `pageSchema`(봉투 없음) 미러 명시 · C4 401 MSW는 refresh도 실패시켜야 진짜 401 분기 도달.
- **OK** — V030 최신(V031 충돌 없음) · CSRF 불필요(Bearer→`SecurityConfig.kt:117` CSRF skip, apiPost 그대로) · DTO↔Zod 9필드 1:1 · files 교집합 0 · BC 격리 예외 정당(성능 인덱스, 단 옵션 A 유지 조건).

### plan-design-review (2026-06-25)
- **BLOCKER B1 (반영)** — syntax 색상 토큰 부재. `--chart-1~5`는 chroma=0 회색조(`index.css:134`) → 5종 색상 구분 불가 + "임의 색상 금지"(DESIGN.md §2) 위반. → **designer task(T8) 신설** — DESIGN.md §2에 `--syntax-keyword/field/operator/string/number` 등록 + §8 대비표 WCAG AA 검증. T4는 이 토큰 클래스만 소비.
- **BLOCKER B2 (반영)** — `--font-mono` 토큰 부재(overlay 정렬 정본 없음). T8에서 DESIGN.md §3 + index.css에 한글 fallback 포함 `--font-mono` 등록.
- **BLOCKER B3 (반영)** — IME 한글 composition 미처리. → T4에 `isComposingRef`(use-mention-autocomplete.ts:70 패턴 미러) + T7에 한글 입력 E2E 케이스 추가.
- **CONCERN (반영)** — C1 트리거 힌트 텍스트(`Cmd/Ctrl+Enter로 검색`) · C2 position은 숫자 대신 overlay 내 destructive underline로 시각화(1방향 확정) · C3 IssueCard 표시전용 코어 재사용 + priorityName i18n(ko.ts:122) · C4 문법오류=입력창 하단/0건=결과영역(위치 분리) · C5 Header 우측 아이콘 클러스터에 lucide `Search`(InboxBell 패턴 미러) · C6 overlay 정렬 체크리스트(box-sizing/padding/letter-spacing) · C7 로딩 "검색 중..." + 입력 보존 + keepPrevious.
- **OK** — page size 20 · role=alert/aria-hidden 접근성 · maxLength 중복방어 · 빈쿼리 disable · 결과클릭 네비.

**종합**. BLOCKER 6건 전부 권장안 반영. plan 7→8 task(designer T8 추가). Maxi 결정 불필요(권장안 명확) — 게이트1에서 일괄 보고.
