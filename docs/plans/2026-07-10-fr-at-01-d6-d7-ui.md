# FR-AT-01 D6(자동화 트리거 선택 UI) + D7(E2E)

> slug: fr-at-01-d6-d7-ui
> type: ui
> agent: frontend-engineer (D7 E2E는 qa-engineer)
> primary_bc: automation
> 생성: 2026-07-10

## Brief

FR-AT-01 자동화 트리거의 D6(프론트 UI) + D7(E2E) 마무리.
백엔드 코어 D1~D5는 PR #251로 머지 완료(automation BC, BTS 9번째 모듈 com.bts.automation).

- 백엔드가 이미 제공: 5종 트리거(ISSUE_CREATED/UPDATED/COMMENTED/SCHEDULED/WEBHOOK) 감지→매칭→q_automation_execution enqueue
- D6 = 자동화 규칙 생성/관리 프론트 화면 (트리거 선택 폼)
- D7 = Playwright E2E

classify: E2E 키워드로 qa 오분류 → UI 주작업이라 ui/frontend-engineer로 정정(선례 #239 ui, #247 frontend).

## 도메인 정리

- **BC**: automation (primary). identity-access(권한코드)·issue-tracking(이벤트)는 백엔드에서 이미 결선, 이 UI PR은 automation view-layer만.
- **새 용어/엔티티**: 없음. 트리거·룰·액션은 glossary 기존 등재. UI는 기존 API 소비만.
- **기존 결정 충돌**: 없음.
- **관련 ADR**: [2026-07-10-fr-at-01-automation-triggers](../decisions/2026-07-10-fr-at-01-automation-triggers.md) (D1~D5 백엔드). 이 UI PR은 신규 ADR 불필요 (기존 project-settings 라우트 패턴 재사용) — 단, 스펙 게이트에서 나온 UX 결정은 plan에 기록.

### 백엔드 계약 (grep 실재 확인 — invent 금지)

API는 **프로젝트 스코프** (`AutomationRuleController`, `@RequestMapping("/api/v1/projects/{projectKey}/automation/rules")`). MANAGE_AUTOMATION = PROJECT_ADMIN → slack 같은 전역 `/admin/*` 아님. 5 엔드포인트.

| 메서드 | 경로 | 응답 | 비고 |
|---|---|---|---|
| POST | `.../rules` | 201 `CreateAutomationRuleResponse` | WEBHOOK이면 `webhookToken` 원문 **1회** 동봉 |
| GET | `.../rules` | 200 `List<AutomationRuleResponse>` | 목록 |
| GET | `.../rules/{id}` | 200 `AutomationRuleResponse` | 토큰 미노출 |
| PATCH | `.../rules/{id}` | 200 `AutomationRuleResponse` | name·enabled·triggerConfig (OCC, `version` 필수) |
| DELETE | `.../rules/{id}` | 204 | soft delete |

**요청 DTO** — `CreateAutomationRuleRequest { name, triggerType(enum), triggerConfig("{}" 기본) }` · `PatchAutomationRuleRequest { version(필수), name?, enabled?, triggerConfig? }`.
**응답 DTO** — `AutomationRuleResponse { id, projectKey, name, enabled, triggerType, triggerConfig(JSON 문자열), hasWebhookToken, nextFireAt?, createdBy, createdAt, updatedAt, version }`. 토큰 원문/해시 필드 아예 없음(타입상 누출 차단). `CreateAutomationRuleResponse { rule, webhookToken? }`.
**에러코드** — 401 `AUTOMATION_UNAUTHENTICATED` · 403 `AUTOMATION_ACCESS_DENIED` · 404 `AUTOMATION_RULE_NOT_FOUND` · 409 `AUTOMATION_RULE_VERSION_CONFLICT`(OCC) · 400 `AUTOMATION_RULE_INVALID`/`AUTOMATION_MALFORMED_REQUEST`. RFC 7807 ProblemDetail(`errorCode` 프로퍼티).

### 트리거 타입 5종 + triggerConfig 형식 (폼 필드 결정 근거)

`TriggerConfig.validate` 기준.
- **ISSUE_CREATED / ISSUE_COMMENTED** — 빈 config `{}` (설정 없음)
- **ISSUE_UPDATED** — `{ fields?: string[] }` (선택. 특정 필드 변경 시만 발화)
- **SCHEDULED** — `{ cron: string }` (필수, Spring `CronExpression` 6필드 파싱)
- **WEBHOOK** — `{}` (생성 시 불투명 토큰 1회 발급)

### 프론트 현황 / 배치

- 프론트에 **automation 코드 0, MANAGE_AUTOMATION 권한코드 0** → 그린필드.
- 배치 후보: **`projects.$projectKey.settings.automation.tsx`** (workflow-scheme·field-permissions와 동열 관리자 설정). RouteAdapter + Page 분리 패턴([[TanStack Router code-based]]) 재사용.
- api 레이어: `apps/web/src/api/automation-rules.ts` (+ `.types.ts` + test), MSW `mocks/automation-rule-handlers.ts`.

### 스펙 게이트에서 결정할 UX (spec 단계로 이월)

1. **스코프 확인** — FR-AT-01 D6 = **트리거 선택만** (name·트리거·config·enabled). 액션(FR-AT-02)/조건(FR-AT-03) 빌더는 별개 FR, 미구현. ⚠️ 트리거만 있는 룰은 `q_automation_execution` enqueue만 하고 실제 동작 없음(FR-AT-02 executor 부재) → "지금 만들 가치" 확인 필요.
2. 페이지 배치 (settings 탭 vs 별도).
3. WEBHOOK 토큰 1회 노출 UX (생성 후 모달 + 복사).
4. SCHEDULED cron 입력 (자유입력 6필드 vs 프리셋).
5. ISSUE_UPDATED fields 입력 (자유 vs 필드 목록).

## 스펙

정본: [docs/specs/2026-07-10-fr-at-01-d6-d7-ui.md](../specs/2026-07-10-fr-at-01-d6-d7-ui.md)

- **스코프 확정 (Maxi)**: 예정대로 트리거 UI 진행. 액션(FR-AT-02)/조건(FR-AT-03) 밖. FR 총수 123 불변.
- **배치**: `projects/$projectKey/settings/automation` (RouteAdapter + Page).
- **핵심 UX 결정 (기본값, 게이트1 검토)**: WEBHOOK 토큰 1회 모달+복사 / SCHEDULED cron 자유입력 6필드+힌트 / ISSUE_UPDATED fields 태그입력(비면 전체). 생성은 enabled=true 고정(백엔드), 토글로 비활성.
- FR 7 / NFR 4 / 엣지 7 / E2E 7 시나리오. 상세는 스펙 §4~§7.

## Brainstorming Check

- **신규 라이브러리 0** — TanStack Router/Query·Zod·MSW·기존 Dialog 재사용.
- **환각 위험 지점**: 백엔드 DTO 필드 invent 금지 → 스펙 §3·도메인 grep 결과만 사용. MSW fixture는 응답 스키마 required 전 필드.
- **회귀 방지 반영**: invalidate-only(플리커) · data-testid(strict mode) · Zod↔DTO 계약 · MSW stateful store · TanStack code-based adapter.
- **단순성**: 액션/조건 없이 트리거만 → 폼 조건부 필드 5종 분기 하나로 최소 구현.

## Plan

> 관례 확인: automation 응답은 **`{data}` 봉투 없음**(bare DTO 직접 반환) — custom-fields의 dataResponseSchema 쓰지 말 것([[frontend-api-convention-per-bc]]). mutation은 `X-XSRF-TOKEN`(readXsrfToken). Query 훅은 `api/use*.ts`. 라우트는 RouteAdapter+Page 분리, router.ts 수동 등록.

### Task 1. automation-rules 타입 + Zod 스키마

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/automation-rules.types.ts`, `apps/web/src/api/automation-rules.types.test.ts`]
- depends-on: []

**RED**: `automationRuleResponseSchema.parse(fixture)`가 실 백엔드 필드(id·projectKey·name·enabled·triggerType·triggerConfig·hasWebhookToken·nextFireAt?·createdBy·createdAt·updatedAt·version)를 통과. `triggerType`은 z.enum 5종. `createAutomationRuleResponseSchema`={rule, webhookToken:null|string}. 잘못된 triggerType은 ZodError.
**GREEN**: Zod 스키마 + 타입 export(`AutomationRule`, `TriggerType`, `CreateAutomationRuleInput`, `PatchAutomationRuleInput`) + triggerConfig 직렬화 헬퍼(구조화입력→JSON 문자열). **봉투 없음**(bare).
**REFACTOR**: nextFireAt은 `.nullable()`, UUID는 v4 fixture(Zod v4 RFC4122 엄격, [[zod-v4-uuid-fixture-strictness]]).
**검증**: `pnpm vitest run automation-rules.types`

### Task 2. MSW 핸들러 + fixture (stateful 공유 store)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/mocks/automation-rule-handlers.ts`, `apps/web/src/mocks/automation-rule-fixtures.ts`, `apps/web/src/mocks/automation-rule-handlers.test.ts`]
- depends-on: [1]

**RED**: 5 엔드포인트 핸들러가 CRUD 반영(생성→목록 증가·PATCH→필드변경·DELETE→목록감소·409 시나리오·WEBHOOK 생성 시 webhookToken 동봉). fixture는 응답 스키마 required 전 필드([[zod-schema-strengthen-inline-mock-fanout]]).
**GREEN**: seed 가능 공유 store([[msw-derived-behavior-shared-store-e2e]]) + 시나리오 토글(localStorage, [[e2e-msw-scenario-toggle-localstorage-flag]]).
**REFACTOR**: transitionKey류 helper 호출 패턴으로 drift 차단.
**검증**: `pnpm vitest run automation-rule-handlers`

### Task 3. api 함수 (list/get/create/patch/delete)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/automation-rules.ts`, `apps/web/src/api/automation-rules.test.ts`]
- depends-on: [1, 2]

**RED**: 각 함수가 올바른 경로(`/api/v1/projects/{key}/automation/rules[/{id}]`)·메서드 호출, 응답 Zod parse, PATCH/DELETE/POST에 X-XSRF-TOKEN 부착, 409/403/404를 ApiError(errorCode)로 표면화.
**GREEN**: `apiGet`(list/get)·`apiPost`(create)·`apiFetch`(PATCH/DELETE) + readXsrfToken. errorCode 추출 헬퍼(custom-fields 선례).
**REFACTOR**: basePath 헬퍼.
**검증**: `pnpm vitest run automation-rules.test`

### Task 4. TanStack Query 훅 (invalidate-only)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/api/useAutomationRules.ts`, `apps/web/src/api/useAutomationRules.test.tsx`]
- depends-on: [3]

**RED**: 목록 쿼리 key `['automation-rules', projectKey]`. create/patch/delete mutation 성공 시 **invalidateQueries만**(부분응답 setQueryData 플리커 금지, [[mutation-setquerydata-partial-response-flicker]]). create가 webhookToken 반환.
**GREEN**: useQuery + useMutation 3종, onSuccess invalidate.
**REFACTOR**: mutationFn 분리.
**검증**: `pnpm vitest run useAutomationRules`

### Task 5. RuleList 컴포넌트 (목록·토글·삭제·빈상태)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/automation/AutomationRuleList.tsx`, `apps/web/src/components/automation/AutomationRuleList.test.tsx`]
- depends-on: [1, 4]

**RED**: 룰 행(이름·트리거배지·enabled배지·nextFireAt)·활성 토글(PATCH)·삭제 확인 모달·빈상태 CTA(실제 폼 연결, dead path 금지 [[dialog-submiterror-ownership-dead-path]]). 액션 버튼 `data-testid`([[playwright-getbyrole-exact-strict-mode]]).
**GREEN**: 기존 Dialog/Badge 재사용.
**REFACTOR**: 행 컴포넌트 분리.
**검증**: `pnpm vitest run AutomationRuleList`

### Task 6. RuleFormDialog (트리거 선택 + 타입별 조건부 필드)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/automation/AutomationRuleFormDialog.tsx`, `apps/web/src/components/automation/AutomationRuleFormDialog.test.tsx`]
- depends-on: [1, 4]

**RED**: 이름 입력 + 트리거 셀렉트 5종 → 조건부 필드(SCHEDULED=cron 텍스트+힌트·ISSUE_UPDATED=fields 태그입력·나머지 없음). 저장 시 triggerConfig JSON 직렬화. cron 빈값 프론트 사전검증. controlled Dialog key 재마운트([[react-usestate-stale-key-prop]]).
**GREEN**: 조건부 렌더 분기 + 직렬화 헬퍼(T1) 호출.
**REFACTOR**: 트리거별 필드 서브컴포넌트.
**검증**: `pnpm vitest run AutomationRuleFormDialog`

### Task 7. WebhookTokenModal (1회 노출 + 복사)

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/components/automation/WebhookTokenModal.tsx`, `apps/web/src/components/automation/WebhookTokenModal.test.tsx`]
- depends-on: [1]

**RED**: webhookToken 원문 표시 + 복사 버튼 + "다시 볼 수 없음" 경고. 닫으면 토큰 소거(상태에만, localStorage/URL 금지 N1). 토큰 null이면 미렌더.
**GREEN**: Dialog + navigator.clipboard(jsdom mock).
**REFACTOR**: 복사 성공 피드백.
**검증**: `pnpm vitest run WebhookTokenModal`

### Task 8. 라우트 페이지 + router.ts 등록 + 설정 네비

**메타**.
- agent: `frontend-engineer`
- files: [`apps/web/src/routes/projects.$projectKey.settings.automation.tsx`, `apps/web/src/routes/__tests__/projects.$projectKey.settings.automation.test.tsx`, `apps/web/src/router.ts`]
- depends-on: [5, 6, 7]

**RED**: RouteAdapter(useParams)+Page(props projectKey) 분리, projectKey 없으면 ProjectNotFoundScreen. List+Form+WebhookModal 조립. 403 시 권한없음 상태. router.ts에 `/projects/$projectKey/settings/automation` requireAuth 등록. 설정 네비에 '자동화' 링크.
**GREEN**: 페이지 조립 + 라우트 등록(adapter import, [[TanStack Router code-based]]).
**REFACTOR**: 페이지 헤더 통일.
**검증**: `pnpm vitest run settings.automation` + `pnpm typecheck`

### Task 9. E2E (Playwright + MSW 시나리오)

**메타**.
- agent: `qa-engineer`
- files: [`apps/web/e2e/automation-rules.spec.ts`, `apps/web/src/mocks/automation-rule-handlers.ts`]
- depends-on: [8]

**RED**: 스펙 §7 T1~T7(목록/빈상태·ISSUE_CREATED 생성·SCHEDULED cron·WEBHOOK 토큰모달+복사+닫기후미표시·활성토글·삭제·409 refetch). MSW serviceWorker block 금지([[e2e-msw-serviceworker-block]]). getByRole exact/컨테이너 한정. **기존 관련 E2E 함께 실행**(UI PR 회귀 잠복 방지 [[ui-pr-defer-e2e-regression-latent]]).
**GREEN**: MSW 시나리오 토글 + addInitScript.
**REFACTOR**: helper 추출. worktree remove 후 5173 orphan kill 유의([[e2e-orphan-vite-after-worktree-remove]]).
**검증**: `pnpm exec playwright test automation-rules`

## Plan 메타

- task 수: 9
- 예상 wave: 7 (T1 → {T2,T7} → T3 → T4 → {T5,T6} → T8 → T9). 파일 겹침 0, depends-on은 계층 의존만.
- TDD 강제: yes (test 커밋이 feat보다 먼저)
- 추가 검증: typecheck, lint, vitest, playwright(qa-engineer)
- 회귀방지 반영: invalidate-only·data-testid·Zod↔DTO bare·MSW stateful store·code-based adapter·XSRF·UI PR E2E 동반

## 리뷰 결과 (← /bts-review-plan 채움)
