// agile-planning BC 도메인 레이어 — Board/BoardColumn 엔티티 (Task 7에서 본격 구현)

package com.bts.agileplanning.domain

/**
 * 아키텍처 룰 검증을 위한 최소 도메인 마커.
 *
 * ArchUnit이 `com.bts.agileplanning` 패키지를 실제로 스캔할 수 있도록
 * 컴파일된 클래스 파일이 존재해야 한다.
 * Board/BoardColumn 실제 도메인 엔티티는 Task 7에서 구현된다.
 */
class BoardPlaceholder
