# 컨텍스트 노트 — 프로젝트 소유 워크플로우

결정과 그 근거를 계속 덧붙인다. 다음 세션이 이걸 읽고 재유도 없이 이어받는다.

---

## 2026-09-08 · 요구 확정

Maxi 요구 3건이 세 번의 메시지로 나뉘어 들어왔다. 합치면 하나다.

1. 「프로젝트 어드민도 워크플로우 생성 권한을 줘야 해」
2. 「현재 프로젝트 권한이 관리자/멤버로 나뉘어 있잖아 관리자가 **프로젝트의** 워크플로우 스킴을 만들 수 있으면 좋겠다는 말이야」
3. 「프로젝트 설정 페이지에서도 지라 클라우드처럼 워크플로우를 수정/저장할 수 있어야 해」

★2번의 **「프로젝트의」**가 모델을 확정했다. 전역 자원에 생성 권한만 여는 것이 아니라 **프로젝트가 워크플로우를 소유**한다. Jira Cloud 로 치면 team-managed 프로젝트다.

내가 처음 제시한 선택지(전역 유지 / 프로젝트 소유 / key 네임스페이싱)에서 Maxi 는 셋 다 고르지 않고 저 문장으로 답했다. 옵션을 고르게 하는 대신 **의도를 말한 것**이고, 그것이 「프로젝트 소유」와 같다.

---

## 조사 결과 — 예상보다 훨씬 작다

착수 전엔 「권한 모델 전면 개편」으로 보였는데 실측하니 **배관이 이미 깔려 있었다.**

| 이미 있는 것 | 근거 |
|---|---|
| `PROJECT_ADMIN` 에 `MANAGE_WORKFLOW` 부여 | `V013__manage_workflow_permission.sql:16` |
| `MANAGE_SCHEME` → `MANAGE_WORKFLOW` 매핑 | `IdentityAccessWorkflowSchemePermissionResolver.kt:98-100` |
| 프로젝트 스코프 판정(멤버십 + 매트릭스) | 같은 파일 `hasProjectPermission()` |
| `WorkflowKeyResolver` 가 `projectKey` 를 받음 | `:57` `:87` — 두 메서드 다 |
| `WorkflowEditorPage` 자립성 | `workflowKey` 만 받는다. 라우트 어댑터 32줄 |

**막는 것은 소유 컬럼 하나다.** `WorkflowSchemeController` 가 CRUD 7곳에서 `WorkflowSchemeScope.Global` 을 하드코딩하는데, 그럴 수밖에 없는 이유가 스킴에 `project_id` 가 없어서 「어느 프로젝트로 물을지」를 모르기 때문이다.

V013 주석이 그 설계를 명시로 못박고 있다 — 「워크플로우 스킴 CRUD(MANAGE_SCHEME/Global)는 시스템 관리자 전용」. **이 작업은 그 결정을 의도적으로 뒤집는다.** 주석도 같은 PR 에서 고쳐야 한다.

---

## 실측이 계획을 두 번 줄였다

### ① `WorkflowKeyResolver` — 30 참조 / 11 파일 / 3 BC

처음엔 이 숫자를 보고 「가장 넓은 task, 20곳 넘으면 분할 재검토」라고 적었다. **틀렸다.**

두 메서드 다 첫 인자가 `projectKey` 다. 프로젝트 → 배정 스킴 → 이슈 타입 → 워크플로우 순으로 풀기 때문에 issue-tracking 은 워크플로우를 **맨키로 특정한 적이 없다.** 그 30 참조는 「고칠 호출부」가 아니라 「이미 프로젝트 스코프인 호출부」였다.

⇒ 이 task 는 **변경이 아니라 동결**로 바뀌었다. 「키 단독 조회 경로가 새로 생기지 않았다」를 판별식으로 막는다.

**교훈.** 참조 수는 폭발 반경이 아니다. 시그니처를 먼저 읽어라.

### ② 프론트 프로젝트 어드민 가드

「프로젝트 스코프 어드민 가드가 필요하다」고 적었는데, 실측하니 **프로젝트 설정 라우트 12종에 그런 가드가 하나도 없다.** 전부 `requireAuthAndPasswordChanged` 다(`router.ts:529`). `ProjectTree.tsx:155` 주석도 「GAP-1 전 인증자 표시 — 게이팅 없음」이라고 스스로 적고 있다.

이 저장소의 관례는 **탭은 모두에게 보이고 API 가 403 을 내면 화면이 안내 카드로 받는 것**이다. `ForbiddenSchemeCard` 가 그 선례다(내가 방금 만진 `projects.$projectKey.settings.workflow-scheme.tsx` 안에 있다).

⇒ 가드를 신설하지 않는다. 관례를 따른다.

---

## 결정 — 별도 테이블이 아니라 nullable `project_id`

전역·프로젝트 워크플로우는 **같은 것**이다. 상태·전환·초안·발행 구조가 동일하고, 프로젝트 워크플로우를 만드는 주 경로가 전역 템플릿 복제다(`POST /workflows/{key}/duplicate` 이미 존재).

테이블을 가르면 스킴 매핑·이슈 전환·이관 마법사가 전부 두 갈래가 된다. 한 테이블 + `project_id NULL` 이 Jira 의 company-managed / team-managed 공존과도 맞는다.

---

## 가장 위험한 자리 — 가짜 그린

`is_default` 시드 4건은 전부 `project_id IS NULL` 이다. 즉 **프로젝트 스코프 경로를 한 번도 타지 않고 기존 테스트가 전부 초록**이다.

프로젝트 소유 픽스처를 만들지 않으면 이 작업의 신규 경로는 **검증 0 회로 머지된다.** 체크리스트 공통 게이트에 못박아 뒀다.

같은 이유로 부분 유니크 인덱스도 위험하다 — 「컬럼이 생겼다」를 재는 테스트는 통과하지만 **인덱스가 실제로 무는지**는 안 잰다. 같은 키를 전역 1건 + 프로젝트 2건 + 삭제 1건으로 넣어 봐야 한다.

---

## PR 을 4개로 가른 이유

BC 격리(한 PR = 한 BC)와 「마이그레이션은 되돌리기 어렵다」를 함께 지키려면 이 형태가 된다.

★①(마이그레이션)을 ②(판정 전환)와 합치지 않는다. 컬럼만 추가하고 아무도 안 읽는 상태는 안전하지만, 판정 전환까지 한 PR 에 넣으면 **롤백이 마이그레이션 되돌리기**가 된다.

---

## 미해결

**FR 처리.** 145 FR 전량 D 완료 상태에서 이게 기존 FR(WF-02 / WF-04 / PM-04)의 범위 확장인지 새 FR 인지 정해야 한다. `docs/rules/fr-sync-checklist.md` 전수 동기화 대상이 달라진다. Maxi 확인 대기.

**스코프 타입 공용화.** `WorkflowDefinitionPermissionResolver` 는 스코프 개념이 아예 없다(`isSystemAdmin` 한 축). 스코프를 도입할 때 `WorkflowSchemeScope` 를 일반화할지 shared-kernel 에 공용 타입을 낼지 — 두 resolver 가 각자 스코프 타입을 만들면 그 순간 **서로를 검사하지 않는 두 목록**이 된다. ADR 대상.

---

## Jira 실물 조회가 설계를 확증하고 하나를 정정했다

`jira-research-guard` 판별식이 푸시를 막았다 — 「도입일(2026-08-27) 이후 plan/spec 은 `## Jira 대조` 절을 가져야 한다」. 규칙이 있는 이유가 명확하다. 2026-08-26 실측에서 ui/design 계획 104건 중 그 절을 가진 것이 13건뿐이었고, 그중에서도 출처 URL 을 남긴 것은 소수라 **나머지는 기억으로 쓰였다.** 기억으로 쓴 Jira 동작은 검증할 수단이 없다.

그래서 실제로 조회했다. 결과가 설계를 **확증**했고 하나를 **정정**했다.

### 확증

- team-managed 는 워크플로우 편집 권한이 「Role: Administrator」이고, 진입 경로가 `Space settings → Work types → Edit workflow` 다. Maxi 요구 3(「프로젝트 설정 페이지에서」)과 정확히 일치한다.
- Atlassian 이 권장하는 우회가 **「워크플로우를 복제해 자기 프로젝트 전용 사본으로 만들면 편집 가능해진다」**다. 내가 `duplicate` 재사용을 주 경로로 잡은 것과 같다.

### ★정정 — 판정 기준은 역할이 아니라 공유 여부다

처음엔 「PROJECT_ADMIN 이면 편집 가능」으로 적었는데, Jira 의 실제 규칙은

> Shared workflows can't be edited by users with the 'Edit Workflows' permission unless they have been assigned the **Administer Jira** global permission.

즉 **「그 워크플로우가 다른 프로젝트와 공유되지 않았을 때만」**이 1차 조건이고 역할은 그 다음이다.

`project_id` 모델이 그 규칙과 정확히 겹친다 — 값이면 비공유라 프로젝트 어드민이 고치고, `NULL` 이면 전 프로젝트 공유라 SYSTEM_ADMIN 전용이다. 이 설계는 Jira 를 흉내 낸 것이 아니라 **같은 불변식에 도달한 것**이고, 그래서 D4(전역 자원은 읽고 복제만)가 임의 제약이 아니라 근거 있는 제약이 됐다.

### 조회가 빠진 task 를 찾아냈다

Jira 가 주 경로로 명시하는 복제가 **task 목록에 없었다.** `POST /workflows/{key}/duplicate` 가 `project_id` 를 실어 주지 않으면 복제본도 전역이 되어 여전히 못 고친다 — 기능이 그 자리에서 죽는다. Task 2-2b 로 넣었다.

**교훈.** 실물 조회는 근거를 대는 절차가 아니라 **설계 검증 절차**다. 이번엔 정정 1건 + 누락 task 1건을 잡았다.

---

## 2026-09-08 · 착수 전 결정 2건 확정 (Maxi)

### D7. FR 은 신설 — `FR-WF-08`, 145 → 146

기존 3 FR(WF-02 · WF-04 · PM-04) 범위 확장이 아니다.

**왜 확장이 아닌가.** 확장하면 완료 선언한 FR 3개가 다시 미완료가 된다 — 「145/145 D 완료」가 거짓이 되고 진척 대시보드가 퇴행한다. 그리고 「이 변경이 어느 FR 의 일인가」가 세 곳으로 흩어진다.

**대가.** 카운트 리터럴 전수 동기화. 다만 `verify-master-plan.sh`(EXIT 4)가 기계로 강제하므로 누락이 막힌다 — 사람이 세는 것이 아니다. 이 저장소가 이미 그 길을 여러 번 갔다(FR-BD-04 신설 때 143→144).

⇒ Task 1-0 으로 PR ① 에 넣었다. **뒤 PR 로 미루면 그 사이 `verify-master-plan` 이 계속 빨간불**이라 다른 작업의 게이트까지 막는다.

### D8. 스코프 타입은 shared-kernel 공용 `WorkflowScope` 신설

세 안을 놓고 골랐다.

| 안 | 왜 아닌가 / 왜 맞는가 |
|---|---|
| `WorkflowSchemeScope` 일반화 | **기각.** 이름에 `Scheme` 이 남는다. `WorkflowDefinitionPermission` KDoc 이 정확히 이 혼동을 경고하고 있고, 그 경고 때문에 **enum 은 이미 둘로 갈라져 있다.** 스코프만 이름을 섞으면 그 결정을 되돌리는 것이다 |
| 정의 전용 스코프 타입 신설 | **기각.** 두 스코프 타입이 서로를 검사하지 않는 두 목록이 된다 — 한쪽에만 새 스코프가 추가돼도 조용히 통과한다. 막으려면 차집합 판별식을 **또** 만들어야 한다 |
| 공용 `WorkflowScope` | **채택.** 중립 이름 하나를 두 resolver 가 공유한다. 마이그레이션은 `WorkflowSchemeScope` 사용처 치환 1회 |

★**이 결정이 PR ③ 의 티어를 올렸다.** `shared-kernel` 을 건드리므로 T2 → **T3**. ADR 이 필요하고 리뷰에 ceo 가 붙는다. 안을 고를 때 이 대가를 같이 봐야 했는데, 티어 변화는 결정 후에야 알아챘다 — **다음엔 선택지에 티어 영향을 적어라.**

### 결과 — 착수 전 확인 4건 전부 해소

실측으로 2건(`WorkflowKeyResolver` 폭발 반경 · 프론트 어드민 가드), Maxi 결정으로 2건(FR · 스코프 타입). **PR ① 부터 착수 가능하다.**
