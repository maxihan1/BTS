// 워크로그 집계 보고 메인 컴포넌트 — 필터 + 4가지 렌더 상태 조립 (FR-TT-02 Task-6)
import type { JSX } from 'react'

/** @internal RED 단계 스텁 — 구현 전 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function WorklogAggregateReport({ projectKey: _projectKey }: { projectKey: string }): JSX.Element {
  throw new Error('WorklogAggregateReport: not implemented')
}
