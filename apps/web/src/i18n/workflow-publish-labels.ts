// 워크플로우 초안·발행·복원 UI 의 E2E 셀렉터 정본 — 라벨 변경 시 단일 진입점
/**
 * 초안 편집 · 발행 · 기본값 복원이 노출하는 한국어 라벨.
 *
 * ★ `workflow-editor-labels.ts` 에 얹지 않고 파일을 나눴다. 그쪽은 목록·편집기 셸의 라벨이고
 * 이미 162줄이라, 발행 계열 40여 항목을 더하면 「정본 한 파일」로 읽히지 않는다. 정본의
 * 성질은 「한 파일」이 아니라 **「한 출처」** 이므로 소비처가 갈리지 않으면 나눠도 된다.
 *
 * ★★ **다이얼로그 제목은 화면 전체에서 고유해야 한다** (§2 즉사 계약). 기존 제목과의 충돌을
 * 착수 전에 훑었다 — `일괄 작업 결과` · `전환 만들기` · `전환 수정` ·
 * `워크플로우에 추가할 상태 선택` 과 겹치지 않는다.
 *
 * ★★★ **부분일치 함정.** Playwright `getByRole(name:)` 은 부분일치가 기본이다.
 * `발행` 은 `발행 이력` 의 substring 이 되므로, 서로 접두를 공유하는 라벨을 새로 만들 때는
 * 그 쌍을 여기 주석에 남기고 E2E 가 `exact` 로 집게 한다.
 */
export const workflowPublishLabels = {
  /** 초안 상태 표시줄 */
  draft: {
    /** 표시줄 전체의 aria-label */
    bar: '초안 상태',
    /** 저장 상태 — 편집 없음 */
    idle: '편집 시작 전',
    /** 저장 상태 — 저장 대기 */
    dirty: '저장하지 않은 변경이 있습니다',
    /** 저장 상태 — 전송 중 */
    saving: '저장 중',
    /** 저장 상태 — 완료 */
    saved: '저장됨',
    /** 저장 상태 — 실패. 이 상태에서는 발행이 잠긴다 */
    error: '저장하지 못했습니다',
    /** 발행 버튼 */
    publish: '발행',
    /** 기본값 복원 버튼 */
    reset: '기본값으로 되돌리기',
    /** 초안 폐기 버튼 */
    discard: '초안 폐기',
    /**
     * 아직 발행되지 않은 편집이 있다는 안내.
     *
     * 「편집 ≠ 배포」가 이 화면의 핵심 계약이라 항상 보이는 자리에 둔다 — 사용자가 저장만
     * 하고 나가면 운영에는 아무 변화가 없다.
     */
    unpublishedNotice: '변경 사항은 발행해야 반영됩니다',
  },

  /** 발행 다이얼로그 */
  publish: {
    /** ★ 다이얼로그 제목. 단계가 바뀌어도 **이 이름은 끝까지 그대로**다 — 접근성 이름이
     *  단계마다 바뀌면 E2E 셀렉터가 굳지 못한다 */
    dialogTitle: '워크플로우 발행',
    /** 다이얼로그 설명 */
    dialogDescription: '발행하면 초안이 운영에 반영됩니다',
    /** 빠지는 상태 목록의 제목 */
    removedHeading: '사라지는 상태',
    /** 빠지는 상태가 없을 때 */
    noRemoved: '사라지는 상태가 없습니다',
    /** 상태별 잔여 이슈 건수 접미 — `${건수}건` */
    issueCountSuffix: '건',
    /** 이슈가 남지 않은 상태 */
    noIssues: '이슈 없음',
    /** 확정 발행 버튼 */
    confirm: '발행하기',
    /** 발행 성공 토스트 접두 — `${prefix} ${회차}회차` */
    successPrefix: '발행했습니다',
    /** 이름·설명 변경도 함께 나간다는 고지 */
    metaChangeNotice: '이름과 설명 변경도 이번 발행에 함께 반영됩니다',
    /**
     * 보드 컬럼 고지.
     *
     * ★ **조건부가 아니라 무조건 뜬다.** 「이 상태를 쓰는 보드 컬럼」을 물을 API 가 없어
     * (보드 조회는 projectKey 가 필수이고 이관 범위 프로젝트 목록은 서버가 일부러 응답에
     * 안 싣는다) 조건 판정을 흉내 내면 그것이 거짓말이 된다. 사라지는 상태가 있으면 언제나
     * 알린다.
     */
    boardWarning: '사라지는 상태를 쓰는 보드 컬럼이 있으면 그 컬럼은 빈 채로 남습니다. 자동 재매핑은 하지 않습니다',
  },

  /**
   * 상태 이관 마법사 안내 (로드맵 PR 10b).
   *
   * `intro` 만 있던 자리에 나머지를 채운다 — 10a 리뷰 C2 가 「소비자 없는 라벨은 화면과 대조된
   * 적 없는 채로 굳는다」로 미룬 것이고, 이 화면이 그 소비자다.
   */
  migration: {
    /** 이관이 필요해 발행이 막혔을 때의 안내. 발행 다이얼로그가 쓴다 */
    intro: '사라지는 상태에 남은 이슈를 어디로 옮길지 정하세요',
    /** 이관 시작 버튼. 사라지는 상태마다 도착지를 전부 고르기 전에는 비활성이다(F5) */
    start: '이관 시작',
    /** 진행률 영역의 aria-label. `role="progressbar"` 요소가 이 이름을 쓴다(J6 · ADS A1) */
    progressLabel: '이관 진행률',
    /** 이관 완료 문구 */
    completed: '이관이 완료됐습니다',
    /** 실패 항목 목록의 제목. `failedCount === 0` 이면 목록 자체를 그리지 않는다 */
    failuresHeading: '옮기지 못한 이슈',
    /**
     * 규칙 미발화 고지 (Jira J8 — 표시 의무).
     *
     * 「검증기가 막아 주겠지」로 오해하면 그 오해가 데이터에 남는다. 그래서 무엇을 고르든
     * 이관은 규칙을 타지 않는다는 사실을 단정문으로 말한다 — 완곡하게 돌리면 오해가 남는다.
     */
    rulesNotTriggeredNotice:
      '이관은 워크플로우의 전환 규칙을 타지 않습니다. 전환 제한·검증 규칙이 있어도 이관을 막지 않고, 후처리 규칙도 자동으로 실행되지 않습니다.',
    /**
     * 보드 컬럼 고지 (X4 — 10a `publish.boardWarning` 과 같은 편차의 마법사 판).
     *
     * ★ **조건부가 아니라 무조건 뜬다.** 「이 상태를 쓰는 보드 컬럼」을 물을 API 가 없어(보드
     * 조회는 projectKey 가 필수이고 이관 범위 프로젝트 목록은 서버가 일부러 응답에 안 싣는다)
     * 조건 판정을 흉내 내면 그것이 거짓말이 된다. 사라지는 상태가 있으면 마법사가 뜨는 즉시
     * 항상 알린다.
     */
    boardColumnsNotice: '이 상태를 쓰는 보드 컬럼이 있다면 이관 후 함께 확인하세요. 컬럼 재배치는 자동으로 되지 않습니다',
  },

  /** 기본값 복원 다이얼로그 */
  reset: {
    /** 다이얼로그 제목 — 화면 전역 고유 */
    dialogTitle: '기본값으로 되돌리기',
    /** 설명. 「초안까지만」이 이 기능의 핵심이다 */
    dialogDescription: '기본 정의를 초안으로 불러옵니다. 발행해야 운영에 반영됩니다',
    /** 확인 버튼 */
    confirm: '초안으로 불러오기',
  },

  /** 초안 폐기 다이얼로그 */
  discard: {
    /** 다이얼로그 제목 — 화면 전역 고유 */
    dialogTitle: '초안 폐기',
    /** 설명 */
    dialogDescription: '편집 중인 초안을 버리고 지금 발행된 정의로 되돌아갑니다',
    /** 확인 버튼 */
    confirm: '폐기하기',
  },

  /** 낙관적 락 충돌 배너 */
  conflict: {
    /** 배너 aria-label */
    banner: '발행 충돌',
    /**
     * 본문.
     *
     * ★ 「다시 시도」를 권하지 않는다. 서버 앵커는 write-once 라 다시 불러와도 저장된 초안의
     * 앵커는 그대로이고 같은 409 가 반복된다. 출구는 폐기뿐이다.
     */
    message: '다른 사용자가 먼저 발행했습니다. 이 초안은 다시 시도해도 발행되지 않습니다',
    /** 유일한 출구 */
    action: '초안을 버리고 다시 편집하기',
  },

  /** 공용 */
  common: {
    cancel: '취소',
  },
} as const
